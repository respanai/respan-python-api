# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.annotation import Annotation
from ..types.annotation_item_detail import AnnotationItemDetail
from ..types.annotation_item_list import AnnotationItemList
from ..types.annotation_item_queue import AnnotationItemQueue
from ..types.annotation_type_enum import AnnotationTypeEnum
from ..types.ch_dataset_log import ChDatasetLog
from ..types.ch_eval_result_list import ChEvalResultList
from ..types.ch_eval_result_list_request_eval_class import ChEvalResultListRequestEvalClass
from ..types.dataset_detail import DatasetDetail
from ..types.dataset_list import DatasetList
from ..types.dataset_llm_run_status_enum import DatasetLlmRunStatusEnum
from ..types.dataset_log_presence_response import DatasetLogPresenceResponse
from ..types.dataset_log_status_create import DatasetLogStatusCreate
from ..types.dataset_log_status_create_status_enum import DatasetLogStatusCreateStatusEnum
from ..types.dataset_logs_import_response import DatasetLogsImportResponse
from ..types.dataset_status_enum import DatasetStatusEnum
from ..types.dataset_task_tracker_run_eval_list import DatasetTaskTrackerRunEvalList
from ..types.dataset_task_tracker_run_evaluation_create import DatasetTaskTrackerRunEvaluationCreate
from ..types.dataset_task_tracker_run_evaluation_detail import DatasetTaskTrackerRunEvaluationDetail
from ..types.dataset_task_tracker_run_logs_create import DatasetTaskTrackerRunLogsCreate
from ..types.dataset_task_tracker_run_logs_detail import DatasetTaskTrackerRunLogsDetail
from ..types.dataset_task_tracker_run_logs_list import DatasetTaskTrackerRunLogsList
from ..types.dataset_type_enum import DatasetTypeEnum
from ..types.eval_result_create import EvalResultCreate
from ..types.eval_result_detail import EvalResultDetail
from ..types.eval_with_results import EvalWithResults
from ..types.evaluator_create_version import EvaluatorCreateVersion
from ..types.evaluator_create_version_request_eval_class import EvaluatorCreateVersionRequestEvalClass
from ..types.evaluator_tag import EvaluatorTag
from ..types.filter_param_dict_pydantic import FilterParamDictPydantic
from ..types.paginated_annotation_item_list_list import PaginatedAnnotationItemListList
from ..types.paginated_annotation_item_queue_list import PaginatedAnnotationItemQueueList
from ..types.paginated_annotation_list import PaginatedAnnotationList
from ..types.paginated_ch_dataset_log_list import PaginatedChDatasetLogList
from ..types.paginated_dataset_list_list import PaginatedDatasetListList
from ..types.paginated_dataset_task_tracker_run_eval_list_list import PaginatedDatasetTaskTrackerRunEvalListList
from ..types.paginated_dataset_task_tracker_run_logs_list_list import PaginatedDatasetTaskTrackerRunLogsListList
from ..types.paginated_eval_with_results_list import PaginatedEvalWithResultsList
from ..types.paginated_evaluator_tag_list import PaginatedEvaluatorTagList
from ..types.paginated_public_ch_eval_result_list_list import PaginatedPublicChEvalResultListList
from ..types.paginated_public_dataset_task_tracker_run_eval_list_list import (
    PaginatedPublicDatasetTaskTrackerRunEvalListList,
)
from ..types.paginated_public_evaluator_list_list import PaginatedPublicEvaluatorListList
from ..types.paginated_public_evaluator_version_list_list import PaginatedPublicEvaluatorVersionListList
from ..types.patched_ch_eval_result_list_request_eval_class import PatchedChEvalResultListRequestEvalClass
from ..types.patched_public_evaluator_list_request_eval_class import PatchedPublicEvaluatorListRequestEvalClass
from ..types.patched_public_evaluator_version_detail_request_eval_class import (
    PatchedPublicEvaluatorVersionDetailRequestEvalClass,
)
from ..types.public_ch_eval_result_list import PublicChEvalResultList
from ..types.public_dataset_task_tracker_run_eval_list import PublicDatasetTaskTrackerRunEvalList
from ..types.public_eval_result_detail import PublicEvalResultDetail
from ..types.public_eval_result_update import PublicEvalResultUpdate
from ..types.public_evaluator_create import PublicEvaluatorCreate
from ..types.public_evaluator_detail import PublicEvaluatorDetail
from ..types.public_evaluator_list import PublicEvaluatorList
from ..types.public_evaluator_list_request_eval_class import PublicEvaluatorListRequestEvalClass
from ..types.public_evaluator_update import PublicEvaluatorUpdate
from ..types.public_evaluator_version_detail import PublicEvaluatorVersionDetail
from ..types.public_evaluator_version_detail_request_eval_class import PublicEvaluatorVersionDetailRequestEvalClass
from ..types.public_evaluator_version_list import PublicEvaluatorVersionList
from ..types.score_value_type_enum import ScoreValueTypeEnum
from ..types.source_type_c74enum import SourceTypeC74Enum
from ..types.status66c_enum import Status66CEnum
from ..types.status_c33enum import StatusC33Enum
from ..types.type0cb_enum import Type0CbEnum
from ..types.type4e2enum import Type4E2Enum
from .raw_client import AsyncRawEvaluationsClient, RawEvaluationsClient
from .types.eval_with_results_request_code_config import EvalWithResultsRequestCodeConfig
from .types.eval_with_results_request_llm_config import EvalWithResultsRequestLlmConfig
from .types.eval_with_results_request_passing_conditions import EvalWithResultsRequestPassingConditions
from .types.patched_eval_with_results_request_code_config import PatchedEvalWithResultsRequestCodeConfig
from .types.patched_eval_with_results_request_llm_config import PatchedEvalWithResultsRequestLlmConfig
from .types.patched_eval_with_results_request_passing_conditions import PatchedEvalWithResultsRequestPassingConditions

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class EvaluationsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawEvaluationsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawEvaluationsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawEvaluationsClient
        """
        return self._raw_client

    def api_annotation_items_list(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[AnnotationItemList]:
        """
        GET /api/annotation-items/

        Simple listing for basic CRUD operations.
        Returns lightweight list of items without pagination.

        Permission: Requires annotation_items:read (admin only)

        Use cases:
        - Quick item lookups
        - Basic CRUD list view (C from CRUD done via /bulk/)
        - For paginated + filtered view, use /list/
        - For annotation work, use /queue/

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[AnnotationItemList]


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_list()
        """
        _response = self._raw_client.api_annotation_items_list(request_options=request_options)
        return _response.data

    def api_annotation_items_create(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_create(
            source_id="source_id",
            source_type="logs",
        )
        """
        _response = self._raw_client.api_annotation_items_create(
            source_id=source_id,
            source_type=source_type,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    def api_annotation_items_update(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_update(
            source_id="source_id",
            source_type="logs",
        )
        """
        _response = self._raw_client.api_annotation_items_update(
            source_id=source_id,
            source_type=source_type,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    def api_annotation_items_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        source_id: typing.Optional[str] = OMIT,
        source_type: typing.Optional[SourceTypeC74Enum] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        source_id : typing.Optional[str]

        source_type : typing.Optional[SourceTypeC74Enum]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_partial_update()
        """
        _response = self._raw_client.api_annotation_items_partial_update(
            id=id,
            source_id=source_id,
            source_type=source_type,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    def api_annotation_items_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AnnotationItemDetail:
        """
        GET/PATCH/DELETE /api/annotation-items/{id}/

        Retrieve, update, or delete a specific annotation item (RUD from CRUD).

        Permission (via ObjectOwnershipMixin config in SuperAdminMixin):
        - Worker (assignee): Can GET/PATCH their OWN assigned items (status only)
        - Org Admin: Can GET/PATCH/DELETE any item in org (can re-assign)
        - Superadmin: Can access all items across all orgs

        GET: Returns full item details with all fields
        PATCH:
          - Worker: Update status, completed_at
          - Admin: Also update assignee_id (re-assign)
        DELETE: Delete item (admin only via ownership check)

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.api_annotation_items_retrieve(id, request_options=request_options)
        return _response.data

    def api_annotation_items_create2(
        self,
        id: str,
        *,
        project: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        project : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_create2(
            id="id",
        )
        """
        _response = self._raw_client.api_annotation_items_create2(
            id, project=project, status=status, completed_at=completed_at, request_options=request_options
        )
        return _response.data

    def api_annotation_items_update2(
        self,
        id: str,
        *,
        project: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        project : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_update2(
            id="id",
        )
        """
        _response = self._raw_client.api_annotation_items_update2(
            id, project=project, status=status, completed_at=completed_at, request_options=request_options
        )
        return _response.data

    def api_annotation_items_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        GET/PATCH/DELETE /api/annotation-items/{id}/

        Retrieve, update, or delete a specific annotation item (RUD from CRUD).

        Permission (via ObjectOwnershipMixin config in SuperAdminMixin):
        - Worker (assignee): Can GET/PATCH their OWN assigned items (status only)
        - Org Admin: Can GET/PATCH/DELETE any item in org (can re-assign)
        - Superadmin: Can access all items across all orgs

        GET: Returns full item details with all fields
        PATCH:
          - Worker: Update status, completed_at
          - Admin: Also update assignee_id (re-assign)
        DELETE: Delete item (admin only via ownership check)

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_destroy(
            id="id",
        )
        """
        _response = self._raw_client.api_annotation_items_destroy(id, request_options=request_options)
        return _response.data

    def api_annotation_items_partial_update2(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_partial_update2(
            id="id",
        )
        """
        _response = self._raw_client.api_annotation_items_partial_update2(id, request_options=request_options)
        return _response.data

    def api_annotation_items_bulk_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        POST /api/annotation-items/bulk/

        Bulk create annotation items (assign logs to workers).
        Supports both "by-reference" and "by-direct-payload" methods.

        Permission: Requires annotation_items:create (admin only)

        Events: `annotation_item_assigned` dispatched via signals (see annotation/signals.py)

        Request Body:
        {
          "assignee_id": "user_123",
          "source_items": [
            {"source_type": "logs", "log_id": "log_1"},
            {"source_type": "logs", "log_id": "log_2", "full_object": {...}}
          ]
        }

        Response (201) — canonical BulkOperationResponse envelope with the
        pre-foundation custom fields (`created_count`, `items`) carried as
        extras via Pydantic ``extra="allow"``:
        {
          "success_count": 2,
          "error_count": 0,
          "errors": [],
          "created_count": 2,
          "items": [...]
        }

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_bulk_create()
        """
        _response = self._raw_client.api_annotation_items_bulk_create(request_options=request_options)
        return _response.data

    def api_annotation_items_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationItemListList:
        """
        GET/POST /api/annotation-items/list/

        Admin/Manager view: Team progress tracking with pagination and filtering.
        Sophisticated listing endpoint for management dashboard (L from CRUDL).

        Permission: Requires annotation_items:read (admin/manager only)

        Features:
        - Pagination (cursor-based)
        - Filtering (source_type, status, etc.)
        - Accepts filters via URL params OR POST body

        Use cases:
        - Management dashboard
        - Track team progress
        - Filter by status, source_type
        - View who's assigned to what

        Query Parameters (GET):
        - page, page_size: Pagination
        - source_type: Filter by source type
        - status: Filter by status

        Request Body (POST):
        {
          "filters": {
            "source_type": {"operator": "in", "value": ["logs", "threads"]},
            "status": {"operator": "", "value": "pending"}
          }
        }

        Response:
        {
          "count": 150,
          "next": "...",
          "previous": "...",
          "results": [
            {
              "id": "item_1",
              "assignee": {"email": "worker@co.com"},
              "status": "pending",
              "source_type": "logs",
              "created_at": "..."
            }
          ],
          "filters_data": {...}
        }

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationItemListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_list_list()
        """
        _response = self._raw_client.api_annotation_items_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_annotation_items_list_create(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        Handle POST requests (for filtering) by delegating to GET.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_list_create(
            source_id="source_id",
            source_type="logs",
        )
        """
        _response = self._raw_client.api_annotation_items_list_create(
            source_id=source_id,
            source_type=source_type,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    def api_annotation_items_list_update(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_list_update(
            source_id="source_id",
            source_type="logs",
        )
        """
        _response = self._raw_client.api_annotation_items_list_update(
            source_id=source_id,
            source_type=source_type,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    def api_annotation_items_list_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        source_id: typing.Optional[str] = OMIT,
        source_type: typing.Optional[SourceTypeC74Enum] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        source_id : typing.Optional[str]

        source_type : typing.Optional[SourceTypeC74Enum]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_list_partial_update()
        """
        _response = self._raw_client.api_annotation_items_list_partial_update(
            id=id,
            source_id=source_id,
            source_type=source_type,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    def api_annotation_items_queue_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationItemQueueList:
        """
        GET/POST /api/annotation-items/queue/

        Worker queue view: MY assigned items for focused annotation work.
        Used by everyone (including admins) when doing annotation work.

        Permission: No permission required (self-filtering to assignee=current_user)

        Features:
        - Prefetch FULL objects for instant navigation
        - Privacy: Only see OWN scores
        - Optional filtering (source_type, status)
        - Pagination support (page, page_size query params)

        Use cases:
        - Workers see their work queue
        - Admins see their queue when annotating
        - Focused view - only MY items

        Query Parameters (GET):
        - page: Page number (default: 1)
        - page_size: Items per page (default: 100, max: 1000)
        - source_type: Filter by source type (optional)
        - status: Filter by status (optional)

        Request Body (POST):
        {
          "filters": {
            "source_type": {"operator": "", "value": "logs"},
            "status": {"operator": "", "value": "pending"}
          }
        }

        Response (paginated):
        {
          "count": 5,
          "next": "/api/annotation-items/queue/?page=2",
          "previous": null,
          "results": [
            {
              "id": "item_1",
              "status": "pending",
              "full_object": {...},
              "scores": [...]
            }
          ]
        }

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationItemQueueList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_queue_list()
        """
        _response = self._raw_client.api_annotation_items_queue_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_annotation_items_queue_create(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        full_object: typing.Any,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemQueue:
        """
        Handle POST requests (for filtering) by delegating to GET.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        full_object : typing.Any

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemQueue


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_queue_create(
            source_id="x",
            source_type="logs",
            full_object={"key": "value"},
        )
        """
        _response = self._raw_client.api_annotation_items_queue_create(
            source_id=source_id,
            source_type=source_type,
            full_object=full_object,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    def api_annotation_items_queue_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationItemQueueList:
        """
        GET/POST /api/annotation-items/queue/list/

        Worker queue list view with filter options metadata.
        Same as /queue/ but includes filters_data in response for FE filter UI.

        Permission: No permission required (self-filtering to assignee=current_user)

        Features:
        - Everything from /queue/ (pagination, filtering, enrichment)
        - Plus: filters_data in response (FilterOptionsMixin)
        - Accepts filters via URL params OR POST body

        Response (paginated):
        {
          "count": 5,
          "next": "/api/annotation-items/queue/list/?page=2",
          "previous": null,
          "results": [...],
          "filters_data": {...}
        }

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationItemQueueList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_queue_list_list()
        """
        _response = self._raw_client.api_annotation_items_queue_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_annotation_items_queue_list_create(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        full_object: typing.Any,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemQueue:
        """
        Handle POST requests (for filtering) by delegating to GET.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        full_object : typing.Any

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemQueue


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_queue_list_create(
            source_id="x",
            source_type="logs",
            full_object={"key": "value"},
        )
        """
        _response = self._raw_client.api_annotation_items_queue_list_create(
            source_id=source_id,
            source_type=source_type,
            full_object=full_object,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    def api_annotation_items_queue_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Get summary statistics for current user's queue only.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_queue_summary_retrieve()
        """
        _response = self._raw_client.api_annotation_items_queue_summary_retrieve(request_options=request_options)
        return _response.data

    def api_annotation_items_summary_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Get summary statistics for annotation items.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_summary_retrieve()
        """
        _response = self._raw_client.api_annotation_items_summary_retrieve(request_options=request_options)
        return _response.data

    def api_annotation_items_summary_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        POST for filtering - delegate to GET

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_summary_create()
        """
        _response = self._raw_client.api_annotation_items_summary_create(request_options=request_options)
        return _response.data

    def api_annotation_items_summary_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_summary_update()
        """
        _response = self._raw_client.api_annotation_items_summary_update(request_options=request_options)
        return _response.data

    def api_annotation_items_summary_partial_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_annotation_items_summary_partial_update()
        """
        _response = self._raw_client.api_annotation_items_summary_partial_update(request_options=request_options)
        return _response.data

    def api_evaluators_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorListList:
        """
        ## Creating an Evaluator

        ### LLM Evaluators

        For LLM evaluators, the frontend should first fetch available evaluation forms from the
        `/eval-forms/` endpoint to get the template configuration, then fill in the form and submit.

        **Required fields:**
        - `name` (str): Display name for the evaluator
        - `evaluator_slug` (str): Unique identifier for the evaluator within the organization
        - `type` (str): llm, human_boolean, human_categorical, human_numerical, human_text
        - `configurations` (dict): Complete evaluation form configuration
        - `description` (str, optional): Description of what this evaluator does
        - `enabled` (bool, optional): Whether the evaluator is active (default: False)

        **Example request body for LLM evaluator:**
        ```json
        {
            "name": "Output Length Checker",
            "type": "llm",
            "description": "Checks if the output meets character count requirements",
            "enabled": true,
            "configurations": {
                "eval_class": "output_char_count",
                "type": "function",
                "note": "",
                "display_name": "Output Character Count",
                "description": "Evaluates the length of the output text",
                "special_fields": [],
                "required_fields": [
                    {
                        "name": "llm_output",
                        "display_name": "LLM Output",
                        "type": "textarea",
                        "description": "The output text to evaluate",
                        "required": true,
                        "default_value": null,
                        "placeholder": "",
                        "choices": [],
                        "value": null
                    }
                ],
                "inference_filters": [],
                "allow_conditions": true,
                "score_mapping": {
                    "primary_score": "output_char_count",
                    "secondary_score": null,
                    "tertiary_score": null,
                    "quaternary_score": null
                },
                "category": "custom"
            }
        }
        ```

        ### Human Annotation Evaluators

        For human annotation evaluators, specify the type and provide choices for categorical evaluators.

        **Human Boolean Evaluator:**
        ```json
        {
            "name": "Quality Check",
            "evaluator_slug": "quality_check",
            "type": "human_boolean",
            "description": "Manual quality assessment"
        }
        ```

        **Human Categorical Evaluator:**
        ```json
        {
            "name": "Sentiment Rating",
            "evaluator_slug": "sentiment_rating",
            "type": "human_categorical",
            "description": "Manual sentiment classification",
            "categorical_choices": [
                {"name": "Positive", "value": 1},
                {"name": "Neutral", "value": 0},
                {"name": "Negative", "value": -1}
            ]
        }
        ```

        **Human Numerical Evaluator:**
        ```json
        {
            "name": "Quality Score",
            "evaluator_slug": "quality_score",
            "type": "human_numerical",
            "description": "Rate quality from 1-10"
        }
        ```

        **Human Text Evaluator:**
        ```json
        {
            "name": "Feedback Comments",
            "evaluator_slug": "feedback_comments",
            "type": "human_text",
            "description": "Detailed feedback comments"
        }
        ```

        ## Response

        Returns the created evaluator with all fields populated, including auto-generated fields like
        `id`, `created_at`, `updated_at`, and `evaluator_slug`.

        ## Validation

        - For LLM evaluators: The `configurations` field is validated against the corresponding
          evaluation form schema from `EVAL_FORMS_MAP`
        - For human categorical evaluators: `categorical_choices` must be a list of objects
          with `name` and `value` fields
        - The `eval_class` in configurations must exist in the available evaluation forms

        ## Notes

        - The `organization`, `created_by`, and `updated_by` fields are automatically set from
          the authenticated user
        - Each evaluator gets a unique `evaluator_slug` within the organization
        - LLM evaluators require a valid `eval_class` that maps to an available evaluation function

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_list()
        """
        _response = self._raw_client.api_evaluators_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_evaluators_update(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_update(
            name="name",
        )
        """
        _response = self._raw_client.api_evaluators_update(
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_partial_update(
        self,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_partial_update()
        """
        _response = self._raw_client.api_evaluators_partial_update(
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_create2(
        self,
        evaluator_id: str,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        evaluator_id : str

        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_create2(
            evaluator_id="evaluator_id",
            name="name",
        )
        """
        _response = self._raw_client.api_evaluators_create2(
            evaluator_id,
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_versions_update(
        self,
        evaluator_id: str,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        evaluator_id : str

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_versions_update(
            evaluator_id="evaluator_id",
            name="name",
        )
        """
        _response = self._raw_client.api_evaluators_versions_update(
            evaluator_id,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_versions_partial_update(
        self,
        evaluator_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        evaluator_id : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_versions_partial_update(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.api_evaluators_versions_partial_update(
            evaluator_id,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_versions_create2(
        self,
        evaluator_id: str,
        version_: int,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionDetail:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        evaluator_id : str

        version_ : int

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        eval_class : typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_versions_create2(
            evaluator_id="evaluator_id",
            version_=1,
            name="name",
        )
        """
        _response = self._raw_client.api_evaluators_versions_create2(
            evaluator_id,
            version_,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            configurations=configurations,
            categorical_choices=categorical_choices,
            eval_class=eval_class,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_versions_list_list(
        self,
        evaluator_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorVersionListList:
        """
        List all versions or create new version (commit).

        GET /api/evaluators/{id}/versions/ - List all versions
        POST /api/evaluators/{id}/versions/ - Commit (create new version)

        Access control via NestedResourceMixin: org identity derived from parent evaluator.
        Superadmin: Can LIST all versions across all organizations via JWT.
        Regular users: Can only access versions in their organization.

        Parameters
        ----------
        evaluator_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorVersionListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_versions_list_list(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.api_evaluators_versions_list_list(
            evaluator_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_evaluators_versions_list_create(
        self,
        evaluator_id: str,
        *,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        score_value_type: typing.Optional[str] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        eval_class: typing.Optional[EvaluatorCreateVersionRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        created_by: typing.Optional[int] = OMIT,
        updated_by: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorCreateVersion:
        """
        Create a new version (commit). Org derived from parent evaluator.

        Parameters
        ----------
        evaluator_id : str

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        name : typing.Optional[str]

        score_value_type : typing.Optional[str]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        eval_class : typing.Optional[EvaluatorCreateVersionRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        created_by : typing.Optional[int]

        updated_by : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorCreateVersion


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_versions_list_create(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.api_evaluators_versions_list_create(
            evaluator_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            name=name,
            score_value_type=score_value_type,
            version_description=version_description,
            description=description,
            type=type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            created_by=created_by,
            updated_by=updated_by,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_versions_list_update(
        self,
        evaluator_id: str,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        evaluator_id : str

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_versions_list_update(
            evaluator_id="evaluator_id",
            name="name",
        )
        """
        _response = self._raw_client.api_evaluators_versions_list_update(
            evaluator_id,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_versions_list_partial_update(
        self,
        evaluator_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        evaluator_id : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_versions_list_partial_update(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.api_evaluators_versions_list_partial_update(
            evaluator_id,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorListList:
        """
        List evaluators for an organization.

        Superadmin: Can see all evaluators across all organizations.
        Regular users: Can only see evaluators in their organization, plus PUBLIC
            (Respan-managed) evaluators when ``is_including_public_evaluators`` is
            truthy. Public evaluators are hidden by default for back-compat.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_list_list()
        """
        _response = self._raw_client.api_evaluators_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_evaluators_list_update(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_list_update(
            name="name",
        )
        """
        _response = self._raw_client.api_evaluators_list_update(
            name=name,
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_list_partial_update(
        self,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PatchedPublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PatchedPublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_list_partial_update()
        """
        _response = self._raw_client.api_evaluators_list_partial_update(
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def api_evaluators_summary_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        GET/POST /evaluations/evaluators/summary/
        GET/POST /api/evaluators/summary/

        Get summary statistics for evaluators.

        Returns:
            {
                "total_count": 15
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_summary_retrieve()
        """
        _response = self._raw_client.api_evaluators_summary_retrieve(request_options=request_options)
        return _response.data

    def api_evaluators_summary_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_summary_update()
        """
        _response = self._raw_client.api_evaluators_summary_update(request_options=request_options)
        return _response.data

    def api_evaluators_summary_partial_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_evaluators_summary_partial_update()
        """
        _response = self._raw_client.api_evaluators_summary_partial_update(request_options=request_options)
        return _response.data

    def clickhouse_dataset_tasks_eval_result_aggs_list(
        self,
        task_tracker_id: int,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicDatasetTaskTrackerRunEvalListList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        task_tracker_id : int

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicDatasetTaskTrackerRunEvalListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_dataset_tasks_eval_result_aggs_list(
            task_tracker_id=1,
        )
        """
        _response = self._raw_client.clickhouse_dataset_tasks_eval_result_aggs_list(
            task_tracker_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def clickhouse_dataset_tasks_eval_result_aggs_create(
        self,
        task_tracker_id: int,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        evaluated_logs_count: typing.Optional[int] = OMIT,
        score: typing.Optional[float] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicDatasetTaskTrackerRunEvalList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        task_tracker_id : int

        task_id : str

        name : str

        dataset_id : str

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        evaluated_logs_count : typing.Optional[int]

        score : typing.Optional[float]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicDatasetTaskTrackerRunEvalList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_dataset_tasks_eval_result_aggs_create(
            task_tracker_id=1,
            task_id="task_id",
            name="name",
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.clickhouse_dataset_tasks_eval_result_aggs_create(
            task_tracker_id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            evaluated_logs_count=evaluated_logs_count,
            score=score,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_result_aggs_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicDatasetTaskTrackerRunEvalListList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicDatasetTaskTrackerRunEvalListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_result_aggs_list()
        """
        _response = self._raw_client.clickhouse_eval_result_aggs_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def clickhouse_eval_result_aggs_create(
        self,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        evaluated_logs_count: typing.Optional[int] = OMIT,
        score: typing.Optional[float] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicDatasetTaskTrackerRunEvalList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        task_id : str

        name : str

        dataset_id : str

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        evaluated_logs_count : typing.Optional[int]

        score : typing.Optional[float]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicDatasetTaskTrackerRunEvalList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_result_aggs_create(
            task_id="task_id",
            name="name",
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.clickhouse_eval_result_aggs_create(
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            evaluated_logs_count=evaluated_logs_count,
            score=score,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicChEvalResultListList:
        """
        Backward compatible ClickHouse-based evaluation results list view

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicChEvalResultListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_list()
        """
        _response = self._raw_client.clickhouse_eval_results_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def clickhouse_eval_results_create(
        self,
        *,
        id: str,
        created_at: dt.datetime,
        type: str,
        environment: str,
        numerical_value: float,
        string_value: str,
        is_passed: bool,
        cost: float,
        evaluator_id: str,
        log_id: str,
        prompt_id: str,
        prompt_version_number: int,
        dataset_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicChEvalResultList:
        """
        Handle POST requests the same as GET for filtering.

        Parameters
        ----------
        id : str

        created_at : dt.datetime

        type : str

        environment : str

        numerical_value : float

        string_value : str

        is_passed : bool

        cost : float

        evaluator_id : str

        log_id : str

        prompt_id : str

        prompt_version_number : int

        dataset_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicChEvalResultList


        Examples
        --------
        import datetime

        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_create(
            id="id",
            created_at=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            type="type",
            environment="environment",
            numerical_value=1.1,
            string_value="string_value",
            is_passed=True,
            cost=1.1,
            evaluator_id="evaluator_id",
            log_id="log_id",
            prompt_id="prompt_id",
            prompt_version_number=1,
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.clickhouse_eval_results_create(
            id=id,
            created_at=created_at,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_update(
        self,
        *,
        id: str,
        created_at: dt.datetime,
        type: str,
        environment: str,
        numerical_value: float,
        string_value: str,
        is_passed: bool,
        cost: float,
        evaluator_id: str,
        log_id: str,
        prompt_id: str,
        prompt_version_number: int,
        dataset_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicChEvalResultList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        created_at : dt.datetime

        type : str

        environment : str

        numerical_value : float

        string_value : str

        is_passed : bool

        cost : float

        evaluator_id : str

        log_id : str

        prompt_id : str

        prompt_version_number : int

        dataset_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicChEvalResultList


        Examples
        --------
        import datetime

        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_update(
            id="id",
            created_at=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            type="type",
            environment="environment",
            numerical_value=1.1,
            string_value="string_value",
            is_passed=True,
            cost=1.1,
            evaluator_id="evaluator_id",
            log_id="log_id",
            prompt_id="prompt_id",
            prompt_version_number=1,
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.clickhouse_eval_results_update(
            id=id,
            created_at=created_at,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        type: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        is_passed: typing.Optional[bool] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        log_id: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicChEvalResultList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        created_at : typing.Optional[dt.datetime]

        type : typing.Optional[str]

        environment : typing.Optional[str]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        is_passed : typing.Optional[bool]

        cost : typing.Optional[float]

        evaluator_id : typing.Optional[str]

        log_id : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        dataset_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicChEvalResultList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_partial_update()
        """
        _response = self._raw_client.clickhouse_eval_results_partial_update(
            id=id,
            created_at=created_at,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicEvalResultDetail:
        """
        Operates on the Postgres-based EvalResult models for update and detail point retrieval
        Synced to clickhouse automatically via evaluation.signals

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvalResultDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.clickhouse_eval_results_retrieve(id, request_options=request_options)
        return _response.data

    def clickhouse_eval_results_create2(
        self,
        id: str,
        *,
        organization: int,
        unique_organization_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        is_passed: typing.Optional[bool] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_id: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvalResultDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        organization : int

        unique_organization_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        json_value : typing.Optional[str]

        is_passed : typing.Optional[bool]

        cost : typing.Optional[float]

        evaluator_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        scorer : typing.Optional[str]

        log_id : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        dataset_id : typing.Optional[str]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvalResultDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_create2(
            id="id",
            organization=1,
        )
        """
        _response = self._raw_client.clickhouse_eval_results_create2(
            id,
            organization=organization,
            unique_organization_id=unique_organization_id,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            json_value=json_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            evaluator_slug=evaluator_slug,
            scorer=scorer,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            status=status,
            error_message=error_message,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_update2(
        self,
        id: str,
        *,
        organization: int,
        unique_organization_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        is_passed: typing.Optional[bool] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_id: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvalResultDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        organization : int

        unique_organization_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        json_value : typing.Optional[str]

        is_passed : typing.Optional[bool]

        cost : typing.Optional[float]

        evaluator_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        scorer : typing.Optional[str]

        log_id : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        dataset_id : typing.Optional[str]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvalResultDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_update2(
            id="id",
            organization=1,
        )
        """
        _response = self._raw_client.clickhouse_eval_results_update2(
            id,
            organization=organization,
            unique_organization_id=unique_organization_id,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            json_value=json_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            evaluator_slug=evaluator_slug,
            scorer=scorer,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            status=status,
            error_message=error_message,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Operates on the Postgres-based EvalResult models for update and detail point retrieval
        Synced to clickhouse automatically via evaluation.signals

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_destroy(
            id="id",
        )
        """
        _response = self._raw_client.clickhouse_eval_results_destroy(id, request_options=request_options)
        return _response.data

    def clickhouse_eval_results_partial_update2(
        self,
        id: str,
        *,
        organization: typing.Optional[int] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        is_passed: typing.Optional[bool] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_id: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvalResultUpdate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : str

        organization : typing.Optional[int]

        unique_organization_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        json_value : typing.Optional[str]

        is_passed : typing.Optional[bool]

        cost : typing.Optional[float]

        evaluator_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        scorer : typing.Optional[str]

        log_id : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        dataset_id : typing.Optional[str]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvalResultUpdate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_partial_update2(
            id="id",
        )
        """
        _response = self._raw_client.clickhouse_eval_results_partial_update2(
            id,
            organization=organization,
            unique_organization_id=unique_organization_id,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            json_value=json_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            evaluator_slug=evaluator_slug,
            scorer=scorer,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            status=status,
            error_message=error_message,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ChEvalResultList:
        """
        Summary view for evaluation results.

        Returns aggregated counts and score summaries grouped by evaluator.

        GET/POST /clickhouse/eval-results/summary/

        Response:
        {
            "summary": {
                "number_of_results": 1250
            },
            "scores": {
                "evaluator-id-1": {
                    "evaluator_id": "evaluator-id-1",
                    "evaluator_slug": "quality_v1",
                    "evaluator_name": "Response Quality",
                    "score_value_type": "numerical",
                    "avg_score": 4.2,
                    "true_count": null,
                    "false_count": null
                }
            }
        }

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChEvalResultList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_summary_retrieve()
        """
        _response = self._raw_client.clickhouse_eval_results_summary_retrieve(request_options=request_options)
        return _response.data

    def clickhouse_eval_results_summary_create(
        self,
        *,
        id: str,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        updated_at: typing.Optional[dt.datetime] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[ChEvalResultListRequestEvalClass] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChEvalResultList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        eval_result_unique_id : typing.Optional[str]

        created_at : typing.Optional[dt.datetime]

        updated_at : typing.Optional[dt.datetime]

        run_at : typing.Optional[dt.datetime]

        log_timestamp : typing.Optional[dt.datetime]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        eval_set_id : typing.Optional[str]

        evaluator_id : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        source : typing.Optional[str]

        eval_class : typing.Optional[ChEvalResultListRequestEvalClass]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[int]

        cost : typing.Optional[float]

        status : typing.Optional[str]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChEvalResultList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_summary_create(
            id="id",
        )
        """
        _response = self._raw_client.clickhouse_eval_results_summary_create(
            id=id,
            eval_result_unique_id=eval_result_unique_id,
            created_at=created_at,
            updated_at=updated_at,
            run_at=run_at,
            log_timestamp=log_timestamp,
            unique_organization_id=unique_organization_id,
            environment=environment,
            type=type,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            eval_set_id=eval_set_id,
            evaluator_id=evaluator_id,
            workflow_version_id=workflow_version_id,
            automation_id=automation_id,
            source=source,
            eval_class=eval_class,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            pipeline_run_id=pipeline_run_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            cost=cost,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_summary_update(
        self,
        *,
        id: str,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        updated_at: typing.Optional[dt.datetime] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[ChEvalResultListRequestEvalClass] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChEvalResultList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        eval_result_unique_id : typing.Optional[str]

        created_at : typing.Optional[dt.datetime]

        updated_at : typing.Optional[dt.datetime]

        run_at : typing.Optional[dt.datetime]

        log_timestamp : typing.Optional[dt.datetime]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        eval_set_id : typing.Optional[str]

        evaluator_id : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        source : typing.Optional[str]

        eval_class : typing.Optional[ChEvalResultListRequestEvalClass]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[int]

        cost : typing.Optional[float]

        status : typing.Optional[str]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChEvalResultList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_summary_update(
            id="id",
        )
        """
        _response = self._raw_client.clickhouse_eval_results_summary_update(
            id=id,
            eval_result_unique_id=eval_result_unique_id,
            created_at=created_at,
            updated_at=updated_at,
            run_at=run_at,
            log_timestamp=log_timestamp,
            unique_organization_id=unique_organization_id,
            environment=environment,
            type=type,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            eval_set_id=eval_set_id,
            evaluator_id=evaluator_id,
            workflow_version_id=workflow_version_id,
            automation_id=automation_id,
            source=source,
            eval_class=eval_class,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            pipeline_run_id=pipeline_run_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            cost=cost,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_results_summary_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        updated_at: typing.Optional[dt.datetime] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[PatchedChEvalResultListRequestEvalClass] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChEvalResultList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        created_at : typing.Optional[dt.datetime]

        updated_at : typing.Optional[dt.datetime]

        run_at : typing.Optional[dt.datetime]

        log_timestamp : typing.Optional[dt.datetime]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        eval_set_id : typing.Optional[str]

        evaluator_id : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        source : typing.Optional[str]

        eval_class : typing.Optional[PatchedChEvalResultListRequestEvalClass]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[int]

        cost : typing.Optional[float]

        status : typing.Optional[str]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChEvalResultList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_results_summary_partial_update()
        """
        _response = self._raw_client.clickhouse_eval_results_summary_partial_update(
            id=id,
            eval_result_unique_id=eval_result_unique_id,
            created_at=created_at,
            updated_at=updated_at,
            run_at=run_at,
            log_timestamp=log_timestamp,
            unique_organization_id=unique_organization_id,
            environment=environment,
            type=type,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            eval_set_id=eval_set_id,
            evaluator_id=evaluator_id,
            workflow_version_id=workflow_version_id,
            automation_id=automation_id,
            source=source,
            eval_class=eval_class,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            pipeline_run_id=pipeline_run_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            cost=cost,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            request_options=request_options,
        )
        return _response.data

    def clickhouse_eval_sets_eval_result_aggs_list(
        self,
        dataset_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicDatasetTaskTrackerRunEvalListList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        dataset_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicDatasetTaskTrackerRunEvalListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_sets_eval_result_aggs_list(
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.clickhouse_eval_sets_eval_result_aggs_list(
            dataset_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def clickhouse_eval_sets_eval_result_aggs_create(
        self,
        dataset_id_: str,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        evaluated_logs_count: typing.Optional[int] = OMIT,
        score: typing.Optional[float] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicDatasetTaskTrackerRunEvalList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        dataset_id_ : str

        task_id : str

        name : str

        dataset_id : str

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        evaluated_logs_count : typing.Optional[int]

        score : typing.Optional[float]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicDatasetTaskTrackerRunEvalList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.clickhouse_eval_sets_eval_result_aggs_create(
            dataset_id_="dataset_id",
            task_id="task_id",
            name="name",
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.clickhouse_eval_sets_eval_result_aggs_create(
            dataset_id_,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            evaluated_logs_count=evaluated_logs_count,
            score=score,
            request_options=request_options,
        )
        return _response.data

    def list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorListList:
        """
        ## Creating an Evaluator

        ### LLM Evaluators

        For LLM evaluators, the frontend should first fetch available evaluation forms from the
        `/eval-forms/` endpoint to get the template configuration, then fill in the form and submit.

        **Required fields:**
        - `name` (str): Display name for the evaluator
        - `evaluator_slug` (str): Unique identifier for the evaluator within the organization
        - `type` (str): llm, human_boolean, human_categorical, human_numerical, human_text
        - `configurations` (dict): Complete evaluation form configuration
        - `description` (str, optional): Description of what this evaluator does
        - `enabled` (bool, optional): Whether the evaluator is active (default: False)

        **Example request body for LLM evaluator:**
        ```json
        {
            "name": "Output Length Checker",
            "type": "llm",
            "description": "Checks if the output meets character count requirements",
            "enabled": true,
            "configurations": {
                "eval_class": "output_char_count",
                "type": "function",
                "note": "",
                "display_name": "Output Character Count",
                "description": "Evaluates the length of the output text",
                "special_fields": [],
                "required_fields": [
                    {
                        "name": "llm_output",
                        "display_name": "LLM Output",
                        "type": "textarea",
                        "description": "The output text to evaluate",
                        "required": true,
                        "default_value": null,
                        "placeholder": "",
                        "choices": [],
                        "value": null
                    }
                ],
                "inference_filters": [],
                "allow_conditions": true,
                "score_mapping": {
                    "primary_score": "output_char_count",
                    "secondary_score": null,
                    "tertiary_score": null,
                    "quaternary_score": null
                },
                "category": "custom"
            }
        }
        ```

        ### Human Annotation Evaluators

        For human annotation evaluators, specify the type and provide choices for categorical evaluators.

        **Human Boolean Evaluator:**
        ```json
        {
            "name": "Quality Check",
            "evaluator_slug": "quality_check",
            "type": "human_boolean",
            "description": "Manual quality assessment"
        }
        ```

        **Human Categorical Evaluator:**
        ```json
        {
            "name": "Sentiment Rating",
            "evaluator_slug": "sentiment_rating",
            "type": "human_categorical",
            "description": "Manual sentiment classification",
            "categorical_choices": [
                {"name": "Positive", "value": 1},
                {"name": "Neutral", "value": 0},
                {"name": "Negative", "value": -1}
            ]
        }
        ```

        **Human Numerical Evaluator:**
        ```json
        {
            "name": "Quality Score",
            "evaluator_slug": "quality_score",
            "type": "human_numerical",
            "description": "Rate quality from 1-10"
        }
        ```

        **Human Text Evaluator:**
        ```json
        {
            "name": "Feedback Comments",
            "evaluator_slug": "feedback_comments",
            "type": "human_text",
            "description": "Detailed feedback comments"
        }
        ```

        ## Response

        Returns the created evaluator with all fields populated, including auto-generated fields like
        `id`, `created_at`, `updated_at`, and `evaluator_slug`.

        ## Validation

        - For LLM evaluators: The `configurations` field is validated against the corresponding
          evaluation form schema from `EVAL_FORMS_MAP`
        - For human categorical evaluators: `categorical_choices` must be a list of objects
          with `name` and `value` fields
        - The `eval_class` in configurations must exist in the available evaluation forms

        ## Notes

        - The `organization`, `created_by`, and `updated_by` fields are automatically set from
          the authenticated user
        - Each evaluator gets a unique `evaluator_slug` within the organization
        - LLM evaluators require a valid `eval_class` that maps to an available evaluation function

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.list()
        """
        _response = self._raw_client.list(page=page, page_size=page_size, request_options=request_options)
        return _response.data

    def create(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.create(
            name="name",
        )
        """
        _response = self._raw_client.create(
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def update(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.update(
            name="name",
        )
        """
        _response = self._raw_client.update(
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def partial_update(
        self,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.partial_update()
        """
        _response = self._raw_client.partial_update(
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def retrieve(
        self, evaluator_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicEvaluatorDetail:
        """
        Get, update, or delete an evaluator's draft version.

        GET /api/evaluators/{evaluator_id}/ - Get draft version (is_read_only=False)
        PATCH /api/evaluators/{evaluator_id}/ - Update draft version
        DELETE /api/evaluators/{evaluator_id}/ - Delete ALL versions

        Superadmin: Can READ any evaluator across all organizations via JWT.
                    Cannot WRITE via JWT - must use API key for write operations.
        Regular users: Can only access evaluators in their organization.

        NOTE: Queryset filters by is_read_only=False, ensuring unique lookup per evaluator_id.
        This allows DRF's standard get_object() to work without manual overrides.
        Delete removes ALL versions of the evaluator.

        Defense-in-depth:
        - SuperAdminMixin: Queryset routing + JWT write protection + object-level ownership

        Parameters
        ----------
        evaluator_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.retrieve(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.retrieve(evaluator_id, request_options=request_options)
        return _response.data

    def create2(
        self,
        evaluator_id: str,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        evaluator_id : str

        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.create2(
            evaluator_id="evaluator_id",
            name="name",
        )
        """
        _response = self._raw_client.create2(
            evaluator_id,
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def update2(
        self,
        evaluator_id: str,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorUpdate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        evaluator_id : str

        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorUpdate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.update2(
            evaluator_id="evaluator_id",
            name="name",
        )
        """
        _response = self._raw_client.update2(
            evaluator_id,
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def destroy(self, evaluator_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete ALL versions of the evaluator.

        Uses SuperAdminMixin's queryset routing for org filtering.
        Cross-org JWT write protection is enforced automatically by ObjectOwnershipPermission
        in get_object() via check_object_permissions().

        Parameters
        ----------
        evaluator_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.destroy(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.destroy(evaluator_id, request_options=request_options)
        return _response.data

    def partial_update2(
        self,
        evaluator_id: str,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorUpdate:
        """
        Update the draft version (queryset already filters is_read_only=False).

        Parameters
        ----------
        evaluator_id : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorUpdate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.partial_update2(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.partial_update2(
            evaluator_id,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def annotations_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationList:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_list()
        """
        _response = self._raw_client.annotations_list(page=page, page_size=page_size, request_options=request_options)
        return _response.data

    def annotations_create(
        self,
        *,
        annotation_config: str,
        organization: int,
        id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        annotation_config : str

        organization : int

        id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_create(
            annotation_config="annotation_config",
            organization=1,
        )
        """
        _response = self._raw_client.annotations_create(
            annotation_config=annotation_config,
            organization=organization,
            id=id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            log=log,
            testset_row=testset_row,
            request_options=request_options,
        )
        return _response.data

    def annotations_retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.annotations_retrieve(id, request_options=request_options)
        return _response.data

    def annotations_update(
        self,
        id_: str,
        *,
        annotation_config: str,
        organization: int,
        id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        id_ : str

        annotation_config : str

        organization : int

        id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_update(
            id_="id",
            annotation_config="annotation_config",
            organization=1,
        )
        """
        _response = self._raw_client.annotations_update(
            id_,
            annotation_config=annotation_config,
            organization=organization,
            id=id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            log=log,
            testset_row=testset_row,
            request_options=request_options,
        )
        return _response.data

    def annotations_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_destroy(
            id="id",
        )
        """
        _response = self._raw_client.annotations_destroy(id, request_options=request_options)
        return _response.data

    def annotations_partial_update(
        self,
        id: str,
        *,
        patched_annotation_request_id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        id : str

        patched_annotation_request_id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_partial_update(
            id="id",
        )
        """
        _response = self._raw_client.annotations_partial_update(
            id,
            patched_annotation_request_id=patched_annotation_request_id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            annotation_config=annotation_config,
            log=log,
            testset_row=testset_row,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def annotations_in_log_list(
        self,
        log_id: int,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationList:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        log_id : int

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_in_log_list(
            log_id=1,
        )
        """
        _response = self._raw_client.annotations_in_log_list(
            log_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def annotations_in_log_create(
        self,
        log_id: int,
        *,
        annotation_config: str,
        organization: int,
        id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        log_id : int

        annotation_config : str

        organization : int

        id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_in_log_create(
            log_id=1,
            annotation_config="annotation_config",
            organization=1,
        )
        """
        _response = self._raw_client.annotations_in_log_create(
            log_id,
            annotation_config=annotation_config,
            organization=organization,
            id=id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            log=log,
            testset_row=testset_row,
            request_options=request_options,
        )
        return _response.data

    def annotations_in_testset_row_list(
        self,
        testset_row_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationList:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        testset_row_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_in_testset_row_list(
            testset_row_id="testset_row_id",
        )
        """
        _response = self._raw_client.annotations_in_testset_row_list(
            testset_row_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def annotations_in_testset_row_create(
        self,
        testset_row_id: str,
        *,
        annotation_config: str,
        organization: int,
        id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        testset_row_id : str

        annotation_config : str

        organization : int

        id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.annotations_in_testset_row_create(
            testset_row_id="testset_row_id",
            annotation_config="annotation_config",
            organization=1,
        )
        """
        _response = self._raw_client.annotations_in_testset_row_create(
            testset_row_id,
            annotation_config=annotation_config,
            organization=organization,
            id=id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            log=log,
            testset_row=testset_row,
            request_options=request_options,
        )
        return _response.data

    def dataset_evaluation_tasks_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetTaskTrackerRunEvalListList:
        """
        View for creating new dataset evaluation tasks.

        Args:
            dataset_id: str, The ID of the dataset to run evaluation on
            evaluator_ids: str[], The IDs of the evaluators to run (preferred)
            evaluator_slugs: str[], Deprecated alias for evaluator_ids (backward compat)

        Returns:
            GET: A list of created tasks
            POST: The created eval task if successful, otherwise a dictionary of errors

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetTaskTrackerRunEvalListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_evaluation_tasks_list()
        """
        _response = self._raw_client.dataset_evaluation_tasks_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def dataset_evaluation_tasks_create(
        self,
        *,
        dataset_id: str,
        evaluator_slug: str,
        unique_organization_id: str,
        experiment_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunEvaluationCreate:
        """
        Create a new dataset evaluation task.

        Accepts both `evaluator_ids` (preferred) and `evaluator_slugs` (deprecated alias).

        Optional: If experiment_id is provided, the experiment's evaluator_slugs
        will be updated to include the new evaluators, making them appear as
        columns in the experiment UI.

        Parameters
        ----------
        dataset_id : str

        evaluator_slug : str

        unique_organization_id : str

        experiment_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvaluationCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_evaluation_tasks_create(
            dataset_id="dataset_id",
            evaluator_slug="evaluator_slug",
            unique_organization_id="unique_organization_id",
        )
        """
        _response = self._raw_client.dataset_evaluation_tasks_create(
            dataset_id=dataset_id,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            experiment_id=experiment_id,
            request_options=request_options,
        )
        return _response.data

    def dataset_evaluation_tasks_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetTaskTrackerRunEvaluationDetail:
        """
        Detail view for individual dataset evaluation tasks.
        Similar to EvalSetDetailView but for dataset evaluation tasks.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvaluationDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_evaluation_tasks_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.dataset_evaluation_tasks_retrieve(id, request_options=request_options)
        return _response.data

    def dataset_evaluation_tasks_update(
        self,
        id: int,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunEvaluationDetail:
        """
        Detail view for individual dataset evaluation tasks.
        Similar to EvalSetDetailView but for dataset evaluation tasks.

        Parameters
        ----------
        id : int

        task_id : str

        name : str

        dataset_id : str

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        metadata : typing.Optional[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvaluationDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_evaluation_tasks_update(
            id=1,
            task_id="task_id",
            name="name",
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.dataset_evaluation_tasks_update(
            id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def dataset_evaluation_tasks_destroy(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Cancel/delete dataset evaluation task.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_evaluation_tasks_destroy(
            id=1,
        )
        """
        _response = self._raw_client.dataset_evaluation_tasks_destroy(id, request_options=request_options)
        return _response.data

    def dataset_evaluation_tasks_partial_update(
        self,
        id: int,
        *,
        task_id: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunEvaluationDetail:
        """
        Update dataset evaluation task metadata.

        Parameters
        ----------
        id : int

        task_id : typing.Optional[str]

        name : typing.Optional[str]

        dataset_id : typing.Optional[str]

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        metadata : typing.Optional[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvaluationDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_evaluation_tasks_partial_update(
            id=1,
        )
        """
        _response = self._raw_client.dataset_evaluation_tasks_partial_update(
            id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def dataset_evaluation_tasks_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetTaskTrackerRunEvalListList:
        """
        List view for dataset evaluation tasks with complex filtering support.
        Similar to EvalSetsListView but for dataset evaluation tasks.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetTaskTrackerRunEvalListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_evaluation_tasks_list_list()
        """
        _response = self._raw_client.dataset_evaluation_tasks_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def dataset_evaluation_tasks_list_create(
        self,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        result_count: typing.Optional[int] = OMIT,
        primary_score_avg: typing.Optional[float] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunEvalList:
        """
        Handle POST requests for complex filtering.

        Parameters
        ----------
        task_id : str

        name : str

        dataset_id : str

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        result_count : typing.Optional[int]

        primary_score_avg : typing.Optional[float]

        run_at : typing.Optional[dt.datetime]

        evaluator_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvalList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_evaluation_tasks_list_create(
            task_id="task_id",
            name="name",
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.dataset_evaluation_tasks_list_create(
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            result_count=result_count,
            primary_score_avg=primary_score_avg,
            run_at=run_at,
            evaluator_id=evaluator_id,
            request_options=request_options,
        )
        return _response.data

    def dataset_run_logs_tasks_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetTaskTrackerRunLogsListList:
        """
        View for creating new dataset run logs tasks.
        Similar to EvalSetsView but for dataset run logs tasks.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetTaskTrackerRunLogsListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_run_logs_tasks_list()
        """
        _response = self._raw_client.dataset_run_logs_tasks_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def dataset_run_logs_tasks_create(
        self, *, dataset_id: str, unique_organization_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetTaskTrackerRunLogsCreate:
        """
        Create a new dataset run logs task.

        Parameters
        ----------
        dataset_id : str

        unique_organization_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_run_logs_tasks_create(
            dataset_id="dataset_id",
            unique_organization_id="unique_organization_id",
        )
        """
        _response = self._raw_client.dataset_run_logs_tasks_create(
            dataset_id=dataset_id, unique_organization_id=unique_organization_id, request_options=request_options
        )
        return _response.data

    def dataset_run_logs_tasks_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetTaskTrackerRunLogsDetail:
        """
        Detail view for individual dataset run logs tasks.
        Similar to EvalSetDetailView but for dataset run logs tasks.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_run_logs_tasks_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.dataset_run_logs_tasks_retrieve(id, request_options=request_options)
        return _response.data

    def dataset_run_logs_tasks_update(
        self,
        id: str,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunLogsDetail:
        """
        Detail view for individual dataset run logs tasks.
        Similar to EvalSetDetailView but for dataset run logs tasks.

        Parameters
        ----------
        id : str

        task_id : str

        name : str

        dataset_id : str

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        metadata : typing.Optional[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_run_logs_tasks_update(
            id="id",
            task_id="task_id",
            name="name",
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.dataset_run_logs_tasks_update(
            id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def dataset_run_logs_tasks_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Cancel/delete dataset run logs task.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_run_logs_tasks_destroy(
            id="id",
        )
        """
        _response = self._raw_client.dataset_run_logs_tasks_destroy(id, request_options=request_options)
        return _response.data

    def dataset_run_logs_tasks_partial_update(
        self,
        id: str,
        *,
        task_id: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunLogsDetail:
        """
        Update dataset run logs task metadata.

        Parameters
        ----------
        id : str

        task_id : typing.Optional[str]

        name : typing.Optional[str]

        dataset_id : typing.Optional[str]

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        metadata : typing.Optional[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_run_logs_tasks_partial_update(
            id="id",
        )
        """
        _response = self._raw_client.dataset_run_logs_tasks_partial_update(
            id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    def dataset_run_logs_tasks_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetTaskTrackerRunLogsListList:
        """
        List view for dataset LLM inference tasks with complex filtering support.
        Similar to EvalSetsListView but for dataset LLM inference tasks.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetTaskTrackerRunLogsListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_run_logs_tasks_list_list()
        """
        _response = self._raw_client.dataset_run_logs_tasks_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def dataset_run_logs_tasks_list_create(
        self,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunLogsList:
        """
        Handle POST requests for complex filtering.

        Parameters
        ----------
        task_id : str

        name : str

        dataset_id : str

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        run_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.dataset_run_logs_tasks_list_create(
            task_id="task_id",
            name="name",
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.dataset_run_logs_tasks_list_create(
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            run_at=run_at,
            request_options=request_options,
        )
        return _response.data

    def datasets_logs_list(
        self,
        dataset_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedChDatasetLogList:
        """
        Import existing logs into dataset based on filter criteria

        Endpoints:
            POST   /evaluations/datasets/{dataset_id}/logs/ - Import existing logs to dataset asynchronously (legacy)
            POST   /api/datasets/{dataset_id}/logs/import/ - Import existing logs to dataset asynchronously
            DELETE /api/datasets/{dataset_id}/logs/import/ - Remove logs from dataset asynchronously

        Args (POST body):
            - start_time (string, required, ISO 8601)
            - end_time (string, required, ISO 8601)
            - filters (object, optional; key name is "filters")
            - sampling_percentage (integer, optional, default 100)

        Returns (POST 200):
            { "message": "Logs are being imported to dataset in the background" }

        Args (DELETE body):
            - is_deleting_all_logs (boolean, required if filters not provided)
            - filters (object, required unless is_deleting_all_logs = true)

        Returns (DELETE 200):
            { "message": "Logs are being removed from dataset in the background" }

        Access Control:
            NestedResourceMixin handles superadmin-aware parent access:
            - Superadmin + JWT + READ: Can view any dataset's logs
            - Superadmin + JWT + WRITE: Blocked (can't import/delete logs in other orgs via JWT)
            - Superadmin + API key: Can import/delete logs in any dataset
            - Regular user: Can only access their org's datasets

        Parameters
        ----------
        dataset_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedChDatasetLogList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.datasets_logs_list(
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.datasets_logs_list(
            dataset_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def datasets_logs_create(
        self,
        dataset_id: str,
        *,
        start_time: dt.datetime,
        end_time: dt.datetime,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        sampling_percentage: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetLogsImportResponse:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        dataset_id : str

        start_time : dt.datetime

        end_time : dt.datetime

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter parameters keyed by metric name.

        sampling_percentage : typing.Optional[int]
            Percent of logs to import (1-100).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogsImportResponse


        Examples
        --------
        import datetime

        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.datasets_logs_create(
            dataset_id="dataset_id",
            start_time=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            end_time=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.datasets_logs_create(
            dataset_id,
            start_time=start_time,
            end_time=end_time,
            filters=filters,
            sampling_percentage=sampling_percentage,
            request_options=request_options,
        )
        return _response.data

    def datasets_logs_update(
        self,
        dataset_id_: str,
        *,
        id: str,
        organization_id: str,
        unique_organization_id: typing.Optional[str] = OMIT,
        organization_name: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        completion_messages: typing.Optional[typing.Any] = OMIT,
        input: typing.Optional[str] = OMIT,
        output: typing.Optional[str] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        temperature: typing.Optional[float] = OMIT,
        max_tokens: typing.Optional[int] = OMIT,
        top_p: typing.Optional[float] = OMIT,
        frequency_penalty: typing.Optional[float] = OMIT,
        presence_penalty: typing.Optional[float] = OMIT,
        stop: typing.Optional[str] = OMIT,
        response_format: typing.Optional[typing.Any] = OMIT,
        matched_meter_ids: typing.Optional[typing.Sequence[typing.Any]] = OMIT,
        unit_prices: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        component_costs: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        original_copy_unique_id: typing.Optional[str] = OMIT,
        comparison_key: typing.Optional[str] = OMIT,
        expected_output: typing.Optional[str] = OMIT,
        custom_identifier: typing.Optional[str] = OMIT,
        group_identifier: typing.Optional[str] = OMIT,
        blurred: typing.Optional[bool] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        timestamp: typing.Optional[dt.datetime] = OMIT,
        load_balance_group_id: typing.Optional[str] = OMIT,
        prompt_tokens: typing.Optional[int] = OMIT,
        completion_tokens: typing.Optional[int] = OMIT,
        prompt_cache_hit_tokens: typing.Optional[int] = OMIT,
        prompt_cache_creation_tokens: typing.Optional[int] = OMIT,
        reasoning_tokens: typing.Optional[int] = OMIT,
        total_request_tokens: typing.Optional[int] = OMIT,
        is_token_count_estimated: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        llm_gateway_markup_rate: typing.Optional[float] = OMIT,
        service_tier: typing.Optional[str] = OMIT,
        model_discount: typing.Optional[float] = OMIT,
        pricing_tier: typing.Optional[str] = OMIT,
        audio_input_file: typing.Optional[str] = OMIT,
        audio_output_file: typing.Optional[str] = OMIT,
        organization_key_id: typing.Optional[str] = OMIT,
        user_email: typing.Optional[str] = OMIT,
        model: typing.Optional[str] = OMIT,
        provider_id: typing.Optional[str] = OMIT,
        category: typing.Optional[str] = OMIT,
        properties: typing.Optional[str] = OMIT,
        cache_bit: typing.Optional[int] = OMIT,
        cache_miss_bit: typing.Optional[int] = OMIT,
        cache_key: typing.Optional[str] = OMIT,
        latency: typing.Optional[float] = OMIT,
        tokens_per_second: typing.Optional[float] = OMIT,
        time_to_first_token: typing.Optional[float] = OMIT,
        routing_time: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        has_tool_calls: typing.Optional[bool] = OMIT,
        status_code: typing.Optional[int] = OMIT,
        log_method: typing.Optional[str] = OMIT,
        log_type: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        stream: typing.Optional[bool] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        customer_email: typing.Optional[str] = OMIT,
        customer_name: typing.Optional[str] = OMIT,
        customer_user_unique_id: typing.Optional[str] = OMIT,
        used_custom_credential: typing.Optional[bool] = OMIT,
        deployment_name: typing.Optional[str] = OMIT,
        deployment_id: typing.Optional[str] = OMIT,
        prompt_name: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        system_text: typing.Optional[str] = OMIT,
        prompt_text: typing.Optional[str] = OMIT,
        completion_text: typing.Optional[str] = OMIT,
        prompt_message_count: typing.Optional[int] = OMIT,
        completion_message_count: typing.Optional[int] = OMIT,
        trace_unique_id: typing.Optional[str] = OMIT,
        span_unique_id: typing.Optional[str] = OMIT,
        span_name: typing.Optional[str] = OMIT,
        span_parent_id: typing.Optional[str] = OMIT,
        span_workflow_name: typing.Optional[str] = OMIT,
        session_identifier: typing.Optional[str] = OMIT,
        span_links: typing.Optional[str] = OMIT,
        trace_group_identifier: typing.Optional[str] = OMIT,
        thread_identifier: typing.Optional[str] = OMIT,
        thread_unique_id: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        period_start: typing.Optional[dt.datetime] = OMIT,
        period_end: typing.Optional[dt.datetime] = OMIT,
        unique_id: typing.Optional[str] = OMIT,
        respan_gateway_request_id: typing.Optional[str] = OMIT,
        full_text: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChDatasetLog:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        dataset_id_ : str

        id : str

        organization_id : str

        unique_organization_id : typing.Optional[str]

        organization_name : typing.Optional[str]

        error_message : typing.Optional[str]

        completion_messages : typing.Optional[typing.Any]

        input : typing.Optional[str]

        output : typing.Optional[str]

        variables : typing.Optional[typing.Any]

        temperature : typing.Optional[float]

        max_tokens : typing.Optional[int]

        top_p : typing.Optional[float]

        frequency_penalty : typing.Optional[float]

        presence_penalty : typing.Optional[float]

        stop : typing.Optional[str]

        response_format : typing.Optional[typing.Any]

        matched_meter_ids : typing.Optional[typing.Sequence[typing.Any]]

        unit_prices : typing.Optional[typing.Dict[str, typing.Any]]

        component_costs : typing.Optional[typing.Dict[str, typing.Any]]

        dataset_id : typing.Optional[str]

        original_copy_unique_id : typing.Optional[str]

        comparison_key : typing.Optional[str]

        expected_output : typing.Optional[str]

        custom_identifier : typing.Optional[str]

        group_identifier : typing.Optional[str]

        blurred : typing.Optional[bool]

        start_time : typing.Optional[dt.datetime]

        timestamp : typing.Optional[dt.datetime]

        load_balance_group_id : typing.Optional[str]

        prompt_tokens : typing.Optional[int]

        completion_tokens : typing.Optional[int]

        prompt_cache_hit_tokens : typing.Optional[int]

        prompt_cache_creation_tokens : typing.Optional[int]

        reasoning_tokens : typing.Optional[int]

        total_request_tokens : typing.Optional[int]

        is_token_count_estimated : typing.Optional[int]

        cost : typing.Optional[float]

        llm_gateway_markup_rate : typing.Optional[float]

        service_tier : typing.Optional[str]

        model_discount : typing.Optional[float]

        pricing_tier : typing.Optional[str]

        audio_input_file : typing.Optional[str]

        audio_output_file : typing.Optional[str]

        organization_key_id : typing.Optional[str]

        user_email : typing.Optional[str]

        model : typing.Optional[str]

        provider_id : typing.Optional[str]

        category : typing.Optional[str]

        properties : typing.Optional[str]

        cache_bit : typing.Optional[int]

        cache_miss_bit : typing.Optional[int]

        cache_key : typing.Optional[str]

        latency : typing.Optional[float]

        tokens_per_second : typing.Optional[float]

        time_to_first_token : typing.Optional[float]

        routing_time : typing.Optional[float]

        status : typing.Optional[str]

        has_tool_calls : typing.Optional[bool]

        status_code : typing.Optional[int]

        log_method : typing.Optional[str]

        log_type : typing.Optional[str]

        environment : typing.Optional[str]

        stream : typing.Optional[bool]

        evaluation_identifier : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        customer_email : typing.Optional[str]

        customer_name : typing.Optional[str]

        customer_user_unique_id : typing.Optional[str]

        used_custom_credential : typing.Optional[bool]

        deployment_name : typing.Optional[str]

        deployment_id : typing.Optional[str]

        prompt_name : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        system_text : typing.Optional[str]

        prompt_text : typing.Optional[str]

        completion_text : typing.Optional[str]

        prompt_message_count : typing.Optional[int]

        completion_message_count : typing.Optional[int]

        trace_unique_id : typing.Optional[str]

        span_unique_id : typing.Optional[str]

        span_name : typing.Optional[str]

        span_parent_id : typing.Optional[str]

        span_workflow_name : typing.Optional[str]

        session_identifier : typing.Optional[str]

        span_links : typing.Optional[str]

        trace_group_identifier : typing.Optional[str]

        thread_identifier : typing.Optional[str]

        thread_unique_id : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        period_start : typing.Optional[dt.datetime]

        period_end : typing.Optional[dt.datetime]

        unique_id : typing.Optional[str]

        respan_gateway_request_id : typing.Optional[str]

        full_text : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChDatasetLog


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.datasets_logs_update(
            dataset_id_="dataset_id",
            id="id",
            organization_id="organization_id",
        )
        """
        _response = self._raw_client.datasets_logs_update(
            dataset_id_,
            id=id,
            organization_id=organization_id,
            unique_organization_id=unique_organization_id,
            organization_name=organization_name,
            error_message=error_message,
            completion_messages=completion_messages,
            input=input,
            output=output,
            variables=variables,
            temperature=temperature,
            max_tokens=max_tokens,
            top_p=top_p,
            frequency_penalty=frequency_penalty,
            presence_penalty=presence_penalty,
            stop=stop,
            response_format=response_format,
            matched_meter_ids=matched_meter_ids,
            unit_prices=unit_prices,
            component_costs=component_costs,
            dataset_id=dataset_id,
            original_copy_unique_id=original_copy_unique_id,
            comparison_key=comparison_key,
            expected_output=expected_output,
            custom_identifier=custom_identifier,
            group_identifier=group_identifier,
            blurred=blurred,
            start_time=start_time,
            timestamp=timestamp,
            load_balance_group_id=load_balance_group_id,
            prompt_tokens=prompt_tokens,
            completion_tokens=completion_tokens,
            prompt_cache_hit_tokens=prompt_cache_hit_tokens,
            prompt_cache_creation_tokens=prompt_cache_creation_tokens,
            reasoning_tokens=reasoning_tokens,
            total_request_tokens=total_request_tokens,
            is_token_count_estimated=is_token_count_estimated,
            cost=cost,
            llm_gateway_markup_rate=llm_gateway_markup_rate,
            service_tier=service_tier,
            model_discount=model_discount,
            pricing_tier=pricing_tier,
            audio_input_file=audio_input_file,
            audio_output_file=audio_output_file,
            organization_key_id=organization_key_id,
            user_email=user_email,
            model=model,
            provider_id=provider_id,
            category=category,
            properties=properties,
            cache_bit=cache_bit,
            cache_miss_bit=cache_miss_bit,
            cache_key=cache_key,
            latency=latency,
            tokens_per_second=tokens_per_second,
            time_to_first_token=time_to_first_token,
            routing_time=routing_time,
            status=status,
            has_tool_calls=has_tool_calls,
            status_code=status_code,
            log_method=log_method,
            log_type=log_type,
            environment=environment,
            stream=stream,
            evaluation_identifier=evaluation_identifier,
            customer_identifier=customer_identifier,
            customer_email=customer_email,
            customer_name=customer_name,
            customer_user_unique_id=customer_user_unique_id,
            used_custom_credential=used_custom_credential,
            deployment_name=deployment_name,
            deployment_id=deployment_id,
            prompt_name=prompt_name,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            system_text=system_text,
            prompt_text=prompt_text,
            completion_text=completion_text,
            prompt_message_count=prompt_message_count,
            completion_message_count=completion_message_count,
            trace_unique_id=trace_unique_id,
            span_unique_id=span_unique_id,
            span_name=span_name,
            span_parent_id=span_parent_id,
            span_workflow_name=span_workflow_name,
            session_identifier=session_identifier,
            span_links=span_links,
            trace_group_identifier=trace_group_identifier,
            thread_identifier=thread_identifier,
            thread_unique_id=thread_unique_id,
            storage_object_key=storage_object_key,
            period_start=period_start,
            period_end=period_end,
            unique_id=unique_id,
            respan_gateway_request_id=respan_gateway_request_id,
            full_text=full_text,
            request_options=request_options,
        )
        return _response.data

    def datasets_logs_destroy(
        self, dataset_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetLogsImportResponse:
        """
        Import existing logs into dataset based on filter criteria

        Endpoints:
            POST   /evaluations/datasets/{dataset_id}/logs/ - Import existing logs to dataset asynchronously (legacy)
            POST   /api/datasets/{dataset_id}/logs/import/ - Import existing logs to dataset asynchronously
            DELETE /api/datasets/{dataset_id}/logs/import/ - Remove logs from dataset asynchronously

        Args (POST body):
            - start_time (string, required, ISO 8601)
            - end_time (string, required, ISO 8601)
            - filters (object, optional; key name is "filters")
            - sampling_percentage (integer, optional, default 100)

        Returns (POST 200):
            { "message": "Logs are being imported to dataset in the background" }

        Args (DELETE body):
            - is_deleting_all_logs (boolean, required if filters not provided)
            - filters (object, required unless is_deleting_all_logs = true)

        Returns (DELETE 200):
            { "message": "Logs are being removed from dataset in the background" }

        Access Control:
            NestedResourceMixin handles superadmin-aware parent access:
            - Superadmin + JWT + READ: Can view any dataset's logs
            - Superadmin + JWT + WRITE: Blocked (can't import/delete logs in other orgs via JWT)
            - Superadmin + API key: Can import/delete logs in any dataset
            - Regular user: Can only access their org's datasets

        Parameters
        ----------
        dataset_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogsImportResponse


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.datasets_logs_destroy(
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.datasets_logs_destroy(dataset_id, request_options=request_options)
        return _response.data

    def datasets_logs_partial_update(
        self,
        dataset_id_: str,
        *,
        id: typing.Optional[str] = OMIT,
        organization_id: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        organization_name: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        completion_messages: typing.Optional[typing.Any] = OMIT,
        input: typing.Optional[str] = OMIT,
        output: typing.Optional[str] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        temperature: typing.Optional[float] = OMIT,
        max_tokens: typing.Optional[int] = OMIT,
        top_p: typing.Optional[float] = OMIT,
        frequency_penalty: typing.Optional[float] = OMIT,
        presence_penalty: typing.Optional[float] = OMIT,
        stop: typing.Optional[str] = OMIT,
        response_format: typing.Optional[typing.Any] = OMIT,
        matched_meter_ids: typing.Optional[typing.Sequence[typing.Any]] = OMIT,
        unit_prices: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        component_costs: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        original_copy_unique_id: typing.Optional[str] = OMIT,
        comparison_key: typing.Optional[str] = OMIT,
        expected_output: typing.Optional[str] = OMIT,
        custom_identifier: typing.Optional[str] = OMIT,
        group_identifier: typing.Optional[str] = OMIT,
        blurred: typing.Optional[bool] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        timestamp: typing.Optional[dt.datetime] = OMIT,
        load_balance_group_id: typing.Optional[str] = OMIT,
        prompt_tokens: typing.Optional[int] = OMIT,
        completion_tokens: typing.Optional[int] = OMIT,
        prompt_cache_hit_tokens: typing.Optional[int] = OMIT,
        prompt_cache_creation_tokens: typing.Optional[int] = OMIT,
        reasoning_tokens: typing.Optional[int] = OMIT,
        total_request_tokens: typing.Optional[int] = OMIT,
        is_token_count_estimated: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        llm_gateway_markup_rate: typing.Optional[float] = OMIT,
        service_tier: typing.Optional[str] = OMIT,
        model_discount: typing.Optional[float] = OMIT,
        pricing_tier: typing.Optional[str] = OMIT,
        audio_input_file: typing.Optional[str] = OMIT,
        audio_output_file: typing.Optional[str] = OMIT,
        organization_key_id: typing.Optional[str] = OMIT,
        user_email: typing.Optional[str] = OMIT,
        model: typing.Optional[str] = OMIT,
        provider_id: typing.Optional[str] = OMIT,
        category: typing.Optional[str] = OMIT,
        properties: typing.Optional[str] = OMIT,
        cache_bit: typing.Optional[int] = OMIT,
        cache_miss_bit: typing.Optional[int] = OMIT,
        cache_key: typing.Optional[str] = OMIT,
        latency: typing.Optional[float] = OMIT,
        tokens_per_second: typing.Optional[float] = OMIT,
        time_to_first_token: typing.Optional[float] = OMIT,
        routing_time: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        has_tool_calls: typing.Optional[bool] = OMIT,
        status_code: typing.Optional[int] = OMIT,
        log_method: typing.Optional[str] = OMIT,
        log_type: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        stream: typing.Optional[bool] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        customer_email: typing.Optional[str] = OMIT,
        customer_name: typing.Optional[str] = OMIT,
        customer_user_unique_id: typing.Optional[str] = OMIT,
        used_custom_credential: typing.Optional[bool] = OMIT,
        deployment_name: typing.Optional[str] = OMIT,
        deployment_id: typing.Optional[str] = OMIT,
        prompt_name: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        system_text: typing.Optional[str] = OMIT,
        prompt_text: typing.Optional[str] = OMIT,
        completion_text: typing.Optional[str] = OMIT,
        prompt_message_count: typing.Optional[int] = OMIT,
        completion_message_count: typing.Optional[int] = OMIT,
        trace_unique_id: typing.Optional[str] = OMIT,
        span_unique_id: typing.Optional[str] = OMIT,
        span_name: typing.Optional[str] = OMIT,
        span_parent_id: typing.Optional[str] = OMIT,
        span_workflow_name: typing.Optional[str] = OMIT,
        session_identifier: typing.Optional[str] = OMIT,
        span_links: typing.Optional[str] = OMIT,
        trace_group_identifier: typing.Optional[str] = OMIT,
        thread_identifier: typing.Optional[str] = OMIT,
        thread_unique_id: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        period_start: typing.Optional[dt.datetime] = OMIT,
        period_end: typing.Optional[dt.datetime] = OMIT,
        unique_id: typing.Optional[str] = OMIT,
        respan_gateway_request_id: typing.Optional[str] = OMIT,
        full_text: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChDatasetLog:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        dataset_id_ : str

        id : typing.Optional[str]

        organization_id : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        organization_name : typing.Optional[str]

        error_message : typing.Optional[str]

        completion_messages : typing.Optional[typing.Any]

        input : typing.Optional[str]

        output : typing.Optional[str]

        variables : typing.Optional[typing.Any]

        temperature : typing.Optional[float]

        max_tokens : typing.Optional[int]

        top_p : typing.Optional[float]

        frequency_penalty : typing.Optional[float]

        presence_penalty : typing.Optional[float]

        stop : typing.Optional[str]

        response_format : typing.Optional[typing.Any]

        matched_meter_ids : typing.Optional[typing.Sequence[typing.Any]]

        unit_prices : typing.Optional[typing.Dict[str, typing.Any]]

        component_costs : typing.Optional[typing.Dict[str, typing.Any]]

        dataset_id : typing.Optional[str]

        original_copy_unique_id : typing.Optional[str]

        comparison_key : typing.Optional[str]

        expected_output : typing.Optional[str]

        custom_identifier : typing.Optional[str]

        group_identifier : typing.Optional[str]

        blurred : typing.Optional[bool]

        start_time : typing.Optional[dt.datetime]

        timestamp : typing.Optional[dt.datetime]

        load_balance_group_id : typing.Optional[str]

        prompt_tokens : typing.Optional[int]

        completion_tokens : typing.Optional[int]

        prompt_cache_hit_tokens : typing.Optional[int]

        prompt_cache_creation_tokens : typing.Optional[int]

        reasoning_tokens : typing.Optional[int]

        total_request_tokens : typing.Optional[int]

        is_token_count_estimated : typing.Optional[int]

        cost : typing.Optional[float]

        llm_gateway_markup_rate : typing.Optional[float]

        service_tier : typing.Optional[str]

        model_discount : typing.Optional[float]

        pricing_tier : typing.Optional[str]

        audio_input_file : typing.Optional[str]

        audio_output_file : typing.Optional[str]

        organization_key_id : typing.Optional[str]

        user_email : typing.Optional[str]

        model : typing.Optional[str]

        provider_id : typing.Optional[str]

        category : typing.Optional[str]

        properties : typing.Optional[str]

        cache_bit : typing.Optional[int]

        cache_miss_bit : typing.Optional[int]

        cache_key : typing.Optional[str]

        latency : typing.Optional[float]

        tokens_per_second : typing.Optional[float]

        time_to_first_token : typing.Optional[float]

        routing_time : typing.Optional[float]

        status : typing.Optional[str]

        has_tool_calls : typing.Optional[bool]

        status_code : typing.Optional[int]

        log_method : typing.Optional[str]

        log_type : typing.Optional[str]

        environment : typing.Optional[str]

        stream : typing.Optional[bool]

        evaluation_identifier : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        customer_email : typing.Optional[str]

        customer_name : typing.Optional[str]

        customer_user_unique_id : typing.Optional[str]

        used_custom_credential : typing.Optional[bool]

        deployment_name : typing.Optional[str]

        deployment_id : typing.Optional[str]

        prompt_name : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        system_text : typing.Optional[str]

        prompt_text : typing.Optional[str]

        completion_text : typing.Optional[str]

        prompt_message_count : typing.Optional[int]

        completion_message_count : typing.Optional[int]

        trace_unique_id : typing.Optional[str]

        span_unique_id : typing.Optional[str]

        span_name : typing.Optional[str]

        span_parent_id : typing.Optional[str]

        span_workflow_name : typing.Optional[str]

        session_identifier : typing.Optional[str]

        span_links : typing.Optional[str]

        trace_group_identifier : typing.Optional[str]

        thread_identifier : typing.Optional[str]

        thread_unique_id : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        period_start : typing.Optional[dt.datetime]

        period_end : typing.Optional[dt.datetime]

        unique_id : typing.Optional[str]

        respan_gateway_request_id : typing.Optional[str]

        full_text : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChDatasetLog


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.datasets_logs_partial_update(
            dataset_id_="dataset_id",
        )
        """
        _response = self._raw_client.datasets_logs_partial_update(
            dataset_id_,
            id=id,
            organization_id=organization_id,
            unique_organization_id=unique_organization_id,
            organization_name=organization_name,
            error_message=error_message,
            completion_messages=completion_messages,
            input=input,
            output=output,
            variables=variables,
            temperature=temperature,
            max_tokens=max_tokens,
            top_p=top_p,
            frequency_penalty=frequency_penalty,
            presence_penalty=presence_penalty,
            stop=stop,
            response_format=response_format,
            matched_meter_ids=matched_meter_ids,
            unit_prices=unit_prices,
            component_costs=component_costs,
            dataset_id=dataset_id,
            original_copy_unique_id=original_copy_unique_id,
            comparison_key=comparison_key,
            expected_output=expected_output,
            custom_identifier=custom_identifier,
            group_identifier=group_identifier,
            blurred=blurred,
            start_time=start_time,
            timestamp=timestamp,
            load_balance_group_id=load_balance_group_id,
            prompt_tokens=prompt_tokens,
            completion_tokens=completion_tokens,
            prompt_cache_hit_tokens=prompt_cache_hit_tokens,
            prompt_cache_creation_tokens=prompt_cache_creation_tokens,
            reasoning_tokens=reasoning_tokens,
            total_request_tokens=total_request_tokens,
            is_token_count_estimated=is_token_count_estimated,
            cost=cost,
            llm_gateway_markup_rate=llm_gateway_markup_rate,
            service_tier=service_tier,
            model_discount=model_discount,
            pricing_tier=pricing_tier,
            audio_input_file=audio_input_file,
            audio_output_file=audio_output_file,
            organization_key_id=organization_key_id,
            user_email=user_email,
            model=model,
            provider_id=provider_id,
            category=category,
            properties=properties,
            cache_bit=cache_bit,
            cache_miss_bit=cache_miss_bit,
            cache_key=cache_key,
            latency=latency,
            tokens_per_second=tokens_per_second,
            time_to_first_token=time_to_first_token,
            routing_time=routing_time,
            status=status,
            has_tool_calls=has_tool_calls,
            status_code=status_code,
            log_method=log_method,
            log_type=log_type,
            environment=environment,
            stream=stream,
            evaluation_identifier=evaluation_identifier,
            customer_identifier=customer_identifier,
            customer_email=customer_email,
            customer_name=customer_name,
            customer_user_unique_id=customer_user_unique_id,
            used_custom_credential=used_custom_credential,
            deployment_name=deployment_name,
            deployment_id=deployment_id,
            prompt_name=prompt_name,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            system_text=system_text,
            prompt_text=prompt_text,
            completion_text=completion_text,
            prompt_message_count=prompt_message_count,
            completion_message_count=completion_message_count,
            trace_unique_id=trace_unique_id,
            span_unique_id=span_unique_id,
            span_name=span_name,
            span_parent_id=span_parent_id,
            span_workflow_name=span_workflow_name,
            session_identifier=session_identifier,
            span_links=span_links,
            trace_group_identifier=trace_group_identifier,
            thread_identifier=thread_identifier,
            thread_unique_id=thread_unique_id,
            storage_object_key=storage_object_key,
            period_start=period_start,
            period_end=period_end,
            unique_id=unique_id,
            respan_gateway_request_id=respan_gateway_request_id,
            full_text=full_text,
            request_options=request_options,
        )
        return _response.data

    def datasets_presence_retrieve(
        self, dataset_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetLogPresenceResponse:
        """
        Get all users currently viewing logs in this dataset from Redis cache.

        Parameters
        ----------
        dataset_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogPresenceResponse


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.datasets_presence_retrieve(
            dataset_id="dataset_id",
        )
        """
        _response = self._raw_client.datasets_presence_retrieve(dataset_id, request_options=request_options)
        return _response.data

    def eval_sets_logs_status_list(
        self, eval_set_id: str, log_unique_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[DatasetLogStatusCreate]:
        """
        A mixin that provides version handling for API views.

        Reads the X-Keywords-AI-Version header and sets self.version.
        Default version is 0 if header is not present or invalid.

        Parameters
        ----------
        eval_set_id : str

        log_unique_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[DatasetLogStatusCreate]


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_logs_status_list(
            eval_set_id="eval_set_id",
            log_unique_id="log_unique_id",
        )
        """
        _response = self._raw_client.eval_sets_logs_status_list(
            eval_set_id, log_unique_id, request_options=request_options
        )
        return _response.data

    def eval_sets_logs_status_create(
        self,
        eval_set_id: str,
        log_unique_id: str,
        *,
        organization: typing.Optional[int] = OMIT,
        completed_by: typing.Optional[int] = OMIT,
        status: typing.Optional[DatasetLogStatusCreateStatusEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetLogStatusCreate:
        """
        A mixin that provides version handling for API views.

        Reads the X-Keywords-AI-Version header and sets self.version.
        Default version is 0 if header is not present or invalid.

        Parameters
        ----------
        eval_set_id : str

        log_unique_id : str

        organization : typing.Optional[int]

        completed_by : typing.Optional[int]

        status : typing.Optional[DatasetLogStatusCreateStatusEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogStatusCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_logs_status_create(
            eval_set_id="eval_set_id",
            log_unique_id="log_unique_id",
        )
        """
        _response = self._raw_client.eval_sets_logs_status_create(
            eval_set_id,
            log_unique_id,
            organization=organization,
            completed_by=completed_by,
            status=status,
            request_options=request_options,
        )
        return _response.data

    def eval_sets_retrieve(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> DatasetDetail:
        """
        Retrieve, update, and delete a dataset

        Endpoints:
            GET /api/datasets/{dataset_id}/
            PATCH /api/datasets/{dataset_id}/
            DELETE /api/datasets/{dataset_id}/

        Superadmin: Can READ any dataset across all organizations via JWT.
                    Cannot WRITE via JWT - must use API key for write operations.
        Regular users: Can only access datasets in their organization.

        Defense-in-depth:

        Args (PATCH):
            - name (Optional): string
            - description (Optional): string

        Returns (GET 200):
            {
              "id": "dataset_id",
              "name": "Support Conversations - July",
              "type": "sampling",
              "description": "Sampled support chats for July",
              "created_at": "2025-07-26T00:00:00Z",
              "updated_at": "2025-07-27T08:10:00Z",
              "organization": 123,
              "initial_log_filters": {"status_code": {"operator": "eq", "value": 200}},
              "unique_organization_ids": [],
              "timestamps": [],
              "log_count": 250,
              "evaluator": null,
              "status": "ready",
              "running_status": "pending",
              "running_progress": 0,
              "running_at": null,
              "completed_annotation_count": 0
            }

        Returns (PATCH 200): Same shape as GET with updated fields
        Returns (DELETE 204): No content

        Defense-in-depth: SuperAdminMixin provides queryset routing + object-level ownership.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.eval_sets_retrieve(id, request_options=request_options)
        return _response.data

    def eval_sets_create(
        self,
        id: str,
        *,
        name: str,
        organization: int,
        initial_log_filters: typing.Optional[FilterParamDictPydantic] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        description: typing.Optional[str] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_at: typing.Optional[dt.datetime] = OMIT,
        unique_organization_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        timestamps: typing.Optional[typing.Sequence[dt.datetime]] = OMIT,
        ingest_workflow_id: typing.Optional[str] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        evaluator: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        name : str

        organization : int

        initial_log_filters : typing.Optional[FilterParamDictPydantic]

        project : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        description : typing.Optional[str]

        running_progress : typing.Optional[float]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_at : typing.Optional[dt.datetime]

        unique_organization_ids : typing.Optional[typing.Sequence[str]]

        timestamps : typing.Optional[typing.Sequence[dt.datetime]]

        ingest_workflow_id : typing.Optional[str]

        starred : typing.Optional[bool]

        evaluator : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_create(
            id="id",
            name="name",
            organization=1,
        )
        """
        _response = self._raw_client.eval_sets_create(
            id,
            name=name,
            organization=organization,
            initial_log_filters=initial_log_filters,
            project=project,
            type=type,
            description=description,
            running_progress=running_progress,
            running_status=running_status,
            running_at=running_at,
            unique_organization_ids=unique_organization_ids,
            timestamps=timestamps,
            ingest_workflow_id=ingest_workflow_id,
            starred=starred,
            evaluator=evaluator,
            request_options=request_options,
        )
        return _response.data

    def eval_sets_update(
        self,
        id: str,
        *,
        name: str,
        organization: int,
        initial_log_filters: typing.Optional[FilterParamDictPydantic] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        description: typing.Optional[str] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_at: typing.Optional[dt.datetime] = OMIT,
        unique_organization_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        timestamps: typing.Optional[typing.Sequence[dt.datetime]] = OMIT,
        ingest_workflow_id: typing.Optional[str] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        evaluator: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        name : str

        organization : int

        initial_log_filters : typing.Optional[FilterParamDictPydantic]

        project : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        description : typing.Optional[str]

        running_progress : typing.Optional[float]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_at : typing.Optional[dt.datetime]

        unique_organization_ids : typing.Optional[typing.Sequence[str]]

        timestamps : typing.Optional[typing.Sequence[dt.datetime]]

        ingest_workflow_id : typing.Optional[str]

        starred : typing.Optional[bool]

        evaluator : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_update(
            id="id",
            name="name",
            organization=1,
        )
        """
        _response = self._raw_client.eval_sets_update(
            id,
            name=name,
            organization=organization,
            initial_log_filters=initial_log_filters,
            project=project,
            type=type,
            description=description,
            running_progress=running_progress,
            running_status=running_status,
            running_at=running_at,
            unique_organization_ids=unique_organization_ids,
            timestamps=timestamps,
            ingest_workflow_id=ingest_workflow_id,
            starred=starred,
            evaluator=evaluator,
            request_options=request_options,
        )
        return _response.data

    def eval_sets_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Retrieve, update, and delete a dataset

        Endpoints:
            GET /api/datasets/{dataset_id}/
            PATCH /api/datasets/{dataset_id}/
            DELETE /api/datasets/{dataset_id}/

        Superadmin: Can READ any dataset across all organizations via JWT.
                    Cannot WRITE via JWT - must use API key for write operations.
        Regular users: Can only access datasets in their organization.

        Defense-in-depth:

        Args (PATCH):
            - name (Optional): string
            - description (Optional): string

        Returns (GET 200):
            {
              "id": "dataset_id",
              "name": "Support Conversations - July",
              "type": "sampling",
              "description": "Sampled support chats for July",
              "created_at": "2025-07-26T00:00:00Z",
              "updated_at": "2025-07-27T08:10:00Z",
              "organization": 123,
              "initial_log_filters": {"status_code": {"operator": "eq", "value": 200}},
              "unique_organization_ids": [],
              "timestamps": [],
              "log_count": 250,
              "evaluator": null,
              "status": "ready",
              "running_status": "pending",
              "running_progress": 0,
              "running_at": null,
              "completed_annotation_count": 0
            }

        Returns (PATCH 200): Same shape as GET with updated fields
        Returns (DELETE 204): No content

        Defense-in-depth: SuperAdminMixin provides queryset routing + object-level ownership.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_destroy(
            id="id",
        )
        """
        _response = self._raw_client.eval_sets_destroy(id, request_options=request_options)
        return _response.data

    def eval_sets_partial_update(
        self,
        id: str,
        *,
        initial_log_filters: typing.Optional[FilterParamDictPydantic] = OMIT,
        project: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        description: typing.Optional[str] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_at: typing.Optional[dt.datetime] = OMIT,
        unique_organization_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        timestamps: typing.Optional[typing.Sequence[dt.datetime]] = OMIT,
        ingest_workflow_id: typing.Optional[str] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        evaluator: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetDetail:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : str

        initial_log_filters : typing.Optional[FilterParamDictPydantic]

        project : typing.Optional[str]

        name : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        description : typing.Optional[str]

        running_progress : typing.Optional[float]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_at : typing.Optional[dt.datetime]

        unique_organization_ids : typing.Optional[typing.Sequence[str]]

        timestamps : typing.Optional[typing.Sequence[dt.datetime]]

        ingest_workflow_id : typing.Optional[str]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        evaluator : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_partial_update(
            id="id",
        )
        """
        _response = self._raw_client.eval_sets_partial_update(
            id,
            initial_log_filters=initial_log_filters,
            project=project,
            name=name,
            type=type,
            description=description,
            running_progress=running_progress,
            running_status=running_status,
            running_at=running_at,
            unique_organization_ids=unique_organization_ids,
            timestamps=timestamps,
            ingest_workflow_id=ingest_workflow_id,
            starred=starred,
            organization=organization,
            evaluator=evaluator,
            request_options=request_options,
        )
        return _response.data

    def eval_sets_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetListList:
        """
        List datasets

        Endpoint:
            GET /api/datasets/list/

        Superadmin: Can see all datasets across all organizations.
        Regular users: Can only see datasets in their organization.

        Returns (200):
            {
              "count": 1,
              "next": null,
              "previous": null,
              "results": [
                {
                  "id": "dataset_id",
                  "organization_id": 123,
                  "updated_by": {"first_name": "Ann", "last_name": "Lee", "email": "ann@example.com"},
                  "log_count": 250,
                  "name": "Support Conversations - July",
                  "log_ids": ["..."],
                  "description": "Sampled support chats for July",
                  "type": "sampling",
                  "status": "ready",
                  "created_at": "2025-07-26T00:00:00Z",
                  "updated_at": "2025-07-27T08:10:00Z",
                  "completed_annotation_count": 0,
                  "running_status": "pending",
                  "running_progress": 0
                }
              ]
            }

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_list_list()
        """
        _response = self._raw_client.eval_sets_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_datasets_list_filtered2(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetListList:
        """
        List datasets with complex filtering via POST body.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter parameters keyed by metric name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.api_datasets_list_filtered2()
        """
        _response = self._raw_client.api_datasets_list_filtered2(
            page=page, page_size=page_size, filters=filters, request_options=request_options
        )
        return _response.data

    def eval_sets_list_update(
        self,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        log_count: typing.Optional[int] = OMIT,
        log_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        status: typing.Optional[DatasetStatusEnum] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        id : typing.Optional[str]

        log_count : typing.Optional[int]

        log_ids : typing.Optional[typing.Sequence[str]]

        description : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        status : typing.Optional[DatasetStatusEnum]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_progress : typing.Optional[float]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_list_update(
            name="name",
        )
        """
        _response = self._raw_client.eval_sets_list_update(
            name=name,
            id=id,
            log_count=log_count,
            log_ids=log_ids,
            description=description,
            type=type,
            status=status,
            running_status=running_status,
            running_progress=running_progress,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def eval_sets_list_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        log_count: typing.Optional[int] = OMIT,
        name: typing.Optional[str] = OMIT,
        log_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        status: typing.Optional[DatasetStatusEnum] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        log_count : typing.Optional[int]

        name : typing.Optional[str]

        log_ids : typing.Optional[typing.Sequence[str]]

        description : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        status : typing.Optional[DatasetStatusEnum]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_progress : typing.Optional[float]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.eval_sets_list_partial_update()
        """
        _response = self._raw_client.eval_sets_list_partial_update(
            id=id,
            log_count=log_count,
            name=name,
            log_ids=log_ids,
            description=description,
            type=type,
            status=status,
            running_status=running_status,
            running_progress=running_progress,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_list(
        self,
        evaluator_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorVersionListList:
        """
        List all versions or create new version (commit).

        GET /api/evaluators/{id}/versions/ - List all versions
        POST /api/evaluators/{id}/versions/ - Commit (create new version)

        Access control via NestedResourceMixin: org identity derived from parent evaluator.
        Superadmin: Can LIST all versions across all organizations via JWT.
        Regular users: Can only access versions in their organization.

        Parameters
        ----------
        evaluator_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorVersionListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_list(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.evaluators_versions_list(
            evaluator_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def evaluators_versions_create(
        self,
        evaluator_id: str,
        *,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        score_value_type: typing.Optional[str] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        eval_class: typing.Optional[EvaluatorCreateVersionRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        created_by: typing.Optional[int] = OMIT,
        updated_by: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorCreateVersion:
        """
        Create a new version (commit). Org derived from parent evaluator.

        Parameters
        ----------
        evaluator_id : str

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        name : typing.Optional[str]

        score_value_type : typing.Optional[str]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        eval_class : typing.Optional[EvaluatorCreateVersionRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        created_by : typing.Optional[int]

        updated_by : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorCreateVersion


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_create(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.evaluators_versions_create(
            evaluator_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            name=name,
            score_value_type=score_value_type,
            version_description=version_description,
            description=description,
            type=type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            created_by=created_by,
            updated_by=updated_by,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_update(
        self,
        evaluator_id: str,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        evaluator_id : str

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_update(
            evaluator_id="evaluator_id",
            name="name",
        )
        """
        _response = self._raw_client.evaluators_versions_update(
            evaluator_id,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_partial_update(
        self,
        evaluator_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        evaluator_id : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_partial_update(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.evaluators_versions_partial_update(
            evaluator_id,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_retrieve(
        self, evaluator_id: str, version: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicEvaluatorVersionDetail:
        """
        Get or edit a specific version by version number.

        GET /api/evaluators/{evaluator_id}/versions/{version}/ - Get specific version
        PATCH /api/evaluators/{evaluator_id}/versions/{version}/ - Edit (only if is_read_only=False)

        NOTE: DELETE is not allowed for specific versions. Delete the entire evaluator instead.
        Versions are immutable history - you can only add new versions, not remove old ones.

        Access control via NestedResourceMixin: org identity derived from parent evaluator.
        Superadmin: Can READ any version across all organizations via JWT.
        Regular users: Can only access versions in their organization.

        Parameters
        ----------
        evaluator_id : str

        version : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_retrieve(
            evaluator_id="evaluator_id",
            version=1,
        )
        """
        _response = self._raw_client.evaluators_versions_retrieve(
            evaluator_id, version, request_options=request_options
        )
        return _response.data

    def evaluators_versions_create2(
        self,
        evaluator_id: str,
        version_: int,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionDetail:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        evaluator_id : str

        version_ : int

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        eval_class : typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_create2(
            evaluator_id="evaluator_id",
            version_=1,
            name="name",
        )
        """
        _response = self._raw_client.evaluators_versions_create2(
            evaluator_id,
            version_,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            configurations=configurations,
            categorical_choices=categorical_choices,
            eval_class=eval_class,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_update2(
        self,
        evaluator_id: str,
        version_: int,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionDetail:
        """
        Full update - only allowed if is_read_only=False.

        Parameters
        ----------
        evaluator_id : str

        version_ : int

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        eval_class : typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_update2(
            evaluator_id="evaluator_id",
            version_=1,
            name="name",
        )
        """
        _response = self._raw_client.evaluators_versions_update2(
            evaluator_id,
            version_,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            configurations=configurations,
            categorical_choices=categorical_choices,
            eval_class=eval_class,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_partial_update2(
        self,
        evaluator_id: str,
        version_: int,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        eval_class: typing.Optional[PatchedPublicEvaluatorVersionDetailRequestEvalClass] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionDetail:
        """
        Edit version - only allowed if is_read_only=False.

        Parameters
        ----------
        evaluator_id : str

        version_ : int

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        eval_class : typing.Optional[PatchedPublicEvaluatorVersionDetailRequestEvalClass]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_partial_update2(
            evaluator_id="evaluator_id",
            version_=1,
        )
        """
        _response = self._raw_client.evaluators_versions_partial_update2(
            evaluator_id,
            version_,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            configurations=configurations,
            categorical_choices=categorical_choices,
            eval_class=eval_class,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_list_list(
        self,
        evaluator_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorVersionListList:
        """
        List all versions or create new version (commit).

        GET /api/evaluators/{id}/versions/ - List all versions
        POST /api/evaluators/{id}/versions/ - Commit (create new version)

        Access control via NestedResourceMixin: org identity derived from parent evaluator.
        Superadmin: Can LIST all versions across all organizations via JWT.
        Regular users: Can only access versions in their organization.

        Parameters
        ----------
        evaluator_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorVersionListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_list_list(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.evaluators_versions_list_list(
            evaluator_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def evaluators_versions_list_create(
        self,
        evaluator_id: str,
        *,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        score_value_type: typing.Optional[str] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        eval_class: typing.Optional[EvaluatorCreateVersionRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        created_by: typing.Optional[int] = OMIT,
        updated_by: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorCreateVersion:
        """
        Create a new version (commit). Org derived from parent evaluator.

        Parameters
        ----------
        evaluator_id : str

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        name : typing.Optional[str]

        score_value_type : typing.Optional[str]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        eval_class : typing.Optional[EvaluatorCreateVersionRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        created_by : typing.Optional[int]

        updated_by : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorCreateVersion


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_list_create(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.evaluators_versions_list_create(
            evaluator_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            name=name,
            score_value_type=score_value_type,
            version_description=version_description,
            description=description,
            type=type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            created_by=created_by,
            updated_by=updated_by,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_list_update(
        self,
        evaluator_id: str,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        evaluator_id : str

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_list_update(
            evaluator_id="evaluator_id",
            name="name",
        )
        """
        _response = self._raw_client.evaluators_versions_list_update(
            evaluator_id,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    def evaluators_versions_list_partial_update(
        self,
        evaluator_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        evaluator_id : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_versions_list_partial_update(
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.evaluators_versions_list_partial_update(
            evaluator_id,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    def evaluators_summary_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        GET/POST /evaluations/evaluators/summary/
        GET/POST /api/evaluators/summary/

        Get summary statistics for evaluators.

        Returns:
            {
                "total_count": 15
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_summary_retrieve()
        """
        _response = self._raw_client.evaluators_summary_retrieve(request_options=request_options)
        return _response.data

    def evaluators_summary_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_summary_create()
        """
        _response = self._raw_client.evaluators_summary_create(request_options=request_options)
        return _response.data

    def evaluators_summary_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_summary_update()
        """
        _response = self._raw_client.evaluators_summary_update(request_options=request_options)
        return _response.data

    def evaluators_summary_partial_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_summary_partial_update()
        """
        _response = self._raw_client.evaluators_summary_partial_update(request_options=request_options)
        return _response.data

    def evaluators_tags_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedEvaluatorTagList:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedEvaluatorTagList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_tags_list()
        """
        _response = self._raw_client.evaluators_tags_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def evaluators_tags_create(
        self,
        *,
        organization: int,
        name: typing.Optional[str] = OMIT,
        color: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorTag:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        organization : int

        name : typing.Optional[str]

        color : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorTag


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_tags_create(
            organization=1,
        )
        """
        _response = self._raw_client.evaluators_tags_create(
            organization=organization, name=name, color=color, request_options=request_options
        )
        return _response.data

    def evaluators_tags_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EvaluatorTag:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorTag


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_tags_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.evaluators_tags_retrieve(id, request_options=request_options)
        return _response.data

    def evaluators_tags_update(
        self,
        id: str,
        *,
        organization: int,
        name: typing.Optional[str] = OMIT,
        color: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorTag:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        id : str

        organization : int

        name : typing.Optional[str]

        color : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorTag


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_tags_update(
            id="id",
            organization=1,
        )
        """
        _response = self._raw_client.evaluators_tags_update(
            id, organization=organization, name=name, color=color, request_options=request_options
        )
        return _response.data

    def evaluators_tags_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_tags_destroy(
            id="id",
        )
        """
        _response = self._raw_client.evaluators_tags_destroy(id, request_options=request_options)
        return _response.data

    def evaluators_tags_partial_update(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        color: typing.Optional[str] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorTag:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        id : str

        name : typing.Optional[str]

        color : typing.Optional[str]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorTag


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.evaluators_tags_partial_update(
            id="id",
        )
        """
        _response = self._raw_client.evaluators_tags_partial_update(
            id, name=name, color=color, organization=organization, request_options=request_options
        )
        return _response.data

    def list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorListList:
        """
        List evaluators for an organization.

        Superadmin: Can see all evaluators across all organizations.
        Regular users: Can only see evaluators in their organization, plus PUBLIC
            (Respan-managed) evaluators when ``is_including_public_evaluators`` is
            truthy. Public evaluators are hidden by default for back-compat.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.list_list()
        """
        _response = self._raw_client.list_list(page=page, page_size=page_size, request_options=request_options)
        return _response.data

    def list_create(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.list_create(
            name="name",
        )
        """
        _response = self._raw_client.list_create(
            name=name,
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def list_update(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.list_update(
            name="name",
        )
        """
        _response = self._raw_client.list_update(
            name=name,
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def list_partial_update(
        self,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PatchedPublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PatchedPublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.list_partial_update()
        """
        _response = self._raw_client.list_partial_update(
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    def results_retrieve(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> EvalResultDetail:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_retrieve(
            id=1,
        )
        """
        _response = self._raw_client.results_retrieve(id, request_options=request_options)
        return _response.data

    def results_create(
        self,
        id: int,
        *,
        organization: int,
        human_text_value: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        prompt_version_id: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        secondary_score: typing.Optional[float] = OMIT,
        tertiary_score: typing.Optional[float] = OMIT,
        quaternary_score: typing.Optional[float] = OMIT,
        score_mapping: typing.Optional[typing.Any] = OMIT,
        human_numerical_value: typing.Optional[float] = OMIT,
        human_categorical_value: typing.Optional[str] = OMIT,
        passed: typing.Optional[bool] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        evaluation: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalResultDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : int

        organization : int

        human_text_value : typing.Optional[str]

        project : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        source : typing.Optional[str]

        cost : typing.Optional[float]

        evaluation_identifier : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        eval_set_id : typing.Optional[str]

        log_timestamp : typing.Optional[dt.datetime]

        prompt_version_id : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        secondary_score : typing.Optional[float]

        tertiary_score : typing.Optional[float]

        quaternary_score : typing.Optional[float]

        score_mapping : typing.Optional[typing.Any]

        human_numerical_value : typing.Optional[float]

        human_categorical_value : typing.Optional[str]

        passed : typing.Optional[bool]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        evaluation : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_create(
            id=1,
            organization=1,
        )
        """
        _response = self._raw_client.results_create(
            id,
            organization=organization,
            human_text_value=human_text_value,
            project=project,
            eval_result_unique_id=eval_result_unique_id,
            type=type,
            environment=environment,
            unique_organization_id=unique_organization_id,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            workflow_version_id=workflow_version_id,
            source=source,
            cost=cost,
            evaluation_identifier=evaluation_identifier,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            eval_set_id=eval_set_id,
            log_timestamp=log_timestamp,
            prompt_version_id=prompt_version_id,
            pipeline_run_id=pipeline_run_id,
            automation_id=automation_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            secondary_score=secondary_score,
            tertiary_score=tertiary_score,
            quaternary_score=quaternary_score,
            score_mapping=score_mapping,
            human_numerical_value=human_numerical_value,
            human_categorical_value=human_categorical_value,
            passed=passed,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            evaluation=evaluation,
            annotation_config=annotation_config,
            log=log,
            request_options=request_options,
        )
        return _response.data

    def results_update(
        self,
        id: int,
        *,
        organization: int,
        human_text_value: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        prompt_version_id: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        secondary_score: typing.Optional[float] = OMIT,
        tertiary_score: typing.Optional[float] = OMIT,
        quaternary_score: typing.Optional[float] = OMIT,
        score_mapping: typing.Optional[typing.Any] = OMIT,
        human_numerical_value: typing.Optional[float] = OMIT,
        human_categorical_value: typing.Optional[str] = OMIT,
        passed: typing.Optional[bool] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        evaluation: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalResultDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : int

        organization : int

        human_text_value : typing.Optional[str]

        project : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        source : typing.Optional[str]

        cost : typing.Optional[float]

        evaluation_identifier : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        eval_set_id : typing.Optional[str]

        log_timestamp : typing.Optional[dt.datetime]

        prompt_version_id : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        secondary_score : typing.Optional[float]

        tertiary_score : typing.Optional[float]

        quaternary_score : typing.Optional[float]

        score_mapping : typing.Optional[typing.Any]

        human_numerical_value : typing.Optional[float]

        human_categorical_value : typing.Optional[str]

        passed : typing.Optional[bool]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        evaluation : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_update(
            id=1,
            organization=1,
        )
        """
        _response = self._raw_client.results_update(
            id,
            organization=organization,
            human_text_value=human_text_value,
            project=project,
            eval_result_unique_id=eval_result_unique_id,
            type=type,
            environment=environment,
            unique_organization_id=unique_organization_id,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            workflow_version_id=workflow_version_id,
            source=source,
            cost=cost,
            evaluation_identifier=evaluation_identifier,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            eval_set_id=eval_set_id,
            log_timestamp=log_timestamp,
            prompt_version_id=prompt_version_id,
            pipeline_run_id=pipeline_run_id,
            automation_id=automation_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            secondary_score=secondary_score,
            tertiary_score=tertiary_score,
            quaternary_score=quaternary_score,
            score_mapping=score_mapping,
            human_numerical_value=human_numerical_value,
            human_categorical_value=human_categorical_value,
            passed=passed,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            evaluation=evaluation,
            annotation_config=annotation_config,
            log=log,
            request_options=request_options,
        )
        return _response.data

    def results_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_destroy(
            id=1,
        )
        """
        _response = self._raw_client.results_destroy(id, request_options=request_options)
        return _response.data

    def results_partial_update(
        self,
        id: int,
        *,
        human_text_value: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        prompt_version_id: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        secondary_score: typing.Optional[float] = OMIT,
        tertiary_score: typing.Optional[float] = OMIT,
        quaternary_score: typing.Optional[float] = OMIT,
        score_mapping: typing.Optional[typing.Any] = OMIT,
        human_numerical_value: typing.Optional[float] = OMIT,
        human_categorical_value: typing.Optional[str] = OMIT,
        passed: typing.Optional[bool] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        organization: typing.Optional[int] = OMIT,
        evaluation: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalResultDetail:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : int

        human_text_value : typing.Optional[str]

        project : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        source : typing.Optional[str]

        cost : typing.Optional[float]

        evaluation_identifier : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        eval_set_id : typing.Optional[str]

        log_timestamp : typing.Optional[dt.datetime]

        prompt_version_id : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        secondary_score : typing.Optional[float]

        tertiary_score : typing.Optional[float]

        quaternary_score : typing.Optional[float]

        score_mapping : typing.Optional[typing.Any]

        human_numerical_value : typing.Optional[float]

        human_categorical_value : typing.Optional[str]

        passed : typing.Optional[bool]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        organization : typing.Optional[int]

        evaluation : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_partial_update(
            id=1,
        )
        """
        _response = self._raw_client.results_partial_update(
            id,
            human_text_value=human_text_value,
            project=project,
            eval_result_unique_id=eval_result_unique_id,
            type=type,
            environment=environment,
            unique_organization_id=unique_organization_id,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            workflow_version_id=workflow_version_id,
            source=source,
            cost=cost,
            evaluation_identifier=evaluation_identifier,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            eval_set_id=eval_set_id,
            log_timestamp=log_timestamp,
            prompt_version_id=prompt_version_id,
            pipeline_run_id=pipeline_run_id,
            automation_id=automation_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            secondary_score=secondary_score,
            tertiary_score=tertiary_score,
            quaternary_score=quaternary_score,
            score_mapping=score_mapping,
            human_numerical_value=human_numerical_value,
            human_categorical_value=human_categorical_value,
            passed=passed,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            organization=organization,
            evaluation=evaluation,
            annotation_config=annotation_config,
            log=log,
            request_options=request_options,
        )
        return _response.data

    def results_related_to_log_list(
        self,
        log_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedEvalWithResultsList:
        """
        Listing all the evaluators along with the results under them within a log

        Args:
            URL args:
                - log_id: The ID of the log to get the evaluators and results for
            Query params:
                - evaluator_type: The type of the evaluator to get the results for
                - timestamp: The timestamp of the log to get the evaluators and results for
                - unique_organization_id: The unique organization ID of the log to get the evaluators and results for
            POST params:
                - evaluation_id: The ID of the evaluation to create the result for
                - evaluator_slug: The slug of the evaluator to create the result for
                - log_unique_id: The unique ID of the log to create the result for
                - eval_result_unique_id: The unique ID of the result to create
                - score_mapping: The score mapping of the result
                - primary_score: The primary score of the result

        Parameters
        ----------
        log_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedEvalWithResultsList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_related_to_log_list(
            log_id="log_id",
        )
        """
        _response = self._raw_client.results_related_to_log_list(
            log_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def results_related_to_log_create(
        self,
        log_id: str,
        *,
        organization: int,
        human_text_value: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        inputs: typing.Optional[typing.Any] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        prompt_version_id: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        secondary_score: typing.Optional[float] = OMIT,
        tertiary_score: typing.Optional[float] = OMIT,
        quaternary_score: typing.Optional[float] = OMIT,
        score_mapping: typing.Optional[typing.Any] = OMIT,
        human_numerical_value: typing.Optional[float] = OMIT,
        human_categorical_value: typing.Optional[str] = OMIT,
        passed: typing.Optional[bool] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        evaluation: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalResultCreate:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        log_id : str

        organization : int

        human_text_value : typing.Optional[str]

        project : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        inputs : typing.Optional[typing.Any]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        source : typing.Optional[str]

        cost : typing.Optional[float]

        evaluation_identifier : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        eval_set_id : typing.Optional[str]

        log_timestamp : typing.Optional[dt.datetime]

        prompt_version_id : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        secondary_score : typing.Optional[float]

        tertiary_score : typing.Optional[float]

        quaternary_score : typing.Optional[float]

        score_mapping : typing.Optional[typing.Any]

        human_numerical_value : typing.Optional[float]

        human_categorical_value : typing.Optional[str]

        passed : typing.Optional[bool]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        evaluation : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_related_to_log_create(
            log_id="log_id",
            organization=1,
        )
        """
        _response = self._raw_client.results_related_to_log_create(
            log_id,
            organization=organization,
            human_text_value=human_text_value,
            project=project,
            eval_result_unique_id=eval_result_unique_id,
            type=type,
            environment=environment,
            unique_organization_id=unique_organization_id,
            inputs=inputs,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            workflow_version_id=workflow_version_id,
            source=source,
            cost=cost,
            evaluation_identifier=evaluation_identifier,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            eval_set_id=eval_set_id,
            log_timestamp=log_timestamp,
            prompt_version_id=prompt_version_id,
            pipeline_run_id=pipeline_run_id,
            automation_id=automation_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            secondary_score=secondary_score,
            tertiary_score=tertiary_score,
            quaternary_score=quaternary_score,
            score_mapping=score_mapping,
            human_numerical_value=human_numerical_value,
            human_categorical_value=human_categorical_value,
            passed=passed,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            evaluation=evaluation,
            annotation_config=annotation_config,
            log=log,
            request_options=request_options,
        )
        return _response.data

    def results_related_to_log_update(
        self,
        log_id: str,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        score_config: typing.Optional[typing.Any] = OMIT,
        passing_conditions: typing.Optional[EvalWithResultsRequestPassingConditions] = OMIT,
        llm_config: typing.Optional[EvalWithResultsRequestLlmConfig] = OMIT,
        code_config: typing.Optional[EvalWithResultsRequestCodeConfig] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Any]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalWithResults:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        log_id : str

        name : str

        version_id : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        score_config : typing.Optional[typing.Any]

        passing_conditions : typing.Optional[EvalWithResultsRequestPassingConditions]

        llm_config : typing.Optional[EvalWithResultsRequestLlmConfig]

        code_config : typing.Optional[EvalWithResultsRequestCodeConfig]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Any]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalWithResults


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_related_to_log_update(
            log_id="log_id",
            name="name",
        )
        """
        _response = self._raw_client.results_related_to_log_update(
            log_id,
            name=name,
            version_id=version_id,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            categorical_choices=categorical_choices,
            starred=starred,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    def results_related_to_log_partial_update(
        self,
        log_id: str,
        *,
        version_id: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        score_config: typing.Optional[typing.Any] = OMIT,
        passing_conditions: typing.Optional[PatchedEvalWithResultsRequestPassingConditions] = OMIT,
        llm_config: typing.Optional[PatchedEvalWithResultsRequestLlmConfig] = OMIT,
        code_config: typing.Optional[PatchedEvalWithResultsRequestCodeConfig] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Any]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalWithResults:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        log_id : str

        version_id : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        score_config : typing.Optional[typing.Any]

        passing_conditions : typing.Optional[PatchedEvalWithResultsRequestPassingConditions]

        llm_config : typing.Optional[PatchedEvalWithResultsRequestLlmConfig]

        code_config : typing.Optional[PatchedEvalWithResultsRequestCodeConfig]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Any]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalWithResults


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.results_related_to_log_partial_update(
            log_id="log_id",
        )
        """
        _response = self._raw_client.results_related_to_log_partial_update(
            log_id,
            version_id=version_id,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            categorical_choices=categorical_choices,
            starred=starred,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    def test_run_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Main entry point for test run evaluations.
        Handles four modes of operation:
        1. Evaluation from raw eval inputs & evaluator id (backward compatibility)
        2. Evaluation from log
        3. Evaluation from evaluator configuration form
        4. Evaluation from raw eval inputs & evaluator id (new public API mode)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.test_run_create()
        """
        _response = self._raw_client.test_run_create(request_options=request_options)
        return _response.data

    def test_run_create2(
        self, log_id: str, evaluator_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Main entry point for test run evaluations.
        Handles four modes of operation:
        1. Evaluation from raw eval inputs & evaluator id (backward compatibility)
        2. Evaluation from log
        3. Evaluation from evaluator configuration form
        4. Evaluation from raw eval inputs & evaluator id (new public API mode)

        Parameters
        ----------
        log_id : str

        evaluator_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.evaluations.test_run_create2(
            log_id="log_id",
            evaluator_id="evaluator_id",
        )
        """
        _response = self._raw_client.test_run_create2(log_id, evaluator_id, request_options=request_options)
        return _response.data


class AsyncEvaluationsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawEvaluationsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawEvaluationsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawEvaluationsClient
        """
        return self._raw_client

    async def api_annotation_items_list(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[AnnotationItemList]:
        """
        GET /api/annotation-items/

        Simple listing for basic CRUD operations.
        Returns lightweight list of items without pagination.

        Permission: Requires annotation_items:read (admin only)

        Use cases:
        - Quick item lookups
        - Basic CRUD list view (C from CRUD done via /bulk/)
        - For paginated + filtered view, use /list/
        - For annotation work, use /queue/

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[AnnotationItemList]


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_list(request_options=request_options)
        return _response.data

    async def api_annotation_items_create(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_create(
                source_id="source_id",
                source_type="logs",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_create(
            source_id=source_id,
            source_type=source_type,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    async def api_annotation_items_update(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_update(
                source_id="source_id",
                source_type="logs",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_update(
            source_id=source_id,
            source_type=source_type,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    async def api_annotation_items_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        source_id: typing.Optional[str] = OMIT,
        source_type: typing.Optional[SourceTypeC74Enum] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        source_id : typing.Optional[str]

        source_type : typing.Optional[SourceTypeC74Enum]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_partial_update(
            id=id,
            source_id=source_id,
            source_type=source_type,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    async def api_annotation_items_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AnnotationItemDetail:
        """
        GET/PATCH/DELETE /api/annotation-items/{id}/

        Retrieve, update, or delete a specific annotation item (RUD from CRUD).

        Permission (via ObjectOwnershipMixin config in SuperAdminMixin):
        - Worker (assignee): Can GET/PATCH their OWN assigned items (status only)
        - Org Admin: Can GET/PATCH/DELETE any item in org (can re-assign)
        - Superadmin: Can access all items across all orgs

        GET: Returns full item details with all fields
        PATCH:
          - Worker: Update status, completed_at
          - Admin: Also update assignee_id (re-assign)
        DELETE: Delete item (admin only via ownership check)

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_retrieve(id, request_options=request_options)
        return _response.data

    async def api_annotation_items_create2(
        self,
        id: str,
        *,
        project: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        project : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_create2(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_create2(
            id, project=project, status=status, completed_at=completed_at, request_options=request_options
        )
        return _response.data

    async def api_annotation_items_update2(
        self,
        id: str,
        *,
        project: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        project : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_update2(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_update2(
            id, project=project, status=status, completed_at=completed_at, request_options=request_options
        )
        return _response.data

    async def api_annotation_items_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        GET/PATCH/DELETE /api/annotation-items/{id}/

        Retrieve, update, or delete a specific annotation item (RUD from CRUD).

        Permission (via ObjectOwnershipMixin config in SuperAdminMixin):
        - Worker (assignee): Can GET/PATCH their OWN assigned items (status only)
        - Org Admin: Can GET/PATCH/DELETE any item in org (can re-assign)
        - Superadmin: Can access all items across all orgs

        GET: Returns full item details with all fields
        PATCH:
          - Worker: Update status, completed_at
          - Admin: Also update assignee_id (re-assign)
        DELETE: Delete item (admin only via ownership check)

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_destroy(id, request_options=request_options)
        return _response.data

    async def api_annotation_items_partial_update2(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_partial_update2(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_partial_update2(id, request_options=request_options)
        return _response.data

    async def api_annotation_items_bulk_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        POST /api/annotation-items/bulk/

        Bulk create annotation items (assign logs to workers).
        Supports both "by-reference" and "by-direct-payload" methods.

        Permission: Requires annotation_items:create (admin only)

        Events: `annotation_item_assigned` dispatched via signals (see annotation/signals.py)

        Request Body:
        {
          "assignee_id": "user_123",
          "source_items": [
            {"source_type": "logs", "log_id": "log_1"},
            {"source_type": "logs", "log_id": "log_2", "full_object": {...}}
          ]
        }

        Response (201) — canonical BulkOperationResponse envelope with the
        pre-foundation custom fields (`created_count`, `items`) carried as
        extras via Pydantic ``extra="allow"``:
        {
          "success_count": 2,
          "error_count": 0,
          "errors": [],
          "created_count": 2,
          "items": [...]
        }

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_bulk_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_bulk_create(request_options=request_options)
        return _response.data

    async def api_annotation_items_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationItemListList:
        """
        GET/POST /api/annotation-items/list/

        Admin/Manager view: Team progress tracking with pagination and filtering.
        Sophisticated listing endpoint for management dashboard (L from CRUDL).

        Permission: Requires annotation_items:read (admin/manager only)

        Features:
        - Pagination (cursor-based)
        - Filtering (source_type, status, etc.)
        - Accepts filters via URL params OR POST body

        Use cases:
        - Management dashboard
        - Track team progress
        - Filter by status, source_type
        - View who's assigned to what

        Query Parameters (GET):
        - page, page_size: Pagination
        - source_type: Filter by source type
        - status: Filter by status

        Request Body (POST):
        {
          "filters": {
            "source_type": {"operator": "in", "value": ["logs", "threads"]},
            "status": {"operator": "", "value": "pending"}
          }
        }

        Response:
        {
          "count": 150,
          "next": "...",
          "previous": "...",
          "results": [
            {
              "id": "item_1",
              "assignee": {"email": "worker@co.com"},
              "status": "pending",
              "source_type": "logs",
              "created_at": "..."
            }
          ],
          "filters_data": {...}
        }

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationItemListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_annotation_items_list_create(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        Handle POST requests (for filtering) by delegating to GET.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_list_create(
                source_id="source_id",
                source_type="logs",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_list_create(
            source_id=source_id,
            source_type=source_type,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    async def api_annotation_items_list_update(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_list_update(
                source_id="source_id",
                source_type="logs",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_list_update(
            source_id=source_id,
            source_type=source_type,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    async def api_annotation_items_list_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        source_id: typing.Optional[str] = OMIT,
        source_type: typing.Optional[SourceTypeC74Enum] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        source_id : typing.Optional[str]

        source_type : typing.Optional[SourceTypeC74Enum]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_list_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_list_partial_update(
            id=id,
            source_id=source_id,
            source_type=source_type,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    async def api_annotation_items_queue_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationItemQueueList:
        """
        GET/POST /api/annotation-items/queue/

        Worker queue view: MY assigned items for focused annotation work.
        Used by everyone (including admins) when doing annotation work.

        Permission: No permission required (self-filtering to assignee=current_user)

        Features:
        - Prefetch FULL objects for instant navigation
        - Privacy: Only see OWN scores
        - Optional filtering (source_type, status)
        - Pagination support (page, page_size query params)

        Use cases:
        - Workers see their work queue
        - Admins see their queue when annotating
        - Focused view - only MY items

        Query Parameters (GET):
        - page: Page number (default: 1)
        - page_size: Items per page (default: 100, max: 1000)
        - source_type: Filter by source type (optional)
        - status: Filter by status (optional)

        Request Body (POST):
        {
          "filters": {
            "source_type": {"operator": "", "value": "logs"},
            "status": {"operator": "", "value": "pending"}
          }
        }

        Response (paginated):
        {
          "count": 5,
          "next": "/api/annotation-items/queue/?page=2",
          "previous": null,
          "results": [
            {
              "id": "item_1",
              "status": "pending",
              "full_object": {...},
              "scores": [...]
            }
          ]
        }

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationItemQueueList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_queue_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_queue_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_annotation_items_queue_create(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        full_object: typing.Any,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemQueue:
        """
        Handle POST requests (for filtering) by delegating to GET.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        full_object : typing.Any

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemQueue


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_queue_create(
                source_id="x",
                source_type="logs",
                full_object={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_queue_create(
            source_id=source_id,
            source_type=source_type,
            full_object=full_object,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    async def api_annotation_items_queue_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationItemQueueList:
        """
        GET/POST /api/annotation-items/queue/list/

        Worker queue list view with filter options metadata.
        Same as /queue/ but includes filters_data in response for FE filter UI.

        Permission: No permission required (self-filtering to assignee=current_user)

        Features:
        - Everything from /queue/ (pagination, filtering, enrichment)
        - Plus: filters_data in response (FilterOptionsMixin)
        - Accepts filters via URL params OR POST body

        Response (paginated):
        {
          "count": 5,
          "next": "/api/annotation-items/queue/list/?page=2",
          "previous": null,
          "results": [...],
          "filters_data": {...}
        }

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationItemQueueList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_queue_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_queue_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_annotation_items_queue_list_create(
        self,
        *,
        source_id: str,
        source_type: SourceTypeC74Enum,
        full_object: typing.Any,
        id: typing.Optional[str] = OMIT,
        source_dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnnotationItemQueue:
        """
        Handle POST requests (for filtering) by delegating to GET.

        Parameters
        ----------
        source_id : str

        source_type : SourceTypeC74Enum

        full_object : typing.Any

        id : typing.Optional[str]

        source_dataset_id : typing.Optional[str]

        status : typing.Optional[Status66CEnum]

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            List of evaluator IDs that define which scoring criteria the assignee should fill out

        completed_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnnotationItemQueue


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_queue_list_create(
                source_id="x",
                source_type="logs",
                full_object={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_queue_list_create(
            source_id=source_id,
            source_type=source_type,
            full_object=full_object,
            id=id,
            source_dataset_id=source_dataset_id,
            status=status,
            evaluator_ids=evaluator_ids,
            completed_at=completed_at,
            request_options=request_options,
        )
        return _response.data

    async def api_annotation_items_queue_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Get summary statistics for current user's queue only.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_queue_summary_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_queue_summary_retrieve(request_options=request_options)
        return _response.data

    async def api_annotation_items_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Get summary statistics for annotation items.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_summary_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_summary_retrieve(request_options=request_options)
        return _response.data

    async def api_annotation_items_summary_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        POST for filtering - delegate to GET

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_summary_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_summary_create(request_options=request_options)
        return _response.data

    async def api_annotation_items_summary_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_summary_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_summary_update(request_options=request_options)
        return _response.data

    async def api_annotation_items_summary_partial_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_annotation_items_summary_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_annotation_items_summary_partial_update(request_options=request_options)
        return _response.data

    async def api_evaluators_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorListList:
        """
        ## Creating an Evaluator

        ### LLM Evaluators

        For LLM evaluators, the frontend should first fetch available evaluation forms from the
        `/eval-forms/` endpoint to get the template configuration, then fill in the form and submit.

        **Required fields:**
        - `name` (str): Display name for the evaluator
        - `evaluator_slug` (str): Unique identifier for the evaluator within the organization
        - `type` (str): llm, human_boolean, human_categorical, human_numerical, human_text
        - `configurations` (dict): Complete evaluation form configuration
        - `description` (str, optional): Description of what this evaluator does
        - `enabled` (bool, optional): Whether the evaluator is active (default: False)

        **Example request body for LLM evaluator:**
        ```json
        {
            "name": "Output Length Checker",
            "type": "llm",
            "description": "Checks if the output meets character count requirements",
            "enabled": true,
            "configurations": {
                "eval_class": "output_char_count",
                "type": "function",
                "note": "",
                "display_name": "Output Character Count",
                "description": "Evaluates the length of the output text",
                "special_fields": [],
                "required_fields": [
                    {
                        "name": "llm_output",
                        "display_name": "LLM Output",
                        "type": "textarea",
                        "description": "The output text to evaluate",
                        "required": true,
                        "default_value": null,
                        "placeholder": "",
                        "choices": [],
                        "value": null
                    }
                ],
                "inference_filters": [],
                "allow_conditions": true,
                "score_mapping": {
                    "primary_score": "output_char_count",
                    "secondary_score": null,
                    "tertiary_score": null,
                    "quaternary_score": null
                },
                "category": "custom"
            }
        }
        ```

        ### Human Annotation Evaluators

        For human annotation evaluators, specify the type and provide choices for categorical evaluators.

        **Human Boolean Evaluator:**
        ```json
        {
            "name": "Quality Check",
            "evaluator_slug": "quality_check",
            "type": "human_boolean",
            "description": "Manual quality assessment"
        }
        ```

        **Human Categorical Evaluator:**
        ```json
        {
            "name": "Sentiment Rating",
            "evaluator_slug": "sentiment_rating",
            "type": "human_categorical",
            "description": "Manual sentiment classification",
            "categorical_choices": [
                {"name": "Positive", "value": 1},
                {"name": "Neutral", "value": 0},
                {"name": "Negative", "value": -1}
            ]
        }
        ```

        **Human Numerical Evaluator:**
        ```json
        {
            "name": "Quality Score",
            "evaluator_slug": "quality_score",
            "type": "human_numerical",
            "description": "Rate quality from 1-10"
        }
        ```

        **Human Text Evaluator:**
        ```json
        {
            "name": "Feedback Comments",
            "evaluator_slug": "feedback_comments",
            "type": "human_text",
            "description": "Detailed feedback comments"
        }
        ```

        ## Response

        Returns the created evaluator with all fields populated, including auto-generated fields like
        `id`, `created_at`, `updated_at`, and `evaluator_slug`.

        ## Validation

        - For LLM evaluators: The `configurations` field is validated against the corresponding
          evaluation form schema from `EVAL_FORMS_MAP`
        - For human categorical evaluators: `categorical_choices` must be a list of objects
          with `name` and `value` fields
        - The `eval_class` in configurations must exist in the available evaluation forms

        ## Notes

        - The `organization`, `created_by`, and `updated_by` fields are automatically set from
          the authenticated user
        - Each evaluator gets a unique `evaluator_slug` within the organization
        - LLM evaluators require a valid `eval_class` that maps to an available evaluation function

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_evaluators_update(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_update(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_update(
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_partial_update(
        self,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_partial_update(
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_create2(
        self,
        evaluator_id: str,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        evaluator_id : str

        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_create2(
                evaluator_id="evaluator_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_create2(
            evaluator_id,
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_versions_update(
        self,
        evaluator_id: str,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        evaluator_id : str

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_versions_update(
                evaluator_id="evaluator_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_versions_update(
            evaluator_id,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_versions_partial_update(
        self,
        evaluator_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        evaluator_id : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_versions_partial_update(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_versions_partial_update(
            evaluator_id,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_versions_create2(
        self,
        evaluator_id: str,
        version_: int,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionDetail:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        evaluator_id : str

        version_ : int

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        eval_class : typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_versions_create2(
                evaluator_id="evaluator_id",
                version_=1,
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_versions_create2(
            evaluator_id,
            version_,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            configurations=configurations,
            categorical_choices=categorical_choices,
            eval_class=eval_class,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_versions_list_list(
        self,
        evaluator_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorVersionListList:
        """
        List all versions or create new version (commit).

        GET /api/evaluators/{id}/versions/ - List all versions
        POST /api/evaluators/{id}/versions/ - Commit (create new version)

        Access control via NestedResourceMixin: org identity derived from parent evaluator.
        Superadmin: Can LIST all versions across all organizations via JWT.
        Regular users: Can only access versions in their organization.

        Parameters
        ----------
        evaluator_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorVersionListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_versions_list_list(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_versions_list_list(
            evaluator_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_evaluators_versions_list_create(
        self,
        evaluator_id: str,
        *,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        score_value_type: typing.Optional[str] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        eval_class: typing.Optional[EvaluatorCreateVersionRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        created_by: typing.Optional[int] = OMIT,
        updated_by: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorCreateVersion:
        """
        Create a new version (commit). Org derived from parent evaluator.

        Parameters
        ----------
        evaluator_id : str

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        name : typing.Optional[str]

        score_value_type : typing.Optional[str]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        eval_class : typing.Optional[EvaluatorCreateVersionRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        created_by : typing.Optional[int]

        updated_by : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorCreateVersion


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_versions_list_create(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_versions_list_create(
            evaluator_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            name=name,
            score_value_type=score_value_type,
            version_description=version_description,
            description=description,
            type=type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            created_by=created_by,
            updated_by=updated_by,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_versions_list_update(
        self,
        evaluator_id: str,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        evaluator_id : str

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_versions_list_update(
                evaluator_id="evaluator_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_versions_list_update(
            evaluator_id,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_versions_list_partial_update(
        self,
        evaluator_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        evaluator_id : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_versions_list_partial_update(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_versions_list_partial_update(
            evaluator_id,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorListList:
        """
        List evaluators for an organization.

        Superadmin: Can see all evaluators across all organizations.
        Regular users: Can only see evaluators in their organization, plus PUBLIC
            (Respan-managed) evaluators when ``is_including_public_evaluators`` is
            truthy. Public evaluators are hidden by default for back-compat.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_evaluators_list_update(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_list_update(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_list_update(
            name=name,
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_list_partial_update(
        self,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PatchedPublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PatchedPublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_list_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_list_partial_update(
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def api_evaluators_summary_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        GET/POST /evaluations/evaluators/summary/
        GET/POST /api/evaluators/summary/

        Get summary statistics for evaluators.

        Returns:
            {
                "total_count": 15
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_summary_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_summary_retrieve(request_options=request_options)
        return _response.data

    async def api_evaluators_summary_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_summary_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_summary_update(request_options=request_options)
        return _response.data

    async def api_evaluators_summary_partial_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_evaluators_summary_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_evaluators_summary_partial_update(request_options=request_options)
        return _response.data

    async def clickhouse_dataset_tasks_eval_result_aggs_list(
        self,
        task_tracker_id: int,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicDatasetTaskTrackerRunEvalListList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        task_tracker_id : int

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicDatasetTaskTrackerRunEvalListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_dataset_tasks_eval_result_aggs_list(
                task_tracker_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_dataset_tasks_eval_result_aggs_list(
            task_tracker_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def clickhouse_dataset_tasks_eval_result_aggs_create(
        self,
        task_tracker_id: int,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        evaluated_logs_count: typing.Optional[int] = OMIT,
        score: typing.Optional[float] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicDatasetTaskTrackerRunEvalList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        task_tracker_id : int

        task_id : str

        name : str

        dataset_id : str

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        evaluated_logs_count : typing.Optional[int]

        score : typing.Optional[float]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicDatasetTaskTrackerRunEvalList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_dataset_tasks_eval_result_aggs_create(
                task_tracker_id=1,
                task_id="task_id",
                name="name",
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_dataset_tasks_eval_result_aggs_create(
            task_tracker_id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            evaluated_logs_count=evaluated_logs_count,
            score=score,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_result_aggs_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicDatasetTaskTrackerRunEvalListList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicDatasetTaskTrackerRunEvalListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_result_aggs_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_result_aggs_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def clickhouse_eval_result_aggs_create(
        self,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        evaluated_logs_count: typing.Optional[int] = OMIT,
        score: typing.Optional[float] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicDatasetTaskTrackerRunEvalList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        task_id : str

        name : str

        dataset_id : str

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        evaluated_logs_count : typing.Optional[int]

        score : typing.Optional[float]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicDatasetTaskTrackerRunEvalList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_result_aggs_create(
                task_id="task_id",
                name="name",
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_result_aggs_create(
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            evaluated_logs_count=evaluated_logs_count,
            score=score,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicChEvalResultListList:
        """
        Backward compatible ClickHouse-based evaluation results list view

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicChEvalResultListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def clickhouse_eval_results_create(
        self,
        *,
        id: str,
        created_at: dt.datetime,
        type: str,
        environment: str,
        numerical_value: float,
        string_value: str,
        is_passed: bool,
        cost: float,
        evaluator_id: str,
        log_id: str,
        prompt_id: str,
        prompt_version_number: int,
        dataset_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicChEvalResultList:
        """
        Handle POST requests the same as GET for filtering.

        Parameters
        ----------
        id : str

        created_at : dt.datetime

        type : str

        environment : str

        numerical_value : float

        string_value : str

        is_passed : bool

        cost : float

        evaluator_id : str

        log_id : str

        prompt_id : str

        prompt_version_number : int

        dataset_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicChEvalResultList


        Examples
        --------
        import asyncio
        import datetime

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_create(
                id="id",
                created_at=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                type="type",
                environment="environment",
                numerical_value=1.1,
                string_value="string_value",
                is_passed=True,
                cost=1.1,
                evaluator_id="evaluator_id",
                log_id="log_id",
                prompt_id="prompt_id",
                prompt_version_number=1,
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_create(
            id=id,
            created_at=created_at,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_update(
        self,
        *,
        id: str,
        created_at: dt.datetime,
        type: str,
        environment: str,
        numerical_value: float,
        string_value: str,
        is_passed: bool,
        cost: float,
        evaluator_id: str,
        log_id: str,
        prompt_id: str,
        prompt_version_number: int,
        dataset_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicChEvalResultList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        created_at : dt.datetime

        type : str

        environment : str

        numerical_value : float

        string_value : str

        is_passed : bool

        cost : float

        evaluator_id : str

        log_id : str

        prompt_id : str

        prompt_version_number : int

        dataset_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicChEvalResultList


        Examples
        --------
        import asyncio
        import datetime

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_update(
                id="id",
                created_at=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                type="type",
                environment="environment",
                numerical_value=1.1,
                string_value="string_value",
                is_passed=True,
                cost=1.1,
                evaluator_id="evaluator_id",
                log_id="log_id",
                prompt_id="prompt_id",
                prompt_version_number=1,
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_update(
            id=id,
            created_at=created_at,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        type: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        is_passed: typing.Optional[bool] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        log_id: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicChEvalResultList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        created_at : typing.Optional[dt.datetime]

        type : typing.Optional[str]

        environment : typing.Optional[str]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        is_passed : typing.Optional[bool]

        cost : typing.Optional[float]

        evaluator_id : typing.Optional[str]

        log_id : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        dataset_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicChEvalResultList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_partial_update(
            id=id,
            created_at=created_at,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicEvalResultDetail:
        """
        Operates on the Postgres-based EvalResult models for update and detail point retrieval
        Synced to clickhouse automatically via evaluation.signals

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvalResultDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_retrieve(id, request_options=request_options)
        return _response.data

    async def clickhouse_eval_results_create2(
        self,
        id: str,
        *,
        organization: int,
        unique_organization_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        is_passed: typing.Optional[bool] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_id: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvalResultDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        organization : int

        unique_organization_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        json_value : typing.Optional[str]

        is_passed : typing.Optional[bool]

        cost : typing.Optional[float]

        evaluator_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        scorer : typing.Optional[str]

        log_id : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        dataset_id : typing.Optional[str]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvalResultDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_create2(
                id="id",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_create2(
            id,
            organization=organization,
            unique_organization_id=unique_organization_id,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            json_value=json_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            evaluator_slug=evaluator_slug,
            scorer=scorer,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            status=status,
            error_message=error_message,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_update2(
        self,
        id: str,
        *,
        organization: int,
        unique_organization_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        is_passed: typing.Optional[bool] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_id: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvalResultDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        organization : int

        unique_organization_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        json_value : typing.Optional[str]

        is_passed : typing.Optional[bool]

        cost : typing.Optional[float]

        evaluator_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        scorer : typing.Optional[str]

        log_id : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        dataset_id : typing.Optional[str]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvalResultDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_update2(
                id="id",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_update2(
            id,
            organization=organization,
            unique_organization_id=unique_organization_id,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            json_value=json_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            evaluator_slug=evaluator_slug,
            scorer=scorer,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            status=status,
            error_message=error_message,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Operates on the Postgres-based EvalResult models for update and detail point retrieval
        Synced to clickhouse automatically via evaluation.signals

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_destroy(id, request_options=request_options)
        return _response.data

    async def clickhouse_eval_results_partial_update2(
        self,
        id: str,
        *,
        organization: typing.Optional[int] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        is_passed: typing.Optional[bool] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_id: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvalResultUpdate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : str

        organization : typing.Optional[int]

        unique_organization_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        json_value : typing.Optional[str]

        is_passed : typing.Optional[bool]

        cost : typing.Optional[float]

        evaluator_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        scorer : typing.Optional[str]

        log_id : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        dataset_id : typing.Optional[str]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvalResultUpdate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_partial_update2(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_partial_update2(
            id,
            organization=organization,
            unique_organization_id=unique_organization_id,
            type=type,
            environment=environment,
            numerical_value=numerical_value,
            string_value=string_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            json_value=json_value,
            is_passed=is_passed,
            cost=cost,
            evaluator_id=evaluator_id,
            evaluator_slug=evaluator_slug,
            scorer=scorer,
            log_id=log_id,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            dataset_id=dataset_id,
            status=status,
            error_message=error_message,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ChEvalResultList:
        """
        Summary view for evaluation results.

        Returns aggregated counts and score summaries grouped by evaluator.

        GET/POST /clickhouse/eval-results/summary/

        Response:
        {
            "summary": {
                "number_of_results": 1250
            },
            "scores": {
                "evaluator-id-1": {
                    "evaluator_id": "evaluator-id-1",
                    "evaluator_slug": "quality_v1",
                    "evaluator_name": "Response Quality",
                    "score_value_type": "numerical",
                    "avg_score": 4.2,
                    "true_count": null,
                    "false_count": null
                }
            }
        }

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChEvalResultList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_summary_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_summary_retrieve(request_options=request_options)
        return _response.data

    async def clickhouse_eval_results_summary_create(
        self,
        *,
        id: str,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        updated_at: typing.Optional[dt.datetime] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[ChEvalResultListRequestEvalClass] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChEvalResultList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        eval_result_unique_id : typing.Optional[str]

        created_at : typing.Optional[dt.datetime]

        updated_at : typing.Optional[dt.datetime]

        run_at : typing.Optional[dt.datetime]

        log_timestamp : typing.Optional[dt.datetime]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        eval_set_id : typing.Optional[str]

        evaluator_id : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        source : typing.Optional[str]

        eval_class : typing.Optional[ChEvalResultListRequestEvalClass]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[int]

        cost : typing.Optional[float]

        status : typing.Optional[str]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChEvalResultList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_summary_create(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_summary_create(
            id=id,
            eval_result_unique_id=eval_result_unique_id,
            created_at=created_at,
            updated_at=updated_at,
            run_at=run_at,
            log_timestamp=log_timestamp,
            unique_organization_id=unique_organization_id,
            environment=environment,
            type=type,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            eval_set_id=eval_set_id,
            evaluator_id=evaluator_id,
            workflow_version_id=workflow_version_id,
            automation_id=automation_id,
            source=source,
            eval_class=eval_class,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            pipeline_run_id=pipeline_run_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            cost=cost,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_summary_update(
        self,
        *,
        id: str,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        updated_at: typing.Optional[dt.datetime] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[ChEvalResultListRequestEvalClass] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChEvalResultList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        eval_result_unique_id : typing.Optional[str]

        created_at : typing.Optional[dt.datetime]

        updated_at : typing.Optional[dt.datetime]

        run_at : typing.Optional[dt.datetime]

        log_timestamp : typing.Optional[dt.datetime]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        eval_set_id : typing.Optional[str]

        evaluator_id : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        source : typing.Optional[str]

        eval_class : typing.Optional[ChEvalResultListRequestEvalClass]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[int]

        cost : typing.Optional[float]

        status : typing.Optional[str]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChEvalResultList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_summary_update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_summary_update(
            id=id,
            eval_result_unique_id=eval_result_unique_id,
            created_at=created_at,
            updated_at=updated_at,
            run_at=run_at,
            log_timestamp=log_timestamp,
            unique_organization_id=unique_organization_id,
            environment=environment,
            type=type,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            eval_set_id=eval_set_id,
            evaluator_id=evaluator_id,
            workflow_version_id=workflow_version_id,
            automation_id=automation_id,
            source=source,
            eval_class=eval_class,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            pipeline_run_id=pipeline_run_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            cost=cost,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_results_summary_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        created_at: typing.Optional[dt.datetime] = OMIT,
        updated_at: typing.Optional[dt.datetime] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[PatchedChEvalResultListRequestEvalClass] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChEvalResultList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        created_at : typing.Optional[dt.datetime]

        updated_at : typing.Optional[dt.datetime]

        run_at : typing.Optional[dt.datetime]

        log_timestamp : typing.Optional[dt.datetime]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        eval_set_id : typing.Optional[str]

        evaluator_id : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        source : typing.Optional[str]

        eval_class : typing.Optional[PatchedChEvalResultListRequestEvalClass]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[int]

        cost : typing.Optional[float]

        status : typing.Optional[str]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChEvalResultList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_results_summary_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_results_summary_partial_update(
            id=id,
            eval_result_unique_id=eval_result_unique_id,
            created_at=created_at,
            updated_at=updated_at,
            run_at=run_at,
            log_timestamp=log_timestamp,
            unique_organization_id=unique_organization_id,
            environment=environment,
            type=type,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            eval_set_id=eval_set_id,
            evaluator_id=evaluator_id,
            workflow_version_id=workflow_version_id,
            automation_id=automation_id,
            source=source,
            eval_class=eval_class,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            pipeline_run_id=pipeline_run_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            cost=cost,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            request_options=request_options,
        )
        return _response.data

    async def clickhouse_eval_sets_eval_result_aggs_list(
        self,
        dataset_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicDatasetTaskTrackerRunEvalListList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        dataset_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicDatasetTaskTrackerRunEvalListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_sets_eval_result_aggs_list(
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_sets_eval_result_aggs_list(
            dataset_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def clickhouse_eval_sets_eval_result_aggs_create(
        self,
        dataset_id_: str,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        evaluated_logs_count: typing.Optional[int] = OMIT,
        score: typing.Optional[float] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicDatasetTaskTrackerRunEvalList:
        """
        View mixin that handles both JWT and API Key authentication.

        Inherits from JWTAuthUtils:
        - is_jwt_auth(request): Post-auth check (reliable, uses DRF's successful_authenticator)
        - is_jwt_token_format(request): Pre-auth heuristic (used here to route authenticators)

        This mixin uses is_jwt_token_format() (pre-auth) in get_authenticators() and get_permissions()
        because those methods run BEFORE authentication completes. For post-auth checks,
        use is_jwt_auth() instead.

        Parameters
        ----------
        dataset_id_ : str

        task_id : str

        name : str

        dataset_id : str

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        evaluated_logs_count : typing.Optional[int]

        score : typing.Optional[float]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicDatasetTaskTrackerRunEvalList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.clickhouse_eval_sets_eval_result_aggs_create(
                dataset_id_="dataset_id",
                task_id="task_id",
                name="name",
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.clickhouse_eval_sets_eval_result_aggs_create(
            dataset_id_,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            evaluated_logs_count=evaluated_logs_count,
            score=score,
            request_options=request_options,
        )
        return _response.data

    async def list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorListList:
        """
        ## Creating an Evaluator

        ### LLM Evaluators

        For LLM evaluators, the frontend should first fetch available evaluation forms from the
        `/eval-forms/` endpoint to get the template configuration, then fill in the form and submit.

        **Required fields:**
        - `name` (str): Display name for the evaluator
        - `evaluator_slug` (str): Unique identifier for the evaluator within the organization
        - `type` (str): llm, human_boolean, human_categorical, human_numerical, human_text
        - `configurations` (dict): Complete evaluation form configuration
        - `description` (str, optional): Description of what this evaluator does
        - `enabled` (bool, optional): Whether the evaluator is active (default: False)

        **Example request body for LLM evaluator:**
        ```json
        {
            "name": "Output Length Checker",
            "type": "llm",
            "description": "Checks if the output meets character count requirements",
            "enabled": true,
            "configurations": {
                "eval_class": "output_char_count",
                "type": "function",
                "note": "",
                "display_name": "Output Character Count",
                "description": "Evaluates the length of the output text",
                "special_fields": [],
                "required_fields": [
                    {
                        "name": "llm_output",
                        "display_name": "LLM Output",
                        "type": "textarea",
                        "description": "The output text to evaluate",
                        "required": true,
                        "default_value": null,
                        "placeholder": "",
                        "choices": [],
                        "value": null
                    }
                ],
                "inference_filters": [],
                "allow_conditions": true,
                "score_mapping": {
                    "primary_score": "output_char_count",
                    "secondary_score": null,
                    "tertiary_score": null,
                    "quaternary_score": null
                },
                "category": "custom"
            }
        }
        ```

        ### Human Annotation Evaluators

        For human annotation evaluators, specify the type and provide choices for categorical evaluators.

        **Human Boolean Evaluator:**
        ```json
        {
            "name": "Quality Check",
            "evaluator_slug": "quality_check",
            "type": "human_boolean",
            "description": "Manual quality assessment"
        }
        ```

        **Human Categorical Evaluator:**
        ```json
        {
            "name": "Sentiment Rating",
            "evaluator_slug": "sentiment_rating",
            "type": "human_categorical",
            "description": "Manual sentiment classification",
            "categorical_choices": [
                {"name": "Positive", "value": 1},
                {"name": "Neutral", "value": 0},
                {"name": "Negative", "value": -1}
            ]
        }
        ```

        **Human Numerical Evaluator:**
        ```json
        {
            "name": "Quality Score",
            "evaluator_slug": "quality_score",
            "type": "human_numerical",
            "description": "Rate quality from 1-10"
        }
        ```

        **Human Text Evaluator:**
        ```json
        {
            "name": "Feedback Comments",
            "evaluator_slug": "feedback_comments",
            "type": "human_text",
            "description": "Detailed feedback comments"
        }
        ```

        ## Response

        Returns the created evaluator with all fields populated, including auto-generated fields like
        `id`, `created_at`, `updated_at`, and `evaluator_slug`.

        ## Validation

        - For LLM evaluators: The `configurations` field is validated against the corresponding
          evaluation form schema from `EVAL_FORMS_MAP`
        - For human categorical evaluators: `categorical_choices` must be a list of objects
          with `name` and `value` fields
        - The `eval_class` in configurations must exist in the available evaluation forms

        ## Notes

        - The `organization`, `created_by`, and `updated_by` fields are automatically set from
          the authenticated user
        - Each evaluator gets a unique `evaluator_slug` within the organization
        - LLM evaluators require a valid `eval_class` that maps to an available evaluation function

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list(page=page, page_size=page_size, request_options=request_options)
        return _response.data

    async def create(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.create(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def update(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.update(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def partial_update(
        self,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorCreate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.partial_update(
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def retrieve(
        self, evaluator_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicEvaluatorDetail:
        """
        Get, update, or delete an evaluator's draft version.

        GET /api/evaluators/{evaluator_id}/ - Get draft version (is_read_only=False)
        PATCH /api/evaluators/{evaluator_id}/ - Update draft version
        DELETE /api/evaluators/{evaluator_id}/ - Delete ALL versions

        Superadmin: Can READ any evaluator across all organizations via JWT.
                    Cannot WRITE via JWT - must use API key for write operations.
        Regular users: Can only access evaluators in their organization.

        NOTE: Queryset filters by is_read_only=False, ensuring unique lookup per evaluator_id.
        This allows DRF's standard get_object() to work without manual overrides.
        Delete removes ALL versions of the evaluator.

        Defense-in-depth:
        - SuperAdminMixin: Queryset routing + JWT write protection + object-level ownership

        Parameters
        ----------
        evaluator_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.retrieve(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve(evaluator_id, request_options=request_options)
        return _response.data

    async def create2(
        self,
        evaluator_id: str,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_class: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        evaluator_id : str

        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        eval_class : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.create2(
                evaluator_id="evaluator_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create2(
            evaluator_id,
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            eval_class=eval_class,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def update2(
        self,
        evaluator_id: str,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorUpdate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        evaluator_id : str

        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorUpdate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.update2(
                evaluator_id="evaluator_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update2(
            evaluator_id,
            name=name,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def destroy(self, evaluator_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete ALL versions of the evaluator.

        Uses SuperAdminMixin's queryset routing for org filtering.
        Cross-org JWT write protection is enforced automatically by ObjectOwnershipPermission
        in get_object() via check_object_permissions().

        Parameters
        ----------
        evaluator_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.destroy(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.destroy(evaluator_id, request_options=request_options)
        return _response.data

    async def partial_update2(
        self,
        evaluator_id: str,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorUpdate:
        """
        Update the draft version (queryset already filters is_read_only=False).

        Parameters
        ----------
        evaluator_id : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorUpdate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.partial_update2(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.partial_update2(
            evaluator_id,
            version_id=version_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def annotations_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationList:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def annotations_create(
        self,
        *,
        annotation_config: str,
        organization: int,
        id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        annotation_config : str

        organization : int

        id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_create(
                annotation_config="annotation_config",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_create(
            annotation_config=annotation_config,
            organization=organization,
            id=id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            log=log,
            testset_row=testset_row,
            request_options=request_options,
        )
        return _response.data

    async def annotations_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_retrieve(id, request_options=request_options)
        return _response.data

    async def annotations_update(
        self,
        id_: str,
        *,
        annotation_config: str,
        organization: int,
        id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        id_ : str

        annotation_config : str

        organization : int

        id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_update(
                id_="id",
                annotation_config="annotation_config",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_update(
            id_,
            annotation_config=annotation_config,
            organization=organization,
            id=id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            log=log,
            testset_row=testset_row,
            request_options=request_options,
        )
        return _response.data

    async def annotations_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_destroy(id, request_options=request_options)
        return _response.data

    async def annotations_partial_update(
        self,
        id: str,
        *,
        patched_annotation_request_id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        id : str

        patched_annotation_request_id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_partial_update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_partial_update(
            id,
            patched_annotation_request_id=patched_annotation_request_id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            annotation_config=annotation_config,
            log=log,
            testset_row=testset_row,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def annotations_in_log_list(
        self,
        log_id: int,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationList:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        log_id : int

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_in_log_list(
                log_id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_in_log_list(
            log_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def annotations_in_log_create(
        self,
        log_id: int,
        *,
        annotation_config: str,
        organization: int,
        id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        log_id : int

        annotation_config : str

        organization : int

        id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_in_log_create(
                log_id=1,
                annotation_config="annotation_config",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_in_log_create(
            log_id,
            annotation_config=annotation_config,
            organization=organization,
            id=id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            log=log,
            testset_row=testset_row,
            request_options=request_options,
        )
        return _response.data

    async def annotations_in_testset_row_list(
        self,
        testset_row_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedAnnotationList:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        testset_row_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedAnnotationList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_in_testset_row_list(
                testset_row_id="testset_row_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_in_testset_row_list(
            testset_row_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def annotations_in_testset_row_create(
        self,
        testset_row_id: str,
        *,
        annotation_config: str,
        organization: int,
        id: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[AnnotationTypeEnum] = OMIT,
        numerical_value: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        testset_row: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        testset_row_id : str

        annotation_config : str

        organization : int

        id : typing.Optional[str]

        project : typing.Optional[str]

        type : typing.Optional[AnnotationTypeEnum]

        numerical_value : typing.Optional[float]

        string_value : typing.Optional[str]

        log : typing.Optional[int]

        testset_row : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.annotations_in_testset_row_create(
                testset_row_id="testset_row_id",
                annotation_config="annotation_config",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.annotations_in_testset_row_create(
            testset_row_id,
            annotation_config=annotation_config,
            organization=organization,
            id=id,
            project=project,
            type=type,
            numerical_value=numerical_value,
            string_value=string_value,
            log=log,
            testset_row=testset_row,
            request_options=request_options,
        )
        return _response.data

    async def dataset_evaluation_tasks_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetTaskTrackerRunEvalListList:
        """
        View for creating new dataset evaluation tasks.

        Args:
            dataset_id: str, The ID of the dataset to run evaluation on
            evaluator_ids: str[], The IDs of the evaluators to run (preferred)
            evaluator_slugs: str[], Deprecated alias for evaluator_ids (backward compat)

        Returns:
            GET: A list of created tasks
            POST: The created eval task if successful, otherwise a dictionary of errors

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetTaskTrackerRunEvalListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_evaluation_tasks_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_evaluation_tasks_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def dataset_evaluation_tasks_create(
        self,
        *,
        dataset_id: str,
        evaluator_slug: str,
        unique_organization_id: str,
        experiment_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunEvaluationCreate:
        """
        Create a new dataset evaluation task.

        Accepts both `evaluator_ids` (preferred) and `evaluator_slugs` (deprecated alias).

        Optional: If experiment_id is provided, the experiment's evaluator_slugs
        will be updated to include the new evaluators, making them appear as
        columns in the experiment UI.

        Parameters
        ----------
        dataset_id : str

        evaluator_slug : str

        unique_organization_id : str

        experiment_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvaluationCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_evaluation_tasks_create(
                dataset_id="dataset_id",
                evaluator_slug="evaluator_slug",
                unique_organization_id="unique_organization_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_evaluation_tasks_create(
            dataset_id=dataset_id,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            experiment_id=experiment_id,
            request_options=request_options,
        )
        return _response.data

    async def dataset_evaluation_tasks_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetTaskTrackerRunEvaluationDetail:
        """
        Detail view for individual dataset evaluation tasks.
        Similar to EvalSetDetailView but for dataset evaluation tasks.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvaluationDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_evaluation_tasks_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_evaluation_tasks_retrieve(id, request_options=request_options)
        return _response.data

    async def dataset_evaluation_tasks_update(
        self,
        id: int,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunEvaluationDetail:
        """
        Detail view for individual dataset evaluation tasks.
        Similar to EvalSetDetailView but for dataset evaluation tasks.

        Parameters
        ----------
        id : int

        task_id : str

        name : str

        dataset_id : str

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        metadata : typing.Optional[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvaluationDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_evaluation_tasks_update(
                id=1,
                task_id="task_id",
                name="name",
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_evaluation_tasks_update(
            id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def dataset_evaluation_tasks_destroy(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Cancel/delete dataset evaluation task.

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_evaluation_tasks_destroy(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_evaluation_tasks_destroy(id, request_options=request_options)
        return _response.data

    async def dataset_evaluation_tasks_partial_update(
        self,
        id: int,
        *,
        task_id: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunEvaluationDetail:
        """
        Update dataset evaluation task metadata.

        Parameters
        ----------
        id : int

        task_id : typing.Optional[str]

        name : typing.Optional[str]

        dataset_id : typing.Optional[str]

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        metadata : typing.Optional[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvaluationDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_evaluation_tasks_partial_update(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_evaluation_tasks_partial_update(
            id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def dataset_evaluation_tasks_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetTaskTrackerRunEvalListList:
        """
        List view for dataset evaluation tasks with complex filtering support.
        Similar to EvalSetsListView but for dataset evaluation tasks.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetTaskTrackerRunEvalListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_evaluation_tasks_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_evaluation_tasks_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def dataset_evaluation_tasks_list_create(
        self,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        result_count: typing.Optional[int] = OMIT,
        primary_score_avg: typing.Optional[float] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        evaluator_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunEvalList:
        """
        Handle POST requests for complex filtering.

        Parameters
        ----------
        task_id : str

        name : str

        dataset_id : str

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        result_count : typing.Optional[int]

        primary_score_avg : typing.Optional[float]

        run_at : typing.Optional[dt.datetime]

        evaluator_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunEvalList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_evaluation_tasks_list_create(
                task_id="task_id",
                name="name",
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_evaluation_tasks_list_create(
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            result_count=result_count,
            primary_score_avg=primary_score_avg,
            run_at=run_at,
            evaluator_id=evaluator_id,
            request_options=request_options,
        )
        return _response.data

    async def dataset_run_logs_tasks_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetTaskTrackerRunLogsListList:
        """
        View for creating new dataset run logs tasks.
        Similar to EvalSetsView but for dataset run logs tasks.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetTaskTrackerRunLogsListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_run_logs_tasks_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_run_logs_tasks_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def dataset_run_logs_tasks_create(
        self, *, dataset_id: str, unique_organization_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetTaskTrackerRunLogsCreate:
        """
        Create a new dataset run logs task.

        Parameters
        ----------
        dataset_id : str

        unique_organization_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_run_logs_tasks_create(
                dataset_id="dataset_id",
                unique_organization_id="unique_organization_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_run_logs_tasks_create(
            dataset_id=dataset_id, unique_organization_id=unique_organization_id, request_options=request_options
        )
        return _response.data

    async def dataset_run_logs_tasks_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetTaskTrackerRunLogsDetail:
        """
        Detail view for individual dataset run logs tasks.
        Similar to EvalSetDetailView but for dataset run logs tasks.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_run_logs_tasks_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_run_logs_tasks_retrieve(id, request_options=request_options)
        return _response.data

    async def dataset_run_logs_tasks_update(
        self,
        id: str,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunLogsDetail:
        """
        Detail view for individual dataset run logs tasks.
        Similar to EvalSetDetailView but for dataset run logs tasks.

        Parameters
        ----------
        id : str

        task_id : str

        name : str

        dataset_id : str

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        metadata : typing.Optional[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_run_logs_tasks_update(
                id="id",
                task_id="task_id",
                name="name",
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_run_logs_tasks_update(
            id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def dataset_run_logs_tasks_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Cancel/delete dataset run logs task.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_run_logs_tasks_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_run_logs_tasks_destroy(id, request_options=request_options)
        return _response.data

    async def dataset_run_logs_tasks_partial_update(
        self,
        id: str,
        *,
        task_id: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        metadata: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunLogsDetail:
        """
        Update dataset run logs task metadata.

        Parameters
        ----------
        id : str

        task_id : typing.Optional[str]

        name : typing.Optional[str]

        dataset_id : typing.Optional[str]

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        metadata : typing.Optional[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_run_logs_tasks_partial_update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_run_logs_tasks_partial_update(
            id,
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            metadata=metadata,
            request_options=request_options,
        )
        return _response.data

    async def dataset_run_logs_tasks_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetTaskTrackerRunLogsListList:
        """
        List view for dataset LLM inference tasks with complex filtering support.
        Similar to EvalSetsListView but for dataset LLM inference tasks.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetTaskTrackerRunLogsListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_run_logs_tasks_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_run_logs_tasks_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def dataset_run_logs_tasks_list_create(
        self,
        *,
        task_id: str,
        name: str,
        dataset_id: str,
        type: typing.Optional[Type0CbEnum] = OMIT,
        status: typing.Optional[Status66CEnum] = OMIT,
        started_at: typing.Optional[dt.datetime] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        run_at: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetTaskTrackerRunLogsList:
        """
        Handle POST requests for complex filtering.

        Parameters
        ----------
        task_id : str

        name : str

        dataset_id : str

        type : typing.Optional[Type0CbEnum]

        status : typing.Optional[Status66CEnum]

        started_at : typing.Optional[dt.datetime]

        completed_at : typing.Optional[dt.datetime]

        error_message : typing.Optional[str]

        run_at : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetTaskTrackerRunLogsList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.dataset_run_logs_tasks_list_create(
                task_id="task_id",
                name="name",
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.dataset_run_logs_tasks_list_create(
            task_id=task_id,
            name=name,
            dataset_id=dataset_id,
            type=type,
            status=status,
            started_at=started_at,
            completed_at=completed_at,
            error_message=error_message,
            run_at=run_at,
            request_options=request_options,
        )
        return _response.data

    async def datasets_logs_list(
        self,
        dataset_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedChDatasetLogList:
        """
        Import existing logs into dataset based on filter criteria

        Endpoints:
            POST   /evaluations/datasets/{dataset_id}/logs/ - Import existing logs to dataset asynchronously (legacy)
            POST   /api/datasets/{dataset_id}/logs/import/ - Import existing logs to dataset asynchronously
            DELETE /api/datasets/{dataset_id}/logs/import/ - Remove logs from dataset asynchronously

        Args (POST body):
            - start_time (string, required, ISO 8601)
            - end_time (string, required, ISO 8601)
            - filters (object, optional; key name is "filters")
            - sampling_percentage (integer, optional, default 100)

        Returns (POST 200):
            { "message": "Logs are being imported to dataset in the background" }

        Args (DELETE body):
            - is_deleting_all_logs (boolean, required if filters not provided)
            - filters (object, required unless is_deleting_all_logs = true)

        Returns (DELETE 200):
            { "message": "Logs are being removed from dataset in the background" }

        Access Control:
            NestedResourceMixin handles superadmin-aware parent access:
            - Superadmin + JWT + READ: Can view any dataset's logs
            - Superadmin + JWT + WRITE: Blocked (can't import/delete logs in other orgs via JWT)
            - Superadmin + API key: Can import/delete logs in any dataset
            - Regular user: Can only access their org's datasets

        Parameters
        ----------
        dataset_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedChDatasetLogList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.datasets_logs_list(
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.datasets_logs_list(
            dataset_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def datasets_logs_create(
        self,
        dataset_id: str,
        *,
        start_time: dt.datetime,
        end_time: dt.datetime,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        sampling_percentage: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetLogsImportResponse:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        dataset_id : str

        start_time : dt.datetime

        end_time : dt.datetime

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter parameters keyed by metric name.

        sampling_percentage : typing.Optional[int]
            Percent of logs to import (1-100).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogsImportResponse


        Examples
        --------
        import asyncio
        import datetime

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.datasets_logs_create(
                dataset_id="dataset_id",
                start_time=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                end_time=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.datasets_logs_create(
            dataset_id,
            start_time=start_time,
            end_time=end_time,
            filters=filters,
            sampling_percentage=sampling_percentage,
            request_options=request_options,
        )
        return _response.data

    async def datasets_logs_update(
        self,
        dataset_id_: str,
        *,
        id: str,
        organization_id: str,
        unique_organization_id: typing.Optional[str] = OMIT,
        organization_name: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        completion_messages: typing.Optional[typing.Any] = OMIT,
        input: typing.Optional[str] = OMIT,
        output: typing.Optional[str] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        temperature: typing.Optional[float] = OMIT,
        max_tokens: typing.Optional[int] = OMIT,
        top_p: typing.Optional[float] = OMIT,
        frequency_penalty: typing.Optional[float] = OMIT,
        presence_penalty: typing.Optional[float] = OMIT,
        stop: typing.Optional[str] = OMIT,
        response_format: typing.Optional[typing.Any] = OMIT,
        matched_meter_ids: typing.Optional[typing.Sequence[typing.Any]] = OMIT,
        unit_prices: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        component_costs: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        original_copy_unique_id: typing.Optional[str] = OMIT,
        comparison_key: typing.Optional[str] = OMIT,
        expected_output: typing.Optional[str] = OMIT,
        custom_identifier: typing.Optional[str] = OMIT,
        group_identifier: typing.Optional[str] = OMIT,
        blurred: typing.Optional[bool] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        timestamp: typing.Optional[dt.datetime] = OMIT,
        load_balance_group_id: typing.Optional[str] = OMIT,
        prompt_tokens: typing.Optional[int] = OMIT,
        completion_tokens: typing.Optional[int] = OMIT,
        prompt_cache_hit_tokens: typing.Optional[int] = OMIT,
        prompt_cache_creation_tokens: typing.Optional[int] = OMIT,
        reasoning_tokens: typing.Optional[int] = OMIT,
        total_request_tokens: typing.Optional[int] = OMIT,
        is_token_count_estimated: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        llm_gateway_markup_rate: typing.Optional[float] = OMIT,
        service_tier: typing.Optional[str] = OMIT,
        model_discount: typing.Optional[float] = OMIT,
        pricing_tier: typing.Optional[str] = OMIT,
        audio_input_file: typing.Optional[str] = OMIT,
        audio_output_file: typing.Optional[str] = OMIT,
        organization_key_id: typing.Optional[str] = OMIT,
        user_email: typing.Optional[str] = OMIT,
        model: typing.Optional[str] = OMIT,
        provider_id: typing.Optional[str] = OMIT,
        category: typing.Optional[str] = OMIT,
        properties: typing.Optional[str] = OMIT,
        cache_bit: typing.Optional[int] = OMIT,
        cache_miss_bit: typing.Optional[int] = OMIT,
        cache_key: typing.Optional[str] = OMIT,
        latency: typing.Optional[float] = OMIT,
        tokens_per_second: typing.Optional[float] = OMIT,
        time_to_first_token: typing.Optional[float] = OMIT,
        routing_time: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        has_tool_calls: typing.Optional[bool] = OMIT,
        status_code: typing.Optional[int] = OMIT,
        log_method: typing.Optional[str] = OMIT,
        log_type: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        stream: typing.Optional[bool] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        customer_email: typing.Optional[str] = OMIT,
        customer_name: typing.Optional[str] = OMIT,
        customer_user_unique_id: typing.Optional[str] = OMIT,
        used_custom_credential: typing.Optional[bool] = OMIT,
        deployment_name: typing.Optional[str] = OMIT,
        deployment_id: typing.Optional[str] = OMIT,
        prompt_name: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        system_text: typing.Optional[str] = OMIT,
        prompt_text: typing.Optional[str] = OMIT,
        completion_text: typing.Optional[str] = OMIT,
        prompt_message_count: typing.Optional[int] = OMIT,
        completion_message_count: typing.Optional[int] = OMIT,
        trace_unique_id: typing.Optional[str] = OMIT,
        span_unique_id: typing.Optional[str] = OMIT,
        span_name: typing.Optional[str] = OMIT,
        span_parent_id: typing.Optional[str] = OMIT,
        span_workflow_name: typing.Optional[str] = OMIT,
        session_identifier: typing.Optional[str] = OMIT,
        span_links: typing.Optional[str] = OMIT,
        trace_group_identifier: typing.Optional[str] = OMIT,
        thread_identifier: typing.Optional[str] = OMIT,
        thread_unique_id: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        period_start: typing.Optional[dt.datetime] = OMIT,
        period_end: typing.Optional[dt.datetime] = OMIT,
        unique_id: typing.Optional[str] = OMIT,
        respan_gateway_request_id: typing.Optional[str] = OMIT,
        full_text: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChDatasetLog:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        dataset_id_ : str

        id : str

        organization_id : str

        unique_organization_id : typing.Optional[str]

        organization_name : typing.Optional[str]

        error_message : typing.Optional[str]

        completion_messages : typing.Optional[typing.Any]

        input : typing.Optional[str]

        output : typing.Optional[str]

        variables : typing.Optional[typing.Any]

        temperature : typing.Optional[float]

        max_tokens : typing.Optional[int]

        top_p : typing.Optional[float]

        frequency_penalty : typing.Optional[float]

        presence_penalty : typing.Optional[float]

        stop : typing.Optional[str]

        response_format : typing.Optional[typing.Any]

        matched_meter_ids : typing.Optional[typing.Sequence[typing.Any]]

        unit_prices : typing.Optional[typing.Dict[str, typing.Any]]

        component_costs : typing.Optional[typing.Dict[str, typing.Any]]

        dataset_id : typing.Optional[str]

        original_copy_unique_id : typing.Optional[str]

        comparison_key : typing.Optional[str]

        expected_output : typing.Optional[str]

        custom_identifier : typing.Optional[str]

        group_identifier : typing.Optional[str]

        blurred : typing.Optional[bool]

        start_time : typing.Optional[dt.datetime]

        timestamp : typing.Optional[dt.datetime]

        load_balance_group_id : typing.Optional[str]

        prompt_tokens : typing.Optional[int]

        completion_tokens : typing.Optional[int]

        prompt_cache_hit_tokens : typing.Optional[int]

        prompt_cache_creation_tokens : typing.Optional[int]

        reasoning_tokens : typing.Optional[int]

        total_request_tokens : typing.Optional[int]

        is_token_count_estimated : typing.Optional[int]

        cost : typing.Optional[float]

        llm_gateway_markup_rate : typing.Optional[float]

        service_tier : typing.Optional[str]

        model_discount : typing.Optional[float]

        pricing_tier : typing.Optional[str]

        audio_input_file : typing.Optional[str]

        audio_output_file : typing.Optional[str]

        organization_key_id : typing.Optional[str]

        user_email : typing.Optional[str]

        model : typing.Optional[str]

        provider_id : typing.Optional[str]

        category : typing.Optional[str]

        properties : typing.Optional[str]

        cache_bit : typing.Optional[int]

        cache_miss_bit : typing.Optional[int]

        cache_key : typing.Optional[str]

        latency : typing.Optional[float]

        tokens_per_second : typing.Optional[float]

        time_to_first_token : typing.Optional[float]

        routing_time : typing.Optional[float]

        status : typing.Optional[str]

        has_tool_calls : typing.Optional[bool]

        status_code : typing.Optional[int]

        log_method : typing.Optional[str]

        log_type : typing.Optional[str]

        environment : typing.Optional[str]

        stream : typing.Optional[bool]

        evaluation_identifier : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        customer_email : typing.Optional[str]

        customer_name : typing.Optional[str]

        customer_user_unique_id : typing.Optional[str]

        used_custom_credential : typing.Optional[bool]

        deployment_name : typing.Optional[str]

        deployment_id : typing.Optional[str]

        prompt_name : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        system_text : typing.Optional[str]

        prompt_text : typing.Optional[str]

        completion_text : typing.Optional[str]

        prompt_message_count : typing.Optional[int]

        completion_message_count : typing.Optional[int]

        trace_unique_id : typing.Optional[str]

        span_unique_id : typing.Optional[str]

        span_name : typing.Optional[str]

        span_parent_id : typing.Optional[str]

        span_workflow_name : typing.Optional[str]

        session_identifier : typing.Optional[str]

        span_links : typing.Optional[str]

        trace_group_identifier : typing.Optional[str]

        thread_identifier : typing.Optional[str]

        thread_unique_id : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        period_start : typing.Optional[dt.datetime]

        period_end : typing.Optional[dt.datetime]

        unique_id : typing.Optional[str]

        respan_gateway_request_id : typing.Optional[str]

        full_text : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChDatasetLog


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.datasets_logs_update(
                dataset_id_="dataset_id",
                id="id",
                organization_id="organization_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.datasets_logs_update(
            dataset_id_,
            id=id,
            organization_id=organization_id,
            unique_organization_id=unique_organization_id,
            organization_name=organization_name,
            error_message=error_message,
            completion_messages=completion_messages,
            input=input,
            output=output,
            variables=variables,
            temperature=temperature,
            max_tokens=max_tokens,
            top_p=top_p,
            frequency_penalty=frequency_penalty,
            presence_penalty=presence_penalty,
            stop=stop,
            response_format=response_format,
            matched_meter_ids=matched_meter_ids,
            unit_prices=unit_prices,
            component_costs=component_costs,
            dataset_id=dataset_id,
            original_copy_unique_id=original_copy_unique_id,
            comparison_key=comparison_key,
            expected_output=expected_output,
            custom_identifier=custom_identifier,
            group_identifier=group_identifier,
            blurred=blurred,
            start_time=start_time,
            timestamp=timestamp,
            load_balance_group_id=load_balance_group_id,
            prompt_tokens=prompt_tokens,
            completion_tokens=completion_tokens,
            prompt_cache_hit_tokens=prompt_cache_hit_tokens,
            prompt_cache_creation_tokens=prompt_cache_creation_tokens,
            reasoning_tokens=reasoning_tokens,
            total_request_tokens=total_request_tokens,
            is_token_count_estimated=is_token_count_estimated,
            cost=cost,
            llm_gateway_markup_rate=llm_gateway_markup_rate,
            service_tier=service_tier,
            model_discount=model_discount,
            pricing_tier=pricing_tier,
            audio_input_file=audio_input_file,
            audio_output_file=audio_output_file,
            organization_key_id=organization_key_id,
            user_email=user_email,
            model=model,
            provider_id=provider_id,
            category=category,
            properties=properties,
            cache_bit=cache_bit,
            cache_miss_bit=cache_miss_bit,
            cache_key=cache_key,
            latency=latency,
            tokens_per_second=tokens_per_second,
            time_to_first_token=time_to_first_token,
            routing_time=routing_time,
            status=status,
            has_tool_calls=has_tool_calls,
            status_code=status_code,
            log_method=log_method,
            log_type=log_type,
            environment=environment,
            stream=stream,
            evaluation_identifier=evaluation_identifier,
            customer_identifier=customer_identifier,
            customer_email=customer_email,
            customer_name=customer_name,
            customer_user_unique_id=customer_user_unique_id,
            used_custom_credential=used_custom_credential,
            deployment_name=deployment_name,
            deployment_id=deployment_id,
            prompt_name=prompt_name,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            system_text=system_text,
            prompt_text=prompt_text,
            completion_text=completion_text,
            prompt_message_count=prompt_message_count,
            completion_message_count=completion_message_count,
            trace_unique_id=trace_unique_id,
            span_unique_id=span_unique_id,
            span_name=span_name,
            span_parent_id=span_parent_id,
            span_workflow_name=span_workflow_name,
            session_identifier=session_identifier,
            span_links=span_links,
            trace_group_identifier=trace_group_identifier,
            thread_identifier=thread_identifier,
            thread_unique_id=thread_unique_id,
            storage_object_key=storage_object_key,
            period_start=period_start,
            period_end=period_end,
            unique_id=unique_id,
            respan_gateway_request_id=respan_gateway_request_id,
            full_text=full_text,
            request_options=request_options,
        )
        return _response.data

    async def datasets_logs_destroy(
        self, dataset_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetLogsImportResponse:
        """
        Import existing logs into dataset based on filter criteria

        Endpoints:
            POST   /evaluations/datasets/{dataset_id}/logs/ - Import existing logs to dataset asynchronously (legacy)
            POST   /api/datasets/{dataset_id}/logs/import/ - Import existing logs to dataset asynchronously
            DELETE /api/datasets/{dataset_id}/logs/import/ - Remove logs from dataset asynchronously

        Args (POST body):
            - start_time (string, required, ISO 8601)
            - end_time (string, required, ISO 8601)
            - filters (object, optional; key name is "filters")
            - sampling_percentage (integer, optional, default 100)

        Returns (POST 200):
            { "message": "Logs are being imported to dataset in the background" }

        Args (DELETE body):
            - is_deleting_all_logs (boolean, required if filters not provided)
            - filters (object, required unless is_deleting_all_logs = true)

        Returns (DELETE 200):
            { "message": "Logs are being removed from dataset in the background" }

        Access Control:
            NestedResourceMixin handles superadmin-aware parent access:
            - Superadmin + JWT + READ: Can view any dataset's logs
            - Superadmin + JWT + WRITE: Blocked (can't import/delete logs in other orgs via JWT)
            - Superadmin + API key: Can import/delete logs in any dataset
            - Regular user: Can only access their org's datasets

        Parameters
        ----------
        dataset_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogsImportResponse


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.datasets_logs_destroy(
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.datasets_logs_destroy(dataset_id, request_options=request_options)
        return _response.data

    async def datasets_logs_partial_update(
        self,
        dataset_id_: str,
        *,
        id: typing.Optional[str] = OMIT,
        organization_id: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        organization_name: typing.Optional[str] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        completion_messages: typing.Optional[typing.Any] = OMIT,
        input: typing.Optional[str] = OMIT,
        output: typing.Optional[str] = OMIT,
        variables: typing.Optional[typing.Any] = OMIT,
        temperature: typing.Optional[float] = OMIT,
        max_tokens: typing.Optional[int] = OMIT,
        top_p: typing.Optional[float] = OMIT,
        frequency_penalty: typing.Optional[float] = OMIT,
        presence_penalty: typing.Optional[float] = OMIT,
        stop: typing.Optional[str] = OMIT,
        response_format: typing.Optional[typing.Any] = OMIT,
        matched_meter_ids: typing.Optional[typing.Sequence[typing.Any]] = OMIT,
        unit_prices: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        component_costs: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        dataset_id: typing.Optional[str] = OMIT,
        original_copy_unique_id: typing.Optional[str] = OMIT,
        comparison_key: typing.Optional[str] = OMIT,
        expected_output: typing.Optional[str] = OMIT,
        custom_identifier: typing.Optional[str] = OMIT,
        group_identifier: typing.Optional[str] = OMIT,
        blurred: typing.Optional[bool] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        timestamp: typing.Optional[dt.datetime] = OMIT,
        load_balance_group_id: typing.Optional[str] = OMIT,
        prompt_tokens: typing.Optional[int] = OMIT,
        completion_tokens: typing.Optional[int] = OMIT,
        prompt_cache_hit_tokens: typing.Optional[int] = OMIT,
        prompt_cache_creation_tokens: typing.Optional[int] = OMIT,
        reasoning_tokens: typing.Optional[int] = OMIT,
        total_request_tokens: typing.Optional[int] = OMIT,
        is_token_count_estimated: typing.Optional[int] = OMIT,
        cost: typing.Optional[float] = OMIT,
        llm_gateway_markup_rate: typing.Optional[float] = OMIT,
        service_tier: typing.Optional[str] = OMIT,
        model_discount: typing.Optional[float] = OMIT,
        pricing_tier: typing.Optional[str] = OMIT,
        audio_input_file: typing.Optional[str] = OMIT,
        audio_output_file: typing.Optional[str] = OMIT,
        organization_key_id: typing.Optional[str] = OMIT,
        user_email: typing.Optional[str] = OMIT,
        model: typing.Optional[str] = OMIT,
        provider_id: typing.Optional[str] = OMIT,
        category: typing.Optional[str] = OMIT,
        properties: typing.Optional[str] = OMIT,
        cache_bit: typing.Optional[int] = OMIT,
        cache_miss_bit: typing.Optional[int] = OMIT,
        cache_key: typing.Optional[str] = OMIT,
        latency: typing.Optional[float] = OMIT,
        tokens_per_second: typing.Optional[float] = OMIT,
        time_to_first_token: typing.Optional[float] = OMIT,
        routing_time: typing.Optional[float] = OMIT,
        status: typing.Optional[str] = OMIT,
        has_tool_calls: typing.Optional[bool] = OMIT,
        status_code: typing.Optional[int] = OMIT,
        log_method: typing.Optional[str] = OMIT,
        log_type: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        stream: typing.Optional[bool] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        customer_email: typing.Optional[str] = OMIT,
        customer_name: typing.Optional[str] = OMIT,
        customer_user_unique_id: typing.Optional[str] = OMIT,
        used_custom_credential: typing.Optional[bool] = OMIT,
        deployment_name: typing.Optional[str] = OMIT,
        deployment_id: typing.Optional[str] = OMIT,
        prompt_name: typing.Optional[str] = OMIT,
        prompt_id: typing.Optional[str] = OMIT,
        prompt_version_number: typing.Optional[int] = OMIT,
        system_text: typing.Optional[str] = OMIT,
        prompt_text: typing.Optional[str] = OMIT,
        completion_text: typing.Optional[str] = OMIT,
        prompt_message_count: typing.Optional[int] = OMIT,
        completion_message_count: typing.Optional[int] = OMIT,
        trace_unique_id: typing.Optional[str] = OMIT,
        span_unique_id: typing.Optional[str] = OMIT,
        span_name: typing.Optional[str] = OMIT,
        span_parent_id: typing.Optional[str] = OMIT,
        span_workflow_name: typing.Optional[str] = OMIT,
        session_identifier: typing.Optional[str] = OMIT,
        span_links: typing.Optional[str] = OMIT,
        trace_group_identifier: typing.Optional[str] = OMIT,
        thread_identifier: typing.Optional[str] = OMIT,
        thread_unique_id: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        period_start: typing.Optional[dt.datetime] = OMIT,
        period_end: typing.Optional[dt.datetime] = OMIT,
        unique_id: typing.Optional[str] = OMIT,
        respan_gateway_request_id: typing.Optional[str] = OMIT,
        full_text: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ChDatasetLog:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        dataset_id_ : str

        id : typing.Optional[str]

        organization_id : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        organization_name : typing.Optional[str]

        error_message : typing.Optional[str]

        completion_messages : typing.Optional[typing.Any]

        input : typing.Optional[str]

        output : typing.Optional[str]

        variables : typing.Optional[typing.Any]

        temperature : typing.Optional[float]

        max_tokens : typing.Optional[int]

        top_p : typing.Optional[float]

        frequency_penalty : typing.Optional[float]

        presence_penalty : typing.Optional[float]

        stop : typing.Optional[str]

        response_format : typing.Optional[typing.Any]

        matched_meter_ids : typing.Optional[typing.Sequence[typing.Any]]

        unit_prices : typing.Optional[typing.Dict[str, typing.Any]]

        component_costs : typing.Optional[typing.Dict[str, typing.Any]]

        dataset_id : typing.Optional[str]

        original_copy_unique_id : typing.Optional[str]

        comparison_key : typing.Optional[str]

        expected_output : typing.Optional[str]

        custom_identifier : typing.Optional[str]

        group_identifier : typing.Optional[str]

        blurred : typing.Optional[bool]

        start_time : typing.Optional[dt.datetime]

        timestamp : typing.Optional[dt.datetime]

        load_balance_group_id : typing.Optional[str]

        prompt_tokens : typing.Optional[int]

        completion_tokens : typing.Optional[int]

        prompt_cache_hit_tokens : typing.Optional[int]

        prompt_cache_creation_tokens : typing.Optional[int]

        reasoning_tokens : typing.Optional[int]

        total_request_tokens : typing.Optional[int]

        is_token_count_estimated : typing.Optional[int]

        cost : typing.Optional[float]

        llm_gateway_markup_rate : typing.Optional[float]

        service_tier : typing.Optional[str]

        model_discount : typing.Optional[float]

        pricing_tier : typing.Optional[str]

        audio_input_file : typing.Optional[str]

        audio_output_file : typing.Optional[str]

        organization_key_id : typing.Optional[str]

        user_email : typing.Optional[str]

        model : typing.Optional[str]

        provider_id : typing.Optional[str]

        category : typing.Optional[str]

        properties : typing.Optional[str]

        cache_bit : typing.Optional[int]

        cache_miss_bit : typing.Optional[int]

        cache_key : typing.Optional[str]

        latency : typing.Optional[float]

        tokens_per_second : typing.Optional[float]

        time_to_first_token : typing.Optional[float]

        routing_time : typing.Optional[float]

        status : typing.Optional[str]

        has_tool_calls : typing.Optional[bool]

        status_code : typing.Optional[int]

        log_method : typing.Optional[str]

        log_type : typing.Optional[str]

        environment : typing.Optional[str]

        stream : typing.Optional[bool]

        evaluation_identifier : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        customer_email : typing.Optional[str]

        customer_name : typing.Optional[str]

        customer_user_unique_id : typing.Optional[str]

        used_custom_credential : typing.Optional[bool]

        deployment_name : typing.Optional[str]

        deployment_id : typing.Optional[str]

        prompt_name : typing.Optional[str]

        prompt_id : typing.Optional[str]

        prompt_version_number : typing.Optional[int]

        system_text : typing.Optional[str]

        prompt_text : typing.Optional[str]

        completion_text : typing.Optional[str]

        prompt_message_count : typing.Optional[int]

        completion_message_count : typing.Optional[int]

        trace_unique_id : typing.Optional[str]

        span_unique_id : typing.Optional[str]

        span_name : typing.Optional[str]

        span_parent_id : typing.Optional[str]

        span_workflow_name : typing.Optional[str]

        session_identifier : typing.Optional[str]

        span_links : typing.Optional[str]

        trace_group_identifier : typing.Optional[str]

        thread_identifier : typing.Optional[str]

        thread_unique_id : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        period_start : typing.Optional[dt.datetime]

        period_end : typing.Optional[dt.datetime]

        unique_id : typing.Optional[str]

        respan_gateway_request_id : typing.Optional[str]

        full_text : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChDatasetLog


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.datasets_logs_partial_update(
                dataset_id_="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.datasets_logs_partial_update(
            dataset_id_,
            id=id,
            organization_id=organization_id,
            unique_organization_id=unique_organization_id,
            organization_name=organization_name,
            error_message=error_message,
            completion_messages=completion_messages,
            input=input,
            output=output,
            variables=variables,
            temperature=temperature,
            max_tokens=max_tokens,
            top_p=top_p,
            frequency_penalty=frequency_penalty,
            presence_penalty=presence_penalty,
            stop=stop,
            response_format=response_format,
            matched_meter_ids=matched_meter_ids,
            unit_prices=unit_prices,
            component_costs=component_costs,
            dataset_id=dataset_id,
            original_copy_unique_id=original_copy_unique_id,
            comparison_key=comparison_key,
            expected_output=expected_output,
            custom_identifier=custom_identifier,
            group_identifier=group_identifier,
            blurred=blurred,
            start_time=start_time,
            timestamp=timestamp,
            load_balance_group_id=load_balance_group_id,
            prompt_tokens=prompt_tokens,
            completion_tokens=completion_tokens,
            prompt_cache_hit_tokens=prompt_cache_hit_tokens,
            prompt_cache_creation_tokens=prompt_cache_creation_tokens,
            reasoning_tokens=reasoning_tokens,
            total_request_tokens=total_request_tokens,
            is_token_count_estimated=is_token_count_estimated,
            cost=cost,
            llm_gateway_markup_rate=llm_gateway_markup_rate,
            service_tier=service_tier,
            model_discount=model_discount,
            pricing_tier=pricing_tier,
            audio_input_file=audio_input_file,
            audio_output_file=audio_output_file,
            organization_key_id=organization_key_id,
            user_email=user_email,
            model=model,
            provider_id=provider_id,
            category=category,
            properties=properties,
            cache_bit=cache_bit,
            cache_miss_bit=cache_miss_bit,
            cache_key=cache_key,
            latency=latency,
            tokens_per_second=tokens_per_second,
            time_to_first_token=time_to_first_token,
            routing_time=routing_time,
            status=status,
            has_tool_calls=has_tool_calls,
            status_code=status_code,
            log_method=log_method,
            log_type=log_type,
            environment=environment,
            stream=stream,
            evaluation_identifier=evaluation_identifier,
            customer_identifier=customer_identifier,
            customer_email=customer_email,
            customer_name=customer_name,
            customer_user_unique_id=customer_user_unique_id,
            used_custom_credential=used_custom_credential,
            deployment_name=deployment_name,
            deployment_id=deployment_id,
            prompt_name=prompt_name,
            prompt_id=prompt_id,
            prompt_version_number=prompt_version_number,
            system_text=system_text,
            prompt_text=prompt_text,
            completion_text=completion_text,
            prompt_message_count=prompt_message_count,
            completion_message_count=completion_message_count,
            trace_unique_id=trace_unique_id,
            span_unique_id=span_unique_id,
            span_name=span_name,
            span_parent_id=span_parent_id,
            span_workflow_name=span_workflow_name,
            session_identifier=session_identifier,
            span_links=span_links,
            trace_group_identifier=trace_group_identifier,
            thread_identifier=thread_identifier,
            thread_unique_id=thread_unique_id,
            storage_object_key=storage_object_key,
            period_start=period_start,
            period_end=period_end,
            unique_id=unique_id,
            respan_gateway_request_id=respan_gateway_request_id,
            full_text=full_text,
            request_options=request_options,
        )
        return _response.data

    async def datasets_presence_retrieve(
        self, dataset_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetLogPresenceResponse:
        """
        Get all users currently viewing logs in this dataset from Redis cache.

        Parameters
        ----------
        dataset_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogPresenceResponse


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.datasets_presence_retrieve(
                dataset_id="dataset_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.datasets_presence_retrieve(dataset_id, request_options=request_options)
        return _response.data

    async def eval_sets_logs_status_list(
        self, eval_set_id: str, log_unique_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[DatasetLogStatusCreate]:
        """
        A mixin that provides version handling for API views.

        Reads the X-Keywords-AI-Version header and sets self.version.
        Default version is 0 if header is not present or invalid.

        Parameters
        ----------
        eval_set_id : str

        log_unique_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[DatasetLogStatusCreate]


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_logs_status_list(
                eval_set_id="eval_set_id",
                log_unique_id="log_unique_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_logs_status_list(
            eval_set_id, log_unique_id, request_options=request_options
        )
        return _response.data

    async def eval_sets_logs_status_create(
        self,
        eval_set_id: str,
        log_unique_id: str,
        *,
        organization: typing.Optional[int] = OMIT,
        completed_by: typing.Optional[int] = OMIT,
        status: typing.Optional[DatasetLogStatusCreateStatusEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetLogStatusCreate:
        """
        A mixin that provides version handling for API views.

        Reads the X-Keywords-AI-Version header and sets self.version.
        Default version is 0 if header is not present or invalid.

        Parameters
        ----------
        eval_set_id : str

        log_unique_id : str

        organization : typing.Optional[int]

        completed_by : typing.Optional[int]

        status : typing.Optional[DatasetLogStatusCreateStatusEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogStatusCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_logs_status_create(
                eval_set_id="eval_set_id",
                log_unique_id="log_unique_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_logs_status_create(
            eval_set_id,
            log_unique_id,
            organization=organization,
            completed_by=completed_by,
            status=status,
            request_options=request_options,
        )
        return _response.data

    async def eval_sets_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> DatasetDetail:
        """
        Retrieve, update, and delete a dataset

        Endpoints:
            GET /api/datasets/{dataset_id}/
            PATCH /api/datasets/{dataset_id}/
            DELETE /api/datasets/{dataset_id}/

        Superadmin: Can READ any dataset across all organizations via JWT.
                    Cannot WRITE via JWT - must use API key for write operations.
        Regular users: Can only access datasets in their organization.

        Defense-in-depth:

        Args (PATCH):
            - name (Optional): string
            - description (Optional): string

        Returns (GET 200):
            {
              "id": "dataset_id",
              "name": "Support Conversations - July",
              "type": "sampling",
              "description": "Sampled support chats for July",
              "created_at": "2025-07-26T00:00:00Z",
              "updated_at": "2025-07-27T08:10:00Z",
              "organization": 123,
              "initial_log_filters": {"status_code": {"operator": "eq", "value": 200}},
              "unique_organization_ids": [],
              "timestamps": [],
              "log_count": 250,
              "evaluator": null,
              "status": "ready",
              "running_status": "pending",
              "running_progress": 0,
              "running_at": null,
              "completed_annotation_count": 0
            }

        Returns (PATCH 200): Same shape as GET with updated fields
        Returns (DELETE 204): No content

        Defense-in-depth: SuperAdminMixin provides queryset routing + object-level ownership.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_retrieve(id, request_options=request_options)
        return _response.data

    async def eval_sets_create(
        self,
        id: str,
        *,
        name: str,
        organization: int,
        initial_log_filters: typing.Optional[FilterParamDictPydantic] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        description: typing.Optional[str] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_at: typing.Optional[dt.datetime] = OMIT,
        unique_organization_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        timestamps: typing.Optional[typing.Sequence[dt.datetime]] = OMIT,
        ingest_workflow_id: typing.Optional[str] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        evaluator: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        name : str

        organization : int

        initial_log_filters : typing.Optional[FilterParamDictPydantic]

        project : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        description : typing.Optional[str]

        running_progress : typing.Optional[float]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_at : typing.Optional[dt.datetime]

        unique_organization_ids : typing.Optional[typing.Sequence[str]]

        timestamps : typing.Optional[typing.Sequence[dt.datetime]]

        ingest_workflow_id : typing.Optional[str]

        starred : typing.Optional[bool]

        evaluator : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_create(
                id="id",
                name="name",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_create(
            id,
            name=name,
            organization=organization,
            initial_log_filters=initial_log_filters,
            project=project,
            type=type,
            description=description,
            running_progress=running_progress,
            running_status=running_status,
            running_at=running_at,
            unique_organization_ids=unique_organization_ids,
            timestamps=timestamps,
            ingest_workflow_id=ingest_workflow_id,
            starred=starred,
            evaluator=evaluator,
            request_options=request_options,
        )
        return _response.data

    async def eval_sets_update(
        self,
        id: str,
        *,
        name: str,
        organization: int,
        initial_log_filters: typing.Optional[FilterParamDictPydantic] = OMIT,
        project: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        description: typing.Optional[str] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_at: typing.Optional[dt.datetime] = OMIT,
        unique_organization_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        timestamps: typing.Optional[typing.Sequence[dt.datetime]] = OMIT,
        ingest_workflow_id: typing.Optional[str] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        evaluator: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        name : str

        organization : int

        initial_log_filters : typing.Optional[FilterParamDictPydantic]

        project : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        description : typing.Optional[str]

        running_progress : typing.Optional[float]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_at : typing.Optional[dt.datetime]

        unique_organization_ids : typing.Optional[typing.Sequence[str]]

        timestamps : typing.Optional[typing.Sequence[dt.datetime]]

        ingest_workflow_id : typing.Optional[str]

        starred : typing.Optional[bool]

        evaluator : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_update(
                id="id",
                name="name",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_update(
            id,
            name=name,
            organization=organization,
            initial_log_filters=initial_log_filters,
            project=project,
            type=type,
            description=description,
            running_progress=running_progress,
            running_status=running_status,
            running_at=running_at,
            unique_organization_ids=unique_organization_ids,
            timestamps=timestamps,
            ingest_workflow_id=ingest_workflow_id,
            starred=starred,
            evaluator=evaluator,
            request_options=request_options,
        )
        return _response.data

    async def eval_sets_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Retrieve, update, and delete a dataset

        Endpoints:
            GET /api/datasets/{dataset_id}/
            PATCH /api/datasets/{dataset_id}/
            DELETE /api/datasets/{dataset_id}/

        Superadmin: Can READ any dataset across all organizations via JWT.
                    Cannot WRITE via JWT - must use API key for write operations.
        Regular users: Can only access datasets in their organization.

        Defense-in-depth:

        Args (PATCH):
            - name (Optional): string
            - description (Optional): string

        Returns (GET 200):
            {
              "id": "dataset_id",
              "name": "Support Conversations - July",
              "type": "sampling",
              "description": "Sampled support chats for July",
              "created_at": "2025-07-26T00:00:00Z",
              "updated_at": "2025-07-27T08:10:00Z",
              "organization": 123,
              "initial_log_filters": {"status_code": {"operator": "eq", "value": 200}},
              "unique_organization_ids": [],
              "timestamps": [],
              "log_count": 250,
              "evaluator": null,
              "status": "ready",
              "running_status": "pending",
              "running_progress": 0,
              "running_at": null,
              "completed_annotation_count": 0
            }

        Returns (PATCH 200): Same shape as GET with updated fields
        Returns (DELETE 204): No content

        Defense-in-depth: SuperAdminMixin provides queryset routing + object-level ownership.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_destroy(id, request_options=request_options)
        return _response.data

    async def eval_sets_partial_update(
        self,
        id: str,
        *,
        initial_log_filters: typing.Optional[FilterParamDictPydantic] = OMIT,
        project: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        description: typing.Optional[str] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_at: typing.Optional[dt.datetime] = OMIT,
        unique_organization_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        timestamps: typing.Optional[typing.Sequence[dt.datetime]] = OMIT,
        ingest_workflow_id: typing.Optional[str] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        evaluator: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetDetail:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : str

        initial_log_filters : typing.Optional[FilterParamDictPydantic]

        project : typing.Optional[str]

        name : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        description : typing.Optional[str]

        running_progress : typing.Optional[float]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_at : typing.Optional[dt.datetime]

        unique_organization_ids : typing.Optional[typing.Sequence[str]]

        timestamps : typing.Optional[typing.Sequence[dt.datetime]]

        ingest_workflow_id : typing.Optional[str]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        evaluator : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_partial_update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_partial_update(
            id,
            initial_log_filters=initial_log_filters,
            project=project,
            name=name,
            type=type,
            description=description,
            running_progress=running_progress,
            running_status=running_status,
            running_at=running_at,
            unique_organization_ids=unique_organization_ids,
            timestamps=timestamps,
            ingest_workflow_id=ingest_workflow_id,
            starred=starred,
            organization=organization,
            evaluator=evaluator,
            request_options=request_options,
        )
        return _response.data

    async def eval_sets_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetListList:
        """
        List datasets

        Endpoint:
            GET /api/datasets/list/

        Superadmin: Can see all datasets across all organizations.
        Regular users: Can only see datasets in their organization.

        Returns (200):
            {
              "count": 1,
              "next": null,
              "previous": null,
              "results": [
                {
                  "id": "dataset_id",
                  "organization_id": 123,
                  "updated_by": {"first_name": "Ann", "last_name": "Lee", "email": "ann@example.com"},
                  "log_count": 250,
                  "name": "Support Conversations - July",
                  "log_ids": ["..."],
                  "description": "Sampled support chats for July",
                  "type": "sampling",
                  "status": "ready",
                  "created_at": "2025-07-26T00:00:00Z",
                  "updated_at": "2025-07-27T08:10:00Z",
                  "completed_annotation_count": 0,
                  "running_status": "pending",
                  "running_progress": 0
                }
              ]
            }

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_datasets_list_filtered2(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetListList:
        """
        List datasets with complex filtering via POST body.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter parameters keyed by metric name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.api_datasets_list_filtered2()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_datasets_list_filtered2(
            page=page, page_size=page_size, filters=filters, request_options=request_options
        )
        return _response.data

    async def eval_sets_list_update(
        self,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        log_count: typing.Optional[int] = OMIT,
        log_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        status: typing.Optional[DatasetStatusEnum] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        id : typing.Optional[str]

        log_count : typing.Optional[int]

        log_ids : typing.Optional[typing.Sequence[str]]

        description : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        status : typing.Optional[DatasetStatusEnum]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_progress : typing.Optional[float]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_list_update(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_list_update(
            name=name,
            id=id,
            log_count=log_count,
            log_ids=log_ids,
            description=description,
            type=type,
            status=status,
            running_status=running_status,
            running_progress=running_progress,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def eval_sets_list_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        log_count: typing.Optional[int] = OMIT,
        name: typing.Optional[str] = OMIT,
        log_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[DatasetTypeEnum] = OMIT,
        status: typing.Optional[DatasetStatusEnum] = OMIT,
        running_status: typing.Optional[DatasetLlmRunStatusEnum] = OMIT,
        running_progress: typing.Optional[float] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        log_count : typing.Optional[int]

        name : typing.Optional[str]

        log_ids : typing.Optional[typing.Sequence[str]]

        description : typing.Optional[str]

        type : typing.Optional[DatasetTypeEnum]

        status : typing.Optional[DatasetStatusEnum]

        running_status : typing.Optional[DatasetLlmRunStatusEnum]

        running_progress : typing.Optional[float]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.eval_sets_list_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.eval_sets_list_partial_update(
            id=id,
            log_count=log_count,
            name=name,
            log_ids=log_ids,
            description=description,
            type=type,
            status=status,
            running_status=running_status,
            running_progress=running_progress,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_list(
        self,
        evaluator_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorVersionListList:
        """
        List all versions or create new version (commit).

        GET /api/evaluators/{id}/versions/ - List all versions
        POST /api/evaluators/{id}/versions/ - Commit (create new version)

        Access control via NestedResourceMixin: org identity derived from parent evaluator.
        Superadmin: Can LIST all versions across all organizations via JWT.
        Regular users: Can only access versions in their organization.

        Parameters
        ----------
        evaluator_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorVersionListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_list(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_list(
            evaluator_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def evaluators_versions_create(
        self,
        evaluator_id: str,
        *,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        score_value_type: typing.Optional[str] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        eval_class: typing.Optional[EvaluatorCreateVersionRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        created_by: typing.Optional[int] = OMIT,
        updated_by: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorCreateVersion:
        """
        Create a new version (commit). Org derived from parent evaluator.

        Parameters
        ----------
        evaluator_id : str

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        name : typing.Optional[str]

        score_value_type : typing.Optional[str]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        eval_class : typing.Optional[EvaluatorCreateVersionRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        created_by : typing.Optional[int]

        updated_by : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorCreateVersion


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_create(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_create(
            evaluator_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            name=name,
            score_value_type=score_value_type,
            version_description=version_description,
            description=description,
            type=type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            created_by=created_by,
            updated_by=updated_by,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_update(
        self,
        evaluator_id: str,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        evaluator_id : str

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_update(
                evaluator_id="evaluator_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_update(
            evaluator_id,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_partial_update(
        self,
        evaluator_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        evaluator_id : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_partial_update(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_partial_update(
            evaluator_id,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_retrieve(
        self, evaluator_id: str, version: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicEvaluatorVersionDetail:
        """
        Get or edit a specific version by version number.

        GET /api/evaluators/{evaluator_id}/versions/{version}/ - Get specific version
        PATCH /api/evaluators/{evaluator_id}/versions/{version}/ - Edit (only if is_read_only=False)

        NOTE: DELETE is not allowed for specific versions. Delete the entire evaluator instead.
        Versions are immutable history - you can only add new versions, not remove old ones.

        Access control via NestedResourceMixin: org identity derived from parent evaluator.
        Superadmin: Can READ any version across all organizations via JWT.
        Regular users: Can only access versions in their organization.

        Parameters
        ----------
        evaluator_id : str

        version : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_retrieve(
                evaluator_id="evaluator_id",
                version=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_retrieve(
            evaluator_id, version, request_options=request_options
        )
        return _response.data

    async def evaluators_versions_create2(
        self,
        evaluator_id: str,
        version_: int,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionDetail:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        evaluator_id : str

        version_ : int

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        eval_class : typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_create2(
                evaluator_id="evaluator_id",
                version_=1,
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_create2(
            evaluator_id,
            version_,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            configurations=configurations,
            categorical_choices=categorical_choices,
            eval_class=eval_class,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_update2(
        self,
        evaluator_id: str,
        version_: int,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionDetail:
        """
        Full update - only allowed if is_read_only=False.

        Parameters
        ----------
        evaluator_id : str

        version_ : int

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        eval_class : typing.Optional[PublicEvaluatorVersionDetailRequestEvalClass]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_update2(
                evaluator_id="evaluator_id",
                version_=1,
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_update2(
            evaluator_id,
            version_,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            configurations=configurations,
            categorical_choices=categorical_choices,
            eval_class=eval_class,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_partial_update2(
        self,
        evaluator_id: str,
        version_: int,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        eval_class: typing.Optional[PatchedPublicEvaluatorVersionDetailRequestEvalClass] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionDetail:
        """
        Edit version - only allowed if is_read_only=False.

        Parameters
        ----------
        evaluator_id : str

        version_ : int

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        eval_class : typing.Optional[PatchedPublicEvaluatorVersionDetailRequestEvalClass]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_partial_update2(
                evaluator_id="evaluator_id",
                version_=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_partial_update2(
            evaluator_id,
            version_,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            configurations=configurations,
            categorical_choices=categorical_choices,
            eval_class=eval_class,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_list_list(
        self,
        evaluator_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorVersionListList:
        """
        List all versions or create new version (commit).

        GET /api/evaluators/{id}/versions/ - List all versions
        POST /api/evaluators/{id}/versions/ - Commit (create new version)

        Access control via NestedResourceMixin: org identity derived from parent evaluator.
        Superadmin: Can LIST all versions across all organizations via JWT.
        Regular users: Can only access versions in their organization.

        Parameters
        ----------
        evaluator_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorVersionListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_list_list(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_list_list(
            evaluator_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def evaluators_versions_list_create(
        self,
        evaluator_id: str,
        *,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        score_value_type: typing.Optional[str] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        eval_class: typing.Optional[EvaluatorCreateVersionRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        created_by: typing.Optional[int] = OMIT,
        updated_by: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorCreateVersion:
        """
        Create a new version (commit). Org derived from parent evaluator.

        Parameters
        ----------
        evaluator_id : str

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Dict[str, typing.Any]]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        name : typing.Optional[str]

        score_value_type : typing.Optional[str]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        eval_class : typing.Optional[EvaluatorCreateVersionRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        created_by : typing.Optional[int]

        updated_by : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorCreateVersion


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_list_create(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_list_create(
            evaluator_id,
            configurations=configurations,
            categorical_choices=categorical_choices,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            name=name,
            score_value_type=score_value_type,
            version_description=version_description,
            description=description,
            type=type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            created_by=created_by,
            updated_by=updated_by,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_list_update(
        self,
        evaluator_id: str,
        *,
        name: str,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        evaluator_id : str

        name : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_list_update(
                evaluator_id="evaluator_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_list_update(
            evaluator_id,
            name=name,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_versions_list_partial_update(
        self,
        evaluator_id: str,
        *,
        id: typing.Optional[str] = OMIT,
        version_id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorVersionList:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        evaluator_id : str

        id : typing.Optional[str]

        version_id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        name : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorVersionList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_versions_list_partial_update(
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_versions_list_partial_update(
            evaluator_id,
            id=id,
            version_id=version_id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            name=name,
            description=description,
            type=type,
            score_value_type=score_value_type,
            request_options=request_options,
        )
        return _response.data

    async def evaluators_summary_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        GET/POST /evaluations/evaluators/summary/
        GET/POST /api/evaluators/summary/

        Get summary statistics for evaluators.

        Returns:
            {
                "total_count": 15
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_summary_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_summary_retrieve(request_options=request_options)
        return _response.data

    async def evaluators_summary_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_summary_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_summary_create(request_options=request_options)
        return _response.data

    async def evaluators_summary_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_summary_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_summary_update(request_options=request_options)
        return _response.data

    async def evaluators_summary_partial_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_summary_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_summary_partial_update(request_options=request_options)
        return _response.data

    async def evaluators_tags_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedEvaluatorTagList:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedEvaluatorTagList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_tags_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_tags_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def evaluators_tags_create(
        self,
        *,
        organization: int,
        name: typing.Optional[str] = OMIT,
        color: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorTag:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        organization : int

        name : typing.Optional[str]

        color : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorTag


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_tags_create(
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_tags_create(
            organization=organization, name=name, color=color, request_options=request_options
        )
        return _response.data

    async def evaluators_tags_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EvaluatorTag:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorTag


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_tags_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_tags_retrieve(id, request_options=request_options)
        return _response.data

    async def evaluators_tags_update(
        self,
        id: str,
        *,
        organization: int,
        name: typing.Optional[str] = OMIT,
        color: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorTag:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        id : str

        organization : int

        name : typing.Optional[str]

        color : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorTag


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_tags_update(
                id="id",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_tags_update(
            id, organization=organization, name=name, color=color, request_options=request_options
        )
        return _response.data

    async def evaluators_tags_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_tags_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_tags_destroy(id, request_options=request_options)
        return _response.data

    async def evaluators_tags_partial_update(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        color: typing.Optional[str] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvaluatorTag:
        """
        Mixin for views that need method-level permission enforcement.

        Supports two approaches for defining permissions:

        1. Auto-generation (Recommended - DRY):
            Set permission_resource to auto-generate CRUD permissions based on HTTP methods:

            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                permission_resource = Resources.LOG
                # Auto-generates:
                # GET -> log:read
                # PATCH -> log:update
                # DELETE -> log:delete

            Override specific methods via permission_map (always use constants):
            class MyView(PermissionMapMixin, ...):
                permission_resource = Resources.LOG
                permission_map: PermissionMap = {
                    "GET": None,  # Override: no permission required for GET
                    "POST": make_permission(Resources.LOG, CRUDActions.READ),  # POST acts as read
                }

        2. Explicit mapping (for non-CRUD or complex cases - always use constants):
            class MyView(PermissionMapMixin, JWTAndAPIKeyAuthenticationViewMixin, APIView):
                permission_map: PermissionMap = {
                    "GET": make_permission(Features.PROXY, Actions.ACCESS),
                    "POST": make_permission(Features.PLAYGROUND, Actions.ACCESS),
                }

        3. Dynamic logic (most flexible):
            def get_required_permission(self, method: str) -> str | None:
                if self.kwargs.get('public'):
                    return None
                return "dataset:read"

        Notes:
        - permission_map acts as an override when permission_resource is set
        - If neither is defined, no permission check is performed (backward compatible)
        - HasJWTPermission automatically enforces permissions when defined

        Parameters
        ----------
        id : str

        name : typing.Optional[str]

        color : typing.Optional[str]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvaluatorTag


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.evaluators_tags_partial_update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.evaluators_tags_partial_update(
            id, name=name, color=color, organization=organization, request_options=request_options
        )
        return _response.data

    async def list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPublicEvaluatorListList:
        """
        List evaluators for an organization.

        Superadmin: Can see all evaluators across all organizations.
        Regular users: Can only see evaluators in their organization, plus PUBLIC
            (Respan-managed) evaluators when ``is_including_public_evaluators`` is
            truthy. Public evaluators are hidden by default for back-compat.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPublicEvaluatorListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_list(page=page, page_size=page_size, request_options=request_options)
        return _response.data

    async def list_create(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.list_create(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_create(
            name=name,
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def list_update(
        self,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.list_update(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_update(
            name=name,
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def list_partial_update(
        self,
        *,
        version_id: typing.Optional[str] = OMIT,
        configurations: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        score_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        passing_conditions: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        llm_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        code_config: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        project: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        eval_class: typing.Optional[PatchedPublicEvaluatorListRequestEvalClass] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PublicEvaluatorList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        version_id : typing.Optional[str]

        configurations : typing.Optional[typing.Dict[str, typing.Any]]

        score_config : typing.Optional[typing.Dict[str, typing.Any]]

        passing_conditions : typing.Optional[typing.Dict[str, typing.Any]]

        llm_config : typing.Optional[typing.Dict[str, typing.Any]]

        code_config : typing.Optional[typing.Dict[str, typing.Any]]

        project : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        eval_class : typing.Optional[PatchedPublicEvaluatorListRequestEvalClass]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicEvaluatorList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.list_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_partial_update(
            version_id=version_id,
            configurations=configurations,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            project=project,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            type=type,
            score_value_type=score_value_type,
            eval_class=eval_class,
            custom_required_fields=custom_required_fields,
            starred=starred,
            organization=organization,
            request_options=request_options,
        )
        return _response.data

    async def results_retrieve(
        self, id: int, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EvalResultDetail:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_retrieve(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_retrieve(id, request_options=request_options)
        return _response.data

    async def results_create(
        self,
        id: int,
        *,
        organization: int,
        human_text_value: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        prompt_version_id: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        secondary_score: typing.Optional[float] = OMIT,
        tertiary_score: typing.Optional[float] = OMIT,
        quaternary_score: typing.Optional[float] = OMIT,
        score_mapping: typing.Optional[typing.Any] = OMIT,
        human_numerical_value: typing.Optional[float] = OMIT,
        human_categorical_value: typing.Optional[str] = OMIT,
        passed: typing.Optional[bool] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        evaluation: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalResultDetail:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : int

        organization : int

        human_text_value : typing.Optional[str]

        project : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        source : typing.Optional[str]

        cost : typing.Optional[float]

        evaluation_identifier : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        eval_set_id : typing.Optional[str]

        log_timestamp : typing.Optional[dt.datetime]

        prompt_version_id : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        secondary_score : typing.Optional[float]

        tertiary_score : typing.Optional[float]

        quaternary_score : typing.Optional[float]

        score_mapping : typing.Optional[typing.Any]

        human_numerical_value : typing.Optional[float]

        human_categorical_value : typing.Optional[str]

        passed : typing.Optional[bool]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        evaluation : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_create(
                id=1,
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_create(
            id,
            organization=organization,
            human_text_value=human_text_value,
            project=project,
            eval_result_unique_id=eval_result_unique_id,
            type=type,
            environment=environment,
            unique_organization_id=unique_organization_id,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            workflow_version_id=workflow_version_id,
            source=source,
            cost=cost,
            evaluation_identifier=evaluation_identifier,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            eval_set_id=eval_set_id,
            log_timestamp=log_timestamp,
            prompt_version_id=prompt_version_id,
            pipeline_run_id=pipeline_run_id,
            automation_id=automation_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            secondary_score=secondary_score,
            tertiary_score=tertiary_score,
            quaternary_score=quaternary_score,
            score_mapping=score_mapping,
            human_numerical_value=human_numerical_value,
            human_categorical_value=human_categorical_value,
            passed=passed,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            evaluation=evaluation,
            annotation_config=annotation_config,
            log=log,
            request_options=request_options,
        )
        return _response.data

    async def results_update(
        self,
        id: int,
        *,
        organization: int,
        human_text_value: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        prompt_version_id: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        secondary_score: typing.Optional[float] = OMIT,
        tertiary_score: typing.Optional[float] = OMIT,
        quaternary_score: typing.Optional[float] = OMIT,
        score_mapping: typing.Optional[typing.Any] = OMIT,
        human_numerical_value: typing.Optional[float] = OMIT,
        human_categorical_value: typing.Optional[str] = OMIT,
        passed: typing.Optional[bool] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        evaluation: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalResultDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : int

        organization : int

        human_text_value : typing.Optional[str]

        project : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        source : typing.Optional[str]

        cost : typing.Optional[float]

        evaluation_identifier : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        eval_set_id : typing.Optional[str]

        log_timestamp : typing.Optional[dt.datetime]

        prompt_version_id : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        secondary_score : typing.Optional[float]

        tertiary_score : typing.Optional[float]

        quaternary_score : typing.Optional[float]

        score_mapping : typing.Optional[typing.Any]

        human_numerical_value : typing.Optional[float]

        human_categorical_value : typing.Optional[str]

        passed : typing.Optional[bool]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        evaluation : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_update(
                id=1,
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_update(
            id,
            organization=organization,
            human_text_value=human_text_value,
            project=project,
            eval_result_unique_id=eval_result_unique_id,
            type=type,
            environment=environment,
            unique_organization_id=unique_organization_id,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            workflow_version_id=workflow_version_id,
            source=source,
            cost=cost,
            evaluation_identifier=evaluation_identifier,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            eval_set_id=eval_set_id,
            log_timestamp=log_timestamp,
            prompt_version_id=prompt_version_id,
            pipeline_run_id=pipeline_run_id,
            automation_id=automation_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            secondary_score=secondary_score,
            tertiary_score=tertiary_score,
            quaternary_score=quaternary_score,
            score_mapping=score_mapping,
            human_numerical_value=human_numerical_value,
            human_categorical_value=human_categorical_value,
            passed=passed,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            evaluation=evaluation,
            annotation_config=annotation_config,
            log=log,
            request_options=request_options,
        )
        return _response.data

    async def results_destroy(self, id: int, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        id : int

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_destroy(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_destroy(id, request_options=request_options)
        return _response.data

    async def results_partial_update(
        self,
        id: int,
        *,
        human_text_value: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        prompt_version_id: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        secondary_score: typing.Optional[float] = OMIT,
        tertiary_score: typing.Optional[float] = OMIT,
        quaternary_score: typing.Optional[float] = OMIT,
        score_mapping: typing.Optional[typing.Any] = OMIT,
        human_numerical_value: typing.Optional[float] = OMIT,
        human_categorical_value: typing.Optional[str] = OMIT,
        passed: typing.Optional[bool] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        organization: typing.Optional[int] = OMIT,
        evaluation: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalResultDetail:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : int

        human_text_value : typing.Optional[str]

        project : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        source : typing.Optional[str]

        cost : typing.Optional[float]

        evaluation_identifier : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        eval_set_id : typing.Optional[str]

        log_timestamp : typing.Optional[dt.datetime]

        prompt_version_id : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        secondary_score : typing.Optional[float]

        tertiary_score : typing.Optional[float]

        quaternary_score : typing.Optional[float]

        score_mapping : typing.Optional[typing.Any]

        human_numerical_value : typing.Optional[float]

        human_categorical_value : typing.Optional[str]

        passed : typing.Optional[bool]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        organization : typing.Optional[int]

        evaluation : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_partial_update(
                id=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_partial_update(
            id,
            human_text_value=human_text_value,
            project=project,
            eval_result_unique_id=eval_result_unique_id,
            type=type,
            environment=environment,
            unique_organization_id=unique_organization_id,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            workflow_version_id=workflow_version_id,
            source=source,
            cost=cost,
            evaluation_identifier=evaluation_identifier,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            eval_set_id=eval_set_id,
            log_timestamp=log_timestamp,
            prompt_version_id=prompt_version_id,
            pipeline_run_id=pipeline_run_id,
            automation_id=automation_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            secondary_score=secondary_score,
            tertiary_score=tertiary_score,
            quaternary_score=quaternary_score,
            score_mapping=score_mapping,
            human_numerical_value=human_numerical_value,
            human_categorical_value=human_categorical_value,
            passed=passed,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            organization=organization,
            evaluation=evaluation,
            annotation_config=annotation_config,
            log=log,
            request_options=request_options,
        )
        return _response.data

    async def results_related_to_log_list(
        self,
        log_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedEvalWithResultsList:
        """
        Listing all the evaluators along with the results under them within a log

        Args:
            URL args:
                - log_id: The ID of the log to get the evaluators and results for
            Query params:
                - evaluator_type: The type of the evaluator to get the results for
                - timestamp: The timestamp of the log to get the evaluators and results for
                - unique_organization_id: The unique organization ID of the log to get the evaluators and results for
            POST params:
                - evaluation_id: The ID of the evaluation to create the result for
                - evaluator_slug: The slug of the evaluator to create the result for
                - log_unique_id: The unique ID of the log to create the result for
                - eval_result_unique_id: The unique ID of the result to create
                - score_mapping: The score mapping of the result
                - primary_score: The primary score of the result

        Parameters
        ----------
        log_id : str

        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedEvalWithResultsList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_related_to_log_list(
                log_id="log_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_related_to_log_list(
            log_id, page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def results_related_to_log_create(
        self,
        log_id: str,
        *,
        organization: int,
        human_text_value: typing.Optional[str] = OMIT,
        project: typing.Optional[str] = OMIT,
        eval_result_unique_id: typing.Optional[str] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        environment: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        inputs: typing.Optional[typing.Any] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        evaluator_name: typing.Optional[str] = OMIT,
        scorer: typing.Optional[str] = OMIT,
        workflow_version_id: typing.Optional[str] = OMIT,
        source: typing.Optional[str] = OMIT,
        cost: typing.Optional[float] = OMIT,
        evaluation_identifier: typing.Optional[str] = OMIT,
        log_unique_id: typing.Optional[str] = OMIT,
        customer_identifier: typing.Optional[str] = OMIT,
        eval_set_id: typing.Optional[str] = OMIT,
        log_timestamp: typing.Optional[dt.datetime] = OMIT,
        prompt_version_id: typing.Optional[str] = OMIT,
        pipeline_run_id: typing.Optional[str] = OMIT,
        automation_id: typing.Optional[str] = OMIT,
        primary_score: typing.Optional[float] = OMIT,
        string_value: typing.Optional[str] = OMIT,
        json_value: typing.Optional[str] = OMIT,
        boolean_value: typing.Optional[bool] = OMIT,
        categorical_value: typing.Optional[typing.Sequence[str]] = OMIT,
        secondary_score: typing.Optional[float] = OMIT,
        tertiary_score: typing.Optional[float] = OMIT,
        quaternary_score: typing.Optional[float] = OMIT,
        score_mapping: typing.Optional[typing.Any] = OMIT,
        human_numerical_value: typing.Optional[float] = OMIT,
        human_categorical_value: typing.Optional[str] = OMIT,
        passed: typing.Optional[bool] = OMIT,
        status: typing.Optional[StatusC33Enum] = OMIT,
        error_message: typing.Optional[str] = OMIT,
        storage_object_key: typing.Optional[str] = OMIT,
        evaluation: typing.Optional[str] = OMIT,
        annotation_config: typing.Optional[str] = OMIT,
        log: typing.Optional[int] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalResultCreate:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        log_id : str

        organization : int

        human_text_value : typing.Optional[str]

        project : typing.Optional[str]

        eval_result_unique_id : typing.Optional[str]

        type : typing.Optional[Type4E2Enum]

        environment : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        inputs : typing.Optional[typing.Any]

        evaluator_slug : typing.Optional[str]

        evaluator_name : typing.Optional[str]

        scorer : typing.Optional[str]

        workflow_version_id : typing.Optional[str]

        source : typing.Optional[str]

        cost : typing.Optional[float]

        evaluation_identifier : typing.Optional[str]

        log_unique_id : typing.Optional[str]

        customer_identifier : typing.Optional[str]

        eval_set_id : typing.Optional[str]

        log_timestamp : typing.Optional[dt.datetime]

        prompt_version_id : typing.Optional[str]

        pipeline_run_id : typing.Optional[str]

        automation_id : typing.Optional[str]

        primary_score : typing.Optional[float]

        string_value : typing.Optional[str]

        json_value : typing.Optional[str]

        boolean_value : typing.Optional[bool]

        categorical_value : typing.Optional[typing.Sequence[str]]

        secondary_score : typing.Optional[float]

        tertiary_score : typing.Optional[float]

        quaternary_score : typing.Optional[float]

        score_mapping : typing.Optional[typing.Any]

        human_numerical_value : typing.Optional[float]

        human_categorical_value : typing.Optional[str]

        passed : typing.Optional[bool]

        status : typing.Optional[StatusC33Enum]

        error_message : typing.Optional[str]

        storage_object_key : typing.Optional[str]

        evaluation : typing.Optional[str]

        annotation_config : typing.Optional[str]

        log : typing.Optional[int]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalResultCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_related_to_log_create(
                log_id="log_id",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_related_to_log_create(
            log_id,
            organization=organization,
            human_text_value=human_text_value,
            project=project,
            eval_result_unique_id=eval_result_unique_id,
            type=type,
            environment=environment,
            unique_organization_id=unique_organization_id,
            inputs=inputs,
            evaluator_slug=evaluator_slug,
            evaluator_name=evaluator_name,
            scorer=scorer,
            workflow_version_id=workflow_version_id,
            source=source,
            cost=cost,
            evaluation_identifier=evaluation_identifier,
            log_unique_id=log_unique_id,
            customer_identifier=customer_identifier,
            eval_set_id=eval_set_id,
            log_timestamp=log_timestamp,
            prompt_version_id=prompt_version_id,
            pipeline_run_id=pipeline_run_id,
            automation_id=automation_id,
            primary_score=primary_score,
            string_value=string_value,
            json_value=json_value,
            boolean_value=boolean_value,
            categorical_value=categorical_value,
            secondary_score=secondary_score,
            tertiary_score=tertiary_score,
            quaternary_score=quaternary_score,
            score_mapping=score_mapping,
            human_numerical_value=human_numerical_value,
            human_categorical_value=human_categorical_value,
            passed=passed,
            status=status,
            error_message=error_message,
            storage_object_key=storage_object_key,
            evaluation=evaluation,
            annotation_config=annotation_config,
            log=log,
            request_options=request_options,
        )
        return _response.data

    async def results_related_to_log_update(
        self,
        log_id: str,
        *,
        name: str,
        version_id: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        score_config: typing.Optional[typing.Any] = OMIT,
        passing_conditions: typing.Optional[EvalWithResultsRequestPassingConditions] = OMIT,
        llm_config: typing.Optional[EvalWithResultsRequestLlmConfig] = OMIT,
        code_config: typing.Optional[EvalWithResultsRequestCodeConfig] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Any]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalWithResults:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        log_id : str

        name : str

        version_id : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        score_config : typing.Optional[typing.Any]

        passing_conditions : typing.Optional[EvalWithResultsRequestPassingConditions]

        llm_config : typing.Optional[EvalWithResultsRequestLlmConfig]

        code_config : typing.Optional[EvalWithResultsRequestCodeConfig]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Any]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalWithResults


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_related_to_log_update(
                log_id="log_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_related_to_log_update(
            log_id,
            name=name,
            version_id=version_id,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            unique_organization_id=unique_organization_id,
            description=description,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            categorical_choices=categorical_choices,
            starred=starred,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    async def results_related_to_log_partial_update(
        self,
        log_id: str,
        *,
        version_id: typing.Optional[str] = OMIT,
        id: typing.Optional[str] = OMIT,
        version: typing.Optional[int] = OMIT,
        is_read_only: typing.Optional[bool] = OMIT,
        version_description: typing.Optional[str] = OMIT,
        evaluator_slug: typing.Optional[str] = OMIT,
        name: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        score_config: typing.Optional[typing.Any] = OMIT,
        passing_conditions: typing.Optional[PatchedEvalWithResultsRequestPassingConditions] = OMIT,
        llm_config: typing.Optional[PatchedEvalWithResultsRequestLlmConfig] = OMIT,
        code_config: typing.Optional[PatchedEvalWithResultsRequestCodeConfig] = OMIT,
        type: typing.Optional[Type4E2Enum] = OMIT,
        score_value_type: typing.Optional[ScoreValueTypeEnum] = OMIT,
        custom_required_fields: typing.Optional[typing.Sequence[str]] = OMIT,
        categorical_choices: typing.Optional[typing.Sequence[typing.Any]] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EvalWithResults:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        log_id : str

        version_id : typing.Optional[str]

        id : typing.Optional[str]

        version : typing.Optional[int]

        is_read_only : typing.Optional[bool]

        version_description : typing.Optional[str]

        evaluator_slug : typing.Optional[str]

        name : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        description : typing.Optional[str]

        score_config : typing.Optional[typing.Any]

        passing_conditions : typing.Optional[PatchedEvalWithResultsRequestPassingConditions]

        llm_config : typing.Optional[PatchedEvalWithResultsRequestLlmConfig]

        code_config : typing.Optional[PatchedEvalWithResultsRequestCodeConfig]

        type : typing.Optional[Type4E2Enum]

        score_value_type : typing.Optional[ScoreValueTypeEnum]

        custom_required_fields : typing.Optional[typing.Sequence[str]]

        categorical_choices : typing.Optional[typing.Sequence[typing.Any]]

        starred : typing.Optional[bool]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EvalWithResults


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.results_related_to_log_partial_update(
                log_id="log_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.results_related_to_log_partial_update(
            log_id,
            version_id=version_id,
            id=id,
            version=version,
            is_read_only=is_read_only,
            version_description=version_description,
            evaluator_slug=evaluator_slug,
            name=name,
            unique_organization_id=unique_organization_id,
            description=description,
            score_config=score_config,
            passing_conditions=passing_conditions,
            llm_config=llm_config,
            code_config=code_config,
            type=type,
            score_value_type=score_value_type,
            custom_required_fields=custom_required_fields,
            categorical_choices=categorical_choices,
            starred=starred,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    async def test_run_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Main entry point for test run evaluations.
        Handles four modes of operation:
        1. Evaluation from raw eval inputs & evaluator id (backward compatibility)
        2. Evaluation from log
        3. Evaluation from evaluator configuration form
        4. Evaluation from raw eval inputs & evaluator id (new public API mode)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.test_run_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.test_run_create(request_options=request_options)
        return _response.data

    async def test_run_create2(
        self, log_id: str, evaluator_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Main entry point for test run evaluations.
        Handles four modes of operation:
        1. Evaluation from raw eval inputs & evaluator id (backward compatibility)
        2. Evaluation from log
        3. Evaluation from evaluator configuration form
        4. Evaluation from raw eval inputs & evaluator id (new public API mode)

        Parameters
        ----------
        log_id : str

        evaluator_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.evaluations.test_run_create2(
                log_id="log_id",
                evaluator_id="evaluator_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.test_run_create2(log_id, evaluator_id, request_options=request_options)
        return _response.data
