# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.active_filter_item_request import ActiveFilterItemRequest
from ..types.paginated_saved_filter_list_list import PaginatedSavedFilterListList
from ..types.saved_filter_create import SavedFilterCreate
from ..types.saved_filter_detail import SavedFilterDetail
from ..types.saved_filter_list import SavedFilterList
from ..types.saved_filter_update import SavedFilterUpdate
from ..types.saved_filters_summary_response import SavedFiltersSummaryResponse
from .raw_client import AsyncRawFiltersClient, RawFiltersClient
from .types.api_filters_retrieve_response_value import ApiFiltersRetrieveResponseValue
from .types.patched_saved_filter_update_request_display_settings import PatchedSavedFilterUpdateRequestDisplaySettings
from .types.patched_saved_filter_update_request_environment import PatchedSavedFilterUpdateRequestEnvironment
from .types.saved_filter_create_request_display_settings import SavedFilterCreateRequestDisplaySettings
from .types.saved_filter_create_request_environment import SavedFilterCreateRequestEnvironment
from .types.saved_filter_list_request_display_settings import SavedFilterListRequestDisplaySettings
from .types.saved_filter_list_request_environment import SavedFilterListRequestEnvironment
from .types.saved_filter_update_request_display_settings import SavedFilterUpdateRequestDisplaySettings
from .types.saved_filter_update_request_environment import SavedFilterUpdateRequestEnvironment

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class FiltersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawFiltersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawFiltersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawFiltersClient
        """
        return self._raw_client

    def api_filters_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Dict[str, ApiFiltersRetrieveResponseValue]:
        """
        Get available filter configurations for a page view.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Dict[str, ApiFiltersRetrieveResponseValue]


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_filters_retrieve()
        """
        _response = self._raw_client.api_filters_retrieve(request_options=request_options)
        return _response.data

    def api_filters_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_filters_create()
        """
        _response = self._raw_client.api_filters_create(request_options=request_options)
        return _response.data

    def api_saved_filter_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedFilterDetail:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filter_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.api_saved_filter_retrieve(id, request_options=request_options)
        return _response.data

    def api_saved_filter_update(
        self,
        id: str,
        *,
        name: str,
        view_type: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterUpdateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterUpdateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedFilterUpdate:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        name : str

        view_type : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterUpdateRequestEnvironment]

        display_settings : typing.Optional[SavedFilterUpdateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterUpdate


        Examples
        --------
        from respan import ActiveFilterItemRequest, RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filter_update(
            id="id",
            name="name",
            view_type="view_type",
            filters=[
                ActiveFilterItemRequest(
                    id="id",
                    metric="metric",
                    value="value",
                    operator="=",
                    value_field_type="text",
                )
            ],
        )
        """
        _response = self._raw_client.api_saved_filter_update(
            id,
            name=name,
            view_type=view_type,
            filters=filters,
            description=description,
            start_time=start_time,
            end_time=end_time,
            time_range_preset=time_range_preset,
            sort_by=sort_by,
            environment=environment,
            display_settings=display_settings,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def api_saved_filter_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filter_destroy(
            id="id",
        )
        """
        _response = self._raw_client.api_saved_filter_destroy(id, request_options=request_options)
        return _response.data

    def api_saved_filter_partial_update(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        view_type: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Sequence[ActiveFilterItemRequest]] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[PatchedSavedFilterUpdateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[PatchedSavedFilterUpdateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedFilterUpdate:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        name : typing.Optional[str]

        description : typing.Optional[str]

        view_type : typing.Optional[str]

        filters : typing.Optional[typing.Sequence[ActiveFilterItemRequest]]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[PatchedSavedFilterUpdateRequestEnvironment]

        display_settings : typing.Optional[PatchedSavedFilterUpdateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterUpdate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filter_partial_update(
            id="id",
        )
        """
        _response = self._raw_client.api_saved_filter_partial_update(
            id,
            name=name,
            description=description,
            view_type=view_type,
            filters=filters,
            start_time=start_time,
            end_time=end_time,
            time_range_preset=time_range_preset,
            sort_by=sort_by,
            environment=environment,
            display_settings=display_settings,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def api_saved_filters_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedSavedFilterListList:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSavedFilterListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filters_list()
        """
        _response = self._raw_client.api_saved_filters_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_saved_filters_create(
        self,
        *,
        name: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        view_type: str,
        organization: int,
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterCreateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterCreateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedFilterCreate:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        name : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        view_type : str

        organization : int

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterCreateRequestEnvironment]

        display_settings : typing.Optional[SavedFilterCreateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterCreate


        Examples
        --------
        from respan import ActiveFilterItemRequest, RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filters_create(
            name="name",
            filters=[
                ActiveFilterItemRequest(
                    id="id",
                    metric="metric",
                    value="value",
                    operator="=",
                    value_field_type="text",
                )
            ],
            view_type="view_type",
            organization=1,
        )
        """
        _response = self._raw_client.api_saved_filters_create(
            name=name,
            filters=filters,
            view_type=view_type,
            organization=organization,
            description=description,
            start_time=start_time,
            end_time=end_time,
            time_range_preset=time_range_preset,
            sort_by=sort_by,
            environment=environment,
            display_settings=display_settings,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def api_saved_filters_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedSavedFilterListList:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSavedFilterListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filters_list_list()
        """
        _response = self._raw_client.api_saved_filters_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_saved_filters_list_create(
        self,
        *,
        name: str,
        view_type: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterListRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterListRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedFilterList:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        name : str

        view_type : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        id : typing.Optional[str]

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterListRequestEnvironment]

        display_settings : typing.Optional[SavedFilterListRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterList


        Examples
        --------
        from respan import ActiveFilterItemRequest, RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filters_list_create(
            name="name",
            view_type="view_type",
            filters=[
                ActiveFilterItemRequest(
                    id="id",
                    metric="metric",
                    value="value",
                    operator="=",
                    value_field_type="text",
                )
            ],
        )
        """
        _response = self._raw_client.api_saved_filters_list_create(
            name=name,
            view_type=view_type,
            filters=filters,
            id=id,
            description=description,
            start_time=start_time,
            end_time=end_time,
            time_range_preset=time_range_preset,
            sort_by=sort_by,
            environment=environment,
            display_settings=display_settings,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    def api_saved_filters_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedFiltersSummaryResponse:
        """
        GET/POST /api/saved-filters/summary/

        Get summary statistics for saved filters (views).

        Returns:
            {
                "total_count": 12
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFiltersSummaryResponse


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filters_summary_retrieve()
        """
        _response = self._raw_client.api_saved_filters_summary_retrieve(request_options=request_options)
        return _response.data

    def api_saved_filters_summary_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedFiltersSummaryResponse:
        """
        GET/POST /api/saved-filters/summary/

        Get summary statistics for saved filters (views).

        Returns:
            {
                "total_count": 12
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFiltersSummaryResponse


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.filters.api_saved_filters_summary_create()
        """
        _response = self._raw_client.api_saved_filters_summary_create(request_options=request_options)
        return _response.data


class AsyncFiltersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawFiltersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawFiltersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawFiltersClient
        """
        return self._raw_client

    async def api_filters_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.Dict[str, ApiFiltersRetrieveResponseValue]:
        """
        Get available filter configurations for a page view.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Dict[str, ApiFiltersRetrieveResponseValue]


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_filters_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_filters_retrieve(request_options=request_options)
        return _response.data

    async def api_filters_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_filters_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_filters_create(request_options=request_options)
        return _response.data

    async def api_saved_filter_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedFilterDetail:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filter_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filter_retrieve(id, request_options=request_options)
        return _response.data

    async def api_saved_filter_update(
        self,
        id: str,
        *,
        name: str,
        view_type: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterUpdateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterUpdateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedFilterUpdate:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        name : str

        view_type : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterUpdateRequestEnvironment]

        display_settings : typing.Optional[SavedFilterUpdateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterUpdate


        Examples
        --------
        import asyncio

        from respan import ActiveFilterItemRequest, AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filter_update(
                id="id",
                name="name",
                view_type="view_type",
                filters=[
                    ActiveFilterItemRequest(
                        id="id",
                        metric="metric",
                        value="value",
                        operator="=",
                        value_field_type="text",
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filter_update(
            id,
            name=name,
            view_type=view_type,
            filters=filters,
            description=description,
            start_time=start_time,
            end_time=end_time,
            time_range_preset=time_range_preset,
            sort_by=sort_by,
            environment=environment,
            display_settings=display_settings,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def api_saved_filter_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filter_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filter_destroy(id, request_options=request_options)
        return _response.data

    async def api_saved_filter_partial_update(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        view_type: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Sequence[ActiveFilterItemRequest]] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[PatchedSavedFilterUpdateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[PatchedSavedFilterUpdateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedFilterUpdate:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        name : typing.Optional[str]

        description : typing.Optional[str]

        view_type : typing.Optional[str]

        filters : typing.Optional[typing.Sequence[ActiveFilterItemRequest]]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[PatchedSavedFilterUpdateRequestEnvironment]

        display_settings : typing.Optional[PatchedSavedFilterUpdateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterUpdate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filter_partial_update(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filter_partial_update(
            id,
            name=name,
            description=description,
            view_type=view_type,
            filters=filters,
            start_time=start_time,
            end_time=end_time,
            time_range_preset=time_range_preset,
            sort_by=sort_by,
            environment=environment,
            display_settings=display_settings,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def api_saved_filters_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedSavedFilterListList:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSavedFilterListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filters_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filters_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_saved_filters_create(
        self,
        *,
        name: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        view_type: str,
        organization: int,
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterCreateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterCreateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedFilterCreate:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        name : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        view_type : str

        organization : int

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterCreateRequestEnvironment]

        display_settings : typing.Optional[SavedFilterCreateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterCreate


        Examples
        --------
        import asyncio

        from respan import ActiveFilterItemRequest, AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filters_create(
                name="name",
                filters=[
                    ActiveFilterItemRequest(
                        id="id",
                        metric="metric",
                        value="value",
                        operator="=",
                        value_field_type="text",
                    )
                ],
                view_type="view_type",
                organization=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filters_create(
            name=name,
            filters=filters,
            view_type=view_type,
            organization=organization,
            description=description,
            start_time=start_time,
            end_time=end_time,
            time_range_preset=time_range_preset,
            sort_by=sort_by,
            environment=environment,
            display_settings=display_settings,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def api_saved_filters_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedSavedFilterListList:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSavedFilterListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filters_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filters_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_saved_filters_list_create(
        self,
        *,
        name: str,
        view_type: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterListRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterListRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedFilterList:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        name : str

        view_type : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        id : typing.Optional[str]

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterListRequestEnvironment]

        display_settings : typing.Optional[SavedFilterListRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFilterList


        Examples
        --------
        import asyncio

        from respan import ActiveFilterItemRequest, AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filters_list_create(
                name="name",
                view_type="view_type",
                filters=[
                    ActiveFilterItemRequest(
                        id="id",
                        metric="metric",
                        value="value",
                        operator="=",
                        value_field_type="text",
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filters_list_create(
            name=name,
            view_type=view_type,
            filters=filters,
            id=id,
            description=description,
            start_time=start_time,
            end_time=end_time,
            time_range_preset=time_range_preset,
            sort_by=sort_by,
            environment=environment,
            display_settings=display_settings,
            starred=starred,
            request_options=request_options,
        )
        return _response.data

    async def api_saved_filters_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedFiltersSummaryResponse:
        """
        GET/POST /api/saved-filters/summary/

        Get summary statistics for saved filters (views).

        Returns:
            {
                "total_count": 12
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFiltersSummaryResponse


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filters_summary_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filters_summary_retrieve(request_options=request_options)
        return _response.data

    async def api_saved_filters_summary_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedFiltersSummaryResponse:
        """
        GET/POST /api/saved-filters/summary/

        Get summary statistics for saved filters (views).

        Returns:
            {
                "total_count": 12
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedFiltersSummaryResponse


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.filters.api_saved_filters_summary_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_saved_filters_summary_create(request_options=request_options)
        return _response.data
