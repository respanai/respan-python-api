# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing
from json.decoder import JSONDecodeError

from ..core.api_error import ApiError
from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.http_response import AsyncHttpResponse, HttpResponse
from ..core.jsonable_encoder import jsonable_encoder
from ..core.pydantic_utilities import parse_obj_as
from ..core.request_options import RequestOptions
from ..core.serialization import convert_and_respect_annotation_metadata
from ..types.active_filter_item_request import ActiveFilterItemRequest
from ..types.paginated_saved_filter_list_list import PaginatedSavedFilterListList
from ..types.saved_filter_create import SavedFilterCreate
from ..types.saved_filter_detail import SavedFilterDetail
from ..types.saved_filter_list import SavedFilterList
from ..types.saved_filter_update import SavedFilterUpdate
from ..types.saved_filters_summary_response import SavedFiltersSummaryResponse
from .types.api_filters_retrieve_response_value import ApiFiltersRetrieveResponseValue
from .types.patched_saved_filter_update_request_display_settings import PatchedSavedFilterUpdateRequestDisplaySettings
from .types.patched_saved_filter_update_request_environment import PatchedSavedFilterUpdateRequestEnvironment
from .types.saved_filter_create_request_display_settings import SavedFilterCreateRequestDisplaySettings
from .types.saved_filter_create_request_environment import SavedFilterCreateRequestEnvironment
from .types.saved_filter_list_request_display_settings import SavedFilterListRequestDisplaySettings
from .types.saved_filter_list_request_environment import SavedFilterListRequestEnvironment
from .types.saved_filter_update_request_display_settings import SavedFilterUpdateRequestDisplaySettings
from .types.saved_filter_update_request_environment import SavedFilterUpdateRequestEnvironment

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class RawFiltersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._client_wrapper = client_wrapper

    def api_filters_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[typing.Dict[str, ApiFiltersRetrieveResponseValue]]:
        """
        Get available filter configurations for a page view.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[typing.Dict[str, ApiFiltersRetrieveResponseValue]]

        """
        _response = self._client_wrapper.httpx_client.request(
            "api/filters/",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    typing.Dict[str, ApiFiltersRetrieveResponseValue],
                    parse_obj_as(
                        type_=typing.Dict[str, ApiFiltersRetrieveResponseValue],  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_filters_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> HttpResponse[None]:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[None]
        """
        _response = self._client_wrapper.httpx_client.request(
            "api/filters/",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                return HttpResponse(response=_response, data=None)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filter_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[SavedFilterDetail]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SavedFilterDetail]

        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/saved-filter/{jsonable_encoder(id)}/",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterDetail,
                    parse_obj_as(
                        type_=SavedFilterDetail,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filter_update(
        self,
        id: str,
        *,
        name: str,
        view_type: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterUpdateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterUpdateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[SavedFilterUpdate]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        name : str

        view_type : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterUpdateRequestEnvironment]

        display_settings : typing.Optional[SavedFilterUpdateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SavedFilterUpdate]

        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/saved-filter/{jsonable_encoder(id)}/",
            method="PUT",
            json={
                "name": name,
                "description": description,
                "view_type": view_type,
                "filters": convert_and_respect_annotation_metadata(
                    object_=filters, annotation=typing.Sequence[ActiveFilterItemRequest], direction="write"
                ),
                "start_time": start_time,
                "end_time": end_time,
                "time_range_preset": time_range_preset,
                "sort_by": sort_by,
                "environment": convert_and_respect_annotation_metadata(
                    object_=environment, annotation=SavedFilterUpdateRequestEnvironment, direction="write"
                ),
                "display_settings": convert_and_respect_annotation_metadata(
                    object_=display_settings, annotation=SavedFilterUpdateRequestDisplaySettings, direction="write"
                ),
                "starred": starred,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterUpdate,
                    parse_obj_as(
                        type_=SavedFilterUpdate,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filter_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[None]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[None]
        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/saved-filter/{jsonable_encoder(id)}/",
            method="DELETE",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                return HttpResponse(response=_response, data=None)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filter_partial_update(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        view_type: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Sequence[ActiveFilterItemRequest]] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[PatchedSavedFilterUpdateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[PatchedSavedFilterUpdateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[SavedFilterUpdate]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        name : typing.Optional[str]

        description : typing.Optional[str]

        view_type : typing.Optional[str]

        filters : typing.Optional[typing.Sequence[ActiveFilterItemRequest]]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[PatchedSavedFilterUpdateRequestEnvironment]

        display_settings : typing.Optional[PatchedSavedFilterUpdateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SavedFilterUpdate]

        """
        _response = self._client_wrapper.httpx_client.request(
            f"api/saved-filter/{jsonable_encoder(id)}/",
            method="PATCH",
            json={
                "name": name,
                "description": description,
                "view_type": view_type,
                "filters": convert_and_respect_annotation_metadata(
                    object_=filters, annotation=typing.Sequence[ActiveFilterItemRequest], direction="write"
                ),
                "start_time": start_time,
                "end_time": end_time,
                "time_range_preset": time_range_preset,
                "sort_by": sort_by,
                "environment": convert_and_respect_annotation_metadata(
                    object_=environment, annotation=PatchedSavedFilterUpdateRequestEnvironment, direction="write"
                ),
                "display_settings": convert_and_respect_annotation_metadata(
                    object_=display_settings,
                    annotation=PatchedSavedFilterUpdateRequestDisplaySettings,
                    direction="write",
                ),
                "starred": starred,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterUpdate,
                    parse_obj_as(
                        type_=SavedFilterUpdate,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filters_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[PaginatedSavedFilterListList]:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[PaginatedSavedFilterListList]

        """
        _response = self._client_wrapper.httpx_client.request(
            "api/saved-filters/",
            method="GET",
            params={
                "page": page,
                "page_size": page_size,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    PaginatedSavedFilterListList,
                    parse_obj_as(
                        type_=PaginatedSavedFilterListList,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filters_create(
        self,
        *,
        name: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        view_type: str,
        organization: int,
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterCreateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterCreateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[SavedFilterCreate]:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        name : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        view_type : str

        organization : int

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterCreateRequestEnvironment]

        display_settings : typing.Optional[SavedFilterCreateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SavedFilterCreate]

        """
        _response = self._client_wrapper.httpx_client.request(
            "api/saved-filters/",
            method="POST",
            json={
                "name": name,
                "description": description,
                "filters": convert_and_respect_annotation_metadata(
                    object_=filters, annotation=typing.Sequence[ActiveFilterItemRequest], direction="write"
                ),
                "view_type": view_type,
                "start_time": start_time,
                "end_time": end_time,
                "time_range_preset": time_range_preset,
                "sort_by": sort_by,
                "environment": convert_and_respect_annotation_metadata(
                    object_=environment, annotation=SavedFilterCreateRequestEnvironment, direction="write"
                ),
                "display_settings": convert_and_respect_annotation_metadata(
                    object_=display_settings, annotation=SavedFilterCreateRequestDisplaySettings, direction="write"
                ),
                "starred": starred,
                "organization": organization,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterCreate,
                    parse_obj_as(
                        type_=SavedFilterCreate,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filters_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[PaginatedSavedFilterListList]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[PaginatedSavedFilterListList]

        """
        _response = self._client_wrapper.httpx_client.request(
            "api/saved-filters/list/",
            method="GET",
            params={
                "page": page,
                "page_size": page_size,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    PaginatedSavedFilterListList,
                    parse_obj_as(
                        type_=PaginatedSavedFilterListList,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filters_list_create(
        self,
        *,
        name: str,
        view_type: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterListRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterListRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> HttpResponse[SavedFilterList]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        name : str

        view_type : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        id : typing.Optional[str]

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterListRequestEnvironment]

        display_settings : typing.Optional[SavedFilterListRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SavedFilterList]

        """
        _response = self._client_wrapper.httpx_client.request(
            "api/saved-filters/list/",
            method="POST",
            json={
                "id": id,
                "name": name,
                "description": description,
                "view_type": view_type,
                "filters": convert_and_respect_annotation_metadata(
                    object_=filters, annotation=typing.Sequence[ActiveFilterItemRequest], direction="write"
                ),
                "start_time": start_time,
                "end_time": end_time,
                "time_range_preset": time_range_preset,
                "sort_by": sort_by,
                "environment": convert_and_respect_annotation_metadata(
                    object_=environment, annotation=SavedFilterListRequestEnvironment, direction="write"
                ),
                "display_settings": convert_and_respect_annotation_metadata(
                    object_=display_settings, annotation=SavedFilterListRequestDisplaySettings, direction="write"
                ),
                "starred": starred,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterList,
                    parse_obj_as(
                        type_=SavedFilterList,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filters_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[SavedFiltersSummaryResponse]:
        """
        GET/POST /api/saved-filters/summary/

        Get summary statistics for saved filters (views).

        Returns:
            {
                "total_count": 12
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SavedFiltersSummaryResponse]

        """
        _response = self._client_wrapper.httpx_client.request(
            "api/saved-filters/summary/",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFiltersSummaryResponse,
                    parse_obj_as(
                        type_=SavedFiltersSummaryResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    def api_saved_filters_summary_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> HttpResponse[SavedFiltersSummaryResponse]:
        """
        GET/POST /api/saved-filters/summary/

        Get summary statistics for saved filters (views).

        Returns:
            {
                "total_count": 12
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        HttpResponse[SavedFiltersSummaryResponse]

        """
        _response = self._client_wrapper.httpx_client.request(
            "api/saved-filters/summary/",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFiltersSummaryResponse,
                    parse_obj_as(
                        type_=SavedFiltersSummaryResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return HttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)


class AsyncRawFiltersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._client_wrapper = client_wrapper

    async def api_filters_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[typing.Dict[str, ApiFiltersRetrieveResponseValue]]:
        """
        Get available filter configurations for a page view.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[typing.Dict[str, ApiFiltersRetrieveResponseValue]]

        """
        _response = await self._client_wrapper.httpx_client.request(
            "api/filters/",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    typing.Dict[str, ApiFiltersRetrieveResponseValue],
                    parse_obj_as(
                        type_=typing.Dict[str, ApiFiltersRetrieveResponseValue],  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_filters_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[None]:
        """
        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[None]
        """
        _response = await self._client_wrapper.httpx_client.request(
            "api/filters/",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                return AsyncHttpResponse(response=_response, data=None)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filter_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[SavedFilterDetail]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SavedFilterDetail]

        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/saved-filter/{jsonable_encoder(id)}/",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterDetail,
                    parse_obj_as(
                        type_=SavedFilterDetail,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filter_update(
        self,
        id: str,
        *,
        name: str,
        view_type: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterUpdateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterUpdateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[SavedFilterUpdate]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        name : str

        view_type : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterUpdateRequestEnvironment]

        display_settings : typing.Optional[SavedFilterUpdateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SavedFilterUpdate]

        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/saved-filter/{jsonable_encoder(id)}/",
            method="PUT",
            json={
                "name": name,
                "description": description,
                "view_type": view_type,
                "filters": convert_and_respect_annotation_metadata(
                    object_=filters, annotation=typing.Sequence[ActiveFilterItemRequest], direction="write"
                ),
                "start_time": start_time,
                "end_time": end_time,
                "time_range_preset": time_range_preset,
                "sort_by": sort_by,
                "environment": convert_and_respect_annotation_metadata(
                    object_=environment, annotation=SavedFilterUpdateRequestEnvironment, direction="write"
                ),
                "display_settings": convert_and_respect_annotation_metadata(
                    object_=display_settings, annotation=SavedFilterUpdateRequestDisplaySettings, direction="write"
                ),
                "starred": starred,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterUpdate,
                    parse_obj_as(
                        type_=SavedFilterUpdate,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filter_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[None]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[None]
        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/saved-filter/{jsonable_encoder(id)}/",
            method="DELETE",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                return AsyncHttpResponse(response=_response, data=None)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filter_partial_update(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        view_type: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Sequence[ActiveFilterItemRequest]] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[PatchedSavedFilterUpdateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[PatchedSavedFilterUpdateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[SavedFilterUpdate]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        id : str

        name : typing.Optional[str]

        description : typing.Optional[str]

        view_type : typing.Optional[str]

        filters : typing.Optional[typing.Sequence[ActiveFilterItemRequest]]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[PatchedSavedFilterUpdateRequestEnvironment]

        display_settings : typing.Optional[PatchedSavedFilterUpdateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SavedFilterUpdate]

        """
        _response = await self._client_wrapper.httpx_client.request(
            f"api/saved-filter/{jsonable_encoder(id)}/",
            method="PATCH",
            json={
                "name": name,
                "description": description,
                "view_type": view_type,
                "filters": convert_and_respect_annotation_metadata(
                    object_=filters, annotation=typing.Sequence[ActiveFilterItemRequest], direction="write"
                ),
                "start_time": start_time,
                "end_time": end_time,
                "time_range_preset": time_range_preset,
                "sort_by": sort_by,
                "environment": convert_and_respect_annotation_metadata(
                    object_=environment, annotation=PatchedSavedFilterUpdateRequestEnvironment, direction="write"
                ),
                "display_settings": convert_and_respect_annotation_metadata(
                    object_=display_settings,
                    annotation=PatchedSavedFilterUpdateRequestDisplaySettings,
                    direction="write",
                ),
                "starred": starred,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterUpdate,
                    parse_obj_as(
                        type_=SavedFilterUpdate,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filters_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[PaginatedSavedFilterListList]:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[PaginatedSavedFilterListList]

        """
        _response = await self._client_wrapper.httpx_client.request(
            "api/saved-filters/",
            method="GET",
            params={
                "page": page,
                "page_size": page_size,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    PaginatedSavedFilterListList,
                    parse_obj_as(
                        type_=PaginatedSavedFilterListList,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filters_create(
        self,
        *,
        name: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        view_type: str,
        organization: int,
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterCreateRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterCreateRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[SavedFilterCreate]:
        """
        Stamp server-controlled fields at save time — never by mutating
        ``request.data``.

        DRF's contract: ``post()`` → ``create()`` → ``serializer.is_valid()`` →
        ``perform_create(serializer)`` → ``serializer.save(**kwargs)``. Server values
        belong in that final ``save(**kwargs)`` — they override ``validated_data``,
        never pass through client validation, and don't need to be *writable*
        serializer fields. The matching serializer field becomes ``read_only=True``
        (or is dropped from ``fields``), shrinking — not widening — the
        mass-assignment surface, and the immutable-``QueryDict`` (multipart) failure
        mode of the old ``request.data[...] =`` pattern disappears.

        Declare the fields to stamp as ``field -> fn(view) -> value`` maps::

            class ExperimentV2sView(ServerStampedFieldsMixin, ...):
                create_stamped_fields = {"created_by": stamp_request_user_id}
            # + serializer: created_by = ...(read_only=True)

        FK columns: when the stamped value is an ``int`` and the field names a
        relation on the serializer's ``Meta.model``, the kwarg is rewritten to
        ``<field>_id`` so ``Model.objects.create`` accepts it (a raw ``int`` on the
        FK attribute itself would raise). Non-relation fields (``scorer`` = email)
        and instance values pass through unchanged.

        Cooperative composition: subclasses that need to stamp *additional* server
        values (e.g. ``OrganizationInjectionMixin`` stamping org/project) override
        ``get_create_save_kwargs`` / ``get_update_save_kwargs`` and merge onto
        ``super()`` — yielding exactly ONE ``serializer.save()`` per request (calling
        ``save()`` twice would re-run create/update side effects).

        Parameters
        ----------
        name : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        view_type : str

        organization : int

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterCreateRequestEnvironment]

        display_settings : typing.Optional[SavedFilterCreateRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SavedFilterCreate]

        """
        _response = await self._client_wrapper.httpx_client.request(
            "api/saved-filters/",
            method="POST",
            json={
                "name": name,
                "description": description,
                "filters": convert_and_respect_annotation_metadata(
                    object_=filters, annotation=typing.Sequence[ActiveFilterItemRequest], direction="write"
                ),
                "view_type": view_type,
                "start_time": start_time,
                "end_time": end_time,
                "time_range_preset": time_range_preset,
                "sort_by": sort_by,
                "environment": convert_and_respect_annotation_metadata(
                    object_=environment, annotation=SavedFilterCreateRequestEnvironment, direction="write"
                ),
                "display_settings": convert_and_respect_annotation_metadata(
                    object_=display_settings, annotation=SavedFilterCreateRequestDisplaySettings, direction="write"
                ),
                "starred": starred,
                "organization": organization,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterCreate,
                    parse_obj_as(
                        type_=SavedFilterCreate,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filters_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[PaginatedSavedFilterListList]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[PaginatedSavedFilterListList]

        """
        _response = await self._client_wrapper.httpx_client.request(
            "api/saved-filters/list/",
            method="GET",
            params={
                "page": page,
                "page_size": page_size,
            },
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    PaginatedSavedFilterListList,
                    parse_obj_as(
                        type_=PaginatedSavedFilterListList,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filters_list_create(
        self,
        *,
        name: str,
        view_type: str,
        filters: typing.Sequence[ActiveFilterItemRequest],
        id: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        time_range_preset: typing.Optional[str] = OMIT,
        sort_by: typing.Optional[str] = OMIT,
        environment: typing.Optional[SavedFilterListRequestEnvironment] = OMIT,
        display_settings: typing.Optional[SavedFilterListRequestDisplaySettings] = OMIT,
        starred: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncHttpResponse[SavedFilterList]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        name : str

        view_type : str

        filters : typing.Sequence[ActiveFilterItemRequest]

        id : typing.Optional[str]

        description : typing.Optional[str]

        start_time : typing.Optional[dt.datetime]

        end_time : typing.Optional[dt.datetime]

        time_range_preset : typing.Optional[str]

        sort_by : typing.Optional[str]

        environment : typing.Optional[SavedFilterListRequestEnvironment]

        display_settings : typing.Optional[SavedFilterListRequestDisplaySettings]

        starred : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SavedFilterList]

        """
        _response = await self._client_wrapper.httpx_client.request(
            "api/saved-filters/list/",
            method="POST",
            json={
                "id": id,
                "name": name,
                "description": description,
                "view_type": view_type,
                "filters": convert_and_respect_annotation_metadata(
                    object_=filters, annotation=typing.Sequence[ActiveFilterItemRequest], direction="write"
                ),
                "start_time": start_time,
                "end_time": end_time,
                "time_range_preset": time_range_preset,
                "sort_by": sort_by,
                "environment": convert_and_respect_annotation_metadata(
                    object_=environment, annotation=SavedFilterListRequestEnvironment, direction="write"
                ),
                "display_settings": convert_and_respect_annotation_metadata(
                    object_=display_settings, annotation=SavedFilterListRequestDisplaySettings, direction="write"
                ),
                "starred": starred,
            },
            headers={
                "content-type": "application/json",
            },
            request_options=request_options,
            omit=OMIT,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFilterList,
                    parse_obj_as(
                        type_=SavedFilterList,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filters_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[SavedFiltersSummaryResponse]:
        """
        GET/POST /api/saved-filters/summary/

        Get summary statistics for saved filters (views).

        Returns:
            {
                "total_count": 12
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SavedFiltersSummaryResponse]

        """
        _response = await self._client_wrapper.httpx_client.request(
            "api/saved-filters/summary/",
            method="GET",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFiltersSummaryResponse,
                    parse_obj_as(
                        type_=SavedFiltersSummaryResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)

    async def api_saved_filters_summary_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AsyncHttpResponse[SavedFiltersSummaryResponse]:
        """
        GET/POST /api/saved-filters/summary/

        Get summary statistics for saved filters (views).

        Returns:
            {
                "total_count": 12
            }

        POST supports filtering via body (POST-for-filtering pattern).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncHttpResponse[SavedFiltersSummaryResponse]

        """
        _response = await self._client_wrapper.httpx_client.request(
            "api/saved-filters/summary/",
            method="POST",
            request_options=request_options,
        )
        try:
            if 200 <= _response.status_code < 300:
                _data = typing.cast(
                    SavedFiltersSummaryResponse,
                    parse_obj_as(
                        type_=SavedFiltersSummaryResponse,  # type: ignore
                        object_=_response.json(),
                    ),
                )
                return AsyncHttpResponse(response=_response, data=_data)
            _response_json = _response.json()
        except JSONDecodeError:
            raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response.text)
        raise ApiError(status_code=_response.status_code, headers=dict(_response.headers), body=_response_json)
