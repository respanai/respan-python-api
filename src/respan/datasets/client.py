# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.filters import Filters
from .raw_client import AsyncRawDatasetsClient, RawDatasetsClient
from .types.add_spans_to_dataset_response import AddSpansToDatasetResponse
from .types.create_dataset_response import CreateDatasetResponse
from .types.create_dataset_span_response import CreateDatasetSpanResponse
from .types.delete_spans_response import DeleteSpansResponse
from .types.list_dataset_spans_response import ListDatasetSpansResponse
from .types.list_datasets_response import ListDatasetsResponse
from .types.list_eval_runs_response import ListEvalRunsResponse
from .types.retrieve_dataset_response import RetrieveDatasetResponse
from .types.retrieve_dataset_span_response import RetrieveDatasetSpanResponse
from .types.run_eval_on_dataset_response import RunEvalOnDatasetResponse
from .types.update_dataset_response import UpdateDatasetResponse
from .types.update_dataset_span_response import UpdateDatasetSpanResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class DatasetsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawDatasetsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawDatasetsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawDatasetsClient
        """
        return self._raw_client

    def retrieve_dataset(
        self, dataset_id: str, *, authorization: str, request_options: typing.Optional[RequestOptions] = None
    ) -> RetrieveDatasetResponse:
        """
        Retrieve a dataset by ID.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrieveDatasetResponse
            Successful response for Retrieve dataset

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.retrieve_dataset(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.retrieve_dataset(
            dataset_id, authorization=authorization, request_options=request_options
        )
        return _response.data

    def delete_dataset(
        self, dataset_id: str, *, authorization: str, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Delete a dataset and all its spans.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.delete_dataset(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.delete_dataset(
            dataset_id, authorization=authorization, request_options=request_options
        )
        return _response.data

    def update_dataset(
        self,
        dataset_id: str,
        *,
        authorization: str,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateDatasetResponse:
        """
        Update a dataset's name or description.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        name : typing.Optional[str]
            Dataset name.

        description : typing.Optional[str]
            Dataset description.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateDatasetResponse
            Successful response for Update dataset

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.update_dataset(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.update_dataset(
            dataset_id, authorization=authorization, name=name, description=description, request_options=request_options
        )
        return _response.data

    def create_dataset(
        self,
        *,
        authorization: str,
        name: str,
        description: typing.Optional[str] = OMIT,
        sampling: typing.Optional[int] = OMIT,
        start_time: typing.Optional[str] = OMIT,
        end_time: typing.Optional[str] = OMIT,
        is_empty: typing.Optional[bool] = OMIT,
        initial_log_filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateDatasetResponse:
        """
        Create a new dataset, either empty or populated from existing spans using filters and sampling.

        Parameters
        ----------
        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        name : str
            Dataset name.

        description : typing.Optional[str]
            Dataset description.

        sampling : typing.Optional[int]
            Sampling rate (0-1). Fraction of matching spans to include.

        start_time : typing.Optional[str]
            Start of time range for selecting spans (ISO 8601).

        end_time : typing.Optional[str]
            End of time range for selecting spans (ISO 8601).

        is_empty : typing.Optional[bool]
            Create an empty dataset (no initial spans).

        initial_log_filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filters for selecting initial spans. Same format as [Filters API](/docs/api-reference/reference/filters-api-reference).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateDatasetResponse
            Created dataset.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.create_dataset(
            authorization="Bearer sk_live_xxxxx",
            name="qa-test-set",
        )
        """
        _response = self._raw_client.create_dataset(
            authorization=authorization,
            name=name,
            description=description,
            sampling=sampling,
            start_time=start_time,
            end_time=end_time,
            is_empty=is_empty,
            initial_log_filters=initial_log_filters,
            request_options=request_options,
        )
        return _response.data

    def list_datasets(
        self,
        *,
        authorization: str,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        filters: typing.Optional[Filters] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListDatasetsResponse:
        """
        List datasets with pagination and filters.

        Parameters
        ----------
        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        page : typing.Optional[int]
            Page number.

        page_size : typing.Optional[int]
            Results per page.

        filters : typing.Optional[Filters]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListDatasetsResponse
            Paginated list of datasets.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.list_datasets(
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.list_datasets(
            authorization=authorization,
            page=page,
            page_size=page_size,
            filters=filters,
            request_options=request_options,
        )
        return _response.data

    def list_dataset_spans(
        self,
        dataset_id: str,
        *,
        authorization: str,
        filters: typing.Optional[Filters] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListDatasetSpansResponse:
        """
        List spans in a dataset with filters and pagination. See [Filters API Reference](/docs/api-reference/reference/filters-api-reference).

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        filters : typing.Optional[Filters]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListDatasetSpansResponse
            Paginated list of spans.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.list_dataset_spans(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.list_dataset_spans(
            dataset_id, authorization=authorization, filters=filters, request_options=request_options
        )
        return _response.data

    def create_dataset_span(
        self,
        dataset_id: str,
        *,
        authorization: str,
        input: str,
        output: str,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        metrics: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateDatasetSpanResponse:
        """
        Create a new span in a dataset.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        input : str
            Span input data.

        output : str
            Span output data.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value metadata.

        metrics : typing.Optional[typing.Dict[str, typing.Any]]
            Performance metrics.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateDatasetSpanResponse
            Created span.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.create_dataset_span(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
            input="What is 2+2?",
            output="4",
        )
        """
        _response = self._raw_client.create_dataset_span(
            dataset_id,
            authorization=authorization,
            input=input,
            output=output,
            metadata=metadata,
            metrics=metrics,
            request_options=request_options,
        )
        return _response.data

    def retrieve_dataset_span(
        self,
        dataset_id: str,
        log_id: str,
        *,
        authorization: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RetrieveDatasetSpanResponse:
        """
        Retrieve a single span from a dataset.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        log_id : str
            Log Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrieveDatasetSpanResponse
            Span details.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.retrieve_dataset_span(
            dataset_id="dataset_id",
            log_id="log_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.retrieve_dataset_span(
            dataset_id, log_id, authorization=authorization, request_options=request_options
        )
        return _response.data

    def delete_dataset_span(
        self,
        dataset_id: str,
        log_id: str,
        *,
        authorization: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Delete a single span from a dataset.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        log_id : str
            Log Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.delete_dataset_span(
            dataset_id="dataset_id",
            log_id="log_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.delete_dataset_span(
            dataset_id, log_id, authorization=authorization, request_options=request_options
        )
        return _response.data

    def update_dataset_span(
        self,
        dataset_id: str,
        log_id: str,
        *,
        authorization: str,
        request: typing.Dict[str, typing.Any],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateDatasetSpanResponse:
        """
        Update fields on a span in a dataset. Only provided fields are updated.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        log_id : str
            Log Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request : typing.Dict[str, typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateDatasetSpanResponse
            Successful response for Update span (partial)

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.update_dataset_span(
            dataset_id="dataset_id",
            log_id="log_id",
            authorization="Bearer sk_live_xxxxx",
            request={"key": "value"},
        )
        """
        _response = self._raw_client.update_dataset_span(
            dataset_id, log_id, authorization=authorization, request=request, request_options=request_options
        )
        return _response.data

    def delete_spans(
        self,
        dataset_id: str,
        *,
        authorization: str,
        is_deleting_all_logs: typing.Optional[bool] = OMIT,
        filters: typing.Optional[Filters] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeleteSpansResponse:
        """
        Delete spans from a dataset by filters, or delete all spans.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        is_deleting_all_logs : typing.Optional[bool]
            Delete all spans in the dataset. Required if no `filters` provided.

        filters : typing.Optional[Filters]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteSpansResponse
            Deletion started.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.delete_spans(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.delete_spans(
            dataset_id,
            authorization=authorization,
            is_deleting_all_logs=is_deleting_all_logs,
            filters=filters,
            request_options=request_options,
        )
        return _response.data

    def add_spans_to_dataset(
        self,
        dataset_id: str,
        *,
        authorization: str,
        log_ids: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AddSpansToDatasetResponse:
        """
        Add existing spans to a dataset by their IDs.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        log_ids : typing.Sequence[str]
            Array of span IDs to add.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AddSpansToDatasetResponse
            Successful response for Add spans to dataset

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.add_spans_to_dataset(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
            log_ids=["log_abc", "log_def"],
        )
        """
        _response = self._raw_client.add_spans_to_dataset(
            dataset_id, authorization=authorization, log_ids=log_ids, request_options=request_options
        )
        return _response.data

    def run_eval_on_dataset(
        self,
        dataset_id: str,
        *,
        authorization: str,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RunEvalOnDatasetResponse:
        """
        Run evaluators on all spans in a dataset.

        Parameters
        ----------
        dataset_id : str
            The ID of the dataset to run evaluations on.

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            Evaluator IDs to run.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RunEvalOnDatasetResponse
            Eval run started.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.run_eval_on_dataset(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.run_eval_on_dataset(
            dataset_id, authorization=authorization, evaluator_ids=evaluator_ids, request_options=request_options
        )
        return _response.data

    def list_eval_runs(
        self, dataset_id: str, *, authorization: str, request_options: typing.Optional[RequestOptions] = None
    ) -> ListEvalRunsResponse:
        """
        List evaluation runs for a dataset.

        Parameters
        ----------
        dataset_id : str
            The ID of the dataset to list evaluation reports for.

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListEvalRunsResponse
            List of eval runs.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient()
        client.datasets.list_eval_runs(
            dataset_id="dataset_id",
            authorization="Bearer sk_live_xxxxx",
        )
        """
        _response = self._raw_client.list_eval_runs(
            dataset_id, authorization=authorization, request_options=request_options
        )
        return _response.data


class AsyncDatasetsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawDatasetsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawDatasetsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawDatasetsClient
        """
        return self._raw_client

    async def retrieve_dataset(
        self, dataset_id: str, *, authorization: str, request_options: typing.Optional[RequestOptions] = None
    ) -> RetrieveDatasetResponse:
        """
        Retrieve a dataset by ID.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrieveDatasetResponse
            Successful response for Retrieve dataset

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.retrieve_dataset(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve_dataset(
            dataset_id, authorization=authorization, request_options=request_options
        )
        return _response.data

    async def delete_dataset(
        self, dataset_id: str, *, authorization: str, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Delete a dataset and all its spans.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.delete_dataset(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_dataset(
            dataset_id, authorization=authorization, request_options=request_options
        )
        return _response.data

    async def update_dataset(
        self,
        dataset_id: str,
        *,
        authorization: str,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateDatasetResponse:
        """
        Update a dataset's name or description.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        name : typing.Optional[str]
            Dataset name.

        description : typing.Optional[str]
            Dataset description.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateDatasetResponse
            Successful response for Update dataset

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.update_dataset(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_dataset(
            dataset_id, authorization=authorization, name=name, description=description, request_options=request_options
        )
        return _response.data

    async def create_dataset(
        self,
        *,
        authorization: str,
        name: str,
        description: typing.Optional[str] = OMIT,
        sampling: typing.Optional[int] = OMIT,
        start_time: typing.Optional[str] = OMIT,
        end_time: typing.Optional[str] = OMIT,
        is_empty: typing.Optional[bool] = OMIT,
        initial_log_filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateDatasetResponse:
        """
        Create a new dataset, either empty or populated from existing spans using filters and sampling.

        Parameters
        ----------
        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        name : str
            Dataset name.

        description : typing.Optional[str]
            Dataset description.

        sampling : typing.Optional[int]
            Sampling rate (0-1). Fraction of matching spans to include.

        start_time : typing.Optional[str]
            Start of time range for selecting spans (ISO 8601).

        end_time : typing.Optional[str]
            End of time range for selecting spans (ISO 8601).

        is_empty : typing.Optional[bool]
            Create an empty dataset (no initial spans).

        initial_log_filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filters for selecting initial spans. Same format as [Filters API](/docs/api-reference/reference/filters-api-reference).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateDatasetResponse
            Created dataset.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.create_dataset(
                authorization="Bearer sk_live_xxxxx",
                name="qa-test-set",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_dataset(
            authorization=authorization,
            name=name,
            description=description,
            sampling=sampling,
            start_time=start_time,
            end_time=end_time,
            is_empty=is_empty,
            initial_log_filters=initial_log_filters,
            request_options=request_options,
        )
        return _response.data

    async def list_datasets(
        self,
        *,
        authorization: str,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        filters: typing.Optional[Filters] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListDatasetsResponse:
        """
        List datasets with pagination and filters.

        Parameters
        ----------
        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        page : typing.Optional[int]
            Page number.

        page_size : typing.Optional[int]
            Results per page.

        filters : typing.Optional[Filters]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListDatasetsResponse
            Paginated list of datasets.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.list_datasets(
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_datasets(
            authorization=authorization,
            page=page,
            page_size=page_size,
            filters=filters,
            request_options=request_options,
        )
        return _response.data

    async def list_dataset_spans(
        self,
        dataset_id: str,
        *,
        authorization: str,
        filters: typing.Optional[Filters] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListDatasetSpansResponse:
        """
        List spans in a dataset with filters and pagination. See [Filters API Reference](/docs/api-reference/reference/filters-api-reference).

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        filters : typing.Optional[Filters]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListDatasetSpansResponse
            Paginated list of spans.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.list_dataset_spans(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_dataset_spans(
            dataset_id, authorization=authorization, filters=filters, request_options=request_options
        )
        return _response.data

    async def create_dataset_span(
        self,
        dataset_id: str,
        *,
        authorization: str,
        input: str,
        output: str,
        metadata: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        metrics: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CreateDatasetSpanResponse:
        """
        Create a new span in a dataset.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        input : str
            Span input data.

        output : str
            Span output data.

        metadata : typing.Optional[typing.Dict[str, typing.Any]]
            Custom key-value metadata.

        metrics : typing.Optional[typing.Dict[str, typing.Any]]
            Performance metrics.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CreateDatasetSpanResponse
            Created span.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.create_dataset_span(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
                input="What is 2+2?",
                output="4",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_dataset_span(
            dataset_id,
            authorization=authorization,
            input=input,
            output=output,
            metadata=metadata,
            metrics=metrics,
            request_options=request_options,
        )
        return _response.data

    async def retrieve_dataset_span(
        self,
        dataset_id: str,
        log_id: str,
        *,
        authorization: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RetrieveDatasetSpanResponse:
        """
        Retrieve a single span from a dataset.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        log_id : str
            Log Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RetrieveDatasetSpanResponse
            Span details.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.retrieve_dataset_span(
                dataset_id="dataset_id",
                log_id="log_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve_dataset_span(
            dataset_id, log_id, authorization=authorization, request_options=request_options
        )
        return _response.data

    async def delete_dataset_span(
        self,
        dataset_id: str,
        log_id: str,
        *,
        authorization: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Delete a single span from a dataset.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        log_id : str
            Log Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.delete_dataset_span(
                dataset_id="dataset_id",
                log_id="log_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_dataset_span(
            dataset_id, log_id, authorization=authorization, request_options=request_options
        )
        return _response.data

    async def update_dataset_span(
        self,
        dataset_id: str,
        log_id: str,
        *,
        authorization: str,
        request: typing.Dict[str, typing.Any],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> UpdateDatasetSpanResponse:
        """
        Update fields on a span in a dataset. Only provided fields are updated.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        log_id : str
            Log Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request : typing.Dict[str, typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        UpdateDatasetSpanResponse
            Successful response for Update span (partial)

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.update_dataset_span(
                dataset_id="dataset_id",
                log_id="log_id",
                authorization="Bearer sk_live_xxxxx",
                request={"key": "value"},
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_dataset_span(
            dataset_id, log_id, authorization=authorization, request=request, request_options=request_options
        )
        return _response.data

    async def delete_spans(
        self,
        dataset_id: str,
        *,
        authorization: str,
        is_deleting_all_logs: typing.Optional[bool] = OMIT,
        filters: typing.Optional[Filters] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeleteSpansResponse:
        """
        Delete spans from a dataset by filters, or delete all spans.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        is_deleting_all_logs : typing.Optional[bool]
            Delete all spans in the dataset. Required if no `filters` provided.

        filters : typing.Optional[Filters]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteSpansResponse
            Deletion started.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.delete_spans(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_spans(
            dataset_id,
            authorization=authorization,
            is_deleting_all_logs=is_deleting_all_logs,
            filters=filters,
            request_options=request_options,
        )
        return _response.data

    async def add_spans_to_dataset(
        self,
        dataset_id: str,
        *,
        authorization: str,
        log_ids: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AddSpansToDatasetResponse:
        """
        Add existing spans to a dataset by their IDs.

        Parameters
        ----------
        dataset_id : str
            Dataset Id

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        log_ids : typing.Sequence[str]
            Array of span IDs to add.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AddSpansToDatasetResponse
            Successful response for Add spans to dataset

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.add_spans_to_dataset(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
                log_ids=["log_abc", "log_def"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.add_spans_to_dataset(
            dataset_id, authorization=authorization, log_ids=log_ids, request_options=request_options
        )
        return _response.data

    async def run_eval_on_dataset(
        self,
        dataset_id: str,
        *,
        authorization: str,
        evaluator_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> RunEvalOnDatasetResponse:
        """
        Run evaluators on all spans in a dataset.

        Parameters
        ----------
        dataset_id : str
            The ID of the dataset to run evaluations on.

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        evaluator_ids : typing.Optional[typing.Sequence[str]]
            Evaluator IDs to run.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        RunEvalOnDatasetResponse
            Eval run started.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.run_eval_on_dataset(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.run_eval_on_dataset(
            dataset_id, authorization=authorization, evaluator_ids=evaluator_ids, request_options=request_options
        )
        return _response.data

    async def list_eval_runs(
        self, dataset_id: str, *, authorization: str, request_options: typing.Optional[RequestOptions] = None
    ) -> ListEvalRunsResponse:
        """
        List evaluation runs for a dataset.

        Parameters
        ----------
        dataset_id : str
            The ID of the dataset to list evaluation reports for.

        authorization : str
            Bearer token. Use `Bearer YOUR_API_KEY`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListEvalRunsResponse
            List of eval runs.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient()


        async def main() -> None:
            await client.datasets.list_eval_runs(
                dataset_id="dataset_id",
                authorization="Bearer sk_live_xxxxx",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_eval_runs(
            dataset_id, authorization=authorization, request_options=request_options
        )
        return _response.data
