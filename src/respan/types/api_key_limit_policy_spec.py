# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .api_key_limit_policy_spec_metric import ApiKeyLimitPolicySpecMetric


class ApiKeyLimitPolicySpec(UniversalBaseModel):
    """
    A spend, token, or request cap to create together with the API key, scoped to that key. Caps are stored and visible as soon as they are created, but the gateway enforces them only where the limit system is enabled for your environment. Where it is not enabled, a cap records no usage and never blocks.
    """

    max_value: float = pydantic.Field()
    """
    The cap, in the unit named by `metric`. The counter blocks when it reaches this value: `max_value` of 3 on `request_count` allows two requests and refuses the third.
    """

    metric: typing.Optional[ApiKeyLimitPolicySpecMetric] = pydantic.Field(default=None)
    """
    What the cap counts. `cost` is US dollars, `token_count` is total tokens, `request_count` is calls.
    """

    period: str = pydantic.Field()
    """
    A named bucket (`minute`, `hour`, `day`, `week`, `month`) or a positive integer of seconds as a string. A lifetime (`null`) period is rejected here; use `POST /api/limit-policies/` for that.
    """

    warning_threshold: typing.Optional[float] = pydantic.Field(default=None)
    """
    Optional soft threshold; must be strictly below `max_value`. Fires an alert without blocking.
    """

    name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Display label. When blank, the dashboard falls back to the key's own name.
    """

    notification_method_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    OrganizationNotificationMethod id to ping when a rule trips. Must belong to the same project.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
