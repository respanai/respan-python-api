# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class PulseSummarizeWorkflowTaskConfig(UniversalBaseModel):
    """
    pulse_summarize task config (DEV-10007).

    No knobs yet by design: the digest prompt, model, and guardrails are
    code-owned in the handler so they iterate via BE deploy — not per-org
    or per-saved-workflow state. The empty config keeps the save shape /
    OpenAPI contract uniform with every other task type.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
