# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .activity_type_enum import ActivityTypeEnum
from .actor_type_enum import ActorTypeEnum
from .feature_type_enum import FeatureTypeEnum


class ActivityList(UniversalBaseModel):
    """
    GET list responses and parent embedding.

    Legacy ``user_*`` keys (``user_id`` numeric, ``user_name``,
    ``user_username`` / ``user_first_name`` / ``user_last_name``,
    ``is_respan_staff``) come from the ``user`` FK via
    ``UserFlatIdentityFieldsMixin`` — unchanged response contract. The
    denormalized ``actor_*`` columns (``actor_type`` / ``actor_id`` (string
    ``user_unique_id``) / ``actor_name``) are exposed alongside as the new
    canonical, join-free shape that also survives the actor's deletion.

    Querysets feeding this serializer must ``select_related("user")`` +
    ``prefetch_related(STAFF_ROLE_PREFETCH)`` to keep the mixin O(1) per row.
    """

    id: typing.Optional[str] = None
    timestamp: typing.Optional[dt.datetime] = None
    feature_type: FeatureTypeEnum = pydantic.Field()
    """
    Which feature this activity belongs to (e.g. prompts, automations)
    
    * `logs` - Logs
    * `threads` - Threads
    * `datasets` - Datasets
    * `dataset_logs` - Dataset Logs
    * `scores` - Scores
    * `evaluators` - Evaluators
    * `experiments` - Experiments
    * `testsets` - Testsets
    * `prompts` - Prompts
    * `models` - Models
    * `providers` - Providers
    * `credentials` - Credentials
    * `members` - Members
    * `api_keys` - Api Keys
    * `customer_users` - Customer Users
    * `monitors` - Monitors
    * `automations` - Automations
    * `workflows` - Workflows
    * `conditions` - Conditions
    * `notification_methods` - Notification Methods
    * `webhooks` - Webhooks
    * `export_sinks` - Export Sinks
    * `caches` - Caches
    * `custom_behaviors` - Custom Behaviors
    * `behaviors` - Behaviors
    * `errors` - Errors
    * `credit_transactions` - Credit Transactions
    * `annotation_items` - Annotation Items
    * `users` - Users
    * `saved_filters` - Saved Filters
    * `trackers` - Trackers
    * `traces` - Traces
    * `dashboard` - Dashboard
    * `agent_conversations` - Agent Conversations
    * `agent_skills` - Agent Skills
    * `limit_policies` - Limit Policies
    * `staff_memberships` - Staff Memberships
    * `organizations` - Organizations
    * `experiments_v2` - Experiments V2
    """

    object_id: str = pydantic.Field()
    """
    ID of the parent entity (prompt id, workflow family id, etc.)
    """

    version_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional version reference (prompt_version id, workflow_version id)
    """

    activity_type: ActivityTypeEnum
    message: typing.Optional[str] = None
    prev_state: typing.Optional[typing.Dict[str, typing.Any]] = None
    new_state: typing.Optional[typing.Dict[str, typing.Any]] = None
    actor_type: typing.Optional[ActorTypeEnum] = pydantic.Field(default=None)
    """
    Who acted: user, staff (Respan admin), or system (cron/Celery).
    
    * `user` - User
    * `staff` - Staff
    * `system` - System
    """

    actor_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Actor's user_unique_id (string) for user/staff actors; null for system.
    """

    actor_name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Display name captured at write time (survives actor changes).
    """

    user_id: typing.Optional[int] = None
    user_name: typing.Optional[str] = None
    user_username: typing.Optional[str] = None
    user_first_name: typing.Optional[str] = None
    user_last_name: typing.Optional[str] = None
    is_respan_staff: typing.Optional[bool] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
