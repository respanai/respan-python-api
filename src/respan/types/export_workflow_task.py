# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel, update_forward_refs
from .export_workflow_task_config import ExportWorkflowTaskConfig


class ExportWorkflowTask(UniversalBaseModel):
    """
    Export workflow task definition (DEV-8327).

    Wraps ExportWorkflowTaskConfig with the id/type/label envelope every
    WorkflowStep union variant carries. ``type: "export"`` is the discriminator
    value the WorkflowStep union dispatches on — without this class registered
    in the union, the API rejects every export task save with "Input tag
    'export' found using 'type' does not match any of the expected tags".
    """

    id: str = pydantic.Field()
    """
    Unique task identifier for cross-task references
    """

    config: ExportWorkflowTaskConfig
    label: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional label for flow control (skip_to targeting)
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


update_forward_refs(ExportWorkflowTask)
