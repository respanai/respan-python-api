# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .get_pulse_workflow_task_config import GetPulseWorkflowTaskConfig


class GetPulseWorkflowTask(UniversalBaseModel):
    """
    get_pulse workflow task definition (DEV-10006).

    Discriminator value ``type: "get_pulse"``. Wraps the config with the
    id/type/label envelope every WorkflowStep union variant carries.
    """

    id: str = pydantic.Field()
    """
    Unique task identifier for cross-task references
    """

    config: typing.Optional[GetPulseWorkflowTaskConfig] = pydantic.Field(default=None)
    """
    Every field has a default, so config is omittable — matching the sibling pulse_summarize task added alongside it.
    """

    label: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional label for flow control (skip_to targeting)
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
