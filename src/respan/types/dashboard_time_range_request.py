# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .dashboard_time_range_request_time_tick import DashboardTimeRangeRequestTimeTick
from .filters import Filters


class DashboardTimeRangeRequest(UniversalBaseModel):
    """
    Common time-range request body for dashboard endpoints. `start_time` / `end_time` are ISO 8601 timestamps; `time_tick` controls bucket granularity for time-series endpoints (ignored by `/summary/` endpoints).
    """

    start_time: dt.datetime = pydantic.Field()
    """
    Inclusive start of the range, ISO 8601.
    """

    end_time: dt.datetime = pydantic.Field()
    """
    Inclusive end of the range, ISO 8601.
    """

    time_tick: typing.Optional[DashboardTimeRangeRequestTimeTick] = pydantic.Field(default=None)
    """
    Bucket granularity for time-series endpoints. If omitted, inferred from the range. Has no effect on `/summary/` endpoints.
    """

    filters: typing.Optional[Filters] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
