# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class LimitPolicyFilterRequestRequest(UniversalBaseModel):
    """
    Request body for ``POST /api/limit-policies/list/`` and
    ``POST /api/limit-policies/summary/`` — both use POST for filtering,
    delegated internally to GET.

    Matches ``DatasetFilterRequestSerializer`` and every other
    POST-for-filtering route in the codebase.
    """

    filters: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Filter parameters keyed by metric name. See ``LIMIT_POLICY_FILTERS`` in ``utils/filter/constants.py`` for the allowlist of filterable fields.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
