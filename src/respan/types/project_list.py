# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .project_list_company_organization import ProjectListCompanyOrganization


class ProjectList(UniversalBaseModel):
    """
    List serializer for a project (a.k.a. organization/team).

    Used by the flat ``/api/projects/list/`` endpoint. Each row carries its
    parent workspace (``company_organization``) so the FE can group a flat,
    cross-workspace project list by workspace from a single call. ``None`` for
    solo projects with no parent workspace. The view select_relateds
    ``company_organization`` so this stays N+1-free.
    """

    id: str
    unique_organization_id: typing.Optional[str] = None
    name: typing.Optional[str] = None
    logo: typing.Optional[str] = None
    company_organization: typing.Optional[ProjectListCompanyOrganization] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
