# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ModelStatusSummary(UniversalBaseModel):
    """
    Scalar 'current status' for the model over the whole window. Normalized
    rate/price fields are public; volume fields (`number_of_requests`, `cost`)
    are admin-only and omitted for public callers — hence all `required=False`.
    """

    uptime_percent: typing.Optional[float] = pydantic.Field(default=None)
    """
    Uptime over the window, 0-100; `null` when there was no traffic.
    """

    average_tps: typing.Optional[float] = pydantic.Field(default=None)
    """
    Average output tokens/second over the window.
    """

    average_ttft: typing.Optional[float] = pydantic.Field(default=None)
    """
    Average time-to-first-token in seconds over the window (streaming requests only).
    """

    average_latency: typing.Optional[float] = pydantic.Field(default=None)
    """
    Average end-to-end latency in seconds over the window.
    """

    cache_hit_percentage: typing.Optional[float] = pydantic.Field(default=None)
    """
    Prompt-cache hit rate (% of prompt tokens) over the window.
    """

    input_cost: typing.Optional[float] = pydantic.Field(default=None)
    """
    Catalog list price for input tokens (published per-token pricing from the model dictionary). Public — this is list price, not revenue.
    """

    number_of_requests: typing.Optional[int] = pydantic.Field(default=None)
    """
    Admin-only. Total requests over the window. Omitted for public callers so traffic volume can't be used to infer revenue.
    """

    cost: typing.Optional[float] = pydantic.Field(default=None)
    """
    Admin-only. Total spend (USD) over the window. Omitted for public callers.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
