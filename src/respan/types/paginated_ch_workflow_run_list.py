# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .ch_workflow_run import ChWorkflowRun


class PaginatedChWorkflowRunList(UniversalBaseModel):
    """
    Paginated monitor/automation run-history page (count + results slice).

    Schema-only — CHWorkflowRunsView.list() builds the {count, results} payload
    by hand off raw CH cursors, so spectacular rendered the response as a bare
    array. Never instantiated at runtime.
    """

    count: int
    results: typing.List[ChWorkflowRun]

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
