# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .algorithm_enum import AlgorithmEnum
from .editor import Editor
from .generic_tag_display import GenericTagDisplay
from .limit_policy_current_state import LimitPolicyCurrentState
from .metric_enum import MetricEnum


class LimitPolicyList(UniversalBaseModel):
    """
    Standard CRUD list — PG fields + live counter state + tags.

    ``threshold_value`` is derived from ``rules`` (there is no stand-
    alone ``amount`` column after #2333 — "rules dominate"). Picks
    the first hard rule's counter trigger value, or the largest soft
    rule value if no hard rule is present. The helper lives in
    ``limit.utils.enrichment`` so the live-``/list/`` endpoint and
    this CRUD list surface the same field semantics.

    ``current_state`` exposes the live Redis counter so the FE table can
    render usage / threshold without a second round-trip. ``tags`` is the
    ``GenericTag`` rows assigned to this policy via the generic tag-
    assignment system (``feature_type=limit_policies``).
    """

    id: typing.Optional[str] = None
    name: typing.Optional[str] = None
    scope: typing.Optional[str] = None
    scope_value: typing.Optional[str] = None
    compose: typing.Optional[typing.List[str]] = None
    metric: typing.Optional[MetricEnum] = None
    algorithm: typing.Optional[AlgorithmEnum] = None
    period: typing.Optional[str] = None
    effective_at: typing.Optional[dt.datetime] = None
    expires_at: typing.Optional[dt.datetime] = None
    priority: typing.Optional[int] = None
    is_active: typing.Optional[bool] = None
    threshold_value: typing.Optional[float] = None
    meter_definition_id: typing.Optional[str] = None
    updated_by: typing.Optional[Editor] = None
    created_at: typing.Optional[dt.datetime] = None
    updated_at: typing.Optional[dt.datetime] = None
    current_state: typing.Optional[LimitPolicyCurrentState] = pydantic.Field(default=None)
    """
    Live counter state for the policy's current window. Sourced from Redis (same key ``apply_limits`` evaluates against). Populated by the view's bulk prefetch on list responses; falls back to a single HGET on detail.
    """

    tags: typing.Optional[typing.List[GenericTagDisplay]] = pydantic.Field(default=None)
    """
    ``GenericTag`` rows assigned to this policy. Managed via ``/api/tag-assignments/limit_policies/...`` — read-only here.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
