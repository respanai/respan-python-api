# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class StaffGroupRequest(UniversalBaseModel):
    """
    Group CRUD shape. ``name`` is the external identifier; the UUID
    ``id`` is read-only.

    POST body:
        {
            "name": "engineering_admins",
            "permissions_override": ["staff_read", "staff_write"],
            "description": "Engineering team admins"
        }

    PATCH body: any subset of {permissions_override, description}.
    Renaming a group is allowed via PATCH(name); the UUID stays
    stable so existing memberships don't break.
    """

    name: str
    description: typing.Optional[str] = None
    permissions_override: typing.Optional[typing.List[str]] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
