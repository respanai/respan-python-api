# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .condition_policy_pydantic import ConditionPolicyPydantic


class ConditionWorkflowTaskConfig(UniversalBaseModel):
    """
    Configuration for condition workflow task.

    Generic condition evaluation that can evaluate ANY arbitrary object against
    condition rules. Supports both condition types:
    - "single_log" (default): Evaluate object fields against filter conditions

    This is a PURE FUNCTION - no state, just comparison logic.
    Uses standard filter syntax to evaluate any object.

    Flow Control:
        - on_true: Action when condition IS met (default: "continue")
        - on_false: Action when condition is NOT met (default: "stop")

    Actions:
        - "continue": Continue to next task
        - "stop": Stop workflow execution

    For conditional routing, use a 'switch' task instead.

    Example:
        {
            "condition_policy": {"cost_sum": {"operator": "gt", "value": 100}},
            "on_true": "continue",
            "on_false": "stop"
        }
    """

    condition_policy: typing.Optional[ConditionPolicyPydantic] = pydantic.Field(default=None)
    """
    Filter conditions using standard filter syntax (resolved at cache time). Uses `ConditionPolicyPydantic` — one condition per metric. The condition editor UI only writes single entries, and nothing in the stack constructs the list / bundle branches `FilterParamDictPydantic` allows, so those shapes are rejected at the schema boundary rather than leaking through to the frontend as a cast-forcing union type.
    """

    on_true: typing.Optional[str] = pydantic.Field(default=None)
    """
    Action when condition IS met: 'continue' or 'stop'
    """

    on_false: typing.Optional[str] = pydantic.Field(default=None)
    """
    Action when condition is NOT met: 'continue' or 'stop'
    """

    condition_name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Condition name stored for display purposes in the UI
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
