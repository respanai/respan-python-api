# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel, update_forward_refs


class ExportWorkflowTaskConfig(UniversalBaseModel):
    """
    Configuration for an export workflow task.

    Each scheduled fire is a ``resume_export()`` cycle on a long-lived
    ``ExportJob`` row whose ``source_workflow_version_id`` was populated at
    save-time auto-provisioning. Lower bound of the window is
    ``ExportJob.export_check_point_at`` (advanced by the exporter every
    cycle); upper bound is ``fire_time_utc`` (pushed forward by the bridge).
    There is no per-task ``window_seconds`` — the watermark drives the window.
    """

    include_fields: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    Optional CHLogV3 column allowlist. When None, the chunk carries the full unified-log row.
    """

    filters: typing.Optional["FilterParamDictPydantic"] = pydantic.Field(default=None)
    """
    Filter predicate, same shape as condition_policy / aggregation filters. Applied as a CH WHERE clause inside the time window.
    """

    export_job_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Continuous ExportJob this task appends to. Auto-resolved at workflow save time (see workflow/signals.py); validation rejects the save if a stale id is supplied.
    """

    is_inline_results: typing.Optional[bool] = pydantic.Field(default=None)
    """
    When True, the handler also writes the queried rows into namespace["export"]["records"] so downstream condition / notification tasks can read them. When False, namespace only gets chunk + record-count metadata.
    """

    sample_percentage: typing.Optional[float] = pydantic.Field(default=None)
    """
    Optional row sample rate (0 < pct <= 100) applied per fire, same as one-shot export sampling (CHLogV3 row subset inside the window). Matches the canonical parse_sample_percentage range; None or 100 exports every row.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


from .filter_bundle_pydantic import FilterBundlePydantic  # noqa: E402, I001
from .filter_param_dict_pydantic import FilterParamDictPydantic  # noqa: E402, I001
from .filter_param_dict_pydantic_value import FilterParamDictPydanticValue  # noqa: E402, I001

update_forward_refs(
    ExportWorkflowTaskConfig,
    FilterBundlePydantic=FilterBundlePydantic,
    FilterParamDictPydantic=FilterParamDictPydantic,
    FilterParamDictPydanticValue=FilterParamDictPydanticValue,
)
