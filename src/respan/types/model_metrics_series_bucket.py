# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ModelMetricsSeriesBucket(UniversalBaseModel):
    """
    One time bucket of the performance metrics series (`metrics_series`); the
    bucket size is `time_tick`. Normalized rate fields are public; volume fields
    (`number_of_requests`, `cost`) are admin-only and omitted for public callers
    — hence `required=False`. Empty buckets carry `null` rates and 0 requests.
    """

    date_group: dt.datetime = pydantic.Field()
    """
    Start of the time bucket (UTC). One bucket per `time_tick` step.
    """

    average_tps: typing.Optional[float] = pydantic.Field(default=None)
    """
    Average output tokens/second in the bucket; `null` if no traffic.
    """

    average_ttft: typing.Optional[float] = pydantic.Field(default=None)
    """
    Average time-to-first-token (seconds) in the bucket, streaming requests only; `null` if none.
    """

    average_latency: typing.Optional[float] = pydantic.Field(default=None)
    """
    Average end-to-end latency (seconds) in the bucket; `null` if no traffic.
    """

    cache_hit_percentage: typing.Optional[float] = pydantic.Field(default=None)
    """
    Prompt-cache hit rate (% of prompt tokens) in the bucket; `null` if no traffic.
    """

    number_of_requests: typing.Optional[int] = pydantic.Field(default=None)
    """
    Admin-only. Request count in the bucket. Omitted for public callers (volume is revenue-inferable).
    """

    cost: typing.Optional[float] = pydantic.Field(default=None)
    """
    Admin-only. Spend (USD) in the bucket. Omitted for public callers.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
