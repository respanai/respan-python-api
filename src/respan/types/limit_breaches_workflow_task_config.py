# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class LimitBreachesWorkflowTaskConfig(UniversalBaseModel):
    """
    limit_breaches task config.

    Reports limit-policy breaches at scheduled fire time, sourced from the
    limit subsystem (PG policies + live Redis counters) — NOT ClickHouse.
    Unlike get_logs there is no window: a breach is "live counter ≥ rule
    threshold right now", so the task reads each policy's current-window
    counter directly. ``scope`` selects which subject the report covers;
    v1 only ``api_key`` (the handler/data layer are scope-agnostic, so
    model/customer/etc. extend ``SUPPORTED_LIMIT_BREACHES_SCOPES`` later).
    """

    scope: typing.Optional[str] = pydantic.Field(default=None)
    """
    Which limit-policy subject to report. v1 supports only 'api_key'.
    """

    max_rows: typing.Optional[int] = pydantic.Field(default=None)
    """
    Hard cap on breach rows returned (highest overage first).
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
