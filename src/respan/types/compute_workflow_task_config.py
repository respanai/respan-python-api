# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .compute_workflow_task_config_function import ComputeWorkflowTaskConfigFunction
from .compute_workflow_task_input_ref import ComputeWorkflowTaskInputRef


class ComputeWorkflowTaskConfig(UniversalBaseModel):
    """
    Configuration for compute workflow task — arithmetic on upstream task outputs.

    Binary functions: ratio (a/b), percentage (a/b × 100), difference (a − b).
      Exactly 2 inputs required (positional: inputs[0] = a, inputs[1] = b).
    N-ary functions: weighted_average (Σ(v×w)/Σ(w)).
      Requires >= 2 inputs, each with an optional weight (default 1.0).
    """

    function: ComputeWorkflowTaskConfigFunction = pydantic.Field()
    """
    Arithmetic function to apply: ratio, percentage, difference, weighted_average
    """

    inputs: typing.List[ComputeWorkflowTaskInputRef] = pydantic.Field()
    """
    Ordered operand references into the task namespace
    """

    label: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional human-readable label for the computed result
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
