# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .aggregation_function_choices import AggregationFunctionChoices
from .condition_policy_pydantic import ConditionPolicyPydantic


class AggregationTaskMetric(UniversalBaseModel):
    """
    Metric definition for aggregation task (what to aggregate).

    This is used by the aggregation workflow task to define WHAT to collect.
    Does NOT include threshold/operator - those belong in the condition task.

    Output Field Naming:
        - If `output_field_name` is provided, uses that as the output key
        - Otherwise, auto-generates: `{field_name}_{aggregation_function}` (e.g., "cost_sum")

    Example:
        {"field_name": "cost", "aggregation_function": "sum"}  # outputs "cost_sum"
        {"field_name": "cost", "aggregation_function": "sum", "output_field_name": "total_cost"}  # outputs "total_cost"
    """

    field_name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Field to aggregate on (e.g., 'cost', 'latency', 'total_tokens'). Optional for count metrics.
    """

    aggregation_function: AggregationFunctionChoices = pydantic.Field()
    """
    Aggregation function to apply (sum, avg, count, min, max)
    """

    output_field_name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Custom output field name. If not provided, defaults to '{field_name}_{aggregation_function}'
    """

    filters: typing.Optional[ConditionPolicyPydantic] = pydantic.Field(default=None)
    """
    Optional per-metric filter conditions in the standard `condition_policy` format. Only events matching these filters are accumulated for this metric. Uses `ConditionPolicyPydantic` (single condition per metric) — same narrow shape as `ConditionWorkflowTaskConfig.condition_policy` so the condition editor UI can edit both fields with one component.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
