# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .bulk_item_error import BulkItemError


class WorkflowRunBulkResponse(UniversalBaseModel):
    """
    Response for POST /api/workflow-runs/bulk/.

    Extends the canonical bulk envelope with the async-dispatch extras that
    run_bulk_async_dispatch attaches (dispatched, total_requested) plus the
    resolved workflow_version_id.
    """

    success_count: int
    error_count: int
    errors: typing.List[BulkItemError]
    dispatched: int = pydantic.Field()
    """
    Number of runs dispatched (== success_count).
    """

    total_requested: int = pydantic.Field()
    """
    Number of source items requested.
    """

    workflow_version_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Resolved workflow version id the runs were dispatched for.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
