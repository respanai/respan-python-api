# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class SubWorkflowTaskConfig(UniversalBaseModel):
    """
    Configuration for a sub-workflow task (nested workflow execution).

    Two shapes are accepted:

    - **Inline**: `tasks` carries an explicit list of child task dicts.
      Used for anonymous sub-workflows defined in-place.
    - **Reference**: `workflow_id` points at the family workflow_id of a
      deployed WorkflowVersion (for example, an automation "Evaluator"
      node that delegates scoring to a deployed evaluator pipeline). The
      handler resolves the deployed version at run time.

    Exactly one of the two must be set.
    """

    tasks: typing.Optional[typing.List[typing.Dict[str, typing.Any]]] = pydantic.Field(default=None)
    """
    Nested task list to execute as a child workflow
    """

    workflow_id: typing.Optional[str] = pydantic.Field(default=None)
    """
    Family workflow_id of a deployed WorkflowVersion to execute as a child workflow. Mutually exclusive with `tasks`.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
