# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ChEvalPipelineScores(UniversalBaseModel):
    """
    Output-score aggregate for one evaluator pipeline (DEV-8439 Phase 2).

    One row — the pipeline's output (rollup) score collapsed across versions, so
    the FE draws one line. FE picks ``avg_primary_score`` (numeric) or
    ``true_ratio`` (boolean, fraction passed; null when no boolean rows → "—").
    Key columns emitted empty: one series needs no key.
    """

    workflow_version_id: str
    evaluator_id: str
    scorer: str
    evaluator_name: str
    avg_primary_score: typing.Optional[float] = None
    true_ratio: typing.Optional[float] = None
    sample_count: int

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
