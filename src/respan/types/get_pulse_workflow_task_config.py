# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class GetPulseWorkflowTaskConfig(UniversalBaseModel):
    """
    get_pulse task config (DEV-10006).

    Pure fetch node — pulls the report window's error-issue pulse facts
    from ``error_issue_agg`` for downstream summarization. No LLM call.
    The window is anchored to the scheduled fire like ``get_logs``
    (previous-fire anchor clamped to [24h, 7d]); ``window_seconds`` only
    covers the anchor-less cases so the pulse facts and the digest
    numbers stay on the same window even then.
    """

    max_issues: typing.Optional[int] = pydantic.Field(default=None)
    """
    Hard cap on top issues returned (highest occurrence first).
    """

    window_seconds: typing.Optional[int] = pydantic.Field(default=None)
    """
    Fallback window width when there is no previous scheduled fire to anchor to (first run / malformed anchor) — set it to the same value as the report's get_logs tasks so the pulse facts cover the digest window even then (mirrors get_logs' fallback). None falls back to 24h. Ignored when an anchor is present.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
