# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ChEvalPipelineScoresTimeSeries(UniversalBaseModel):
    """
    Time-bucketed output score for one evaluator pipeline (graphs).

    Same aggregate as ``CHEvalPipelineScoresSerializer`` but one row per time
    bucket — the single output-score series for the scores-over-time chart,
    ordered by bucket ascending. ``date_group`` is a DateTimeField over the
    bare CH bucket for the same tz reason as the workflow-metrics one.
    """

    date_group: dt.datetime
    evaluator_id: str
    scorer: str
    evaluator_name: str
    avg_primary_score: typing.Optional[float] = None
    true_ratio: typing.Optional[float] = None
    sample_count: int

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
