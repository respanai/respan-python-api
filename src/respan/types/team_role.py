# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .organization_list import OrganizationList
from .team_role_company_organization import TeamRoleCompanyOrganization
from .team_role_role import TeamRoleRole


class TeamRole(UniversalBaseModel):
    """
    Schema-only shape of one ``GET /auth/teams/`` row.

    Never used at runtime — the view returns OrganizationUserRoleTeamSerializer
    rows plus synthesized sibling-org rows (``id=None, role=None`` — orgs in the
    company the user has no membership in); this documents that union so the
    generated client stops emitting a bodiless response.
    """

    id: typing.Optional[int] = None
    role: TeamRoleRole
    organization: OrganizationList
    company_organization: TeamRoleCompanyOrganization

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
