# This file was auto-generated by Fern from our API Definition.

from __future__ import annotations

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel, update_forward_refs
from .switch_case import SwitchCase


class SwitchWorkflowTaskConfig(UniversalBaseModel):
    """
    Configuration for switch workflow task — multi-branch conditional routing.

    Evaluates ordered cases against a data payload. First match wins,
    routing the workflow to the matched case's target via skip_to action.

    Cases use inline condition_policy.

    Example:
        {
            "cases": [
                {
                    "condition_policy": {
                        "category": {"operator": "eq", "value": "billing"}
                    },
                    "target": "billing_agent"
                },
                {
                    "condition_policy": {
                        "category": {"operator": "eq", "value": "technical"}
                    },
                    "target": "tech_agent"
                }
            ],
            "default": "summary_agent"
        }
    """

    cases: typing.List[SwitchCase] = pydantic.Field()
    """
    Ordered list of condition/target pairs (first match wins)
    """

    default: typing.Optional[str] = pydantic.Field(default=None)
    """
    Fallback target when no case matches
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow


update_forward_refs(SwitchWorkflowTaskConfig)
