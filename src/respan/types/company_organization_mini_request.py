# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class CompanyOrganizationMiniRequest(UniversalBaseModel):
    """
    Lean company-org identity for embedding in other payloads (grouping key).

    Kept intentionally minimal — just the identity + display name — so it can be
    prejoined onto per-team rows (see the ``/auth/teams/`` payload) without
    leaking billing fields or bloating the response.
    """

    unique_company_organization_id: typing.Optional[str] = None
    name: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
