# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class TeamsNotificationMethod(UniversalBaseModel):
    """
    Posts to a Teams channel via a Workflows webhook or the Graph OAuth path.

    Teams addressing is two-level, so the OAuth branch requires BOTH ``team_id``
    and ``channel_id`` (Slack's flat model needs only ``channel_id``). The
    webhook branch needs only ``webhook_url`` — its Workflows URL carries auth
    as a signature in the query string, so ``notification_target`` surfaces the
    host only for that path.
    """

    webhook_url: typing.Optional[str] = None
    headers: typing.Optional[typing.Dict[str, str]] = None
    method: typing.Optional[str] = None
    payload_format: typing.Optional[str] = None
    oauth_integration_id: typing.Optional[str] = None
    team_id: typing.Optional[str] = None
    team_name: typing.Optional[str] = None
    channel_id: typing.Optional[str] = None
    channel_name: typing.Optional[str] = None
    unique_organization_id: typing.Optional[str] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
