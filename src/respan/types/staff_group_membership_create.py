# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class StaffGroupMembershipCreate(UniversalBaseModel):
    """
    POST /api/admin/staff-groups/{name}/memberships/  body: {"email": "alice@x"}

    Creates or reinstates a membership linking ``email`` to the
    group identified by the URL path. Group must be active; user
    must already exist (no signup flow).

    Self-grant block fires here too — a principal cannot add themselves
    to a group, even one they already belong to.
    """

    email: str

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
