# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ObjectInteraction(UniversalBaseModel):
    """
    One object-interaction aggregate row, enriched with the resource name.

    One row per object the user has interacted with: the
    ``ObjectInteractionsQueryBuilder`` aggregate columns plus an enriched
    ``name``. ``name`` is attached after serialization (the view's
    ``enrich_results``) so it is absent on the raw row; it is null when the object
    was deleted after its interactions were recorded, or its feature_type has no
    name lookup (the row is still returned). Row order follows the requested
    ``sort_by`` (recency by default); the serializer preserves it.
    """

    feature_type: str = pydantic.Field()
    """
    The resource kind (datasets, prompts, ...).
    """

    object_id: str = pydantic.Field()
    """
    The resource's id (dataset id, prompt_id, ...).
    """

    name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Display name of the resource; null when it no longer exists.
    """

    interaction_count: int = pydantic.Field()
    """
    Recorded interactions for the object (count(id) on ch_activity_event).
    """

    last_interaction_at: typing.Optional[dt.datetime] = pydantic.Field(default=None)
    """
    Timestamp of the most recent interaction, or null.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
