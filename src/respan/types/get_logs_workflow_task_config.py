# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .get_logs_order_by import GetLogsOrderBy


class GetLogsWorkflowTaskConfig(UniversalBaseModel):
    """
    get_logs task config (DEV-8327).

    Window: ``window_end = fire_time_utc - GET_LOGS_LAG_SECONDS``,
    ``window_start = window_end - window_seconds``.

    A non-empty ``group_by`` produces a grouped rollup (``SELECT group_by,
    metrics FROM ch_log_v3 ... GROUP BY``); an empty ``group_by`` produces
    a single summary row over the window (no ``GROUP BY``). ``breakdowns``
    produces one grouped rollup per entry over the shared window, with the
    window/prior totals queried once. Raw-row dumps belong in the ``export``
    task.

    ``group_by`` entries must be bare CHLogV3 column identifiers — custom
    ``metadata.X`` / ``properties.X`` paths are rejected by the SQL
    builder's ``GET_LOGS_SAFE_IDENTIFIER_PATTERN``. Filtering is also
    not exposed — the slice is fixed by ``unique_organization_id`` and
    the window bounds.
    """

    window_seconds: int = pydantic.Field()
    """
    Window width in seconds, ≤ 7 days.
    """

    group_by: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    Bare CHLogV3 column names to GROUP BY. Empty = no grouping (one summary row over the window). A multi-element list is ONE composite grouping — use ``breakdowns`` for independent dimensions.
    """

    breakdowns: typing.Optional[typing.List[typing.List[str]]] = pydantic.Field(default=None)
    """
    Independent breakdown dimensions — each entry a group_by column list rolled up separately over the shared window/metrics. Mutually exclusive with ``group_by``.
    """

    metrics: typing.List[str] = pydantic.Field()
    """
    Metric names referencing DASHBOARD_METRICS.
    """

    order_by: typing.Optional[GetLogsOrderBy] = pydantic.Field(default=None)
    """
    Ordering; defaults to first-metric DESC.
    """

    max_groups: typing.Optional[int] = pydantic.Field(default=None)
    """
    Hard cap on groups returned.
    """

    include_prior_period: typing.Optional[bool] = pydantic.Field(default=None)
    """
    When True, runs a second ungrouped query over the equal-length window immediately preceding the report window and emits the results under GET_LOGS_OUTPUT_PRIOR_TOTALS_KEY for delta rendering downstream. Doubles per-task CH read volume — gated by the same per-org rate limit.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
