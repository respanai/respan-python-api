# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .code_enum import CodeEnum


class SqlQueryErrorResponse(UniversalBaseModel):
    """
    Error response serializer for the SQL Query Editor endpoint.

    Every 4xx/413 error is returned as ``{code, detail, error, technical_detail?}``.
    ``code`` is a stable machine-readable category the frontend maps to its own
    copy; ``detail`` is the server-default human message (safe fallback);
    ``error`` mirrors ``detail`` for backward compatibility with the current FE
    and is removed once it migrates to ``code``/``detail``;
    ``technical_detail`` is the optional sanitized raw engine message.
    """

    code: CodeEnum = pydantic.Field()
    """
    Stable machine-readable error category (e.g. 'syntax_error', 'expensive_query', 'timeout'). Map this to user-facing copy.
    
    * `syntax_error` - syntax_error
    * `table_not_found` - table_not_found
    * `column_not_found` - column_not_found
    * `unknown_function` - unknown_function
    * `type_mismatch` - type_mismatch
    * `expensive_query` - expensive_query
    * `timeout` - timeout
    * `query_cancelled` - query_cancelled
    * `not_select` - not_select
    * `unsupported_feature` - unsupported_feature
    * `query_too_long` - query_too_long
    * `empty_query` - empty_query
    * `access_denied` - access_denied
    * `unknown_error` - unknown_error
    """

    detail: str = pydantic.Field()
    """
    Server-default human message describing the failure.
    """

    error: str = pydantic.Field()
    """
    Deprecated alias of `detail`, mirrored for backward compatibility with the current SQL-editor frontend. Prefer `detail`; this field will be removed once the frontend migrates to `code`/`detail`.
    """

    technical_detail: typing.Optional[str] = pydantic.Field(default=None)
    """
    Optional sanitized raw engine message (e.g. the ClickHouse 'Code: N' line) for power users / debugging.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
