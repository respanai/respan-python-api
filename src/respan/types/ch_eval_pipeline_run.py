# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .ch_eval_pipeline_run_grader_score import ChEvalPipelineRunGraderScore


class ChEvalPipelineRun(UniversalBaseModel):
    """
    One evaluator-pipeline run = one graded log (DEV-8439 run-history table).

    Each row is a log this pipeline graded: the original log fields
    (``src_*``, joined from ``ch_log_v3`` by ``unique_id = log_unique_id``) plus
    every grader's score for that log (``grader_scores``). The FE pivots
    ``grader_scores`` into one score column per grader, mirroring the chart
    above. ``src_*`` fields are null when the original log is outside retention.
    ``src_timestamp`` is the source log's own start time (not grading time
    ``run_at``); the FE anchors the open-log link's window on it, omitting the
    link when it's null (source log not retrievable).
    """

    log_unique_id: str
    run_at: dt.datetime
    eval_cost: typing.Optional[float] = None
    output_primary_score: typing.Optional[float] = None
    output_boolean_value: typing.Optional[int] = None
    src_input: typing.Optional[str] = None
    src_output: typing.Optional[str] = None
    src_model: typing.Optional[str] = None
    src_status: typing.Optional[str] = None
    src_trace_unique_id: typing.Optional[str] = None
    src_timestamp: typing.Optional[dt.datetime] = None
    grader_scores: typing.Optional[typing.List[ChEvalPipelineRunGraderScore]] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
