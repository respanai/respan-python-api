# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ChWorkflowRun(UniversalBaseModel):
    """
    One monitor/automation run (per-run Runs-tab table row).

    Newest-first per-run rows off ch_log_v3 workflow root rows — replaces the
    per-version rollup table. ``workflow_status`` is the run outcome
    (completed / stopped / failed); delivery counts are the notification
    methods that fired / errored on this run.
    """

    unique_id: str
    timestamp: dt.datetime
    workflow_version_id: str
    workflow_version_type: str
    workflow_status: str
    delivery_fired_count: int
    delivery_failed_count: int

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
