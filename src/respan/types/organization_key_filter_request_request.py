# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class OrganizationKeyFilterRequestRequest(UniversalBaseModel):
    """
    Request body for POST-for-filtering on /api/keys/list/ and
    /api/keys/summary/ — distinguishes the filter POST from key creation.

    `filters` is an open dict (matches DatasetFilterRequestSerializer): the
    filter shape is dynamic and the frontend owns a typed FilterRequest, so a
    strict per-field schema would only fight the FE's filter types.
    """

    filters: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Filter parameters keyed by field name.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
