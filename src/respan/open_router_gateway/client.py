# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from .raw_client import AsyncRawOpenRouterGatewayClient, RawOpenRouterGatewayClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class OpenRouterGatewayClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawOpenRouterGatewayClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawOpenRouterGatewayClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawOpenRouterGatewayClient
        """
        return self._raw_client

    def openrouter_native_chat_completions(
        self,
        *,
        openrouter_api_key: str,
        model: str,
        messages: typing.Sequence[typing.Dict[str, typing.Any]],
        stream: typing.Optional[bool] = OMIT,
        provider: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        reasoning: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        respan_params: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Dict[str, typing.Any]:
        """
        Relay an OpenRouter chat-completions payload through Respan with nothing filtered out, so OpenRouter-owned fields such as provider and reasoning survive the call. This endpoint is BYOK: Authorization carries your Respan API key and x-openrouter-api-key carries your OpenRouter API key. There is no fallback to Respan gateway credits. OpenRouter's response, status code, and event stream are relayed back unchanged.

        Parameters
        ----------
        openrouter_api_key : str
            Your OpenRouter API key. Authorization carries the Respan API key on this operation.

        model : str
            OpenRouter model slug, for example anthropic/claude-sonnet-4-5. An openrouter/ prefix is also accepted and is stripped before the call goes upstream.

        messages : typing.Sequence[typing.Dict[str, typing.Any]]

        stream : typing.Optional[bool]

        provider : typing.Optional[typing.Dict[str, typing.Any]]
            OpenRouter provider routing block, forwarded unchanged.

        reasoning : typing.Optional[typing.Dict[str, typing.Any]]
            OpenRouter reasoning block, forwarded unchanged.

        respan_params : typing.Optional[typing.Dict[str, typing.Any]]
            Respan gateway metadata. Stripped before the payload reaches OpenRouter.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Dict[str, typing.Any]
            OpenRouter response or event stream.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )
        client.open_router_gateway.openrouter_native_chat_completions(
            openrouter_api_key="x-openrouter-api-key",
            model="model",
            messages=[{"key": "value"}],
        )
        """
        _response = self._raw_client.openrouter_native_chat_completions(
            openrouter_api_key=openrouter_api_key,
            model=model,
            messages=messages,
            stream=stream,
            provider=provider,
            reasoning=reasoning,
            respan_params=respan_params,
            request_options=request_options,
        )
        return _response.data


class AsyncOpenRouterGatewayClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawOpenRouterGatewayClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawOpenRouterGatewayClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawOpenRouterGatewayClient
        """
        return self._raw_client

    async def openrouter_native_chat_completions(
        self,
        *,
        openrouter_api_key: str,
        model: str,
        messages: typing.Sequence[typing.Dict[str, typing.Any]],
        stream: typing.Optional[bool] = OMIT,
        provider: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        reasoning: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        respan_params: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.Dict[str, typing.Any]:
        """
        Relay an OpenRouter chat-completions payload through Respan with nothing filtered out, so OpenRouter-owned fields such as provider and reasoning survive the call. This endpoint is BYOK: Authorization carries your Respan API key and x-openrouter-api-key carries your OpenRouter API key. There is no fallback to Respan gateway credits. OpenRouter's response, status code, and event stream are relayed back unchanged.

        Parameters
        ----------
        openrouter_api_key : str
            Your OpenRouter API key. Authorization carries the Respan API key on this operation.

        model : str
            OpenRouter model slug, for example anthropic/claude-sonnet-4-5. An openrouter/ prefix is also accepted and is stripped before the call goes upstream.

        messages : typing.Sequence[typing.Dict[str, typing.Any]]

        stream : typing.Optional[bool]

        provider : typing.Optional[typing.Dict[str, typing.Any]]
            OpenRouter provider routing block, forwarded unchanged.

        reasoning : typing.Optional[typing.Dict[str, typing.Any]]
            OpenRouter reasoning block, forwarded unchanged.

        respan_params : typing.Optional[typing.Dict[str, typing.Any]]
            Respan gateway metadata. Stripped before the payload reaches OpenRouter.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.Dict[str, typing.Any]
            OpenRouter response or event stream.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )


        async def main() -> None:
            await client.open_router_gateway.openrouter_native_chat_completions(
                openrouter_api_key="x-openrouter-api-key",
                model="model",
                messages=[{"key": "value"}],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.openrouter_native_chat_completions(
            openrouter_api_key=openrouter_api_key,
            model=model,
            messages=messages,
            stream=stream,
            provider=provider,
            reasoning=reasoning,
            respan_params=respan_params,
            request_options=request_options,
        )
        return _response.data
