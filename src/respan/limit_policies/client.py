# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.limit_policy import LimitPolicy
from ..types.limit_policy_rule import LimitPolicyRule
from .raw_client import AsyncRawLimitPoliciesClient, RawLimitPoliciesClient
from .types.limit_policy_create_request_algorithm import LimitPolicyCreateRequestAlgorithm
from .types.limit_policy_create_request_anchor import LimitPolicyCreateRequestAnchor
from .types.limit_policy_create_request_metric import LimitPolicyCreateRequestMetric
from .types.list_limit_policies_response import ListLimitPoliciesResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class LimitPoliciesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawLimitPoliciesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawLimitPoliciesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawLimitPoliciesClient
        """
        return self._raw_client

    def list_limit_policies(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        ordering: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListLimitPoliciesResponse:
        """
        List every configured policy in your organization, including zero-traffic policies. Each carries its live `current_state`.

        Parameters
        ----------
        page : typing.Optional[int]

        page_size : typing.Optional[int]

        ordering : typing.Optional[str]
            Prefix with `-` for descending.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListLimitPoliciesResponse
            Paginated list of policies.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )
        client.limit_policies.list_limit_policies()
        """
        _response = self._raw_client.list_limit_policies(
            page=page, page_size=page_size, ordering=ordering, request_options=request_options
        )
        return _response.data

    def create_limit_policy(
        self,
        *,
        name: str,
        scope_value: str,
        metric: LimitPolicyCreateRequestMetric,
        rules: typing.Sequence[LimitPolicyRule],
        scope: typing.Optional[str] = OMIT,
        compose: typing.Optional[typing.Sequence[str]] = OMIT,
        algorithm: typing.Optional[LimitPolicyCreateRequestAlgorithm] = OMIT,
        period: typing.Optional[str] = OMIT,
        anchor: typing.Optional[LimitPolicyCreateRequestAnchor] = OMIT,
        anchor_at: typing.Optional[dt.datetime] = OMIT,
        effective_at: typing.Optional[dt.datetime] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        priority: typing.Optional[int] = OMIT,
        is_active: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> LimitPolicy:
        """
        Create a policy. Returns the full policy, matching the retrieve response. Caps are stored and visible as soon as they are created, but the gateway enforces them only where the limit system is enabled for your environment. Where it is not enabled, a cap records no usage and never blocks.

        Parameters
        ----------
        name : str

        scope_value : str
            Identity to meter. For composites (`compose` length >= 2), a JSON list of values in `compose` order.

        metric : LimitPolicyCreateRequestMetric
            What the cap counts. `cost` is US dollars, `token_count` is total tokens, `request_count` is calls.

        rules : typing.Sequence[LimitPolicyRule]

        scope : typing.Optional[str]
            Display category. Not used for matching.

        compose : typing.Optional[typing.Sequence[str]]

        algorithm : typing.Optional[LimitPolicyCreateRequestAlgorithm]

        period : typing.Optional[str]
            Named bucket, integer-seconds string, or `null` for cumulative.

        anchor : typing.Optional[LimitPolicyCreateRequestAnchor]

        anchor_at : typing.Optional[dt.datetime]

        effective_at : typing.Optional[dt.datetime]

        expires_at : typing.Optional[dt.datetime]

        priority : typing.Optional[int]

        is_active : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LimitPolicy
            The created policy.

        Examples
        --------
        from respan import LimitPolicyRule, RespanClient

        client = RespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )
        client.limit_policies.create_limit_policy(
            name="name",
            scope_value="scope_value",
            metric="cost",
            rules=[
                LimitPolicyRule(
                    trigger={"key": "value"},
                    severity="soft",
                )
            ],
        )
        """
        _response = self._raw_client.create_limit_policy(
            name=name,
            scope_value=scope_value,
            metric=metric,
            rules=rules,
            scope=scope,
            compose=compose,
            algorithm=algorithm,
            period=period,
            anchor=anchor,
            anchor_at=anchor_at,
            effective_at=effective_at,
            expires_at=expires_at,
            priority=priority,
            is_active=is_active,
            request_options=request_options,
        )
        return _response.data

    def retrieve_limit_policy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> LimitPolicy:
        """
        Retrieve one policy with its live `current_state`.

        Parameters
        ----------
        id : str
            Limit policy id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LimitPolicy
            The policy.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )
        client.limit_policies.retrieve_limit_policy(
            id="id",
        )
        """
        _response = self._raw_client.retrieve_limit_policy(id, request_options=request_options)
        return _response.data

    def delete_limit_policy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Hard-delete the policy. Returns 204.

        Parameters
        ----------
        id : str
            Limit policy id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )
        client.limit_policies.delete_limit_policy(
            id="id",
        )
        """
        _response = self._raw_client.delete_limit_policy(id, request_options=request_options)
        return _response.data

    def update_limit_policy(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        scope: typing.Optional[str] = OMIT,
        rules: typing.Optional[typing.Sequence[LimitPolicyRule]] = OMIT,
        priority: typing.Optional[int] = OMIT,
        is_active: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> LimitPolicy:
        """
        Update mutable fields (`name`, `scope`, `rules`, `priority`, `is_active`). Immutable fields are ignored.

        Parameters
        ----------
        id : str
            Limit policy id.

        name : typing.Optional[str]

        scope : typing.Optional[str]

        rules : typing.Optional[typing.Sequence[LimitPolicyRule]]

        priority : typing.Optional[int]

        is_active : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LimitPolicy
            The updated policy.

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )
        client.limit_policies.update_limit_policy(
            id="id",
        )
        """
        _response = self._raw_client.update_limit_policy(
            id,
            name=name,
            scope=scope,
            rules=rules,
            priority=priority,
            is_active=is_active,
            request_options=request_options,
        )
        return _response.data


class AsyncLimitPoliciesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawLimitPoliciesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawLimitPoliciesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawLimitPoliciesClient
        """
        return self._raw_client

    async def list_limit_policies(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        ordering: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListLimitPoliciesResponse:
        """
        List every configured policy in your organization, including zero-traffic policies. Each carries its live `current_state`.

        Parameters
        ----------
        page : typing.Optional[int]

        page_size : typing.Optional[int]

        ordering : typing.Optional[str]
            Prefix with `-` for descending.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListLimitPoliciesResponse
            Paginated list of policies.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )


        async def main() -> None:
            await client.limit_policies.list_limit_policies()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_limit_policies(
            page=page, page_size=page_size, ordering=ordering, request_options=request_options
        )
        return _response.data

    async def create_limit_policy(
        self,
        *,
        name: str,
        scope_value: str,
        metric: LimitPolicyCreateRequestMetric,
        rules: typing.Sequence[LimitPolicyRule],
        scope: typing.Optional[str] = OMIT,
        compose: typing.Optional[typing.Sequence[str]] = OMIT,
        algorithm: typing.Optional[LimitPolicyCreateRequestAlgorithm] = OMIT,
        period: typing.Optional[str] = OMIT,
        anchor: typing.Optional[LimitPolicyCreateRequestAnchor] = OMIT,
        anchor_at: typing.Optional[dt.datetime] = OMIT,
        effective_at: typing.Optional[dt.datetime] = OMIT,
        expires_at: typing.Optional[dt.datetime] = OMIT,
        priority: typing.Optional[int] = OMIT,
        is_active: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> LimitPolicy:
        """
        Create a policy. Returns the full policy, matching the retrieve response. Caps are stored and visible as soon as they are created, but the gateway enforces them only where the limit system is enabled for your environment. Where it is not enabled, a cap records no usage and never blocks.

        Parameters
        ----------
        name : str

        scope_value : str
            Identity to meter. For composites (`compose` length >= 2), a JSON list of values in `compose` order.

        metric : LimitPolicyCreateRequestMetric
            What the cap counts. `cost` is US dollars, `token_count` is total tokens, `request_count` is calls.

        rules : typing.Sequence[LimitPolicyRule]

        scope : typing.Optional[str]
            Display category. Not used for matching.

        compose : typing.Optional[typing.Sequence[str]]

        algorithm : typing.Optional[LimitPolicyCreateRequestAlgorithm]

        period : typing.Optional[str]
            Named bucket, integer-seconds string, or `null` for cumulative.

        anchor : typing.Optional[LimitPolicyCreateRequestAnchor]

        anchor_at : typing.Optional[dt.datetime]

        effective_at : typing.Optional[dt.datetime]

        expires_at : typing.Optional[dt.datetime]

        priority : typing.Optional[int]

        is_active : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LimitPolicy
            The created policy.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient, LimitPolicyRule

        client = AsyncRespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )


        async def main() -> None:
            await client.limit_policies.create_limit_policy(
                name="name",
                scope_value="scope_value",
                metric="cost",
                rules=[
                    LimitPolicyRule(
                        trigger={"key": "value"},
                        severity="soft",
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create_limit_policy(
            name=name,
            scope_value=scope_value,
            metric=metric,
            rules=rules,
            scope=scope,
            compose=compose,
            algorithm=algorithm,
            period=period,
            anchor=anchor,
            anchor_at=anchor_at,
            effective_at=effective_at,
            expires_at=expires_at,
            priority=priority,
            is_active=is_active,
            request_options=request_options,
        )
        return _response.data

    async def retrieve_limit_policy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> LimitPolicy:
        """
        Retrieve one policy with its live `current_state`.

        Parameters
        ----------
        id : str
            Limit policy id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LimitPolicy
            The policy.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )


        async def main() -> None:
            await client.limit_policies.retrieve_limit_policy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve_limit_policy(id, request_options=request_options)
        return _response.data

    async def delete_limit_policy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Hard-delete the policy. Returns 204.

        Parameters
        ----------
        id : str
            Limit policy id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )


        async def main() -> None:
            await client.limit_policies.delete_limit_policy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_limit_policy(id, request_options=request_options)
        return _response.data

    async def update_limit_policy(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        scope: typing.Optional[str] = OMIT,
        rules: typing.Optional[typing.Sequence[LimitPolicyRule]] = OMIT,
        priority: typing.Optional[int] = OMIT,
        is_active: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> LimitPolicy:
        """
        Update mutable fields (`name`, `scope`, `rules`, `priority`, `is_active`). Immutable fields are ignored.

        Parameters
        ----------
        id : str
            Limit policy id.

        name : typing.Optional[str]

        scope : typing.Optional[str]

        rules : typing.Optional[typing.Sequence[LimitPolicyRule]]

        priority : typing.Optional[int]

        is_active : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        LimitPolicy
            The updated policy.

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            authorization="YOUR_AUTHORIZATION",
            respan_api_key="YOUR_RESPAN_API_KEY",
        )


        async def main() -> None:
            await client.limit_policies.update_limit_policy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_limit_policy(
            id,
            name=name,
            scope=scope,
            rules=rules,
            priority=priority,
            is_active=is_active,
            request_options=request_options,
        )
        return _response.data
