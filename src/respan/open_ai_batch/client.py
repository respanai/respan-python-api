# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.batch_job_list import BatchJobList
from ..types.batch_job_list_status_enum import BatchJobListStatusEnum
from .raw_client import AsyncRawOpenAiBatchClient, RawOpenAiBatchClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class OpenAiBatchClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawOpenAiBatchClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawOpenAiBatchClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawOpenAiBatchClient
        """
        return self._raw_client

    def list_files(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Retrieve file info from the provider.

        GET /api/files/ - List all files
        GET /api/files/{file_id}/ - Get file metadata
        GET /api/files/{file_id}/content/ - Get file content

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.list_files()
        """
        _response = self._raw_client.list_files(request_options=request_options)
        return _response.data

    def upload_file(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Upload a file to the provider.

        Required fields:
        - file: The file to upload (multipart/form-data)
        - purpose: The purpose of the file (e.g., "batch", "assistants")
        - provider_id: The provider to upload to (e.g., "openai", "parasail")

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.upload_file()
        """
        _response = self._raw_client.upload_file(request_options=request_options)
        return _response.data

    def retrieve_file(self, file_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Retrieve file info from the provider.

        GET /api/files/ - List all files
        GET /api/files/{file_id}/ - Get file metadata
        GET /api/files/{file_id}/content/ - Get file content

        Parameters
        ----------
        file_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.retrieve_file(
            file_id="file_id",
        )
        """
        _response = self._raw_client.retrieve_file(file_id, request_options=request_options)
        return _response.data

    def delete_file(self, file_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete a file from the provider.

        Parameters
        ----------
        file_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.delete_file(
            file_id="file_id",
        )
        """
        _response = self._raw_client.delete_file(file_id, request_options=request_options)
        return _response.data

    def retrieve_file_content(self, file_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Retrieve file info from the provider.

        GET /api/files/ - List all files
        GET /api/files/{file_id}/ - Get file metadata
        GET /api/files/{file_id}/content/ - Get file content

        Parameters
        ----------
        file_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.retrieve_file_content(
            file_id="file_id",
        )
        """
        _response = self._raw_client.retrieve_file_content(file_id, request_options=request_options)
        return _response.data

    def create_batch(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Handle POST requests (create batch or cancel batch).

        Thin view: BatchAPIProxy owns the whole pipeline — preprocessing (built-in
        ``_preprocess``), key-usage + billing enforcement, generation, and the
        gateway error policy. raw_payload is captured before the proxy mutates
        ``request.data`` during preprocessing.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.create_batch()
        """
        _response = self._raw_client.create_batch(request_options=request_options)
        return _response.data

    def retrieve_batch(self, batch_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Handle GET requests (retrieve batch or list batches).

        Parameters
        ----------
        batch_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.retrieve_batch(
            batch_id="batch_id",
        )
        """
        _response = self._raw_client.retrieve_batch(batch_id, request_options=request_options)
        return _response.data

    def cancel_batch(self, batch_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Handle POST requests (create batch or cancel batch).

        Thin view: BatchAPIProxy owns the whole pipeline — preprocessing (built-in
        ``_preprocess``), key-usage + billing enforcement, generation, and the
        gateway error policy. raw_payload is captured before the proxy mutates
        ``request.data`` during preprocessing.

        Parameters
        ----------
        batch_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.cancel_batch(
            batch_id="batch_id",
        )
        """
        _response = self._raw_client.cancel_batch(batch_id, request_options=request_options)
        return _response.data

    def filter_batch_jobs(
        self,
        *,
        id: str,
        provider_id: str,
        start_log_unique_id: str,
        status: typing.Optional[BatchJobListStatusEnum] = OMIT,
        request_count: typing.Optional[int] = OMIT,
        completed_count: typing.Optional[int] = OMIT,
        failed_count: typing.Optional[int] = OMIT,
        total_cost: typing.Optional[float] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        last_polled_at: typing.Optional[dt.datetime] = OMIT,
        completion_log_unique_id: typing.Optional[str] = OMIT,
        provider_data: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BatchJobList:
        """
        POST for filtering (not creation) - delegate to GET.

        Parameters
        ----------
        id : str
            Provider's batch ID (e.g., batch_...)

        provider_id : str
            Provider ID matching litellm_provider_id (e.g., 'openai', 'parasail')

        start_log_unique_id : str
            unique_id of the start log (log_type=BATCH)

        status : typing.Optional[BatchJobListStatusEnum]
            Batch job status (OpenAI-compatible)

            * `pending` - Pending
            * `validating` - Validating
            * `in_progress` - In Progress
            * `finalizing` - Finalizing
            * `completed` - Completed
            * `failed` - Failed
            * `cancelling` - Cancelling
            * `cancelled` - Cancelled
            * `expired` - Expired

        request_count : typing.Optional[int]

        completed_count : typing.Optional[int]

        failed_count : typing.Optional[int]

        total_cost : typing.Optional[float]
            Total cost calculated from output (populated on completion)

        completed_at : typing.Optional[dt.datetime]
            When batch reached terminal status

        last_polled_at : typing.Optional[dt.datetime]
            Last time status was polled (for smart backoff)

        completion_log_unique_id : typing.Optional[str]
            unique_id of the completion log (populated when status=completed and logged)

        provider_data : typing.Optional[typing.Any]
            Provider metadata (input_file_id, endpoint, etc.)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BatchJobList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.filter_batch_jobs(
            id="id",
            provider_id="provider_id",
            start_log_unique_id="start_log_unique_id",
        )
        """
        _response = self._raw_client.filter_batch_jobs(
            id=id,
            provider_id=provider_id,
            start_log_unique_id=start_log_unique_id,
            status=status,
            request_count=request_count,
            completed_count=completed_count,
            failed_count=failed_count,
            total_cost=total_cost,
            completed_at=completed_at,
            last_polled_at=last_polled_at,
            completion_log_unique_id=completion_log_unique_id,
            provider_data=provider_data,
            request_options=request_options,
        )
        return _response.data

    def filter_batch_jobs_summary(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        POST for filtering - delegate to GET.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.open_ai_batch.filter_batch_jobs_summary()
        """
        _response = self._raw_client.filter_batch_jobs_summary(request_options=request_options)
        return _response.data


class AsyncOpenAiBatchClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawOpenAiBatchClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawOpenAiBatchClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawOpenAiBatchClient
        """
        return self._raw_client

    async def list_files(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Retrieve file info from the provider.

        GET /api/files/ - List all files
        GET /api/files/{file_id}/ - Get file metadata
        GET /api/files/{file_id}/content/ - Get file content

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.list_files()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_files(request_options=request_options)
        return _response.data

    async def upload_file(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Upload a file to the provider.

        Required fields:
        - file: The file to upload (multipart/form-data)
        - purpose: The purpose of the file (e.g., "batch", "assistants")
        - provider_id: The provider to upload to (e.g., "openai", "parasail")

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.upload_file()


        asyncio.run(main())
        """
        _response = await self._raw_client.upload_file(request_options=request_options)
        return _response.data

    async def retrieve_file(self, file_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Retrieve file info from the provider.

        GET /api/files/ - List all files
        GET /api/files/{file_id}/ - Get file metadata
        GET /api/files/{file_id}/content/ - Get file content

        Parameters
        ----------
        file_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.retrieve_file(
                file_id="file_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve_file(file_id, request_options=request_options)
        return _response.data

    async def delete_file(self, file_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Delete a file from the provider.

        Parameters
        ----------
        file_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.delete_file(
                file_id="file_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_file(file_id, request_options=request_options)
        return _response.data

    async def retrieve_file_content(
        self, file_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Retrieve file info from the provider.

        GET /api/files/ - List all files
        GET /api/files/{file_id}/ - Get file metadata
        GET /api/files/{file_id}/content/ - Get file content

        Parameters
        ----------
        file_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.retrieve_file_content(
                file_id="file_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve_file_content(file_id, request_options=request_options)
        return _response.data

    async def create_batch(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Handle POST requests (create batch or cancel batch).

        Thin view: BatchAPIProxy owns the whole pipeline — preprocessing (built-in
        ``_preprocess``), key-usage + billing enforcement, generation, and the
        gateway error policy. raw_payload is captured before the proxy mutates
        ``request.data`` during preprocessing.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.create_batch()


        asyncio.run(main())
        """
        _response = await self._raw_client.create_batch(request_options=request_options)
        return _response.data

    async def retrieve_batch(self, batch_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Handle GET requests (retrieve batch or list batches).

        Parameters
        ----------
        batch_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.retrieve_batch(
                batch_id="batch_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve_batch(batch_id, request_options=request_options)
        return _response.data

    async def cancel_batch(self, batch_id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Handle POST requests (create batch or cancel batch).

        Thin view: BatchAPIProxy owns the whole pipeline — preprocessing (built-in
        ``_preprocess``), key-usage + billing enforcement, generation, and the
        gateway error policy. raw_payload is captured before the proxy mutates
        ``request.data`` during preprocessing.

        Parameters
        ----------
        batch_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.cancel_batch(
                batch_id="batch_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.cancel_batch(batch_id, request_options=request_options)
        return _response.data

    async def filter_batch_jobs(
        self,
        *,
        id: str,
        provider_id: str,
        start_log_unique_id: str,
        status: typing.Optional[BatchJobListStatusEnum] = OMIT,
        request_count: typing.Optional[int] = OMIT,
        completed_count: typing.Optional[int] = OMIT,
        failed_count: typing.Optional[int] = OMIT,
        total_cost: typing.Optional[float] = OMIT,
        completed_at: typing.Optional[dt.datetime] = OMIT,
        last_polled_at: typing.Optional[dt.datetime] = OMIT,
        completion_log_unique_id: typing.Optional[str] = OMIT,
        provider_data: typing.Optional[typing.Any] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> BatchJobList:
        """
        POST for filtering (not creation) - delegate to GET.

        Parameters
        ----------
        id : str
            Provider's batch ID (e.g., batch_...)

        provider_id : str
            Provider ID matching litellm_provider_id (e.g., 'openai', 'parasail')

        start_log_unique_id : str
            unique_id of the start log (log_type=BATCH)

        status : typing.Optional[BatchJobListStatusEnum]
            Batch job status (OpenAI-compatible)

            * `pending` - Pending
            * `validating` - Validating
            * `in_progress` - In Progress
            * `finalizing` - Finalizing
            * `completed` - Completed
            * `failed` - Failed
            * `cancelling` - Cancelling
            * `cancelled` - Cancelled
            * `expired` - Expired

        request_count : typing.Optional[int]

        completed_count : typing.Optional[int]

        failed_count : typing.Optional[int]

        total_cost : typing.Optional[float]
            Total cost calculated from output (populated on completion)

        completed_at : typing.Optional[dt.datetime]
            When batch reached terminal status

        last_polled_at : typing.Optional[dt.datetime]
            Last time status was polled (for smart backoff)

        completion_log_unique_id : typing.Optional[str]
            unique_id of the completion log (populated when status=completed and logged)

        provider_data : typing.Optional[typing.Any]
            Provider metadata (input_file_id, endpoint, etc.)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BatchJobList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.filter_batch_jobs(
                id="id",
                provider_id="provider_id",
                start_log_unique_id="start_log_unique_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.filter_batch_jobs(
            id=id,
            provider_id=provider_id,
            start_log_unique_id=start_log_unique_id,
            status=status,
            request_count=request_count,
            completed_count=completed_count,
            failed_count=failed_count,
            total_cost=total_cost,
            completed_at=completed_at,
            last_polled_at=last_polled_at,
            completion_log_unique_id=completion_log_unique_id,
            provider_data=provider_data,
            request_options=request_options,
        )
        return _response.data

    async def filter_batch_jobs_summary(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        POST for filtering - delegate to GET.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.open_ai_batch.filter_batch_jobs_summary()


        asyncio.run(main())
        """
        _response = await self._raw_client.filter_batch_jobs_summary(request_options=request_options)
        return _response.data
