# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.column_type_enum import ColumnTypeEnum
from ..types.dataset_logs_bulk_create_response import DatasetLogsBulkCreateResponse
from ..types.generation_method_enum import GenerationMethodEnum
from ..types.paginated_playground_list_list import PaginatedPlaygroundListList
from ..types.playground_column_create import PlaygroundColumnCreate
from ..types.playground_column_detail import PlaygroundColumnDetail
from ..types.playground_column_list import PlaygroundColumnList
from ..types.playground_create import PlaygroundCreate
from ..types.playground_detail import PlaygroundDetail
from ..types.playground_rows_list_response import PlaygroundRowsListResponse
from ..types.playground_rows_summary import PlaygroundRowsSummary
from ..types.playground_run import PlaygroundRun
from ..types.source_type775enum import SourceType775Enum
from .raw_client import AsyncRawPlaygroundsClient, RawPlaygroundsClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class PlaygroundsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawPlaygroundsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawPlaygroundsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawPlaygroundsClient
        """
        return self._raw_client

    def api_playgrounds_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPlaygroundListList:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPlaygroundListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_list()
        """
        _response = self._raw_client.api_playgrounds_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def api_playgrounds_create(
        self,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundCreate:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        description : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_create(
            name="name",
        )
        """
        _response = self._raw_client.api_playgrounds_create(
            name=name, description=description, request_options=request_options
        )
        return _response.data

    def api_playgrounds_retrieve(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PlaygroundDetail:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_retrieve(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_retrieve(playground_id, request_options=request_options)
        return _response.data

    def api_playgrounds_update(
        self,
        playground_id: str,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        playground_id : str

        name : str

        description : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_update(
            playground_id="playground_id",
            name="name",
        )
        """
        _response = self._raw_client.api_playgrounds_update(
            playground_id, name=name, description=description, request_options=request_options
        )
        return _response.data

    def api_playgrounds_destroy(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_destroy(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_destroy(playground_id, request_options=request_options)
        return _response.data

    def api_playgrounds_partial_update(
        self,
        playground_id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundDetail:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        playground_id : str

        name : typing.Optional[str]

        description : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_partial_update(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_partial_update(
            playground_id, name=name, description=description, request_options=request_options
        )
        return _response.data

    def api_playgrounds_columns_list(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[PlaygroundColumnList]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[PlaygroundColumnList]


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_columns_list(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_columns_list(playground_id, request_options=request_options)
        return _response.data

    def api_playgrounds_columns_create(
        self,
        playground_id: str,
        *,
        playground: str,
        name: str,
        order: typing.Optional[int] = OMIT,
        column_type: typing.Optional[ColumnTypeEnum] = OMIT,
        source_type: typing.Optional[SourceType775Enum] = OMIT,
        linked_prompt_id: typing.Optional[str] = OMIT,
        linked_prompt_version: typing.Optional[int] = OMIT,
        prompt: typing.Optional[typing.Any] = OMIT,
        is_archived: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundColumnCreate:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        playground : str

        name : str

        order : typing.Optional[int]

        column_type : typing.Optional[ColumnTypeEnum]

        source_type : typing.Optional[SourceType775Enum]

        linked_prompt_id : typing.Optional[str]

        linked_prompt_version : typing.Optional[int]

        prompt : typing.Optional[typing.Any]

        is_archived : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundColumnCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_columns_create(
            playground_id="playground_id",
            playground="playground",
            name="name",
        )
        """
        _response = self._raw_client.api_playgrounds_columns_create(
            playground_id,
            playground=playground,
            name=name,
            order=order,
            column_type=column_type,
            source_type=source_type,
            linked_prompt_id=linked_prompt_id,
            linked_prompt_version=linked_prompt_version,
            prompt=prompt,
            is_archived=is_archived,
            request_options=request_options,
        )
        return _response.data

    def api_playgrounds_columns_retrieve(
        self, playground_id: str, column_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PlaygroundColumnDetail:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        column_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundColumnDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_columns_retrieve(
            playground_id="playground_id",
            column_id="column_id",
        )
        """
        _response = self._raw_client.api_playgrounds_columns_retrieve(
            playground_id, column_id, request_options=request_options
        )
        return _response.data

    def api_playgrounds_columns_update(
        self,
        playground_id: str,
        column_id: str,
        *,
        name: str,
        order: typing.Optional[int] = OMIT,
        column_type: typing.Optional[ColumnTypeEnum] = OMIT,
        source_type: typing.Optional[SourceType775Enum] = OMIT,
        linked_prompt_id: typing.Optional[str] = OMIT,
        linked_prompt_version: typing.Optional[int] = OMIT,
        prompt: typing.Optional[typing.Any] = OMIT,
        is_archived: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundColumnDetail:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        playground_id : str

        column_id : str

        name : str

        order : typing.Optional[int]

        column_type : typing.Optional[ColumnTypeEnum]

        source_type : typing.Optional[SourceType775Enum]

        linked_prompt_id : typing.Optional[str]

        linked_prompt_version : typing.Optional[int]

        prompt : typing.Optional[typing.Any]

        is_archived : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundColumnDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_columns_update(
            playground_id="playground_id",
            column_id="column_id",
            name="name",
        )
        """
        _response = self._raw_client.api_playgrounds_columns_update(
            playground_id,
            column_id,
            name=name,
            order=order,
            column_type=column_type,
            source_type=source_type,
            linked_prompt_id=linked_prompt_id,
            linked_prompt_version=linked_prompt_version,
            prompt=prompt,
            is_archived=is_archived,
            request_options=request_options,
        )
        return _response.data

    def api_playgrounds_columns_destroy(
        self, playground_id: str, column_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        column_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_columns_destroy(
            playground_id="playground_id",
            column_id="column_id",
        )
        """
        _response = self._raw_client.api_playgrounds_columns_destroy(
            playground_id, column_id, request_options=request_options
        )
        return _response.data

    def api_playgrounds_columns_partial_update(
        self,
        playground_id: str,
        column_id: str,
        *,
        name: typing.Optional[str] = OMIT,
        order: typing.Optional[int] = OMIT,
        column_type: typing.Optional[ColumnTypeEnum] = OMIT,
        source_type: typing.Optional[SourceType775Enum] = OMIT,
        linked_prompt_id: typing.Optional[str] = OMIT,
        linked_prompt_version: typing.Optional[int] = OMIT,
        prompt: typing.Optional[typing.Any] = OMIT,
        is_archived: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundColumnDetail:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        playground_id : str

        column_id : str

        name : typing.Optional[str]

        order : typing.Optional[int]

        column_type : typing.Optional[ColumnTypeEnum]

        source_type : typing.Optional[SourceType775Enum]

        linked_prompt_id : typing.Optional[str]

        linked_prompt_version : typing.Optional[int]

        prompt : typing.Optional[typing.Any]

        is_archived : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundColumnDetail


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_columns_partial_update(
            playground_id="playground_id",
            column_id="column_id",
        )
        """
        _response = self._raw_client.api_playgrounds_columns_partial_update(
            playground_id,
            column_id,
            name=name,
            order=order,
            column_type=column_type,
            source_type=source_type,
            linked_prompt_id=linked_prompt_id,
            linked_prompt_version=linked_prompt_version,
            prompt=prompt,
            is_archived=is_archived,
            request_options=request_options,
        )
        return _response.data

    def api_playgrounds_rows_bulk_create(
        self,
        playground_id: str,
        *,
        logs: typing.Sequence[typing.Any],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetLogsBulkCreateResponse:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        logs : typing.Sequence[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogsBulkCreateResponse


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_rows_bulk_create(
            playground_id="playground_id",
            logs=[],
        )
        """
        _response = self._raw_client.api_playgrounds_rows_bulk_create(
            playground_id, logs=logs, request_options=request_options
        )
        return _response.data

    def api_playgrounds_rows_list_list(
        self,
        playground_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        sort_by: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundRowsListResponse:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        page : typing.Optional[int]
            Page number for grouped Playground rows.

        page_size : typing.Optional[int]
            Page size for grouped Playground rows.

        sort_by : typing.Optional[str]
            Sort grouped rows. Defaults to comparison_key. Supports row fields and cell sorting via cells__<experiment_id>.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRowsListResponse


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_rows_list_list(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_rows_list_list(
            playground_id, page=page, page_size=page_size, sort_by=sort_by, request_options=request_options
        )
        return _response.data

    def api_playgrounds_rows_list_filtered(
        self,
        playground_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        sort_by: typing.Optional[str] = None,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundRowsListResponse:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        page : typing.Optional[int]
            Page number for grouped Playground rows.

        page_size : typing.Optional[int]
            Page size for grouped Playground rows.

        sort_by : typing.Optional[str]
            Sort grouped rows. Defaults to comparison_key. Supports row fields and cell sorting via cells__<experiment_id>.

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter parameters keyed by metric name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRowsListResponse


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_rows_list_filtered(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_rows_list_filtered(
            playground_id,
            page=page,
            page_size=page_size,
            sort_by=sort_by,
            filters=filters,
            request_options=request_options,
        )
        return _response.data

    def api_playgrounds_rows_summary_retrieve(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PlaygroundRowsSummary:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRowsSummary


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_rows_summary_retrieve(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_rows_summary_retrieve(
            playground_id, request_options=request_options
        )
        return _response.data

    def api_playgrounds_rows_summary_filtered(
        self,
        playground_id: str,
        *,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundRowsSummary:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter parameters keyed by metric name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRowsSummary


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_rows_summary_filtered(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_rows_summary_filtered(
            playground_id, filters=filters, request_options=request_options
        )
        return _response.data

    def api_playgrounds_runs_retrieve(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PlaygroundRun:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRun


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_runs_retrieve(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_runs_retrieve(playground_id, request_options=request_options)
        return _response.data

    def api_playgrounds_runs_create(
        self,
        playground_id: str,
        *,
        column_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        row_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        batch_size: typing.Optional[int] = OMIT,
        concurrency: typing.Optional[int] = OMIT,
        is_tracing_enabled: typing.Optional[bool] = OMIT,
        generation_method: typing.Optional[GenerationMethodEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundRun:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        column_ids : typing.Optional[typing.Sequence[str]]

        row_ids : typing.Optional[typing.Sequence[str]]

        batch_size : typing.Optional[int]

        concurrency : typing.Optional[int]

        is_tracing_enabled : typing.Optional[bool]

        generation_method : typing.Optional[GenerationMethodEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRun


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.playgrounds.api_playgrounds_runs_create(
            playground_id="playground_id",
        )
        """
        _response = self._raw_client.api_playgrounds_runs_create(
            playground_id,
            column_ids=column_ids,
            row_ids=row_ids,
            batch_size=batch_size,
            concurrency=concurrency,
            is_tracing_enabled=is_tracing_enabled,
            generation_method=generation_method,
            request_options=request_options,
        )
        return _response.data


class AsyncPlaygroundsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawPlaygroundsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawPlaygroundsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawPlaygroundsClient
        """
        return self._raw_client

    async def api_playgrounds_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedPlaygroundListList:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedPlaygroundListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_create(
        self,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundCreate:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        description : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_create(
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_create(
            name=name, description=description, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_retrieve(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PlaygroundDetail:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_retrieve(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_retrieve(playground_id, request_options=request_options)
        return _response.data

    async def api_playgrounds_update(
        self,
        playground_id: str,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundDetail:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        playground_id : str

        name : str

        description : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_update(
                playground_id="playground_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_update(
            playground_id, name=name, description=description, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_destroy(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_destroy(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_destroy(playground_id, request_options=request_options)
        return _response.data

    async def api_playgrounds_partial_update(
        self,
        playground_id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundDetail:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        playground_id : str

        name : typing.Optional[str]

        description : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_partial_update(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_partial_update(
            playground_id, name=name, description=description, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_columns_list(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> typing.List[PlaygroundColumnList]:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[PlaygroundColumnList]


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_columns_list(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_columns_list(playground_id, request_options=request_options)
        return _response.data

    async def api_playgrounds_columns_create(
        self,
        playground_id: str,
        *,
        playground: str,
        name: str,
        order: typing.Optional[int] = OMIT,
        column_type: typing.Optional[ColumnTypeEnum] = OMIT,
        source_type: typing.Optional[SourceType775Enum] = OMIT,
        linked_prompt_id: typing.Optional[str] = OMIT,
        linked_prompt_version: typing.Optional[int] = OMIT,
        prompt: typing.Optional[typing.Any] = OMIT,
        is_archived: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundColumnCreate:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        playground : str

        name : str

        order : typing.Optional[int]

        column_type : typing.Optional[ColumnTypeEnum]

        source_type : typing.Optional[SourceType775Enum]

        linked_prompt_id : typing.Optional[str]

        linked_prompt_version : typing.Optional[int]

        prompt : typing.Optional[typing.Any]

        is_archived : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundColumnCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_columns_create(
                playground_id="playground_id",
                playground="playground",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_columns_create(
            playground_id,
            playground=playground,
            name=name,
            order=order,
            column_type=column_type,
            source_type=source_type,
            linked_prompt_id=linked_prompt_id,
            linked_prompt_version=linked_prompt_version,
            prompt=prompt,
            is_archived=is_archived,
            request_options=request_options,
        )
        return _response.data

    async def api_playgrounds_columns_retrieve(
        self, playground_id: str, column_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PlaygroundColumnDetail:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        column_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundColumnDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_columns_retrieve(
                playground_id="playground_id",
                column_id="column_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_columns_retrieve(
            playground_id, column_id, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_columns_update(
        self,
        playground_id: str,
        column_id: str,
        *,
        name: str,
        order: typing.Optional[int] = OMIT,
        column_type: typing.Optional[ColumnTypeEnum] = OMIT,
        source_type: typing.Optional[SourceType775Enum] = OMIT,
        linked_prompt_id: typing.Optional[str] = OMIT,
        linked_prompt_version: typing.Optional[int] = OMIT,
        prompt: typing.Optional[typing.Any] = OMIT,
        is_archived: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundColumnDetail:
        """
        Default PUT handler with automatic organization injection.

        Same behavior as patch() - preserves ownership for superadmins,
        forces user's org for regular users.

        Parameters
        ----------
        playground_id : str

        column_id : str

        name : str

        order : typing.Optional[int]

        column_type : typing.Optional[ColumnTypeEnum]

        source_type : typing.Optional[SourceType775Enum]

        linked_prompt_id : typing.Optional[str]

        linked_prompt_version : typing.Optional[int]

        prompt : typing.Optional[typing.Any]

        is_archived : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundColumnDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_columns_update(
                playground_id="playground_id",
                column_id="column_id",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_columns_update(
            playground_id,
            column_id,
            name=name,
            order=order,
            column_type=column_type,
            source_type=source_type,
            linked_prompt_id=linked_prompt_id,
            linked_prompt_version=linked_prompt_version,
            prompt=prompt,
            is_archived=is_archived,
            request_options=request_options,
        )
        return _response.data

    async def api_playgrounds_columns_destroy(
        self, playground_id: str, column_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        column_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_columns_destroy(
                playground_id="playground_id",
                column_id="column_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_columns_destroy(
            playground_id, column_id, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_columns_partial_update(
        self,
        playground_id: str,
        column_id: str,
        *,
        name: typing.Optional[str] = OMIT,
        order: typing.Optional[int] = OMIT,
        column_type: typing.Optional[ColumnTypeEnum] = OMIT,
        source_type: typing.Optional[SourceType775Enum] = OMIT,
        linked_prompt_id: typing.Optional[str] = OMIT,
        linked_prompt_version: typing.Optional[int] = OMIT,
        prompt: typing.Optional[typing.Any] = OMIT,
        is_archived: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundColumnDetail:
        """
        Default PATCH handler with automatic organization injection.

        For UPDATE operations:
        - Superadmins preserve original ownership (org fields removed from request)
        - Regular users are forced to their own organization

        Override this method only for custom pre-update logic.

        Parameters
        ----------
        playground_id : str

        column_id : str

        name : typing.Optional[str]

        order : typing.Optional[int]

        column_type : typing.Optional[ColumnTypeEnum]

        source_type : typing.Optional[SourceType775Enum]

        linked_prompt_id : typing.Optional[str]

        linked_prompt_version : typing.Optional[int]

        prompt : typing.Optional[typing.Any]

        is_archived : typing.Optional[bool]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundColumnDetail


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_columns_partial_update(
                playground_id="playground_id",
                column_id="column_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_columns_partial_update(
            playground_id,
            column_id,
            name=name,
            order=order,
            column_type=column_type,
            source_type=source_type,
            linked_prompt_id=linked_prompt_id,
            linked_prompt_version=linked_prompt_version,
            prompt=prompt,
            is_archived=is_archived,
            request_options=request_options,
        )
        return _response.data

    async def api_playgrounds_rows_bulk_create(
        self,
        playground_id: str,
        *,
        logs: typing.Sequence[typing.Any],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetLogsBulkCreateResponse:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        logs : typing.Sequence[typing.Any]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetLogsBulkCreateResponse


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_rows_bulk_create(
                playground_id="playground_id",
                logs=[],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_rows_bulk_create(
            playground_id, logs=logs, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_rows_list_list(
        self,
        playground_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        sort_by: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundRowsListResponse:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        page : typing.Optional[int]
            Page number for grouped Playground rows.

        page_size : typing.Optional[int]
            Page size for grouped Playground rows.

        sort_by : typing.Optional[str]
            Sort grouped rows. Defaults to comparison_key. Supports row fields and cell sorting via cells__<experiment_id>.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRowsListResponse


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_rows_list_list(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_rows_list_list(
            playground_id, page=page, page_size=page_size, sort_by=sort_by, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_rows_list_filtered(
        self,
        playground_id: str,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        sort_by: typing.Optional[str] = None,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundRowsListResponse:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        page : typing.Optional[int]
            Page number for grouped Playground rows.

        page_size : typing.Optional[int]
            Page size for grouped Playground rows.

        sort_by : typing.Optional[str]
            Sort grouped rows. Defaults to comparison_key. Supports row fields and cell sorting via cells__<experiment_id>.

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter parameters keyed by metric name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRowsListResponse


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_rows_list_filtered(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_rows_list_filtered(
            playground_id,
            page=page,
            page_size=page_size,
            sort_by=sort_by,
            filters=filters,
            request_options=request_options,
        )
        return _response.data

    async def api_playgrounds_rows_summary_retrieve(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PlaygroundRowsSummary:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRowsSummary


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_rows_summary_retrieve(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_rows_summary_retrieve(
            playground_id, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_rows_summary_filtered(
        self,
        playground_id: str,
        *,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundRowsSummary:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Filter parameters keyed by metric name.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRowsSummary


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_rows_summary_filtered(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_rows_summary_filtered(
            playground_id, filters=filters, request_options=request_options
        )
        return _response.data

    async def api_playgrounds_runs_retrieve(
        self, playground_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PlaygroundRun:
        """
        Declare per-endpoint throttling configuration attributes for views.

        Endpoints can inherit this mixin and set:
        - endpoint_default_rate_limit_per_user_per_min: float | None

        When set (not None), throttling will use ONLY this default RPM for the endpoint
        without requiring API key / org attributes on the request. When unset, the
        standard API key / organization / subscription limits apply.

        Parameters
        ----------
        playground_id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRun


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_runs_retrieve(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_runs_retrieve(playground_id, request_options=request_options)
        return _response.data

    async def api_playgrounds_runs_create(
        self,
        playground_id: str,
        *,
        column_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        row_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        batch_size: typing.Optional[int] = OMIT,
        concurrency: typing.Optional[int] = OMIT,
        is_tracing_enabled: typing.Optional[bool] = OMIT,
        generation_method: typing.Optional[GenerationMethodEnum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PlaygroundRun:
        """
        Default POST handler with automatic organization injection.

        For CREATE operations:
        - Superadmins can specify organization_id in request body (API key only)
        - Regular users always use their own organization

        Override this method for:
        - POST-for-filtering pattern (delegate to self.get())
        - Custom pre-create validation

        Note: ``inject_target_organization`` is a DEPRECATED ``request.data``-mutating
        shim kept for backward-compat during the DEV-9410 migration. The blessed
        path stamps org via ``get_create_save_kwargs`` → ``perform_create`` → a
        ``read_only`` serializer field. The shim (and these overrides' reliance on
        it) is removed in C18 (DEV-9430) once every view's org field is read_only.

        Parameters
        ----------
        playground_id : str

        column_ids : typing.Optional[typing.Sequence[str]]

        row_ids : typing.Optional[typing.Sequence[str]]

        batch_size : typing.Optional[int]

        concurrency : typing.Optional[int]

        is_tracing_enabled : typing.Optional[bool]

        generation_method : typing.Optional[GenerationMethodEnum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PlaygroundRun


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.playgrounds.api_playgrounds_runs_create(
                playground_id="playground_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.api_playgrounds_runs_create(
            playground_id,
            column_ids=column_ids,
            row_ids=row_ids,
            batch_size=batch_size,
            concurrency=concurrency,
            is_tracing_enabled=is_tracing_enabled,
            generation_method=generation_method,
            request_options=request_options,
        )
        return _response.data
