# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.ch_eval_pipeline_scores import ChEvalPipelineScores
from ..types.ch_eval_pipeline_scores_time_series import ChEvalPipelineScoresTimeSeries
from ..types.ch_workflow_metrics_time_series import ChWorkflowMetricsTimeSeries
from ..types.custom_identifier_list import CustomIdentifierList
from ..types.dashboard_filtering_request import DashboardFilteringRequest
from ..types.paginated_ch_eval_pipeline_run_list import PaginatedChEvalPipelineRunList
from ..types.paginated_ch_workflow_run_list import PaginatedChWorkflowRunList
from ..types.paginated_custom_identifier_list_list import PaginatedCustomIdentifierListList
from ..types.paginated_saved_sql_query_list_list import PaginatedSavedSqlQueryListList
from ..types.saved_sql_query_create import SavedSqlQueryCreate
from ..types.saved_sql_query_create_request_plotting_config import SavedSqlQueryCreateRequestPlottingConfig
from ..types.saved_sql_query_list import SavedSqlQueryList
from ..types.saved_sql_query_list_request_plotting_config import SavedSqlQueryListRequestPlottingConfig
from ..types.saved_sql_query_summary import SavedSqlQuerySummary
from ..types.saved_sql_query_update import SavedSqlQueryUpdate
from ..types.source141enum import Source141Enum
from ..types.sql_query_request_environment_enum import SqlQueryRequestEnvironmentEnum
from ..types.sql_query_response import SqlQueryResponse
from .raw_client import AsyncRawClickhouseClient, RawClickhouseClient
from .types.clickhouse_workflows_eval_runs_list_request_scope import ClickhouseWorkflowsEvalRunsListRequestScope
from .types.clickhouse_workflows_eval_scores_list_request_scope import ClickhouseWorkflowsEvalScoresListRequestScope
from .types.clickhouse_workflows_eval_scores_time_series_list_request_scope import (
    ClickhouseWorkflowsEvalScoresTimeSeriesListRequestScope,
)
from .types.clickhouse_workflows_eval_scores_time_series_list_request_time_tick import (
    ClickhouseWorkflowsEvalScoresTimeSeriesListRequestTimeTick,
)
from .types.clickhouse_workflows_runs_summary_request_time_tick import ClickhouseWorkflowsRunsSummaryRequestTimeTick
from .types.patched_saved_sql_query_create_request_plotting_config import (
    PatchedSavedSqlQueryCreateRequestPlottingConfig,
)
from .types.patched_saved_sql_query_list_request_plotting_config import PatchedSavedSqlQueryListRequestPlottingConfig
from .types.patched_saved_sql_query_update_request_plotting_config import (
    PatchedSavedSqlQueryUpdateRequestPlottingConfig,
)
from .types.saved_sql_query_update_request_plotting_config import SavedSqlQueryUpdateRequestPlottingConfig

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ClickhouseClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawClickhouseClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawClickhouseClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawClickhouseClient
        """
        return self._raw_client

    def custom_identifiers_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedCustomIdentifierListList:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedCustomIdentifierListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_list()
        """
        _response = self._raw_client.custom_identifiers_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def custom_identifiers_create(
        self,
        *,
        id: str,
        custom_identifier: str,
        environment: str,
        number_of_requests: int,
        total_cost: float,
        total_tokens: int,
        total_prompt_tokens: int,
        total_completion_tokens: int,
        error_count: int,
        average_latency: float,
        average_ttft: float,
        average_tps: float,
        first_seen: dt.datetime,
        last_active: dt.datetime,
        unique_organization_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        custom_identifier : str

        environment : str

        number_of_requests : int

        total_cost : float

        total_tokens : int

        total_prompt_tokens : int

        total_completion_tokens : int

        error_count : int

        average_latency : float

        average_ttft : float

        average_tps : float

        first_seen : dt.datetime

        last_active : dt.datetime

        unique_organization_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import datetime

        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_create(
            id="id",
            custom_identifier="custom_identifier",
            environment="environment",
            number_of_requests=1,
            total_cost=1.1,
            total_tokens=1,
            total_prompt_tokens=1,
            total_completion_tokens=1,
            error_count=1,
            average_latency=1.1,
            average_ttft=1.1,
            average_tps=1.1,
            first_seen=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            last_active=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.custom_identifiers_create(
            id=id,
            custom_identifier=custom_identifier,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            unique_organization_id=unique_organization_id,
            request_options=request_options,
        )
        return _response.data

    def custom_identifiers_update(
        self,
        *,
        id: str,
        custom_identifier: str,
        environment: str,
        number_of_requests: int,
        total_cost: float,
        total_tokens: int,
        total_prompt_tokens: int,
        total_completion_tokens: int,
        error_count: int,
        average_latency: float,
        average_ttft: float,
        average_tps: float,
        first_seen: dt.datetime,
        last_active: dt.datetime,
        unique_organization_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        custom_identifier : str

        environment : str

        number_of_requests : int

        total_cost : float

        total_tokens : int

        total_prompt_tokens : int

        total_completion_tokens : int

        error_count : int

        average_latency : float

        average_ttft : float

        average_tps : float

        first_seen : dt.datetime

        last_active : dt.datetime

        unique_organization_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import datetime

        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_update(
            id="id",
            custom_identifier="custom_identifier",
            environment="environment",
            number_of_requests=1,
            total_cost=1.1,
            total_tokens=1,
            total_prompt_tokens=1,
            total_completion_tokens=1,
            error_count=1,
            average_latency=1.1,
            average_ttft=1.1,
            average_tps=1.1,
            first_seen=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            last_active=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.custom_identifiers_update(
            id=id,
            custom_identifier=custom_identifier,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            unique_organization_id=unique_organization_id,
            request_options=request_options,
        )
        return _response.data

    def custom_identifiers_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        custom_identifier: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        number_of_requests: typing.Optional[int] = OMIT,
        total_cost: typing.Optional[float] = OMIT,
        total_tokens: typing.Optional[int] = OMIT,
        total_prompt_tokens: typing.Optional[int] = OMIT,
        total_completion_tokens: typing.Optional[int] = OMIT,
        error_count: typing.Optional[int] = OMIT,
        average_latency: typing.Optional[float] = OMIT,
        average_ttft: typing.Optional[float] = OMIT,
        average_tps: typing.Optional[float] = OMIT,
        first_seen: typing.Optional[dt.datetime] = OMIT,
        last_active: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        custom_identifier : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        number_of_requests : typing.Optional[int]

        total_cost : typing.Optional[float]

        total_tokens : typing.Optional[int]

        total_prompt_tokens : typing.Optional[int]

        total_completion_tokens : typing.Optional[int]

        error_count : typing.Optional[int]

        average_latency : typing.Optional[float]

        average_ttft : typing.Optional[float]

        average_tps : typing.Optional[float]

        first_seen : typing.Optional[dt.datetime]

        last_active : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_partial_update()
        """
        _response = self._raw_client.custom_identifiers_partial_update(
            id=id,
            custom_identifier=custom_identifier,
            unique_organization_id=unique_organization_id,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            request_options=request_options,
        )
        return _response.data

    def custom_identifiers_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedCustomIdentifierListList:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedCustomIdentifierListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_list_list()
        """
        _response = self._raw_client.custom_identifiers_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def custom_identifiers_list_create(
        self,
        *,
        id: str,
        custom_identifier: str,
        environment: str,
        number_of_requests: int,
        total_cost: float,
        total_tokens: int,
        total_prompt_tokens: int,
        total_completion_tokens: int,
        error_count: int,
        average_latency: float,
        average_ttft: float,
        average_tps: float,
        first_seen: dt.datetime,
        last_active: dt.datetime,
        unique_organization_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        custom_identifier : str

        environment : str

        number_of_requests : int

        total_cost : float

        total_tokens : int

        total_prompt_tokens : int

        total_completion_tokens : int

        error_count : int

        average_latency : float

        average_ttft : float

        average_tps : float

        first_seen : dt.datetime

        last_active : dt.datetime

        unique_organization_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import datetime

        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_list_create(
            id="id",
            custom_identifier="custom_identifier",
            environment="environment",
            number_of_requests=1,
            total_cost=1.1,
            total_tokens=1,
            total_prompt_tokens=1,
            total_completion_tokens=1,
            error_count=1,
            average_latency=1.1,
            average_ttft=1.1,
            average_tps=1.1,
            first_seen=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            last_active=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.custom_identifiers_list_create(
            id=id,
            custom_identifier=custom_identifier,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            unique_organization_id=unique_organization_id,
            request_options=request_options,
        )
        return _response.data

    def custom_identifiers_list_update(
        self,
        *,
        id: str,
        custom_identifier: str,
        environment: str,
        number_of_requests: int,
        total_cost: float,
        total_tokens: int,
        total_prompt_tokens: int,
        total_completion_tokens: int,
        error_count: int,
        average_latency: float,
        average_ttft: float,
        average_tps: float,
        first_seen: dt.datetime,
        last_active: dt.datetime,
        unique_organization_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        custom_identifier : str

        environment : str

        number_of_requests : int

        total_cost : float

        total_tokens : int

        total_prompt_tokens : int

        total_completion_tokens : int

        error_count : int

        average_latency : float

        average_ttft : float

        average_tps : float

        first_seen : dt.datetime

        last_active : dt.datetime

        unique_organization_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import datetime

        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_list_update(
            id="id",
            custom_identifier="custom_identifier",
            environment="environment",
            number_of_requests=1,
            total_cost=1.1,
            total_tokens=1,
            total_prompt_tokens=1,
            total_completion_tokens=1,
            error_count=1,
            average_latency=1.1,
            average_ttft=1.1,
            average_tps=1.1,
            first_seen=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            last_active=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.custom_identifiers_list_update(
            id=id,
            custom_identifier=custom_identifier,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            unique_organization_id=unique_organization_id,
            request_options=request_options,
        )
        return _response.data

    def custom_identifiers_list_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        custom_identifier: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        number_of_requests: typing.Optional[int] = OMIT,
        total_cost: typing.Optional[float] = OMIT,
        total_tokens: typing.Optional[int] = OMIT,
        total_prompt_tokens: typing.Optional[int] = OMIT,
        total_completion_tokens: typing.Optional[int] = OMIT,
        error_count: typing.Optional[int] = OMIT,
        average_latency: typing.Optional[float] = OMIT,
        average_ttft: typing.Optional[float] = OMIT,
        average_tps: typing.Optional[float] = OMIT,
        first_seen: typing.Optional[dt.datetime] = OMIT,
        last_active: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        custom_identifier : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        number_of_requests : typing.Optional[int]

        total_cost : typing.Optional[float]

        total_tokens : typing.Optional[int]

        total_prompt_tokens : typing.Optional[int]

        total_completion_tokens : typing.Optional[int]

        error_count : typing.Optional[int]

        average_latency : typing.Optional[float]

        average_ttft : typing.Optional[float]

        average_tps : typing.Optional[float]

        first_seen : typing.Optional[dt.datetime]

        last_active : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_list_partial_update()
        """
        _response = self._raw_client.custom_identifiers_list_partial_update(
            id=id,
            custom_identifier=custom_identifier,
            unique_organization_id=unique_organization_id,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            request_options=request_options,
        )
        return _response.data

    def custom_identifiers_summary_retrieve(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_summary_retrieve()
        """
        _response = self._raw_client.custom_identifiers_summary_retrieve(request_options=request_options)
        return _response.data

    def custom_identifiers_summary_create(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_summary_create()
        """
        _response = self._raw_client.custom_identifiers_summary_create(request_options=request_options)
        return _response.data

    def custom_identifiers_summary_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_summary_update()
        """
        _response = self._raw_client.custom_identifiers_summary_update(request_options=request_options)
        return _response.data

    def custom_identifiers_summary_partial_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.custom_identifiers_summary_partial_update()
        """
        _response = self._raw_client.custom_identifiers_summary_partial_update(request_options=request_options)
        return _response.data

    def saved_sql_queries_create(
        self,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryCreateRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryCreate:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryCreateRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_create(
            name="name",
            query="query",
        )
        """
        _response = self._raw_client.saved_sql_queries_create(
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_update(
        self,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryCreateRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryCreate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryCreateRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_update(
            name="name",
            query="query",
        )
        """
        _response = self._raw_client.saved_sql_queries_update(
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_partial_update(
        self,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        query: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[PatchedSavedSqlQueryCreateRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryCreate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        name : typing.Optional[str]

        description : typing.Optional[str]

        query : typing.Optional[str]

        plotting_config : typing.Optional[PatchedSavedSqlQueryCreateRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryCreate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_partial_update()
        """
        _response = self._raw_client.saved_sql_queries_partial_update(
            name=name,
            description=description,
            query=query,
            plotting_config=plotting_config,
            source=source,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedSqlQueryList:
        """
        Retrieve, update, or delete a single saved SQL query.

        GET    /clickhouse/saved-sql-queries/<id>/
        PATCH  /clickhouse/saved-sql-queries/<id>/ — Partial update (name, description, query).
        DELETE /clickhouse/saved-sql-queries/<id>/

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_retrieve(
            id="id",
        )
        """
        _response = self._raw_client.saved_sql_queries_retrieve(id, request_options=request_options)
        return _response.data

    def saved_sql_queries_create2(
        self,
        id: str,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryListRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryListRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_create2(
            id="id",
            name="name",
            query="query",
        )
        """
        _response = self._raw_client.saved_sql_queries_create2(
            id,
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_update2(
        self,
        id: str,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryUpdateRequestPlottingConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryUpdate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryUpdateRequestPlottingConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryUpdate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_update2(
            id="id",
            name="name",
            query="query",
        )
        """
        _response = self._raw_client.saved_sql_queries_update2(
            id,
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_destroy(self, id: str, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        Retrieve, update, or delete a single saved SQL query.

        GET    /clickhouse/saved-sql-queries/<id>/
        PATCH  /clickhouse/saved-sql-queries/<id>/ — Partial update (name, description, query).
        DELETE /clickhouse/saved-sql-queries/<id>/

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_destroy(
            id="id",
        )
        """
        _response = self._raw_client.saved_sql_queries_destroy(id, request_options=request_options)
        return _response.data

    def saved_sql_queries_partial_update2(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        query: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[PatchedSavedSqlQueryUpdateRequestPlottingConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryUpdate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : str

        name : typing.Optional[str]

        description : typing.Optional[str]

        query : typing.Optional[str]

        plotting_config : typing.Optional[PatchedSavedSqlQueryUpdateRequestPlottingConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryUpdate


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_partial_update2(
            id="id",
        )
        """
        _response = self._raw_client.saved_sql_queries_partial_update2(
            id,
            name=name,
            description=description,
            query=query,
            plotting_config=plotting_config,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedSavedSqlQueryListList:
        """
        List saved SQL queries with pagination and filtering.

        GET  /clickhouse/saved-sql-queries/list/
        POST /clickhouse/saved-sql-queries/list/ — POST-for-filtering pattern.

        Supports: pagination (page, page_size), sorting (sort_by), filtering via POST body.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSavedSqlQueryListList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_list_list()
        """
        _response = self._raw_client.saved_sql_queries_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    def saved_sql_queries_list_create(
        self,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryListRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryListRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_list_create(
            name="name",
            query="query",
        )
        """
        _response = self._raw_client.saved_sql_queries_list_create(
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_list_update(
        self,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryListRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryListRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_list_update(
            name="name",
            query="query",
        )
        """
        _response = self._raw_client.saved_sql_queries_list_update(
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_list_partial_update(
        self,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        query: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[PatchedSavedSqlQueryListRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        name : typing.Optional[str]

        description : typing.Optional[str]

        query : typing.Optional[str]

        plotting_config : typing.Optional[PatchedSavedSqlQueryListRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_list_partial_update()
        """
        _response = self._raw_client.saved_sql_queries_list_partial_update(
            name=name,
            description=description,
            query=query,
            plotting_config=plotting_config,
            source=source,
            request_options=request_options,
        )
        return _response.data

    def saved_sql_queries_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedSqlQuerySummary:
        """
        Summary statistics for saved SQL queries.

        GET  /clickhouse/saved-sql-queries/summary/
        POST /clickhouse/saved-sql-queries/summary/ — POST-for-filtering pattern.

        Returns: { total_count }

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQuerySummary


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_summary_retrieve()
        """
        _response = self._raw_client.saved_sql_queries_summary_retrieve(request_options=request_options)
        return _response.data

    def saved_sql_queries_summary_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedSqlQuerySummary:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQuerySummary


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_summary_create()
        """
        _response = self._raw_client.saved_sql_queries_summary_create(request_options=request_options)
        return _response.data

    def saved_sql_queries_summary_update(self, *, request_options: typing.Optional[RequestOptions] = None) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_summary_update()
        """
        _response = self._raw_client.saved_sql_queries_summary_update(request_options=request_options)
        return _response.data

    def saved_sql_queries_summary_partial_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.saved_sql_queries_summary_partial_update()
        """
        _response = self._raw_client.saved_sql_queries_summary_partial_update(request_options=request_options)
        return _response.data

    def sql_queries_create(
        self,
        *,
        query: str,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        environment: typing.Optional[SqlQueryRequestEnvironmentEnum] = OMIT,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        dashboard_filtering: typing.Optional[DashboardFilteringRequest] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SqlQueryResponse:
        """
        Execute a SQL query against the organization's ClickHouse data.

        POST /clickhouse/sql-queries/
        {
            "query": "SELECT model, count(*) as cnt FROM logs GROUP BY model ORDER BY cnt DESC",
            "parameters": {}  // reserved for future ClickHouse parameterized query support
        }

        Returns:
        {
            "columns": ["model", "cnt"],
            "rows": [["gpt-4", 150], ["claude-3", 120], ...],
            "row_count": 25,
            "execution_time_ms": 342
        }

        Parameters
        ----------
        query : str
            ClickHouse SQL query. Only SELECT statements allowed.

        start_time : typing.Optional[dt.datetime]
            Optional. Bound into `{{ start_time }}` references.

        end_time : typing.Optional[dt.datetime]
            Optional. Bound into `{{ end_time }}` references. Must be after start_time when both are set.

        environment : typing.Optional[SqlQueryRequestEnvironmentEnum]
            Optional environment filter; non-`all` is injected into the rewritten SQL as an equality filter on `environment`.

            * `all` - all
            * `prod` - prod
            * `test` - test

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Optional dashboard filters for saved SQL queries that opt in.

        dashboard_filtering : typing.Optional[DashboardFilteringRequest]
            Optional dashboard filter context for saved SQL execution.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SqlQueryResponse


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.sql_queries_create(
            query="query",
        )
        """
        _response = self._raw_client.sql_queries_create(
            query=query,
            start_time=start_time,
            end_time=end_time,
            environment=environment,
            filters=filters,
            dashboard_filtering=dashboard_filtering,
            request_options=request_options,
        )
        return _response.data

    def workflows_eval_runs_list(
        self,
        workflow_id: str,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        scope: typing.Optional[ClickhouseWorkflowsEvalRunsListRequestScope] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedChEvalPipelineRunList:
        """
        Run history for one evaluator pipeline — one row per graded log.

        Newest-first list of the logs this pipeline graded, each with every grader's
        score (``grader_scores``) plus the original log's input/output/model/status.
        Backs the evaluator metrics tab's lower table (replaces the per-version
        rollup): each row is an actual pipeline run, not a version. Keyed by the
        workflow family id; the family→versions fan-out matches the run-metrics views.
        With ``scope=automation`` the family id is an automation's and both the
        output rows and the grader sub-scores match on the stamped caller
        ``automation_id`` — the automation's run history across every evaluator
        pipeline it runs.

        Parameters
        ----------
        workflow_id : str

        limit : typing.Optional[int]
            Max runs to return (default 100, capped at 500).

        offset : typing.Optional[int]
            Number of runs to skip (pagination).

        scope : typing.Optional[ClickhouseWorkflowsEvalRunsListRequestScope]
            Set to 'automation' when the URL family id is an automation's — scores are then matched by the caller automation_id stamped on each row instead of the evaluator pipeline's version ids.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedChEvalPipelineRunList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.workflows_eval_runs_list(
            workflow_id="workflow_id",
        )
        """
        _response = self._raw_client.workflows_eval_runs_list(
            workflow_id, limit=limit, offset=offset, scope=scope, request_options=request_options
        )
        return _response.data

    def workflows_eval_scores_list(
        self,
        workflow_id: str,
        *,
        scope: typing.Optional[ClickhouseWorkflowsEvalScoresListRequestScope] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ChEvalPipelineScores]:
        """
        Output-score aggregate for one evaluator pipeline, keyed by family id.

        One row — the pipeline's output (rollup) score, not each grader. Numeric
        output uses ``avg_primary_score``, boolean uses ``true_ratio``. The FE plots
        it as the one line on the scores-over-time chart. With ``scope=automation``
        the family id is an automation's and rows match on the stamped caller
        ``automation_id`` — only that automation's scores, across every evaluator
        pipeline it runs.

        Parameters
        ----------
        workflow_id : str

        scope : typing.Optional[ClickhouseWorkflowsEvalScoresListRequestScope]
            Set to 'automation' when the URL family id is an automation's — scores are then matched by the caller automation_id stamped on each row instead of the evaluator pipeline's version ids.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ChEvalPipelineScores]


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.workflows_eval_scores_list(
            workflow_id="workflow_id",
        )
        """
        _response = self._raw_client.workflows_eval_scores_list(
            workflow_id, scope=scope, request_options=request_options
        )
        return _response.data

    def workflows_eval_scores_time_series_list(
        self,
        workflow_id: str,
        *,
        scope: typing.Optional[ClickhouseWorkflowsEvalScoresTimeSeriesListRequestScope] = None,
        time_tick: typing.Optional[ClickhouseWorkflowsEvalScoresTimeSeriesListRequestTimeTick] = None,
        version_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ChEvalPipelineScoresTimeSeries]:
        """
        Time-bucketed output score for one evaluator pipeline (graphs).

        One series — the pipeline's output (rollup) score over time. Optional
        ``version_id`` scopes to a single version. With ``scope=automation`` the
        family id is an automation's and rows match on the stamped caller
        ``automation_id`` — only that automation's scores over time.

        Parameters
        ----------
        workflow_id : str

        scope : typing.Optional[ClickhouseWorkflowsEvalScoresTimeSeriesListRequestScope]
            Set to 'automation' when the URL family id is an automation's — scores are then matched by the caller automation_id stamped on each row instead of the evaluator pipeline's version ids.

        time_tick : typing.Optional[ClickhouseWorkflowsEvalScoresTimeSeriesListRequestTimeTick]
            Time bucket granularity (default hour). Case-insensitive.

        version_id : typing.Optional[str]
            Scope scores to a single evaluator pipeline version. Not supported with scope=automation (400).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ChEvalPipelineScoresTimeSeries]


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.workflows_eval_scores_time_series_list(
            workflow_id="workflow_id",
        )
        """
        _response = self._raw_client.workflows_eval_scores_time_series_list(
            workflow_id, scope=scope, time_tick=time_tick, version_id=version_id, request_options=request_options
        )
        return _response.data

    def workflows_runs_list(
        self,
        workflow_id: str,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedChWorkflowRunList:
        """
        Run history for one monitor/automation — one row per run, newest first.

        Keyed by the workflow family id, same fan-out and row filter as the
        summary view. With the ``production_traces`` sink disabled (#3926), rows
        come from the executor's delivery-gated direct writes — each row is one
        fired (or delivery-failed) alert.

        Parameters
        ----------
        workflow_id : str

        limit : typing.Optional[int]
            Max runs to return (default 100, capped at 500).

        offset : typing.Optional[int]
            Number of runs to skip (pagination).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedChWorkflowRunList


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.workflows_runs_list(
            workflow_id="workflow_id",
        )
        """
        _response = self._raw_client.workflows_runs_list(
            workflow_id, limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    def workflows_runs_summary(
        self,
        workflow_id: str,
        *,
        time_tick: typing.Optional[ClickhouseWorkflowsRunsSummaryRequestTimeTick] = None,
        version_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ChWorkflowMetricsTimeSeries]:
        """
        Run summary for one monitor/automation — time-bucketed counts.

        One row per bucket (hour/day/minute via ``time_tick``), ordered ascending:
        completed / stopped / failed run counts plus delivery sums. Powers the
        Runs tab's chart and its summed header stats. Optional ``version_id``
        scopes to a single version.

        Parameters
        ----------
        workflow_id : str

        time_tick : typing.Optional[ClickhouseWorkflowsRunsSummaryRequestTimeTick]
            Time bucket granularity (default hour). Case-insensitive.

        version_id : typing.Optional[str]
            Scope metrics to a single workflow version.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ChWorkflowMetricsTimeSeries]


        Examples
        --------
        from respan import RespanClient

        client = RespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )
        client.clickhouse.workflows_runs_summary(
            workflow_id="workflow_id",
        )
        """
        _response = self._raw_client.workflows_runs_summary(
            workflow_id, time_tick=time_tick, version_id=version_id, request_options=request_options
        )
        return _response.data


class AsyncClickhouseClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawClickhouseClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawClickhouseClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawClickhouseClient
        """
        return self._raw_client

    async def custom_identifiers_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedCustomIdentifierListList:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedCustomIdentifierListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def custom_identifiers_create(
        self,
        *,
        id: str,
        custom_identifier: str,
        environment: str,
        number_of_requests: int,
        total_cost: float,
        total_tokens: int,
        total_prompt_tokens: int,
        total_completion_tokens: int,
        error_count: int,
        average_latency: float,
        average_ttft: float,
        average_tps: float,
        first_seen: dt.datetime,
        last_active: dt.datetime,
        unique_organization_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        custom_identifier : str

        environment : str

        number_of_requests : int

        total_cost : float

        total_tokens : int

        total_prompt_tokens : int

        total_completion_tokens : int

        error_count : int

        average_latency : float

        average_ttft : float

        average_tps : float

        first_seen : dt.datetime

        last_active : dt.datetime

        unique_organization_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import asyncio
        import datetime

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_create(
                id="id",
                custom_identifier="custom_identifier",
                environment="environment",
                number_of_requests=1,
                total_cost=1.1,
                total_tokens=1,
                total_prompt_tokens=1,
                total_completion_tokens=1,
                error_count=1,
                average_latency=1.1,
                average_ttft=1.1,
                average_tps=1.1,
                first_seen=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                last_active=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_create(
            id=id,
            custom_identifier=custom_identifier,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            unique_organization_id=unique_organization_id,
            request_options=request_options,
        )
        return _response.data

    async def custom_identifiers_update(
        self,
        *,
        id: str,
        custom_identifier: str,
        environment: str,
        number_of_requests: int,
        total_cost: float,
        total_tokens: int,
        total_prompt_tokens: int,
        total_completion_tokens: int,
        error_count: int,
        average_latency: float,
        average_ttft: float,
        average_tps: float,
        first_seen: dt.datetime,
        last_active: dt.datetime,
        unique_organization_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        custom_identifier : str

        environment : str

        number_of_requests : int

        total_cost : float

        total_tokens : int

        total_prompt_tokens : int

        total_completion_tokens : int

        error_count : int

        average_latency : float

        average_ttft : float

        average_tps : float

        first_seen : dt.datetime

        last_active : dt.datetime

        unique_organization_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import asyncio
        import datetime

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_update(
                id="id",
                custom_identifier="custom_identifier",
                environment="environment",
                number_of_requests=1,
                total_cost=1.1,
                total_tokens=1,
                total_prompt_tokens=1,
                total_completion_tokens=1,
                error_count=1,
                average_latency=1.1,
                average_ttft=1.1,
                average_tps=1.1,
                first_seen=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                last_active=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_update(
            id=id,
            custom_identifier=custom_identifier,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            unique_organization_id=unique_organization_id,
            request_options=request_options,
        )
        return _response.data

    async def custom_identifiers_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        custom_identifier: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        number_of_requests: typing.Optional[int] = OMIT,
        total_cost: typing.Optional[float] = OMIT,
        total_tokens: typing.Optional[int] = OMIT,
        total_prompt_tokens: typing.Optional[int] = OMIT,
        total_completion_tokens: typing.Optional[int] = OMIT,
        error_count: typing.Optional[int] = OMIT,
        average_latency: typing.Optional[float] = OMIT,
        average_ttft: typing.Optional[float] = OMIT,
        average_tps: typing.Optional[float] = OMIT,
        first_seen: typing.Optional[dt.datetime] = OMIT,
        last_active: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        custom_identifier : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        number_of_requests : typing.Optional[int]

        total_cost : typing.Optional[float]

        total_tokens : typing.Optional[int]

        total_prompt_tokens : typing.Optional[int]

        total_completion_tokens : typing.Optional[int]

        error_count : typing.Optional[int]

        average_latency : typing.Optional[float]

        average_ttft : typing.Optional[float]

        average_tps : typing.Optional[float]

        first_seen : typing.Optional[dt.datetime]

        last_active : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_partial_update(
            id=id,
            custom_identifier=custom_identifier,
            unique_organization_id=unique_organization_id,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            request_options=request_options,
        )
        return _response.data

    async def custom_identifiers_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedCustomIdentifierListList:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedCustomIdentifierListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def custom_identifiers_list_create(
        self,
        *,
        id: str,
        custom_identifier: str,
        environment: str,
        number_of_requests: int,
        total_cost: float,
        total_tokens: int,
        total_prompt_tokens: int,
        total_completion_tokens: int,
        error_count: int,
        average_latency: float,
        average_ttft: float,
        average_tps: float,
        first_seen: dt.datetime,
        last_active: dt.datetime,
        unique_organization_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        custom_identifier : str

        environment : str

        number_of_requests : int

        total_cost : float

        total_tokens : int

        total_prompt_tokens : int

        total_completion_tokens : int

        error_count : int

        average_latency : float

        average_ttft : float

        average_tps : float

        first_seen : dt.datetime

        last_active : dt.datetime

        unique_organization_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import asyncio
        import datetime

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_list_create(
                id="id",
                custom_identifier="custom_identifier",
                environment="environment",
                number_of_requests=1,
                total_cost=1.1,
                total_tokens=1,
                total_prompt_tokens=1,
                total_completion_tokens=1,
                error_count=1,
                average_latency=1.1,
                average_ttft=1.1,
                average_tps=1.1,
                first_seen=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                last_active=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_list_create(
            id=id,
            custom_identifier=custom_identifier,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            unique_organization_id=unique_organization_id,
            request_options=request_options,
        )
        return _response.data

    async def custom_identifiers_list_update(
        self,
        *,
        id: str,
        custom_identifier: str,
        environment: str,
        number_of_requests: int,
        total_cost: float,
        total_tokens: int,
        total_prompt_tokens: int,
        total_completion_tokens: int,
        error_count: int,
        average_latency: float,
        average_ttft: float,
        average_tps: float,
        first_seen: dt.datetime,
        last_active: dt.datetime,
        unique_organization_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        custom_identifier : str

        environment : str

        number_of_requests : int

        total_cost : float

        total_tokens : int

        total_prompt_tokens : int

        total_completion_tokens : int

        error_count : int

        average_latency : float

        average_ttft : float

        average_tps : float

        first_seen : dt.datetime

        last_active : dt.datetime

        unique_organization_id : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import asyncio
        import datetime

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_list_update(
                id="id",
                custom_identifier="custom_identifier",
                environment="environment",
                number_of_requests=1,
                total_cost=1.1,
                total_tokens=1,
                total_prompt_tokens=1,
                total_completion_tokens=1,
                error_count=1,
                average_latency=1.1,
                average_ttft=1.1,
                average_tps=1.1,
                first_seen=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                last_active=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_list_update(
            id=id,
            custom_identifier=custom_identifier,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            unique_organization_id=unique_organization_id,
            request_options=request_options,
        )
        return _response.data

    async def custom_identifiers_list_partial_update(
        self,
        *,
        id: typing.Optional[str] = OMIT,
        custom_identifier: typing.Optional[str] = OMIT,
        unique_organization_id: typing.Optional[str] = OMIT,
        environment: typing.Optional[str] = OMIT,
        number_of_requests: typing.Optional[int] = OMIT,
        total_cost: typing.Optional[float] = OMIT,
        total_tokens: typing.Optional[int] = OMIT,
        total_prompt_tokens: typing.Optional[int] = OMIT,
        total_completion_tokens: typing.Optional[int] = OMIT,
        error_count: typing.Optional[int] = OMIT,
        average_latency: typing.Optional[float] = OMIT,
        average_ttft: typing.Optional[float] = OMIT,
        average_tps: typing.Optional[float] = OMIT,
        first_seen: typing.Optional[dt.datetime] = OMIT,
        last_active: typing.Optional[dt.datetime] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CustomIdentifierList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : typing.Optional[str]

        custom_identifier : typing.Optional[str]

        unique_organization_id : typing.Optional[str]

        environment : typing.Optional[str]

        number_of_requests : typing.Optional[int]

        total_cost : typing.Optional[float]

        total_tokens : typing.Optional[int]

        total_prompt_tokens : typing.Optional[int]

        total_completion_tokens : typing.Optional[int]

        error_count : typing.Optional[int]

        average_latency : typing.Optional[float]

        average_ttft : typing.Optional[float]

        average_tps : typing.Optional[float]

        first_seen : typing.Optional[dt.datetime]

        last_active : typing.Optional[dt.datetime]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CustomIdentifierList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_list_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_list_partial_update(
            id=id,
            custom_identifier=custom_identifier,
            unique_organization_id=unique_organization_id,
            environment=environment,
            number_of_requests=number_of_requests,
            total_cost=total_cost,
            total_tokens=total_tokens,
            total_prompt_tokens=total_prompt_tokens,
            total_completion_tokens=total_completion_tokens,
            error_count=error_count,
            average_latency=average_latency,
            average_ttft=average_ttft,
            average_tps=average_tps,
            first_seen=first_seen,
            last_active=last_active,
            request_options=request_options,
        )
        return _response.data

    async def custom_identifiers_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Mixin for views that need superadmin access to all resources.

        Provides FOUR key features (all bundled - no separate mixins needed):
        1. Queryset routing (superadmin sees all, regular user sees own org)
        2. Organization injection (post/patch/put auto-inject org)
        3. Object ownership checking (auto-registers ObjectOwnershipPermission)
        4. Superadmin-only field protection (certain fields can only be modified by superadmins)

        Inherits from:
        - ObjectOwnershipMixin: Config attributes + auto-permission registration
        - OrganizationInjectionMixin: Cross-org write protection + org injection

        Config attributes (inherited from ObjectOwnershipMixin):
        - ownership_object_field_name: Field on object (default: "organization_id")
        - ownership_user_field_name: Field on user (default: "curr_org_id")
        - is_allowing_global_object_read: Allow reading global objects (default: False)
        - is_allowing_org_admin_access: Allow org admins access to any object in their org (default: False)
        - is_requiring_org_admin_for_write: Require org admin for writes (default: False)

        Config attributes (superadmin-only fields):
        - superadmin_only_fields: List of field names that only superadmins can modify (default: [])
          On CREATE: Fields are stripped from non-superadmin requests (model defaults apply)
          On UPDATE: Non-superadmins trying to change these fields get PermissionDenied

        - superadmin_lock_field: Field name that locks the entire object for non-superadmins (default: None)
          When this field is truthy on the instance, non-superadmins cannot modify ANY field.
          Common use case: is_managed=True means Keywords AI manages this resource, users can't edit it.

        Note: Writing to global objects (ownership field is None) always requires superadmin.

        Usage (detail view):

            class MyDetailView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                # Optional: customize ownership config (inherited from ObjectOwnershipMixin)
                is_allowing_global_object_read = True

                def get_regular_user_queryset(self):
                    return MyModel.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return MyModel.objects.all()

        Usage (superadmin-only fields):

            class IntegrationView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                superadmin_only_fields = ['is_managed']  # Only superadmins can modify is_managed
                superadmin_lock_field = 'is_managed'  # When is_managed=True, object is locked for non-superadmins

                def get_regular_user_queryset(self):
                    return Integration.objects.filter(organization_id=self.request.user.curr_org_id)

                def get_superadmin_queryset(self):
                    return Integration.objects.all()

        Usage (related-object org pattern like PromptVersion - PREFERRED: annotate queryset):

            from django.db.models import F

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_regular_user_queryset(self):
                    # Annotate organization_id so ownership checks work automatically
                    return PromptVersion.objects.filter(
                        prompt__organization_id=self.request.user.curr_org_id
                    ).annotate(organization_id=F("prompt__organization_id"))

                def get_superadmin_queryset(self):
                    return PromptVersion.objects.annotate(organization_id=F("prompt__organization_id"))

        Alternative (override method - only if annotation not possible):

            class PromptVersionView(SuperAdminMixin, JWTAndAPIKeyAuthenticationViewMixin, RetrieveUpdateDestroyAPIView):
                def get_affiliated_object_organization_id(self, instance):
                    return instance.prompt.organization_id  # Org is on parent object

        DO NOT use inline checks like this:
            # ❌ BAD - easy to forget in branching code
            def get_queryset(self):
                if has_staff_role(self.request.user):
                    return MyModel.objects.all()
                return MyModel.objects.filter(...)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_summary_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_summary_retrieve(request_options=request_options)
        return _response.data

    async def custom_identifiers_summary_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_summary_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_summary_create(request_options=request_options)
        return _response.data

    async def custom_identifiers_summary_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_summary_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_summary_update(request_options=request_options)
        return _response.data

    async def custom_identifiers_summary_partial_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.custom_identifiers_summary_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.custom_identifiers_summary_partial_update(request_options=request_options)
        return _response.data

    async def saved_sql_queries_create(
        self,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryCreateRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryCreate:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryCreateRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_create(
                name="name",
                query="query",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_create(
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_update(
        self,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryCreateRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryCreate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryCreateRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_update(
                name="name",
                query="query",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_update(
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_partial_update(
        self,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        query: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[PatchedSavedSqlQueryCreateRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        organization: typing.Optional[int] = OMIT,
        project: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryCreate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        name : typing.Optional[str]

        description : typing.Optional[str]

        query : typing.Optional[str]

        plotting_config : typing.Optional[PatchedSavedSqlQueryCreateRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        organization : typing.Optional[int]

        project : typing.Optional[str]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryCreate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_partial_update(
            name=name,
            description=description,
            query=query,
            plotting_config=plotting_config,
            source=source,
            organization=organization,
            project=project,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_retrieve(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedSqlQueryList:
        """
        Retrieve, update, or delete a single saved SQL query.

        GET    /clickhouse/saved-sql-queries/<id>/
        PATCH  /clickhouse/saved-sql-queries/<id>/ — Partial update (name, description, query).
        DELETE /clickhouse/saved-sql-queries/<id>/

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_retrieve(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_retrieve(id, request_options=request_options)
        return _response.data

    async def saved_sql_queries_create2(
        self,
        id: str,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryListRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        id : str

        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryListRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_create2(
                id="id",
                name="name",
                query="query",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_create2(
            id,
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_update2(
        self,
        id: str,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryUpdateRequestPlottingConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryUpdate:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        id : str

        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryUpdateRequestPlottingConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryUpdate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_update2(
                id="id",
                name="name",
                query="query",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_update2(
            id,
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_destroy(
        self, id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Retrieve, update, or delete a single saved SQL query.

        GET    /clickhouse/saved-sql-queries/<id>/
        PATCH  /clickhouse/saved-sql-queries/<id>/ — Partial update (name, description, query).
        DELETE /clickhouse/saved-sql-queries/<id>/

        Parameters
        ----------
        id : str

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_destroy(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_destroy(id, request_options=request_options)
        return _response.data

    async def saved_sql_queries_partial_update2(
        self,
        id: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        query: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[PatchedSavedSqlQueryUpdateRequestPlottingConfig] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryUpdate:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        id : str

        name : typing.Optional[str]

        description : typing.Optional[str]

        query : typing.Optional[str]

        plotting_config : typing.Optional[PatchedSavedSqlQueryUpdateRequestPlottingConfig]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryUpdate


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_partial_update2(
                id="id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_partial_update2(
            id,
            name=name,
            description=description,
            query=query,
            plotting_config=plotting_config,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_list_list(
        self,
        *,
        page: typing.Optional[int] = None,
        page_size: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedSavedSqlQueryListList:
        """
        List saved SQL queries with pagination and filtering.

        GET  /clickhouse/saved-sql-queries/list/
        POST /clickhouse/saved-sql-queries/list/ — POST-for-filtering pattern.

        Supports: pagination (page, page_size), sorting (sort_by), filtering via POST body.

        Parameters
        ----------
        page : typing.Optional[int]
            A page number within the paginated result set.

        page_size : typing.Optional[int]
            Number of results to return per page.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSavedSqlQueryListList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_list_list()


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_list_list(
            page=page, page_size=page_size, request_options=request_options
        )
        return _response.data

    async def saved_sql_queries_list_create(
        self,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryListRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryList:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryListRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_list_create(
                name="name",
                query="query",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_list_create(
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_list_update(
        self,
        *,
        name: str,
        query: str,
        description: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[SavedSqlQueryListRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryList:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        name : str

        query : str

        description : typing.Optional[str]

        plotting_config : typing.Optional[SavedSqlQueryListRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_list_update(
                name="name",
                query="query",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_list_update(
            name=name,
            query=query,
            description=description,
            plotting_config=plotting_config,
            source=source,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_list_partial_update(
        self,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        query: typing.Optional[str] = OMIT,
        plotting_config: typing.Optional[PatchedSavedSqlQueryListRequestPlottingConfig] = OMIT,
        source: typing.Optional[Source141Enum] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSqlQueryList:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        name : typing.Optional[str]

        description : typing.Optional[str]

        query : typing.Optional[str]

        plotting_config : typing.Optional[PatchedSavedSqlQueryListRequestPlottingConfig]

        source : typing.Optional[Source141Enum]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQueryList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_list_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_list_partial_update(
            name=name,
            description=description,
            query=query,
            plotting_config=plotting_config,
            source=source,
            request_options=request_options,
        )
        return _response.data

    async def saved_sql_queries_summary_retrieve(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedSqlQuerySummary:
        """
        Summary statistics for saved SQL queries.

        GET  /clickhouse/saved-sql-queries/summary/
        POST /clickhouse/saved-sql-queries/summary/ — POST-for-filtering pattern.

        Returns: { total_count }

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQuerySummary


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_summary_retrieve()


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_summary_retrieve(request_options=request_options)
        return _response.data

    async def saved_sql_queries_summary_create(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedSqlQuerySummary:
        """
        POST handler with superadmin-only field protection.

        Strips superadmin-only fields from non-superadmin requests before
        delegating to OrganizationInjectionMixin.post() for org injection.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSqlQuerySummary


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_summary_create()


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_summary_create(request_options=request_options)
        return _response.data

    async def saved_sql_queries_summary_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PUT handler with superadmin lock and field protection.

        Same as patch() - checks lock and field protection before delegating.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_summary_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_summary_update(request_options=request_options)
        return _response.data

    async def saved_sql_queries_summary_partial_update(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        PATCH handler with superadmin lock and field protection.

        Checks:
        1. Object lock (is_managed=True -> non-superadmins can't modify)
        2. Field protection (non-superadmins can't modify specific fields)

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.saved_sql_queries_summary_partial_update()


        asyncio.run(main())
        """
        _response = await self._raw_client.saved_sql_queries_summary_partial_update(request_options=request_options)
        return _response.data

    async def sql_queries_create(
        self,
        *,
        query: str,
        start_time: typing.Optional[dt.datetime] = OMIT,
        end_time: typing.Optional[dt.datetime] = OMIT,
        environment: typing.Optional[SqlQueryRequestEnvironmentEnum] = OMIT,
        filters: typing.Optional[typing.Dict[str, typing.Any]] = OMIT,
        dashboard_filtering: typing.Optional[DashboardFilteringRequest] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SqlQueryResponse:
        """
        Execute a SQL query against the organization's ClickHouse data.

        POST /clickhouse/sql-queries/
        {
            "query": "SELECT model, count(*) as cnt FROM logs GROUP BY model ORDER BY cnt DESC",
            "parameters": {}  // reserved for future ClickHouse parameterized query support
        }

        Returns:
        {
            "columns": ["model", "cnt"],
            "rows": [["gpt-4", 150], ["claude-3", 120], ...],
            "row_count": 25,
            "execution_time_ms": 342
        }

        Parameters
        ----------
        query : str
            ClickHouse SQL query. Only SELECT statements allowed.

        start_time : typing.Optional[dt.datetime]
            Optional. Bound into `{{ start_time }}` references.

        end_time : typing.Optional[dt.datetime]
            Optional. Bound into `{{ end_time }}` references. Must be after start_time when both are set.

        environment : typing.Optional[SqlQueryRequestEnvironmentEnum]
            Optional environment filter; non-`all` is injected into the rewritten SQL as an equality filter on `environment`.

            * `all` - all
            * `prod` - prod
            * `test` - test

        filters : typing.Optional[typing.Dict[str, typing.Any]]
            Optional dashboard filters for saved SQL queries that opt in.

        dashboard_filtering : typing.Optional[DashboardFilteringRequest]
            Optional dashboard filter context for saved SQL execution.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SqlQueryResponse


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.sql_queries_create(
                query="query",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.sql_queries_create(
            query=query,
            start_time=start_time,
            end_time=end_time,
            environment=environment,
            filters=filters,
            dashboard_filtering=dashboard_filtering,
            request_options=request_options,
        )
        return _response.data

    async def workflows_eval_runs_list(
        self,
        workflow_id: str,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        scope: typing.Optional[ClickhouseWorkflowsEvalRunsListRequestScope] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedChEvalPipelineRunList:
        """
        Run history for one evaluator pipeline — one row per graded log.

        Newest-first list of the logs this pipeline graded, each with every grader's
        score (``grader_scores``) plus the original log's input/output/model/status.
        Backs the evaluator metrics tab's lower table (replaces the per-version
        rollup): each row is an actual pipeline run, not a version. Keyed by the
        workflow family id; the family→versions fan-out matches the run-metrics views.
        With ``scope=automation`` the family id is an automation's and both the
        output rows and the grader sub-scores match on the stamped caller
        ``automation_id`` — the automation's run history across every evaluator
        pipeline it runs.

        Parameters
        ----------
        workflow_id : str

        limit : typing.Optional[int]
            Max runs to return (default 100, capped at 500).

        offset : typing.Optional[int]
            Number of runs to skip (pagination).

        scope : typing.Optional[ClickhouseWorkflowsEvalRunsListRequestScope]
            Set to 'automation' when the URL family id is an automation's — scores are then matched by the caller automation_id stamped on each row instead of the evaluator pipeline's version ids.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedChEvalPipelineRunList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.workflows_eval_runs_list(
                workflow_id="workflow_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.workflows_eval_runs_list(
            workflow_id, limit=limit, offset=offset, scope=scope, request_options=request_options
        )
        return _response.data

    async def workflows_eval_scores_list(
        self,
        workflow_id: str,
        *,
        scope: typing.Optional[ClickhouseWorkflowsEvalScoresListRequestScope] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ChEvalPipelineScores]:
        """
        Output-score aggregate for one evaluator pipeline, keyed by family id.

        One row — the pipeline's output (rollup) score, not each grader. Numeric
        output uses ``avg_primary_score``, boolean uses ``true_ratio``. The FE plots
        it as the one line on the scores-over-time chart. With ``scope=automation``
        the family id is an automation's and rows match on the stamped caller
        ``automation_id`` — only that automation's scores, across every evaluator
        pipeline it runs.

        Parameters
        ----------
        workflow_id : str

        scope : typing.Optional[ClickhouseWorkflowsEvalScoresListRequestScope]
            Set to 'automation' when the URL family id is an automation's — scores are then matched by the caller automation_id stamped on each row instead of the evaluator pipeline's version ids.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ChEvalPipelineScores]


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.workflows_eval_scores_list(
                workflow_id="workflow_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.workflows_eval_scores_list(
            workflow_id, scope=scope, request_options=request_options
        )
        return _response.data

    async def workflows_eval_scores_time_series_list(
        self,
        workflow_id: str,
        *,
        scope: typing.Optional[ClickhouseWorkflowsEvalScoresTimeSeriesListRequestScope] = None,
        time_tick: typing.Optional[ClickhouseWorkflowsEvalScoresTimeSeriesListRequestTimeTick] = None,
        version_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ChEvalPipelineScoresTimeSeries]:
        """
        Time-bucketed output score for one evaluator pipeline (graphs).

        One series — the pipeline's output (rollup) score over time. Optional
        ``version_id`` scopes to a single version. With ``scope=automation`` the
        family id is an automation's and rows match on the stamped caller
        ``automation_id`` — only that automation's scores over time.

        Parameters
        ----------
        workflow_id : str

        scope : typing.Optional[ClickhouseWorkflowsEvalScoresTimeSeriesListRequestScope]
            Set to 'automation' when the URL family id is an automation's — scores are then matched by the caller automation_id stamped on each row instead of the evaluator pipeline's version ids.

        time_tick : typing.Optional[ClickhouseWorkflowsEvalScoresTimeSeriesListRequestTimeTick]
            Time bucket granularity (default hour). Case-insensitive.

        version_id : typing.Optional[str]
            Scope scores to a single evaluator pipeline version. Not supported with scope=automation (400).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ChEvalPipelineScoresTimeSeries]


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.workflows_eval_scores_time_series_list(
                workflow_id="workflow_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.workflows_eval_scores_time_series_list(
            workflow_id, scope=scope, time_tick=time_tick, version_id=version_id, request_options=request_options
        )
        return _response.data

    async def workflows_runs_list(
        self,
        workflow_id: str,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedChWorkflowRunList:
        """
        Run history for one monitor/automation — one row per run, newest first.

        Keyed by the workflow family id, same fan-out and row filter as the
        summary view. With the ``production_traces`` sink disabled (#3926), rows
        come from the executor's delivery-gated direct writes — each row is one
        fired (or delivery-failed) alert.

        Parameters
        ----------
        workflow_id : str

        limit : typing.Optional[int]
            Max runs to return (default 100, capped at 500).

        offset : typing.Optional[int]
            Number of runs to skip (pagination).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedChWorkflowRunList


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.workflows_runs_list(
                workflow_id="workflow_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.workflows_runs_list(
            workflow_id, limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    async def workflows_runs_summary(
        self,
        workflow_id: str,
        *,
        time_tick: typing.Optional[ClickhouseWorkflowsRunsSummaryRequestTimeTick] = None,
        version_id: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> typing.List[ChWorkflowMetricsTimeSeries]:
        """
        Run summary for one monitor/automation — time-bucketed counts.

        One row per bucket (hour/day/minute via ``time_tick``), ordered ascending:
        completed / stopped / failed run counts plus delivery sums. Powers the
        Runs tab's chart and its summed header stats. Optional ``version_id``
        scopes to a single version.

        Parameters
        ----------
        workflow_id : str

        time_tick : typing.Optional[ClickhouseWorkflowsRunsSummaryRequestTimeTick]
            Time bucket granularity (default hour). Case-insensitive.

        version_id : typing.Optional[str]
            Scope metrics to a single workflow version.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        typing.List[ChWorkflowMetricsTimeSeries]


        Examples
        --------
        import asyncio

        from respan import AsyncRespanClient

        client = AsyncRespanClient(
            respan_deployment_token="YOUR_RESPAN_DEPLOYMENT_TOKEN",
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.clickhouse.workflows_runs_summary(
                workflow_id="workflow_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.workflows_runs_summary(
            workflow_id, time_tick=time_tick, version_id=version_id, request_options=request_options
        )
        return _response.data
